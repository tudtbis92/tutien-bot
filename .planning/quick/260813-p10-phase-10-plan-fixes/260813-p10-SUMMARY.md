---
quick_id: 260813-p10
slug: phase-10-plan-fixes
date: 2026-08-13
status: complete
---

# Quick Task 260813-p10 — Phase 10 Plan Fixes (F1..F9)

Applied the 9 review findings (sequential-thinking analysis + context7/tavily-verified
research) to the Phase 10 planning artifacts. Docs-only — no source code changed.

## Fixes applied

| # | Fix | Files |
|---|-----|-------|
| F1 | Starter set-2 heroIds `zhang_jue`/`dong_zhuo` do not exist → `truong_giac` (Trương Giác) / `dong_trac` (Đổng Trác), verified in `sanguo-classifications.json`; updated the JSON authoring step + acceptance cross-check + the DB probe SQL | 10-04 |
| F2 | Same starter key fix in `STARTER_SET_2` + tests | 10-07 |
| F3 | `seed: bigint('seed')` → `bigint('seed', { mode: 'number' })` — drizzle requires an explicit mode (in-repo convention `{ mode: 'bigint' }`); mode 'number' keeps the replay seed a JS `number` for `runBattle(seed: number)`; updated must_haves + acceptance grep | 10-02 |
| F4 | Abandoned-capture routing: `encounterPending` re-fetch renders the CAPTURE VIEW when a completed + player-won `sanguo_battles` row exists for the pending encounter, else fight/skip — closes the walk-away-after-win re-battle gap | 10-06 (flagged assumption + travel.ts task step) |
| F5 | `crypto.randomInt` → `crypto.randomInt(2 ** 48)` — node requires `max` (< 2^48 keeps a safe JS integer); updated must_haves + Task 1 step 5 | 10-05 |
| F6 | `depends_on` += `10-04` (base-stats content seed precedes DB-gated battle/capture verification) | 10-05 |
| F7 | `startEncounterBattle` locks `user_sanguo_state` with `FOR UPDATE` — single-writer with the 10-07 companion switch | 10-05 (must_haves + Task 1 step 3) |
| F8 | D-20 re-sign: `E[outflow]` must use EFFECTIVE chances (base × hpFactor × tierMult, incl. flee) — hpFactor ≈ 1/3 at full HP makes effective R1 ≈ 0.27, not 0.80; gross < ~416/hr must hold under realistic T2/T3 usage | 10-03 (flagged assumption + checkpoint context + Task 3 step) |
| F9 | `/sanguo hero` lookup disambiguation when the user owns duplicate rows of one heroId: prefer the active companion, else earliest copy | 10-07 |

## Evidence anchors

- `node:crypto` docs (tavily): `crypto.randomInt([min, ]max)` — `max` REQUIRED; range must be < 2^48.
- pure-rand 8.x README (tavily): `xoroshiro128plus` + `uniformFloat64(rng)` import paths verified — engine imports (10-01) are correct, no change needed.
- drizzle in-repo convention (`walletTransactions.ts`/`users.ts`): `bigint(name, { mode: ... })` explicit mode required → F3.
- `scripts/data/sanguo-classifications.json` (132 keys) + `heroes-v1.json`: `zhang_jue`/`dong_zhuo` absent; `truong_giac`/`dong_trac` present → F1/F2.

## Verification

- Grep confirms the fixed phrases present in the owning files; stale tokens (`zhang_jue`, `dong_zhuo`, `bigint('seed')`, bare `crypto.randomInt`) removed except in intentional "NOT ..." explanatory notes.
- Frontmatter intact across all 6 edited PLAN files; quick PLAN.md/SUMMARY.md added.
- No source code touched — `npm run typecheck` / `npm test` unaffected.
