---
phase: quick
plan: 260629-8pw
type: execute
wave: 1
depends_on: []
files_modified:
  - src/jobs/footballFetchFixtures.ts
  - src/jobs/footballAnnounceMatches.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "When ESPN updates a placeholder team name (e.g. Group A 2nd Place → Brazil), the DB row reflects the real team name"
    - "Matches with placeholder team names are not announced for betting"
  artifacts:
    - "src/jobs/footballFetchFixtures.ts — homeTeamName and awayTeamName in onConflictDoUpdate set clause"
    - "src/jobs/footballAnnounceMatches.ts — placeholder name filter before announcing matches"
  key_links:
    - "onConflictDoUpdate set clause → DB column home_team_name / away_team_name"
    - "announce query WHERE clause → placeholder detection gating"
---

<objective>
Fix football placeholder team names in bet notifications. World Cup 1/32 knockout matches show "Group A 2nd Place"
instead of real team names because (a) the DB upsert never updates team name columns once a row exists, and
(b) no placeholder detection prevents these ghost-team matches from being announced for betting.

Purpose: Ensure bet notifications always show real team names, not ESPN placeholder strings.
Output: Two targeted fixes in existing job files — no new files, no schema changes.
</objective>

<execution_context>
@C:/Users/saeth/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/saeth/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/jobs/footballFetchFixtures.ts
@src/jobs/footballAnnounceMatches.ts
@src/db/schema/footballMatches.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add homeTeamName and awayTeamName to onConflictDoUpdate set clause</name>
  <files>src/jobs/footballFetchFixtures.ts</files>
  <action>
    In the `onConflictDoUpdate` `set` object (lines 147-171), add two entries for the team name columns
    immediately after the `kickoffAt` line. Since `homeTeamName` and `awayTeamName` are NOT NULL
    in the schema (ESPN always provides a value), use direct `excluded.column` assignment
    (same pattern as `kickoffAt`), NOT the COALESCE wrapper used for nullable fields like logos/odds.

    Add after line 153 (`kickoffAt: sql`excluded.kickoff_at`,`):
    ```
    homeTeamName: sql`excluded.home_team_name`,
    awayTeamName: sql`excluded.away_team_name`,
    ```

    This ensures that when ESPN resolves a placeholder name ("Group A 2nd Place") to a real team name
    ("Brazil"), the DB row is updated — the `setWhere` clause (`status IN ('NS', 'PST')`) already
    gates updates to pre-match states, which is when placeholder names are still present.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    `homeTeamName` and `awayTeamName` are present in the `onConflictDoUpdate` `set` clause,
    TypeScript compiles cleanly. On next ESPN poll cycle, existing DB rows with placeholder
    names will receive real team names when ESPN provides them.
  </done>
</task>

<task type="auto">
  <name>Task 2: Filter placeholder team names from match announcements</name>
  <files>src/jobs/footballAnnounceMatches.ts</files>
  <action>
    Add a placeholder-detection helper and filter the query results before calling `postPredictionEmbed`.

    **Helper function** — add a module-level function before `runFootballAnnounceMatches`:
    ```
    function hasPlaceholderTeamName(name: string): boolean {
      return /^(Group\s+[A-H]|Winner\s+(of|Group)|Runner[\s-]?up|TBD)\b/i.test(name);
    }
    ```

    **Filter** — after the `matchesToAnnounce` DB query (line 29) and before the for-loop (line 39),
    add a filter that skips matches where either `homeTeamName` or `awayTeamName` matches
    the placeholder pattern:

    ```
    const validMatches = matchesToAnnounce.filter(
      (m) => !hasPlaceholderTeamName(m.homeTeamName) && !hasPlaceholderTeamName(m.awayTeamName)
    );

    const skippedCount = matchesToAnnounce.length - validMatches.length;
    if (skippedCount > 0) {
      logger.info('FootballAnnounceMatches', `Skipped ${skippedCount} match(es) with placeholder team names (waiting for real names).`);
    }
    ```

    Then iterate over `validMatches` instead of `matchesToAnnounce` in the for-loop.
    Update the job-completion log to reference `validMatches.length` rather than
    `matchesToAnnounce.length` for the processed count.

    **Note:** Do NOT add placeholder filtering inside `footballFetchFixtures.ts` — we want
    placeholder rows in the DB so Task 1 can later update them with real names. The filter
    should only gate the *announcement* path.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    Matches whose `homeTeamName` or `awayTeamName` matches a placeholder pattern (Group letter
    prefixes, "Winner of...", "Runner-up...", "TBD") are silently skipped during the announce
    scan. They remain in the DB with NS status so Task 1's upsert can populate real names on
    the next poll cycle. Once real names arrive, the announce job picks them up normally.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ESPN API → DB | External data (team names) ingested and stored |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-quick-01 | Tampering | ESPN displayName field | low | accept | ESPN is the trusted source — if ESPN provides incorrect names, DB reflects ESPN. No injection risk (parametrized Drizzle ORM). |
| T-quick-02 | Denial of Service | Placeholder regex | low | accept | Regex is bounded to a short string (team name ≤ 200 chars). No catastrophic backtracking risk — the pattern uses only anchored alternation with no nested quantifiers. |
</threat_model>

<verification>
1. `npx tsc --noEmit` passes (both files)
2. After deploy + next ESPN poll cycle, existing World Cup 1/32 matches in DB should show real team names (verify via `SELECT home_team_name, away_team_name FROM football_matches WHERE league_id = 'world.cup'`)
3. Announce job logs should show "Skipped N match(es) with placeholder team names" for matches not yet resolved
</verification>

<success_criteria>
- [ ] `onConflictDoUpdate` includes `homeTeamName` and `awayTeamName` fields
- [ ] Announce job skips matches with placeholder team names (logged)
- [ ] No TypeScript compilation errors
- [ ] No new files created — minimal diff to two existing job files
</success_criteria>

<output>
Create `.planning/quick/260629-8pw-fix-football-placeholder-team-names-in-b/SUMMARY.md` when done
</output>
