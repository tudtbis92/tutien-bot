---
phase: quick-260810-f7j-upgrade-deps
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - vitest.config.ts
  - tsconfig.json
  - eslint.config.mjs
autonomous: true
requirements: []
user_setup: []

estimate:
  tokens: 14000
  raw_tokens: 14000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - All dependencies reach the researched target versions with zero package.json drift
    - typecheck, lint, test, and build all pass green on the upgraded toolchain (ESLint 10, TS 6.0.3, vitest 4)
    - vitest 4 runs with no `poolOptions` deprecation (config migrated to top-level options)
    - Runtime majors (ioredis 6 RESP3, discord.js 14.27, fastify, pg-boss, pg, i18next, zod) pass regression tests
    - pm2 7.0.3 and lint-staged 17.3.0 aligned with server versions, configs unchanged
  artifacts:
    - package.json (researched version bumps)
    - package-lock.json (reinstalled, lockfile consistent)
    - vitest.config.ts (vitest 4 top-level pool options)
  key_links:
    - vitest.config.ts <-> vitest 4 (poolOptions removed, isolate/maxWorkers now top-level)
    - eslint.config.mjs <-> typescript-eslint 8.66 + ESLint 10 flat config
    - tsconfig.json <-> TS 6.0.3 option set (no baseUrl, no node10, ES2022)
    - src/cache/redis.ts <-> ioredis 6 RESP3 client contract (set/get/ping/pttl/defineCommand)
    - ecosystem.config.cjs <-> pm2 7.0.3 fork mode (unchanged)
---

<objective>
Upgrade the full dependency set to the researched target versions, with ESLint 9 (EOL 2026-08-06) forced off the stack.

Purpose: Close the EOL/high-severity exposure (ESLint 9 is end-of-life) and align the local toolchain with the production server (pm2 7.0.3, Node 22). All target versions are pre-verified in the RESEARCH SUMMARY — no re-research, no exploratory upgrades.

Output: Bumped `package.json`/`package-lock.json`, a vitest-4-migrated `vitest.config.ts`, and a green typecheck → lint → test → build pipeline. Held packages (discord.js-selfbot-v13, @types/node 22.x, TypeScript 7, discord.js v15, Node runtime) are untouched.
</objective>

<execution_context>
@C:/Users/901107/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/901107/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

# Source of truth for every version below (do NOT re-research):
# RESEARCH SUMMARY in planning context — items 1-8 list exact target versions and hold items.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump all dependency versions to researched targets and reinstall</name>
  <files>package.json, package-lock.json</files>
  <precondition>Active Node is ≥22.22.1 — local shell defaults to Node v26.3.0, so run `nvm use 22` first to match .nvmrc (22) and server (22.22.2) before npm install/verify. Check with `node -v`; halt if < 22.22.1.</precondition>
  <action>Edit package.json dependencies: @discordjs/rest 2.6.1→2.6.3, @napi-rs/canvas ^1.0.0→^1.0.5, discord.js 14.26.2→14.27.0, dotenv 17.4.1→17.4.2, fastify 5.8.4→5.11.3, i18next 26.0.4→26.3.6, i18next-fs-backend 2.6.3→2.6.7, ioredis 5.10.1→6.0.0, pg 8.20.0→8.23.0, pg-boss 12.15.0→12.27.0, playwright ^1.60.0→^1.62.1, proxy-agent ^8.0.1→^8.0.2, zod 4.3.6→4.4.3. Edit devDependencies: @types/pg 8.20.0→8.21.0, @typescript-eslint/eslint-plugin 8.58.1→8.66.0, @typescript-eslint/parser 8.58.1→8.66.0, eslint 9.39.4→10.8.1, eslint-plugin-i18next 6.1.3→6.1.5, lint-staged 16.4.0→17.3.0, pm2 6.0.14→7.0.3, tsc-alias 1.8.16→1.9.1, tsx 4.21.0→4.23.12, typescript 5.8.3→6.0.3, typescript-eslint 8.58.1→8.66.0, vitest 3.1.2→4.1.10. DO NOT touch (per RESEARCH HOLD list): discord.js-selfbot-v13 3.7.1 (archived), @types/node (stay 22.x), drizzle-orm 0.45.2 / drizzle-kit 0.31.10, husky. Leave lint-staged config object in package.json as-is (v17 keeps this format). Then run `npm install` and inspect the lockfile diff for unexpected transitive additions before committing.</action>
  <verify>
    <automated>npm ls eslint typescript vitest ioredis pm2 lint-staged discord.js fastify pg-boss pg i18next zod --depth=0</automated>
  </verify>
  <done>npm ls reports eslint@10.8.1, typescript@6.0.3, vitest@4.1.10, ioredis@6.0.0, pm2@7.0.3, lint-staged@17.3.0 and the listed minor/patch targets; discord.js-selfbot-v13 and @types/node 22.x remain unchanged; `npm install` completed with exit 0 and no EOVERRIDE/engine errors.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate vitest.config.ts to vitest 4 top-level pool options</name>
  <files>vitest.config.ts</files>
  <action>Vitest 4 removed `test.poolOptions` entirely (verified against vitest 4.1.6 migration docs) — all previous pool options are now top-level. In vitest.config.ts: delete the `poolOptions: { threads: { isolate: true } }` block and add top-level `isolate: true`. Keep `pool: 'threads'`, `environment: 'node'`, `include: ['src/**/__tests__/**/*.test.ts']`, and `setupFiles: ['./src/testSetup.ts']` unchanged. No workspace→projects migration is needed (project has no vitest workspace file). If `npm test` surfaces any test file using a v3-only API, fix that test minimally; do not change test semantics.</action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>`npm test` passes with the full existing suite green and no `poolOptions was removed` deprecation warning in output; vitest.config.ts has no `poolOptions` key.</done>
</task>

<task type="auto">
  <name>Task 3: Validate TypeScript 6.0.3 and ESLint 10 toolchain configs</name>
  <files>tsconfig.json, eslint.config.mjs</files>
  <action>tsconfig.json is already TS 6-safe: `strict: true` (TS 6 default), `target: ES2022` (ES5 target removed in TS 6 — not used), `module: Node16` + `moduleResolution: Node16` (node/node10 resolution removed — not used), and NO `baseUrl` (removed in TS 6 — do not add one). Do not edit tsconfig.json unless `tsc --noEmit` reports a TS-6-specific config/option error. eslint.config.mjs: flat config is the only format in ESLint 10 — the file as-is (typescript-eslint 8.66 `recommended` sets + i18next `flat/recommended` + the src rule overrides) is expected to load without changes. Only edit it if ESLint 10 errors on rule/plugin loading (e.g., a removed deprecated rule reference); never add new rules. Then run the toolchain gates — both must be clean.</action>
  <verify>
    <automated>npm run typecheck</automated>
    <automated>npm run lint</automated>
  </verify>
  <done>`npm run typecheck` (tsc --noEmit) exits 0 with zero errors across src/; `npm run lint` (eslint src --max-warnings=0) exits 0 with zero warnings; tsconfig.json/eslint.config.mjs unchanged unless a TS6/ESLint10-specific error forced a minimal edit.</done>
</task>

<task type="auto">
  <name>Task 4: Runtime and pipeline regression gate (ioredis 6, pm2 7, lint-staged 17)</name>
  <files>src/cache/redis.ts (verify-only; edit ONLY if a runtime test exposes an ioredis 6 break)</files>
  <action>Verify the runtime majors end-to-end. ioredis 6 defaults to RESP3 (HELLO 3): server Redis 7.0.15 supports RESP3 and Node 22.22.2 ≥ required 20, so no protocol:2 fallback is added; src/cache/redis.ts contract (redis.set/get/ping/pttl/defineCommand) is unchanged in v6 — do not modify unless the test suite proves otherwise. discord.js 14.27.0, fastify 5.11.3, pg-boss 12.27.0, pg 8.23.0, i18next 26.3.6, zod 4.4.3 are minor/patch — no migration expected; treat any regression as a task-failing finding, not a config change. ecosystem.config.cjs fork mode is unchanged for pm2 7; lint-staged 17.3.0 keeps the package.json object config. Run the full per-phase pipeline in order (typecheck → lint → test → build); if tests go red, stop and fix only the code area that the failure traces to a bumped dependency, otherwise halt and report.</action>
  <verify>
    <automated>npm run typecheck; if ($?) { npm run lint }; if ($?) { npm test }; if ($?) { npm run build }</automated>
    <automated>npx pm2 --version</automated>
    <automated>npx lint-staged --version</automated>
  </verify>
  <done>Full pipeline (typecheck, lint with --max-warnings=0, vitest run, tsc + tsc-alias build) all exit 0 on the upgraded stack; `npx pm2 --version` prints 7.0.3; `npx lint-staged --version` prints 17.3.0; no runtime dependency regression observed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → node_modules | Untrusted package content enters the runtime and build chain |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-260810-01 | Tampering | npm registry supply chain | medium | mitigate | Only version bumps of already-installed, previously vetted packages — zero new packages introduced (package-legitimacy gate satisfied by construction; `[ASSUMED]` status carried from prior phases). Verify lockfile integrity via `npm install` + `npm ls --depth=0` (Task 1) and review the package-lock diff for unexpected transitive additions before committing. |
| T-260810-02 | Denial of Service | src/cache/redis.ts (ioredis 6 RESP3) | low | mitigate | ioredis 6 switches to RESP3 (HELLO 3) by default. Server Redis 7.0.15 verified RESP3-capable and Node 22.22.2 satisfies the ≥20 engine. Redis client contract (set/get/ping/pttl/defineCommand) unchanged in v6; full test suite (Task 2) and pipeline gate (Task 4) must stay green, else the plan halts. |
| T-260810-03 | Elevation of Privilege | eslint flat config / tsconfig | low | accept | ESLint 10 and TS 6.0.3 config validation runs in CI-style local gates only; no new rules or paths are introduced. Config edits are minimal and only in response to tool-flagged errors. |
| T-260810-SC | Tampering | npm/pip/cargo installs | high | mitigate | package-legitimacy gate: NO new packages are installed by this task — every entry is a version bump of a package already present in package.json and previously audited. No `[ASSUMED]`/`[SUS]`/`[SLOP]` entries introduced, therefore no blocking human checkpoint is required. |
</threat_model>

<verification>
Run the four tasks in order. Task 1 must leave a clean install (`npm ls` exit 0). Task 2 must migrate vitest config with zero suite regressions. Task 3 must hold typecheck and lint clean. Task 4 is the final full-pipeline gate — typecheck → lint → test → build, stop on red per RESEARCH point 9. Held packages (discord.js-selfbot-v13, @types/node 22.x, drizzle-orm, drizzle-kit, husky) must remain untouched in the final git diff.
</verification>

<success_criteria>
- `npm ls --depth=0` shows every researched target version; no ESLint 9 anywhere in the tree
- `npm run typecheck`, `npm run lint`, `npm test`, `npm run build` all pass consecutively
- vitest.config.ts contains no `poolOptions` key; no deprecation warnings
- pm2 7.0.3 and lint-staged 17.3.0 verified; ecosystem.config.cjs and the package.json lint-staged block unchanged
- Git diff touches only: package.json, package-lock.json, vitest.config.ts (plus tsconfig.json/eslint.config.mjs only if a tool-flagged error forced a minimal edit)
</success_criteria>

<output>
Create `.planning/quick/260810-f7j-upgrade-dependencies-eslint-9-to-10-eol-/260810-f7j-SUMMARY.md` when done
</output>
