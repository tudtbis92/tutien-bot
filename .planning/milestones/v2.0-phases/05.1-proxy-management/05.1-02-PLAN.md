---
phase: 05.1-proxy-management
plan: 02
type: execute
wave: 2
depends_on: ["05.1-01"]
files_modified: [src/utils/adminGuard.ts, src/commands/game/farming.ts, src/commands/admin/proxy.ts]
autonomous: true
requirements: [FARM-08]

must_haves:
  truths:
    - "Proxy admin commands are restricted to authorized Guild/User (Milestone v2 Security)"
    - "Admin can add, list, remove, and view stats for proxies"
    - "Farming registration automatically assigns a proxy from the pool"
    - "System handles re-assignment for accounts on dead proxies"
  artifacts:
    - path: "src/utils/adminGuard.ts"
      provides: "Centralized security check for admin commands"
    - path: "src/commands/admin/proxy.ts"
      provides: "Admin CLI for proxy pool management"
---

<objective>
Implement centralized admin security, proxy management commands, and integrate the proxy pool into the farming registration flow with reliability features.

Purpose: Provide administrators with tools to manage the proxy pool and ensure all users are automatically assigned a proxy for safe farming.
Output: Admin security utility, /proxy slash commands, updated /farming_setup flow, and re-assignment logic.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05.1-proxy-management/05.1-CONTEXT.md
@.planning/phases/05.1-proxy-management/05.1-RESEARCH.md
@src/commands/game/farming.ts
@src/services/farming/proxyService.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Centralized Admin Security Utility (per D-04)</name>
  <files>src/utils/adminGuard.ts, src/commands/game/farming.ts</files>
  <action>
    Create `src/utils/adminGuard.ts` implementing `isAuthorizedAdmin` with the following IDs:
    - AUTHORIZED_GUILD: '1465226886018760839'
    - AUTHORIZED_USER: '898126643598606367'
    
    Then, update `src/commands/game/farming.ts` to use this utility instead of hardcoded checks.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Security utility implemented and used in existing farming command.</done>
</task>

<task type="auto">
  <name>Task 2: Proxy Admin Commands (per D-05)</name>
  <files>src/commands/admin/proxy.ts</files>
  <action>
    Create `src/commands/admin/proxy.ts` with subcommands:
    - `add [url] [location] [provider]`: Adds a proxy to the pool.
    - `list`: Displays all proxies, their status, and usage count.
    - `remove [id]`: Sets a proxy status to 'dead'.
    - `stats`: Shows total capacity (proxies * 3) vs current usage.
    
    Apply `isAuthorizedAdmin` check at the start of the `execute` function.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>/proxy commands implemented with security guards.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate with Farming Registration (per D-02)</name>
  <files>src/commands/game/farming.ts</files>
  <action>
    Update `handleFarmingTokenModal` in `src/commands/game/farming.ts` to:
    1. Check if the user already has a `proxyId`.
    2. If not, call `ProxyService.assignProxy(userRow.id)`.
    3. Ensure the registration proceeds even if no proxy is available (log a warning), falling back to user-provided `proxyUrl` if any.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Farming registration now auto-assigns proxies from the pool.</done>
</task>

<task type="auto">
  <name>Task 4: Dead Proxy Re-assignment Logic</name>
  <files>src/services/farming/proxyService.ts, src/commands/admin/proxy.ts</files>
  <behavior>
    - When a proxy is marked as 'dead' via `/proxy remove`, trigger re-assignment for all affected accounts.
  </behavior>
  <action>
    Modify `ProxyService.ts` to include `reassignAccountsFromProxy(proxyId)`.
    Update `/proxy remove` command to call this method.
    IMPORTANT: After reassignment, the command MUST send an IPC signal (`{ type: 'FARMING_ACCOUNT_UPDATED', userId }`) to the ShardingManager for each affected user, so SelfBotMaster immediately restarts those bots on the new proxy.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>System handles re-assignment for dead proxies.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → Admin Commands | Untrusted input, strictly guarded by ID check |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.1-03 | Elevation of Privilege | Proxy Commands | mitigate | Strict ID-based white-listing (Milestone v2) |
| T-05.1-04 | Information Disclosure | Proxy List | mitigate | Command only accessible to authorized admins |
</threat_model>

<verification>
1. **Security:** Verify `/proxy` command is blocked for unauthorized users.
2. **Auto-assignment:** Run `/farming_setup`, submit token, verify `proxy_id` is set in DB.
3. **Capacity:** Add 1 proxy, register 3 users, verify 4th user gets no proxy (but still registers).
4. **Re-assignment:** Mark a proxy as 'dead', verify accounts on it are moved to a new 'active' proxy.
</verification>

<success_criteria>
- Admin security utility centralizes authorization logic.
- /proxy commands functional for authorized users.
- New farming registrations successfully get assigned a proxy from the pool.
</success_criteria>

<output>
Create `.planning/phases/05.1-proxy-management/05.1-02-SUMMARY.md` when done
</output>
