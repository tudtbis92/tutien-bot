---
phase: 06-Farming-Logic-Captcha-Handling
plan: 03
type: execute
wave: 2
depends_on: ["06-02"]
files_modified: [src/workers/selfBotWorker.ts]
autonomous: true
requirements: [FARM-03, FARM-07]

must_haves:
  truths:
    - "Self-bot detects broken gems and automatically reapplies them."
    - "Self-bot automatically sacrifices animals for Essence and upgrades Huntbot."
    - "Self-bot manages Huntbot (buy, collect, refill) automatically."
    - "Self-bot synchronizes with 'owo checklist' every 6 hours."
  artifacts:
    - path: "src/workers/selfBotWorker.ts"
      provides: "Response parsing and smart logic handlers"
---

<objective>
Implement advanced response parsing and "Smart Logic" for automated asset management (Gems, Essence, Huntbot) and checklist synchronization.

Purpose: Maximize efficiency and passive gains without requiring manual user oversight of inventory or Huntbot status.
Output: Integrated message handlers and asset management logic in selfBotWorker.ts.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-Farming-Logic-Captcha-Handling/06-02-SUMMARY.md
@src/workers/selfBotWorker.ts
@src/types/farming.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Response Parsing and Gem Logic</name>
  <files>src/workers/selfBotWorker.ts</files>
  <action>
    - Add a `messageCreate` listener to the client in `WorkerManager` (or the specific bot client).
    - Implement logic to parse OwO's responses for "gem broken" notifications.
    - If a gem breaks, trigger `owo inv` to inspect available gems.
    - Implement a parser for `owo inv` response to extract and map available gem IDs (Hunting, Lucky, Empowering) by tier.
    - Combine up to 3 preferred gems and send them in a single command `owo use [gem1_id] [gem2_id] [gem3_id]` to minimize message counts.
    - Periodically run `owo inv` (e.g., every 1h) as a fallback to ensure gems are active.
  </action>
  <verify>
    <automated># Manual verification: Simulate 'gem broken' response and verify combined owo use command in logs</automated>
  </verify>
  <done>Gem re-application logic parses inventory and combines gem usage into a single command.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Essence and Huntbot Synergy</name>
  <files>src/workers/selfBotWorker.ts</files>
  <action>
    - Parse `owo hunt` results for animal counts.
    - Implement periodic `owo sacrifice [rank] all` (e.g., every 2 hours) based on `settings.economy.sacrificeRanks`.
    - Implement `owo upgrade hb [trait]` using collected Essence according to `settings.economy.upgradePriority`.
    - Automate Huntbot management: `owo hb buy` (if expired), `owo hb collect`, and `owo hb refill` (per D-02).
  </action>
  <verify>
    <automated>grep -q "sacrifice" src/workers/selfBotWorker.ts && grep -q "hb buy" src/workers/selfBotWorker.ts</automated>
  </verify>
  <done>Automated Essence sacrifice and Huntbot management are operational.</done>
</task>

<task type="auto">
  <name>Task 3: Checklist Synchronization</name>
  <files>src/workers/selfBotWorker.ts</files>
  <action>
    - Schedule `owo checklist` every 6 hours.
    - Parse the result to identify missing daily/vote/streak tasks.
    - If `daily` is missing, execute `owo daily`.
    - For `vote`, emit a notification status (to be handled in Plan 04) since bots can't vote.
  </action>
  <verify>
    <automated>grep -q "checklist" src/workers/selfBotWorker.ts</automated>
  </verify>
  <done>Checklist synchronization ensures daily tasks are completed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Discord → Worker | Parsing incoming messages from OwO bot. Patterns must be robust against minor text changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Information Disclosure | Inventory Data | mitigate | Use regex only for needed info (IDs, counts); do not log or store full inventory state. |
| T-06-06 | Tampering | Command Triggers | mitigate | Only react to messages from the official OwO bot ID in the designated channel. |
</threat_model>

<verification>
Check logs for: "Gem broken detected, checking inventory", "Sacrificing common animals for Essence", "Huntbot collected".
</verification>

<success_criteria>
1. Broken gems are replaced using items from `owo inv`.
2. `sacrifice` and `upgrade hb` commands are sent periodically.
3. `hb collect/refill` is automated.
4. `owo daily` is triggered if missing in `checklist`.
</success_criteria>

<output>
Create `.planning/phases/06-Farming-Logic-Captcha-Handling/06-03-SUMMARY.md` when done
</output>
