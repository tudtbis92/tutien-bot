---
phase: 06-Farming-Logic-Captcha-Handling
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified: [src/workers/selfBotWorker.ts]
autonomous: true
requirements: [FARM-03, FARM-07]

must_haves:
  truths:
    - "Self-bots execute hunt, battle, pray, curse, and gamble commands."
    - "Commands use randomized delays (15-25s) with recursive setTimeout."
    - "Auto-money transfer logic executes when balance exceeds threshold."
    - "Bots periodically 'sleep' and send social chatter to mimic humans."
  artifacts:
    - path: "src/workers/selfBotWorker.ts"
      provides: "FarmingLoop class with command rotation"
---

<objective>
Implement the core farming loop and command rotation, including human simulation and automatic money management.

Purpose: Provide the foundational automation that mimics human interaction while maximizing efficiency and safety.
Output: Comprehensive FarmingLoop class in selfBotWorker.ts.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-Farming-Logic-Captcha-Handling/06-01-SUMMARY.md
@src/workers/selfBotWorker.ts
@src/types/farming.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement FarmingLoop with Command Rotation and Safety Locks</name>
  <files>src/workers/selfBotWorker.ts</files>
  <behavior>
    - Loop executes commands: 'owo hunt', 'owo battle' (primary high-frequency loop).
    - Uses recursive `setTimeout` with `Math.random()` based delays (15-25s).
    - Safe execution lock (`isCommandInProgress: boolean` or command queue) prevents overlapping commands due to network lag.
    - Decoupled scheduling: Run `pray`/`curse` on a separate 5-minute interval timer (with random jitter) instead of the primary hunt/battle loop to avoid spamming.
    - If money transfer enabled: Run check and execute `owo give [target] [amount]` when balance threshold is met.
  </behavior>
  <action>
    - Create `FarmingLoop` class in `src/workers/selfBotWorker.ts`.
    - Implement `executeCommand(cmd: string)` helper with an execution lock to prevent command overlaps.
    - Implement the core `hunt` and `battle` execution loop.
    - Implement independent timers for `pray`/`curse` and money transfer checks.
    - Add logic to track Cowoncy balance (from message parsing or `owo cash`) and trigger `owo give` per FARM-07.
    - Ensure loop stops cleanly via a `stop()` method.
  </action>
  <verify>
    <automated># Manual verification via worker logs during execution</automated>
  </verify>
  <done>FarmingLoop class supports safe command locking, decoupled long cooldowns, and money transfer.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Human Simulation (Sleep & Chatter)</name>
  <files>src/workers/selfBotWorker.ts</files>
  <action>
    - Add `periodicSleep()` logic to `FarmingLoop`: pause for 5-15 mins every 1-2 hours of continuous farming (per D-05).
    - Add `socialChatter()` logic: occasionally send random phrases (from a static list in `src/constants/chatter.ts`) to the channel (per D-02).
    - Add `rotateTeams()` logic: send `owo setteam 1/2` every 30-60 mins.
  </action>
  <verify>
    <automated>grep -q "periodicSleep" src/workers/selfBotWorker.ts && grep -q "socialChatter" src/workers/selfBotWorker.ts</automated>
  </verify>
  <done>Anti-ban/Human simulation features integrated into the farming loop.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate into WorkerManager</name>
  <files>src/workers/selfBotWorker.ts</files>
  <action>
    - Update `WorkerManager` to maintain `Map<string, FarmingLoop>`.
    - Instantiate and `start()` the loop when bot becomes 'ready'.
    - Ensure `stopBots` correctly stops and deletes the `FarmingLoop`.
  </action>
  <verify>
    <automated>grep -q "new FarmingLoop" src/workers/selfBotWorker.ts</automated>
  </verify>
  <done>WorkerManager successfully manages the lifecycle of farming loops.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Worker → Discord | Outgoing commands must appear natural to avoid bot-detection filters. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Detection | Timing Patterns | mitigate | Randomized delays (15-25s) and periodic sleep periods. |
| T-06-04 | Tampering | Command Sequence | accept | Commands are generated internally based on user settings. |
</threat_model>

<verification>
Check worker logs for entries like: "Executing: owo hunt", "Executing: owo give...", "Entering periodic sleep for 8m".
</verification>

<success_criteria>
1. `FarmingLoop` executes the full set of enabled commands.
2. Randomized delays are applied between every command.
3. Auto-money transfer logic executes correctly based on balance.
4. Social chatter and periodic sleep are observable in logs.
</success_criteria>

<output>
Create `.planning/phases/06-Farming-Logic-Captcha-Handling/06-02-SUMMARY.md` when done
</output>
