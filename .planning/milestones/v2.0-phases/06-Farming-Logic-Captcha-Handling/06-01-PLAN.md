---
phase: 06-Farming-Logic-Captcha-Handling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/db/schema/farming.ts, src/types/farming.ts, src/workers/selfBotMaster.ts, src/bot.ts]
autonomous: true
requirements: [FARM-07]

must_haves:
  truths:
    - "The farming_accounts table has a settings JSONB column."
    - "Farming settings have a well-defined TypeScript interface."
    - "SelfBotMaster receives the ShardingManager instance to broadcast events."
    - "SelfBotMaster sends settings payload to worker processes."
  artifacts:
    - path: "src/db/schema/farming.ts"
      provides: "Updated schema with settings column"
    - path: "src/types/farming.ts"
      provides: "FarmingSettings interface"
    - path: "src/workers/selfBotMaster.ts"
      provides: "Updated IPC and manager integration"
---

<objective>
Establish the data structure and infrastructure for persistent farming configurations and IPC-based status reporting.

Purpose: Allow users to customize their farming behavior and enable the master process to notify shards about security events.
Output: Migration file, setting type definitions, and enhanced SelfBotMaster.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/db/schema/farming.ts
@src/workers/selfBotMaster.ts
@src/bot.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define FarmingSettings Type, Constants and Update Schema</name>
  <files>src/types/farming.ts, src/db/schema/farming.ts</files>
  <action>
    - Create `src/types/farming.ts` and define `FarmingSettings` interface based on D-02 in 06-CONTEXT.md.
    - Define official `OWO_BOT_ID = '408785106942164992'` as a constant in `src/types/farming.ts`.
    - Add `moneyTransfer` field to settings: `{ enabled: boolean, mainAccountId: string, threshold: number }` per FARM-07.
    - Add `settings` column to `farmingAccounts` table in `src/db/schema/farming.ts` using `jsonb('settings').$type<FarmingSettings>()`.
    - Provide a `DEFAULT_SETTINGS` object in `src/types/farming.ts`.
    - Run `npm run db:generate` and `npm run db:migrate` (ensure Drizzle kit is configured).
  </action>
  <verify>
    <automated>npm run db:check</automated>
  </verify>
  <done>Settings type, constants defined and DB schema updated with migration applied.</done>
</task>

<task type="auto">
  <name>Task 2: Update SelfBotMaster for Manager Integration and Settings IPC</name>
  <files>src/workers/selfBotMaster.ts, src/bot.ts</files>
  <action>
    - Update `SelfBotMaster.start()` to accept `manager: ShardingManager`.
    - Update `src/bot.ts` to pass the manager instance to `SelfBotMaster.getInstance().start(manager)`.
    - Update `SelfBotMaster.rebalance()` to fetch the `settings` column.
    - Update the `START_BOTS` payload to include `settings` for each bot.
    - Add `CAPTCHA_DETECTED` status handling in `handleWorkerStatus`:
      - Update DB status to `captcha_waiting` (per D-03).
      - Trigger `manager.broadcast({ type: 'NOTIFY_CAPTCHA', userId: botId })`.
  </action>
  <verify>
    <automated>grep -q "manager: ShardingManager" src/workers/selfBotMaster.ts && grep -q "NOTIFY_CAPTCHA" src/workers/selfBotMaster.ts</automated>
  </verify>
  <done>SelfBotMaster integrated with ShardingManager and passing settings to workers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB → Master | Settings JSONB must conform to FarmingSettings interface. |
| Master → Worker | IPC payload contains sensitive settings (though mostly behavioral). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | Settings in DB | mitigate | Use Drizzle-safe JSONB; ensure no tokens are stored in the settings blob itself. |
| T-06-02 | Tampering | IPC Payload | accept | Local process IPC is trusted in this environment. |
</threat_model>

<verification>
Check that `src/types/farming.ts` exists. Verify `farming_accounts` has `settings` column via DB tool. Check `SelfBotMaster.ts` for `manager.broadcast` call.
</verification>

<success_criteria>
1. `FarmingSettings` interface matches requirement (Gems, Essence, Huntbot, MoneyTransfer).
2. Database migration adds `settings` column.
3. `SelfBotMaster` passes settings to workers.
4. `SelfBotMaster` broadcasts `NOTIFY_CAPTCHA` on captcha detection.
</success_criteria>

<output>
Create `.planning/phases/06-Farming-Logic-Captcha-Handling/06-01-SUMMARY.md` when done
</output>
