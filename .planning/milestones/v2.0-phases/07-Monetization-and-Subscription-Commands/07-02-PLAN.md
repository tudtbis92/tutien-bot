---
phase: 07-Monetization-and-Subscription-Commands
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified: [src/commands/game/farming.ts, src/commands/game/__tests__/farming.test.ts]
autonomous: true
requirements: [FARM-02, FARM-05]

must_haves:
  truths:
    - "User can buy subscriptions via /farming buy or setup buttons"
    - "User can see their subscription status in /farming status"
    - "SelfBotMaster is notified when a subscription is updated"
  artifacts:
    - path: "src/commands/game/farming.ts"
      provides: "Updated /farming command with purchase logic"
---

<objective>
Implement the user-facing interactions for purchasing and managing subscriptions, ensuring the UI reflects the user's current status and triggers worker reloads.

Purpose: Provide the UX for spending Linh Thạch on farming services.
Output: Updated /farming command with buy/status/setup functionality and supporting tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-Monetization-and-Subscription-Commands/07-01-SUMMARY.md
@src/commands/game/farming.ts
@src/services/farming/subscriptionService.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement /farming buy & Purchase Handlers</name>
  <files>src/commands/game/farming.ts, src/commands/game/__tests__/farming.test.ts</files>
  <behavior>
    - Command registration: `/farming buy` subcommand exists.
    - Button click 'buy_weekly' -> calls `FarmingSubscriptionService.purchasePlan(userId, 'basic', 7)`.
    - Button click 'buy_monthly' -> calls `FarmingSubscriptionService.purchasePlan(userId, 'basic', 30)`.
    - Button click 'upgrade_vip' -> calls `FarmingSubscriptionService.upgradePlan(userId)` with calculated upgrade fee.
    - Post-purchase: `process.send` is called with `type: 'FARMING_ACCOUNT_UPDATED'`.
    - **2-step confirmation flow** (all purchase/upgrade actions):
      1. User clicks buy/upgrade button → Show confirmation embed with: price, current balance, plan details, and overwrite warning (if active plan exists: "⚠️ Mua gói mới sẽ thay thế gói hiện tại").
      2. User clicks "Xác nhận" button → Execute transaction → Show success/error response.
  </behavior>
  <action>
    - Add `/farming buy` subcommand to `SlashCommandBuilder` in `src/commands/game/farming.ts`.
    - Implement `handleFarmingBuyWeeklyButton`, `handleFarmingBuyMonthlyButton`, and `handleFarmingUpgradeVIPButton`.
    - **Add `farming:upgrade_vip` button** to the `/farming setup` embed — display conditionally only when user has an active basic plan.
    - Implement 2-step confirmation flow: first show price/overwrite info, then execute on confirm.
    - Create `src/commands/game/__tests__/farming.test.ts` to mock the service and verify button interactions.
  </action>
  <verify>
    <automated>npm test src/commands/game/__tests__/farming.test.ts</automated>
  </verify>
  <done>User can successfully purchase plans via buttons and commands, verified by unit tests.</done>
</task>

<task type="auto">
  <name>Task 2: Update /farming status & setup UI</name>
  <files>src/commands/game/farming.ts</files>
  <action>
    - Update `execute` (status subcommand) to fetch and display subscription info from `farming_subscriptions`.
    - Update `execute` (setup subcommand) to show current subscription status in the embed.
    - Format dates using `dayjs.utc()` and use localized strings from `game.json`.
    - Show `farming:upgrade_vip` button in setup embed conditionally (only when user has active basic plan).
  </action>
  <verify>
    <automated>npm run lint src/commands/game/farming.ts</automated>
  </verify>
  <done>Subscription status and expiry are visible in status and setup interfaces.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → Discord UI | Interaction with buttons/commands |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | Information Disclosure | Expiry Date | accept | Expiry date is public to the user themselves; no PII involved. |
</threat_model>

<verification>
- Run command logic tests: `npm test src/commands/game/__tests__/farming.test.ts`.
- Ensure all strings are localized and appear correctly in the mock embeds.
</verification>

<success_criteria>
- `/farming buy` is functional.
- `/farming status` shows plan type and expiry.
- IPC notification is sent on every successful purchase.
- 2-step confirmation flow prevents accidental purchases.
- VIP upgrade button appears conditionally in setup embed.
- Command logic is covered by unit tests.
</success_criteria>

<output>
Create `.planning/phases/07-Monetization-and-Subscription-Commands/07-02-SUMMARY.md` when done
</output>
