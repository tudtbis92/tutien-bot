---
phase: 07-Monetization-and-Subscription-Commands
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified: [src/workers/selfBotMaster.ts, src/services/farming/__tests__/integration.test.ts]
autonomous: true
requirements: [FARM-02, FARM-05, FARM-07]

must_haves:
  truths:
    - "Self-bot workers are correctly sanitized based on subscription tier"
    - "Expired subscriptions stop the self-bot worker"
    - "Free plan users are never started"
    - "Full purchase-to-worker-reload flow is verified"
  artifacts:
    - path: "src/workers/selfBotMaster.ts"
      provides: "Subscription-aware rebalance logic"
---

<objective>
Integrate the subscription system into the core worker management loop and perform end-to-end verification to ensure tiers are enforced and expired bots are stopped.

Purpose: Finalize the enforcement layer of the monetization system.
Output: Integrated SelfBotMaster and comprehensive integration test suite.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-Monetization-and-Subscription-Commands/07-01-SUMMARY.md
@.planning/phases/07-Monetization-and-Subscription-Commands/07-02-SUMMARY.md
@.planning/phases/07-Monetization-and-Subscription-Commands/07-VALIDATION.md
@src/workers/selfBotMaster.ts
@src/services/farming/subscriptionService.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate Subscription Logic into SelfBotMaster</name>
  <files>src/workers/selfBotMaster.ts</files>
  <action>
    - Update `SelfBotMaster.rebalance()`:
      - Join `farmingAccounts` with `farming_subscriptions`.
      - Filter accounts: exclude `planType === 'free'` AND exclude expired subscriptions (`expiresAt <= now()`).
      - Only start bots for users with active, non-free, non-expired subscriptions.
      - If a user has an active subscription but it's not 'premium', call `FarmingSubscriptionService.sanitizeFarmingSettings` before starting the worker.
      - If subscription expired or is 'free', the worker should be stopped (not started in the current rebalance cycle).
    - Ensure `loadOrUpdateAccount` triggers rebalance.
    - **BREAKING CHANGE WARNING:** Existing users without a `farming_subscriptions` record will lose service. Document this in deploy notes — admin should announce before deployment.
  </action>
  <verify>
    <automated>npm run lint src/workers/selfBotMaster.ts</automated>
  </verify>
  <done>SelfBotMaster correctly enforces subscription tiers and expiry.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: End-to-End Integration Tests</name>
  <files>src/services/farming/__tests__/integration.test.ts</files>
  <behavior>
    - Test Flow 1: User buys Basic -> Master starts bot with Hunt/Battle only (sanitized).
    - Test Flow 2: User upgrades to VIP -> Master restarts bot with all features enabled (unsanitized).
    - Test Flow 3: Subscription expires -> Master stops bot on next rebalance.
    - Test Flow 4: Free plan user (no purchase) -> Master does NOT start bot.
    - Test Flow 5: User overwrites existing plan -> old plan replaced, worker restarted with new settings.
    - Test Flow 6: Concurrent purchase (rapid button click) -> only one deduction succeeds.
  </behavior>
  <action>
    - Create `src/services/farming/__tests__/integration.test.ts`.
    - Mock `ChildProcess.send` to verify the payload sent to workers.
    - Verify database state matches expected outcomes.
    - Reference `07-VALIDATION.md` for specific test cases TC-07-01 to TC-07-13.
  </action>
  <verify>
    <automated>npm test src/services/farming/__tests__/integration.test.ts</automated>
  </verify>
  <done>Integration tests confirm the full monetization lifecycle works as intended.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Master → Worker | Trusted instruction set with sanitized settings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Tampering | Farming Settings | mitigate | Enforce sanitization in the Master process (trusted) before sending to Workers. |
</threat_model>

<verification>
- Run full test suite: `npm test`.
- Verify that a bot with an expired subscription is correctly removed from the active pool in `rebalance()`.
</verification>

<success_criteria>
- Tier enforcement is active and verified.
- Free plan users are never started by rebalance.
- Expiry handling is automatic via the rebalance loop.
- All integration tests pass (including flows 4-6).
</success_criteria>

<output>
Create `.planning/phases/07-Monetization-and-Subscription-Commands/07-03-SUMMARY.md` when done
</output>
