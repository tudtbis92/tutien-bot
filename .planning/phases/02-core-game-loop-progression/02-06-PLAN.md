---
phase: 02-core-game-loop-progression
plan: "06"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/commands/game/nghenghiep.ts
  - src/ui/embeds/buildProfessionEmbed.ts
autonomous: true
requirements:
  - PROG-05
  - PROG-08

must_haves:
  truths:
    - "/nghề_nghiệp view shows all 10 professions with current skill point allocation and remaining allocatable points"
    - "/nghề_nghiệp allocate <profession> <amount> deducts from available points and adds to the specified profession"
    - "Total available skill points = realm_id (lifetime total per D-24); total allocated cannot exceed realm_id"
    - "Allocating to an unknown profession key returns a validation error"
    - "profession_points JSONB is validated with ProfessionPointsSchema (Zod) on every read"
  artifacts:
    - path: "src/commands/game/nghenghiep.ts"
      provides: "/nghề_nghiệp slash command (view + allocate subcommands)"
      exports: ["data", "execute"]
    - path: "src/ui/embeds/buildProfessionEmbed.ts"
      provides: "typed embed for profession allocation display"
      exports: ["buildProfessionEmbed"]
  key_links:
    - from: "src/commands/game/nghenghiep.ts"
      to: "src/types/professions.ts"
      via: "ProfessionPointsSchema.safeParse() + getProfessionLevel()"
      pattern: "ProfessionPointsSchema\|getProfessionLevel"
    - from: "src/commands/game/nghenghiep.ts"
      to: "src/db/schema/characters.ts"
      via: "UPDATE characters SET profession_points WHERE id = characterId"
      pattern: "professionPoints"
---

<objective>
Implement the profession system: a /nghề_nghiệp command with view and allocate subcommands, backed by the Zod-validated JSONB profession_points column on characters. Each realm tier advanced grants 1 skill point (lifetime total = realm_id).

Purpose: Professions are the long-term character specialization system; skill points create meaningful choices that build toward gathering and crafting capabilities.
Output: /nghề_nghiệp command (2 subcommands) + buildProfessionEmbed.ts.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-core-game-loop-progression/02-CONTEXT.md
@.planning/phases/02-core-game-loop-progression/02-RESEARCH.md
@.planning/phases/01-foundation/01-SUMMARY.md

<interfaces>
From src/types/professions.ts (Plan 01):
```typescript
export const PROFESSION_KEYS = ['luyen_dan','luyen_khi_nc','tran_phap','linh_tru','luyen_co',
  'duoc_su','thuan_thu','luyen_kim','khai_linh','thuat_su'] as const;
export type ProfessionKey = typeof PROFESSION_KEYS[number];
export const ProfessionPointsSchema: z.ZodObject<...>;
export type ProfessionPoints = z.infer<typeof ProfessionPointsSchema>;
export function getProfessionLevel(raw: unknown, key: ProfessionKey): number;
export function getTotalProfessionPoints(points: ProfessionPoints): number;
```

From src/constants/itemAttributes.ts (Plan 01):
```typescript
export const PROFESSION_UNIQUE_ARCHETYPES;  // 10 entries with professionType + uniqueItemNameI18nKey
```

From src/db/schema/characters.ts (Plan 01):
```typescript
// realmId: SMALLINT — total available points = realmId (lifetime)
// professionPoints: JSONB — allocated points per profession
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: /nghề_nghiệp command (view + allocate subcommands) + buildProfessionEmbed.ts</name>
  <files>
    src/commands/game/nghenghiep.ts,
    src/ui/embeds/buildProfessionEmbed.ts
  </files>
  <read_first>
    - src/commands/game/ping.ts (command export pattern)
    - src/types/professions.ts (PROFESSION_KEYS, ProfessionPointsSchema, getProfessionLevel, getTotalProfessionPoints)
    - src/db/schema/characters.ts (profession_points JSONB column, realmId)
    - src/ui/theme.ts (COLORS, embedFooter)
    - locales/vi/game.json (profession.title, profession.points_remaining, profession.no_points, profession.allocated, profession.insufficient_points, profession.names.luyen_dan etc.)
    - src/constants/itemAttributes.ts (PROFESSION_UNIQUE_ARCHETYPES for embed display — show unique item archetype name per profession)
    - .planning/phases/02-core-game-loop-progression/02-CONTEXT.md (D-22..D-25: profession rules, D-24: points = realm_id, no respec)
  </read_first>
  <action>
    **src/ui/embeds/buildProfessionEmbed.ts:**
    ```typescript
    export interface ProfessionEmbedData {
      points: ProfessionPoints;
      realmId: number;  // Total available points = realmId
    }

    export function buildProfessionEmbed(data: ProfessionEmbedData, t: TFunction): EmbedBuilder
    ```
    - Title: `t('game:profession.title')`
    - Total available = data.realmId; totalAllocated = getTotalProfessionPoints(data.points)
    - Remaining: totalAvailable - totalAllocated
    - Add field: `t('game:profession.points_remaining', { points: remaining })`
    - For each of the 10 PROFESSION_KEYS: add a field showing `profName: X pts | UniqueItemType`
      - profName from `t(\`game:profession.names.\${key}\`)`
      - Points from `getProfessionLevel(data.points, key)`
      - UniqueItemType from PROFESSION_UNIQUE_ARCHETYPES — find entry by professionType, use uniqueItemNameI18nKey
    - Use COLORS.PRIMARY, embedFooter(), display as 2-column inline fields

    **src/commands/game/nghenghiep.ts:**
    SlashCommandBuilder with name 'nghề_nghiệp' and TWO subcommands:
    1. `addSubcommand(sub => sub.setName('xem').setDescription('Xem nghề nghiệp của bạn'))`
    2. `addSubcommand(sub => sub.setName('phân_bổ').setDescription('Phân bổ điểm kỹ năng').addStringOption(opt => opt.setName('nghề').setDescription('Tên nghề nghiệp').setRequired(true).addChoices( ...PROFESSION_KEYS.map(k => ({ name: t_fallback(k), value: k })) )).addIntegerOption(opt => opt.setName('điểm').setDescription('Số điểm').setMinValue(1).setRequired(true)))`

    Note: slash command builder choices can be defined at module load time using a static mapping.

    **execute() function:**
    1. `await interaction.deferReply()`
    2. Resolve locale → t
    3. SELECT character WHERE discordId — if not found: error embed
    4. Parse professionPoints: `const points = ProfessionPointsSchema.safeParse(char.professionPoints ?? {}).data ?? {}`
    5. If subcommand === 'xem': build profession embed, reply
    6. If subcommand === 'phân_bổ':
       - `const prof = interaction.options.getString('nghề', true) as ProfessionKey`
       - Validate: `if (!PROFESSION_KEYS.includes(prof))` → error (shouldn't happen with choices, but belt+suspenders)
       - `const amount = interaction.options.getInteger('điểm', true)`
       - Total available = char.realmId; totalAllocated = getTotalProfessionPoints(points)
       - If totalAllocated + amount > char.realmId → error: `t('game:profession.insufficient_points')`
       - Update: `{ ...points, [prof]: (points[prof] ?? 0) + amount }`
       - `UPDATE characters SET profession_points = $newPoints WHERE id = char.id`
       - Reply: `t('game:profession.allocated', { amount, profession: t(\`game:profession.names.\${prof}\`) })`
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1</automated>
    <automated>npm run lint 2>&1</automated>
    <automated>npm run check-i18n 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - src/commands/game/nghenghiep.ts: `grep "addSubcommand" src/commands/game/nghenghiep.ts` returns 2 matches (xem + phân_bổ)
    - src/commands/game/nghenghiep.ts: `grep "ProfessionPointsSchema" src/commands/game/nghenghiep.ts` returns match (Zod validation)
    - src/commands/game/nghenghiep.ts: `grep "getTotalProfessionPoints\|totalAllocated" src/commands/game/nghenghiep.ts` returns match
    - src/commands/game/nghenghiep.ts: `grep "insufficient_points" src/commands/game/nghenghiep.ts` returns match (point cap guard)
    - src/commands/game/nghenghiep.ts: `grep "realmId" src/commands/game/nghenghiep.ts` returns match (points = realmId)
    - src/ui/embeds/buildProfessionEmbed.ts: `grep "PROFESSION_KEYS\|getProfessionLevel" src/ui/embeds/buildProfessionEmbed.ts` returns match
    - `npx tsc --noEmit` exits 0; `npm run lint` exits 0; `npm run check-i18n` exits 0
  </acceptance_criteria>
  <done>/nghề_nghiệp xem shows 10 professions with allocation; /nghề_nghiệp phân_bổ validates points cap (realm_id), updates JSONB atomically; Zod validates on every read; TypeScript and lint pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → JSONB write | Slash option values written into JSONB column |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-PROF-01 | Tampering | profession_points JSONB overflow | mitigate | totalAllocated + amount <= char.realmId enforced before write; Zod validates on read |
| T-02-PROF-02 | Tampering | Unknown profession key injection | mitigate | SlashCommandBuilder choices constrain input to PROFESSION_KEYS; runtime PROFESSION_KEYS.includes() check as backstop |
| T-02-PROF-03 | Tampering | Negative point allocation | mitigate | addIntegerOption.setMinValue(1) — Discord rejects < 1; no respec means points only increase |
| T-02-PROF-04 | Tampering | JSONB $type bypass | mitigate | ProfessionPointsSchema.safeParse() validates ALL reads from DB; unknown keys stripped; NaN coerced to 0 |
</threat_model>

<verification>
1. User at realmId=5 → /nghề_nghiệp xem → embed shows 5 available points, all professions at 0
2. /nghề_nghiệp phân_bổ nghề:luyen_dan điểm:3 → allocates 3, remaining = 2
3. /nghề_nghiệp phân_bổ nghề:luyen_kim điểm:3 → error "insufficient points" (only 2 remain)
4. /nghề_nghiệp xem → shows luyen_dan: 3, luyen_kim: 0, others: 0
5. `npx tsc --noEmit`, `npm run lint`, `npm run check-i18n` pass
</verification>

<success_criteria>
- 10 professions visible in embed with point allocations
- Total points capped at char.realmId (never over-allocate)
- No respec (allocate only increases, never decreases)
- JSONB validated with Zod on every read — NaN/unknown keys never reach game logic
- All profession names come from i18n (no hardcoded strings)
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-game-loop-progression/02-06-SUMMARY.md`
</output>
