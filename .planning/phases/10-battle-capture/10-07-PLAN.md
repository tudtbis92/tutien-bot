---
phase: 10-battle-capture
plan: 07
type: execute
wave: 4
depends_on:
  - 10-04
  - 10-06
files_modified:
  - src/commands/sanguo/heroes.ts
  - src/commands/sanguo/hero.ts
  - src/commands/sanguo/map.ts
  - src/events/interactionCreate.ts
  - src/ui/embeds/buildSanguoHeroesEmbed.ts
  - src/ui/embeds/buildSanguoHeroEmbed.ts
  - src/ui/components/sanguoStarterButtons.ts
  - src/ui/components/sanguoHeroesZoneMenu.ts
  - src/ui/components/sanguoHeroCompanionButton.ts
  - locales/vi/sanguo.json
  - locales/en/sanguo.json
  - locales/zh-cn/sanguo.json
  - src/commands/sanguo/__tests__/heroes.test.ts
  - src/commands/sanguo/__tests__/hero.test.ts
autonomous: true
requirements:
  - TQC-12
  - TQC-13

estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 3
  confidence: low

assumption_delta: "no-change — orchestrator-confirmed: the 'can CHOOSE 1 free starter' copy is a vocabulary false-positive, not a config-model transition."

must_haves:
  truths:
    - "`/sanguo heroes` on an EMPTY collection renders the starter picker (the onboarding surface — D-14): exactly 3 buttons (set 1 = Tào Tháo / Lưu Bị / Tôn Kiên, `sanguo:heroes:starter:{heroId}`) in ONE ActionRow + `heroes.empty_title/body` copy. On the 4th+ invocation without picking (starter_views counter ≥ 3 in user_sanguo_state) the pool ROTATES to set 2 (Trương Giác / Viên Thiệu / Đổng Trác) — no 4th option ever exists in set 1 (UI-SPEC zero-one-many contract)."
    - "Picking a starter grants the hero FREE (NO wallet call — the ONLY faucet, D-19 exception): 6× crypto.randomInt(0,32) IVs rolled at pick (same distribution as capture), hp_current = base HP, user_sanguo_state.activeHeroId set to the new hero (becomes the active companion), starter_views reset."
    - "`/sanguo heroes` on a NON-empty collection renders the owned-only collection grouped by zone: one line per hero `{{emoji}} {{name}} • {{stars}} • {{grade}}{{active}}` — stars = public heroes.tier (★1-5, NEVER rarity), grade = IV grade keys (Hoàng Kim/Hồng ngọc/Lam cấp/Lục cấp/Hôi cấp), active badge ⭐ on exactly one hero (D-12 never-render: no raw IV numbers, no rarity); title count reflects the filtered total; zone filter select (sanguo:heroes:zone) in its OWN ActionRow (CR-09-01)."
    - "`/sanguo hero {hero}` renders the detail: emoji, per-locale name, stars, IV grade, HP/MP (base-only, D-05), companion status label (⭐ Hero đồng hành), and the 'Chọn làm hero đồng hành' button (disabled when already active — D-16); a hero the user does NOT own renders `hero.error` (DANGER)."
    - "The companion switch (sanguo:hero:companion:{heroId}) updates user_sanguo_state.activeHeroId in a FOR UPDATE transaction — the switch is the D-04 recovery path when the active hero is fainted."
    - "`/sanguo map` now renders the player's ACTUAL current position (from player_travel_state via getCurrentPosition) in the current_position field — TQC-13 SC5 ('/sanguo map scaffold shows current position')."
    - "All collection/hero/starter strings live in the sanguo namespace (heroes.* / hero.* / cmd.heroes.* / cmd.hero.*) with identical key sets across vi/en/zh-cn (check-i18n); embeds use theme.ts COLORS only (SEASON default, DANGER errors, SUCCESS starter acquired); every embed has embedFooter(shardId) + setTimestamp()."
  artifacts:
    - src/commands/sanguo/heroes.ts — heroesSubcommand + execute + handleStarterPick + handleZoneFilterSelect
    - src/commands/sanguo/hero.ts — heroSubcommand + execute + handleCompanionPress
    - src/ui/embeds/buildSanguoHeroesEmbed.ts — collection + starter-picker states
    - src/ui/embeds/buildSanguoHeroEmbed.ts — detail
    - src/ui/components/sanguoStarterButtons.ts — STARTER_PICK_PREFIX + buildStarterButtons + STARTER_SET_1/STARTER_SET_2
    - src/ui/components/sanguoHeroesZoneMenu.ts — ZONE_MENU_ID + buildZoneFilterMenu
    - src/ui/components/sanguoHeroCompanionButton.ts — COMPANION_PREFIX + buildCompanionButton
    - src/commands/sanguo/map.ts — heroes/hero subcommands + current-position fix
    - src/events/interactionCreate.ts — sanguo:heroes:*/sanguo:hero:* routes
    - locales/{vi,en,zh-cn}/sanguo.json — heroes.*/hero.*/cmd.heroes.*/cmd.hero.* keys
  key_links:
    - "starter picker ↔ user_sanguo_state.starterViews — the D-14 rotation counter: every /sanguo heroes invocation while the collection is empty increments it; ≥3 → set 2; a pick resets it."
    - "starter grant ↔ 10-05 IV roll discipline — crypto.randomInt(0,32)×6, same as capture (TQC-12); the insert mirrors captureService's user_heroes write (hp_current = base HP)."
    - "collection line ↔ heroes.tier (public) + IV grade — stars from tier, grade from the iv_* columns via the grade bands (STATE.md:79); rarity never read (D-12)."
    - "companion switch ↔ battleCheckInService HP gate (10-05) — switching the active companion changes which hero's HP gates the next battle (D-04)."
  prohibitions:
    - "No raw IV numbers, IV sum, or rarity rendered anywhere in the collection/hero/starter surfaces — grade keys and tier stars only (D-12 hard rule)."
    - "No wallet call on the starter pick — the faucet is FREE (D-19 exception); any balance deduction here is a defect."
    - "No 4th starter option in set 1 and no more than 3 buttons per starter row (UI-SPEC zero-one-many)."
    - "No zone-filter value injection: the select value is a validated zone code; unknown/empty values fall back to the full collection or the filtered-empty state — never a crash."
    - "No second file exporting data with name 'sanguo' (Pitfall 3 — heroes.ts/hero.ts export subcommand builders only; map.ts owns the command)."
  flagged_assumptions:
    - "FLAGGED ASSUMPTION (probe TQC-13 empty, unresolved): an empty collection WITH a zone filter selected is not a starter-picker trigger (the picker shows only when the collection is entirely empty) — the filtered view renders the empty-hint line; the starter picker is the ONLY empty-collection state."
    - "FLAGGED ASSUMPTION (probe TQC-13 ordering, unresolved): collection lines are ordered by heroes.id ascending (seed order — stable across renders); heroes with equal ids cannot exist (serial PK), so output order is always stable; the zone filter select lists zones in map_zones.sortOrder."
    - "FLAGGED ASSUMPTION (probe TQC-13 adjacency, unresolved): the zone filter value is matched EXACTLY against user_heroes.captured_zone (A5 snapshot) — heroes with NULL captured_zone (starter grants) appear under a 'uncaptured' bucket label or in the unfiltered view only; documented in the embed."
    - "FLAGGED ASSUMPTION (D-14 rotation): the starter_views counter increments on EVERY /sanguo heroes invocation while the collection is empty, INCLUDING the 3 set-1 calls — the 4th call shows set 2; picking from set 2 is allowed (both sets are valid starters)."
    - "FLAGGED ASSUMPTION (UI-SPEC backstop, heroes-collection loading): the collection fetch is an async DB read rendered via deferReply → editReply within the 3s window — held-out interaction test (no skeleton exists on Discord)."
    - "FLAGGED ASSUMPTION (SC5 map position): map.ts's current_position field switches from rows[0] to getCurrentPosition(user.id) — the player's real position (TQC-13); the map zone markers/content stay unchanged."

---

<objective>
What this plan accomplishes: **the collection, starter onboarding, and hero-detail surfaces** — the final link of the vertical loop. `/sanguo heroes` (TQC-13 collection + D-14 starter picker when empty + D-15 zone filter), `/sanguo hero` (D-16 detail + companion switch), the starter-grant faucet (TQC-12 — the ONLY faucet, free), the zone filter select and starter/companion buttons, the map current-position fix (TQC-13 SC5), the router routes, and the full `heroes.*`/`hero.*` i18n key sets.

Purpose: after capture, the player must SEE their collection (stars + IV grade, hidden mechanics intact) and manage the active companion (D-04 HP persistence). The starter picker is the game's only faucet — onboarding IS the collection command's empty state.

Output: heroes/hero commands + embeds + components + routes + i18n + tests.
</objective>

<execution_context>
@C:/Users/901107/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/901107/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-battle-capture/10-CONTEXT.md
@.planning/phases/10-battle-capture/10-RESEARCH.md
@.planning/phases/10-battle-capture/10-PATTERNS.md
@.planning/phases/10-battle-capture/10-UI-SPEC.md
@src/commands/sanguo/map.ts
@src/commands/sanguo/travel.ts
@src/events/interactionCreate.ts
@src/ui/embeds/buildSanguoMapEmbed.ts
@src/ui/components/sanguoTravelDestinationMenu.ts
@src/ui/theme.ts
@src/assets/sanguoEmojis.ts
@src/db/schema/userHeroes.ts
@src/db/schema/userSanguoState.ts
@src/db/schema/heroes.ts
@src/services/sanguo/travelService.ts
@locales/vi/sanguo.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: /sanguo heroes — collection embed + starter picker (D-14 rotation) + zone filter + starter grant (free faucet)</name>
  <files>src/commands/sanguo/heroes.ts, src/ui/embeds/buildSanguoHeroesEmbed.ts, src/ui/components/sanguoStarterButtons.ts, src/ui/components/sanguoHeroesZoneMenu.ts, src/commands/sanguo/__tests__/heroes.test.ts</files>
  <read_first>
    - src/commands/sanguo/travel.ts (command module pattern: subcommand builder, execute, select/button handlers)
    - src/ui/embeds/buildSanguoMapEmbed.ts (collection-style embed with fields + per-locale names)
    - src/ui/components/sanguoTravelDestinationMenu.ts (StringSelectMenu pattern — emoji via option.setEmoji, stable values, own ActionRow)
    - src/db/schema/userHeroes.ts + userSanguoState.ts + heroes.ts (rows read/written: iv_*, hp_current, captured_zone, tier, rarity-hidden)
    - src/assets/sanguoEmojis.ts (heroEmoji() with the EMOJI_NOT_FOUND guard — map.ts:98 pattern)
    - .planning/phases/10-battle-capture/10-RESEARCH.md §Pattern 3 (IV grade — lines 289-291, 418-424) + §Common Operation 2 (crypto IV roll)
    - .planning/phases/10-battle-capture/10-PATTERNS.md §heroes.ts (lines 234-289) + §starter buttons/zone menu (lines 328-363)
    - .planning/phases/10-battle-capture/10-UI-SPEC.md §Component & CustomId Contract (sanguo:heroes:starter:{heroId}, sanguo:heroes:zone) + §Copywriting Contract (heroes.* keys)
    - .planning/STATE.md line 79 (IV grade bands: 100=gold, 90-99=ruby, 80-89=sapphire, 60-79=jade, <60=gray)
  </read_first>
  <behavior>
    - Test 1: execute with an EMPTY collection renders the starter picker (heroes.empty_title/body + exactly 3 starter buttons, set 1) and increments user_sanguo_state.starterViews; on the 4th empty invocation (views ≥ 3) the pool is set 2 (Trương Giác/Viên Thiệu/Đổng Trác).
    - Test 2: handleStarterPick grants the hero FREE: inserts user_heroes with 6 IVs each in [0,31] (injected crypto), hp_current = the hero's base HP, no wallet call (grep-verified — the ONLY faucet), sets user_sanguo_state.activeHeroId, resets starterViews, and replies the SUCCESS 'starter acquired' embed.
    - Test 3: execute with a NON-empty collection renders one line per owned hero: emoji + per-locale name + ★ stars (from heroes.tier) + IV grade key + active badge on exactly one hero; NO raw IV number and NO rarity anywhere in the embed data (D-12).
    - Test 4: the zone filter select (sanguo:heroes:zone) is in its OWN ActionRow (CR-09-01); selecting a zone re-renders the collection with only that zone's heroes and the title count reflecting the filtered total.
    - Test 5: a collection with 1 hero and with many heroes render the same line format; the title count matches; exactly one active badge.
  </behavior>
  <action>
    Create `src/ui/components/sanguoStarterButtons.ts`:
    - `export const STARTER_PICK_PREFIX = 'sanguo:heroes:starter';`
    - `buildStarterButtons(t, heroes: { heroId: string; name: string; emoji?: string }[]): ButtonBuilder[]` — exactly 3 buttons (caller passes set 1 or set 2); customId `${STARTER_PICK_PREFIX}:${heroId}`; label `t('sanguo:heroes.starter_button', { name })`; emoji via `.setEmoji(heroEmoji(heroId))` with the try/catch name-only guard.
    - `export const STARTER_SET_1 = ['cao_cao', 'liu_bei', 'sun_jian']` and `STARTER_SET_2 = ['truong_giac', 'yuan_shao', 'dong_trac']` (heroId strings — verified against sanguo-classifications.json; set 2 uses truong_giac/dong_trac, NOT zhang_jue/dong_zhuo which do not exist — P10-review F2; D-14 names locked).

    Create `src/ui/components/sanguoHeroesZoneMenu.ts`:
    - `export const ZONE_MENU_ID = 'sanguo:heroes:zone';`
    - `buildZoneFilterMenu(t, zones: { code: string; label: string; emoji?: string }[]): StringSelectMenuBuilder` — `.setCustomId(ZONE_MENU_ID).setPlaceholder(t('sanguo:heroes.zone_filter')).setMinValues(1).setMaxValues(1)`; options: label = per-locale zone name (map_zones, D-07), value = stable zone code, emoji via `option.setEmoji` NEVER in the label (CR-09-02/03).

    Create `src/ui/embeds/buildSanguoHeroesEmbed.ts`:
    - `export interface SanguoHeroesEmbedData { count: number; lines: { emoji?: string; name: string; stars: string; gradeKey: string; active: boolean }[]; zoneLabel?: string; shardId?: number }`.
    - `buildSanguoHeroesEmbed(data, t)` — two states: starter picker (empty): `.setColor(COLORS.SEASON)`, title `t('sanguo:heroes.empty_title')`, description `t('sanguo:heroes.empty_body')`; collection: `.setColor(COLORS.SEASON)`, title `t('sanguo:heroes.title', { count })`, field value = one line per hero via `t('sanguo:heroes.line', { emoji, name, stars, grade: t(gradeKey), active })`, ≤ 1,024-char field budget (zone filter bounds rows — the mandated overflow control); footer embedFooter + timestamp.
    - IV grade keys: `iv_grade.gold/ruby/sapphire/jade/gray` (STATE.md bands) — add the `iv_grade` section to the locale files in Task 3's locale edit if not already present.
    - Stars string: `'★'.repeat(tier)` (★1-5 from heroes.tier).

    Create `src/commands/sanguo/heroes.ts`:
    - `heroesSubcommand` — `.setName('heroes')` + localized descriptions; NO options.
    - `execute(interaction)` — defer → fetchCommandContext → not-registered guard → read user_sanguo_state (starterViews) + count user_heroes rows: EMPTY → render starter picker with (starterViews >= 3 ? set 2 : set 1), INCREMENT starterViews (FOR UPDATE on user_sanguo_state row — one row per user, mirror the single-writer rule; INSERT the state row if missing) → editReply embed + starter button row; NON-EMPTY → query owned heroes joined heroes (iv_*, tier, captured_zone, name per locale) + user_sanguo_state.activeHeroId → render the collection embed (ordered by heroes.id) + zone filter select row.
    - `handleStarterPick(interaction: ButtonInteraction)` — `deferUpdate()` → parse heroId from `sanguo:heroes:starter:{heroId}` (slice + validate against BOTH sets — a stale/fabricated heroId outside the sets → heroes.error); FOR UPDATE tx: re-check the collection is still empty (a second pick must not double-grant — the tx lock serializes); roll 6× crypto.randomInt(0,32); INSERT user_heroes (heroId resolved via heroes.heroId→id, level 1, ivs, hpCurrent = heroes.hp, capturedZone NULL — starter grant, A5); UPDATE user_sanguo_state SET activeHeroId = new row id, starterViews = 0; reply SUCCESS-color embed (starter acquired — UI-SPEC SUCCESS accent).
    - `handleZoneFilterSelect(interaction: StringSelectMenuInteraction)` — `deferUpdate()` → validate the zone code against map_zones (unknown → fall back to the full collection — never crash); re-query owned heroes filtered by captured_zone = code; render the collection embed with the filtered count + zone label + the same zone menu row.
    - All service calls key on user.id (users.id).

    Create `src/commands/sanguo/__tests__/heroes.test.ts` with the 5 behaviors — mock db/client + commandContext + heroEmoji (try/catch guard); inject a deterministic ivRoll for the starter test; assert no wallet mock call in the starter path (the faucet-free proof).
  </action>
  <verify>
    <automated>npx vitest run src/commands/sanguo/__tests__/heroes.test.ts; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Command: `npx vitest run src/commands/sanguo/__tests__/heroes.test.ts` exits 0 (behaviors 1-5 green).
    - Command: `npm run typecheck` exits 0.
    - Source: `sanguoStarterButtons.ts` exports `STARTER_PICK_PREFIX`/`buildStarterButtons`/`STARTER_SET_1`/`STARTER_SET_2` (each set exactly 3 heroIds).
    - Source: `buildSanguoHeroesEmbed.ts` renders the grade via an `iv_grade.*` key and stars via `'★'.repeat(tier)`; it contains NO reference to a rarity column or raw iv rendering.
    - Source: `src/commands/sanguo/heroes.ts` handleStarterPick inserts with `hpCurrent` = the hero's base HP and contains NO `deductBalance` import/call (grep `grep -v "^[[:space:]]*//" src/commands/sanguo/heroes.ts | grep -cE "deductBalance|services/wallet"` == 0 — the faucet is free, D-19).
    - Source: `heroes.ts` execute increments `starterViews` only when the collection is empty (the D-14 rotation counter).
  </acceptance_criteria>
  <done>The collection surface works both ways: empty → starter picker with the D-14 rotation + free grant (no wallet), non-empty → per-zone collection with stars + IV grade + active badge and the zone filter in its own row. 5 behavior tests green.</done>
  <reversibility rating="reversible">Implements D-14/D-15 — starter roster is seed data and the rotation is a counter; both are changeable without schema or economy impact (the free grant itself is the locked D-19 faucet).</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: /sanguo hero — detail embed + companion switch (D-16/D-04) + i18n heroes/hero keys</name>
  <files>src/commands/sanguo/hero.ts, src/ui/embeds/buildSanguoHeroEmbed.ts, src/ui/components/sanguoHeroCompanionButton.ts, locales/vi/sanguo.json, locales/en/sanguo.json, locales/zh-cn/sanguo.json, src/commands/sanguo/__tests__/hero.test.ts</files>
  <read_first>
    - src/commands/sanguo/travel.ts (subcommand builder + button-handler pattern)
    - src/ui/embeds/buildSanguoAckEmbed.ts (fixed-field detail-embed pattern)
    - src/db/schema/userHeroes.ts + userSanguoState.ts + heroes.ts (rows read/written)
    - .planning/phases/10-battle-capture/10-UI-SPEC.md §Copywriting Contract (hero.* keys) + §Component & CustomId Contract (sanguo:hero:companion:{heroId})
    - .planning/phases/10-battle-capture/10-PATTERNS.md §hero.ts (lines 234-289)
  </read_first>
  <behavior>
    - Test 1: /sanguo hero with an OWNED hero renders the detail: emoji, per-locale name, ★ stars, IV grade key, HP/MP numbers (base-only — D-05), companion status label when active, companion button present (disabled when already active).
    - Test 2: /sanguo hero with a hero the user does NOT own → `hero.error` DANGER embed (no data leak of the hero's stats beyond the name lookup).
    - Test 3: handleCompanionPress (sanguo:hero:companion:{heroId}) switches user_sanguo_state.activeHeroId inside a FOR UPDATE tx; pressing on the already-active hero is a no-op (button is disabled anyway — defense in depth); a non-owned heroId → hero.error.
    - Test 4: the detail embed renders NO raw IV numbers and NO rarity (D-12) — only the grade key and stars.
  </behavior>
  <action>
    Create `src/ui/components/sanguoHeroCompanionButton.ts`:
    - `export const COMPANION_PREFIX = 'sanguo:hero:companion';`
    - `buildCompanionButton(t, heroId: number, disabled = false): ButtonBuilder` — `.setCustomId(`${COMPANION_PREFIX}:${heroId}`).setLabel(t('sanguo:hero.companion_button')).setStyle(ButtonStyle.Primary).setDisabled(disabled)`.

    Create `src/ui/embeds/buildSanguoHeroEmbed.ts`:
    - `export interface SanguoHeroEmbedData { emoji?: string; name: string; stars: string; gradeKey: string; hpCurrent: number; hpMax: number; mp: number; isActive: boolean; fainted: boolean; shardId?: number }`.
    - `buildSanguoHeroEmbed(data, t)` — `.setColor(COLORS.SEASON)`; title `t('sanguo:hero.title', { name })` ('🗡️ {{name}}'); fields: stars (`'★'.repeat(tier)`), grade (`t('iv_grade.<key>')`), HP/MP as numbers (base-only — `hp_current/hpMax/mp`), companion status `t('sanguo:hero.companion_label')` when active, `t('sanguo:hero.fainted')` badge when hpCurrent = 0; footer embedFooter + timestamp. NEVER render IV numbers or rarity (D-12).

    Create `src/commands/sanguo/hero.ts`:
    - `heroSubcommand` — `.setName('hero')` + localized descriptions + `.addStringOption` for the hero (name `hero`, localized description — the option lets the user identify an owned hero; autocomplete OFF in this phase, D-16 scope).
    - `execute(interaction)` — defer → fetchCommandContext → not-registered guard → read the hero option → resolve against the user's OWNED user_heroes (join heroes — match by name per locale or heroes.id); NOT owned → `hero.error` DANGER embed (no stat leak); owned → build the detail embed + companion button (disabled when `activeHeroId === thisHeroId`) → editReply. Duplicate-ownership disambiguation (P10-review F9): user_heroes ALLOWS multiple rows per (userId, heroId) — when the name/id matches >1 owned copy, prefer the ACTIVE companion if it matches, else the earliest captured copy (lowest userHeroes.id), and note the resolution in the embed.
    - `handleCompanionPress(interaction: ButtonInteraction)` — `deferUpdate()` → parse heroId from `sanguo:hero:companion:{heroId}` (parseInt + isNaN guard) → FOR UPDATE tx: verify ownership (SELECT user_heroes WHERE userId AND id) → UPDATE user_sanguo_state SET activeHeroId = heroId, updatedAt = now → reply the detail embed with the companion button now disabled.
    - All keys on user.id (users.id).

    Edit `locales/vi/sanguo.json`, `locales/en/sanguo.json`, `locales/zh-cn/sanguo.json` — add `cmd.heroes.description` ('Xem bộ sưu tập hero của bạn'), `cmd.hero.description` ('Xem chi tiết một hero'), top-level `heroes.*` (empty_title '🌱 Chọn hero khởi đầu', empty_body 'Bạn chưa có hero nào. Chọn **1 hero miễn phí** để bắt đầu hành trình:', starter_button 'Chọn {{name}}', title '📜 Bộ sưu tập ({{count}})', line '{{emoji}} {{name}} • {{stars}} • {{grade}}{{active}}', active_badge '⭐', zone_filter 'Lọc theo vùng', error 'Có lỗi khi tải bộ sưu tập. Hãy thử lại.', success '🎉 Bạn đã chọn **{{name}}** làm hero khởi đầu!'), `hero.*` (title '🗡️ {{name}}', companion_button 'Chọn làm hero đồng hành', companion_label '⭐ Hero đồng hành', fainted '💀 Kiệt sức', error 'Bạn chưa sở hữu hero này.'), and `iv_grade.*` (gold 'Hoàng Kim', ruby 'Hồng ngọc', sapphire 'Lam cấp', jade 'Lục cấp', gray 'Hôi cấp') if not already present — IDENTICAL key structures across all 3 locales (check-i18n).

    Create `src/commands/sanguo/__tests__/hero.test.ts` with the 4 behaviors — mock db/client + commandContext; assert the ownership gate, the disabled button state, the FOR UPDATE switch, and the D-12 never-render (no iv number assertions).
  </action>
  <verify>
    <automated>npx vitest run src/commands/sanguo/__tests__/hero.test.ts; npm run check-i18n; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Command: `npx vitest run src/commands/sanguo/__tests__/hero.test.ts` exits 0 (behaviors 1-4 green).
    - Command: `npm run check-i18n` exits 0; `npm run typecheck` exits 0.
    - Source: `sanguoHeroCompanionButton.ts` exports `COMPANION_PREFIX`/`buildCompanionButton` with the disabled-state parameter.
    - Source: `src/commands/sanguo/hero.ts` ownership-gates the detail (no stat render for non-owned heroes) and the companion switch runs inside a `for('update')` tx.
    - Config: all three locale files contain `heroes.empty_title`, `heroes.line`, `heroes.starter_button`, `hero.title`, `hero.companion_button`, `iv_grade.gold`...`iv_grade.gray`, `cmd.heroes.description`, `cmd.hero.description`.
    - Grep (identity): `grep -cE "switchActiveHero\(char\.id|heroCompanion.*char\.id" src/commands/sanguo/hero.ts` outputs `0`.
  </acceptance_criteria>
  <done>The hero detail + companion switch work: ownership-gated detail embed (stars + grade + HP/MP, D-12 clean), FOR UPDATE companion switch, disabled-when-active button, and the full heroes/hero/iv_grade i18n key sets in 3 locales. Tests + check-i18n + typecheck green.</done>
  <reversibility rating="reversible">Implements D-16 — a new subcommand surface; switching the active companion is user-state-only (no economy/schema impact beyond the 10-02 state row).</reversibility>
</task>

<task type="auto">
  <name>Task 3: map.ts subcommand registration (heroes + hero) + map current-position fix + interactionCreate routes + phase verification</name>
  <files>src/commands/sanguo/map.ts, src/events/interactionCreate.ts, src/commands/sanguo/__tests__/heroes.test.ts, src/commands/sanguo/__tests__/hero.test.ts</files>
  <read_first>
    - src/commands/sanguo/map.ts (the file being modified — subcommand builder lines 22-39; execute dispatch lines 59-81; the current_position data at line 119 currently uses rows[0] — the SC5 fix swaps it for getCurrentPosition(user.id))
    - src/events/interactionCreate.ts (the file being modified — SanguoComponentHandlers interface lines 28-32; the sanguo select/button branches; the string-select branch around lines 36-61)
    - src/services/sanguo/travelService.ts (getCurrentPosition — the SC5 position source)
    - src/commands/sanguo/heroes.ts + hero.ts (the subcommand/handler exports to wire)
    - .planning/phases/10-battle-capture/10-PATTERNS.md §map.ts (lines 279-285)
  </read_first>
  <behavior>
    - Test 5 (map position): map.ts execute sets current_position from getCurrentPosition(user.id) (the player's real node), not rows[0]; the zone markers/content render unchanged.
    - Test 6 (routing): interactionCreate dispatches `sanguo:heroes:starter:{heroId}` (prefix), `sanguo:heroes:zone` (===), `sanguo:hero:companion:{heroId}` (prefix) to the sanguo command module handlers BEFORE the chat-input gate; each branch try/catch + logger.error.
    - Test 7 (phase regression): `npm run lint` passes (eslint max-warnings 0 — no hardcoded strings, no emoji-markup-in-label regressions); the full sanguo test suite runs green.
  </behavior>
  <action>
    Edit `src/commands/sanguo/map.ts`:
    - Append `.addSubcommand(heroesSubcommand)` and `.addSubcommand(heroSubcommand)` to the `data` builder (imports from './heroes.js' + './hero.js').
    - Dispatch: `if (subcommand === 'heroes') { await heroesExecute(interaction); return; }` and `if (subcommand === 'hero') { await heroExecute(interaction); return; }` (before the map fallback).
    - Re-export the new component handlers alongside the battle ones: `export { handleStarterPick, handleZoneFilterSelect } from './heroes.js';` + `export { handleCompanionPress } from './hero.js';` (keep the battle re-exports from 10-06).
    - SC5 fix: in the map branch, replace the current_position data source — `const pos = await getCurrentPosition(user.id); const [currentNode] = ...fetchNodeName(pos.nodeId)` — set `currentZoneName` from the player's actual current node (per-locale name), NOT `rows[0]`; keep the zones content + node list as-is.
    - The map execute signature already receives `user` from fetchCommandContext (users.id) — use `user.id` for getCurrentPosition.

    Edit `src/events/interactionCreate.ts`:
    - Extend `SanguoComponentHandlers` with `handleStarterPick`, `handleZoneFilterSelect`, `handleCompanionPress`.
    - Add routes in the appropriate interaction-type branches: StringSelectMenu branch — `customId === ZONE_MENU_ID` → dispatch `handleZoneFilterSelect`; Button branch — `customId.startsWith(STARTER_PICK_PREFIX)` → dispatch `handleStarterPick`; `customId.startsWith(COMPANION_PREFIX)` → dispatch `handleCompanionPress`. All wrapped in try/catch + `logger.error('InteractionCreate', ...)`.

    Extend `heroes.test.ts`/`hero.test.ts` with behaviors 5-7 where applicable (map position via mocked getCurrentPosition; routing via a router-level test or assertion on the interactionCreate branches).
  </action>
  <verify>
    <automated>npx vitest run src/commands/sanguo/__tests__/; npm run check-i18n; npm run typecheck; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Command: `npx vitest run src/commands/sanguo/__tests__/` exits 0 (heroes 1-5 + hero 1-4 + routing/map tests green).
    - Command: `npm run check-i18n` exits 0; `npm run typecheck` exits 0; `npm run lint` exits 0 (max-warnings 0).
    - Source: `src/commands/sanguo/map.ts` contains `.addSubcommand(heroesSubcommand)` and `.addSubcommand(heroSubcommand)` and the current_position source is `getCurrentPosition` (grep `grep -c "getCurrentPosition" src/commands/sanguo/map.ts` >= 1; the old rows[0] current-position assignment is gone).
    - Source: `src/events/interactionCreate.ts` contains `ZONE_MENU_ID`, `STARTER_PICK_PREFIX`, and `COMPANION_PREFIX` dispatch branches; `SanguoComponentHandlers` includes the three new handler signatures.
    - Grep (no second command file): `grep -rl "setName('sanguo')" src/commands/sanguo/` returns only `map.ts` — heroes.ts/hero.ts export subcommand builders, never the parent command.
    - Grep (faucet gate, whole plan): `grep -r "deductBalance" src/commands/sanguo/heroes.ts src/commands/sanguo/hero.ts` outputs `0` — no wallet call anywhere in the collection/starter/hero surfaces.
  </acceptance_criteria>
  <done>The collection/starter/hero surfaces are fully wired: heroes + hero subcommands registered on the /sanguo command, the router dispatches the starter/zone/companion customIds, the map shows the player's real position (SC5), and the whole sanguo suite + check-i18n + typecheck + lint pass. The vertical loop is closed: starter → travel → encounter → battle → capture → collection.</done>
  <reversibility rating="reversible">Implements TQC-13 wiring (subcommand registration, routes, map position) — additive surface changes only; the map position fix reads existing travelService state.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| interaction payload → heroes/hero handlers | Starter heroIds and companion heroIds in customIds cross here — validated against owned rows / the starter sets server-side (V5). |
| command layer → user_sanguo_state | The companion switch and starter-views counter mutate the one-row-per-user state — single-writer FOR UPDATE. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-07-01 | Tampering | Starter double-grant (two picks race) | high | mitigate | handleStarterPick runs in a FOR UPDATE tx that re-checks the collection is empty before inserting — the second pick serializes and finds a non-empty collection → heroes.error (mirrors the 10-05 single-writer rule). |
| T-10-07-02 | Tampering | Faucet exploitation (starter granting Linh thạch or multiple heroes) | critical | mitigate | The starter path contains NO wallet call (grep gate == 0 — D-19 ONLY faucet); exactly one user_heroes insert per pick, guarded by the empty-collection re-check + starter_views reset. |
| T-10-07-03 | Spoofing | Crafted companion/starter heroIds | medium | mitigate | heroIds are validated server-side: starter heroIds must match STARTER_SET_1/2; companion heroIds must match an owned user_heroes row (ownership gate) — a forged id → hero.error, no state change. |
| T-10-07-04 | Information disclosure | D-12 leakage (IV/rarity in collection/detail) | high | mitigate | Collection/detail render grade keys + tier stars only; no IV number, no rarity column read in any UI path (prohibitions + test assertions). |
| T-10-07-05 | Spoofing | Zone-filter value injection | low | mitigate | The select value is validated against map_zones codes; unknown values fall back to the full collection — no injection surface (drizzle parameterization). |
| T-10-07-06 | Tampering | Companion switch race (two switches) | low | mitigate | The switch runs in a FOR UPDATE tx on user_sanguo_state — serialized; last-writer-wins is the intended semantic. |
| T-10-07-SC | Tampering | npm/pip/cargo installs | — | not-triggered | This plan installs no packages. |
</threat_model>

<verification>
- `npx vitest run src/commands/sanguo/__tests__/` green (heroes 1-5, hero 1-4, routing/map 5-7); `npm run check-i18n` green; `npm run typecheck` green; `npm run lint` green.
- Grep gates: zero `deductBalance` in heroes.ts/hero.ts (the faucet is free); exactly one `setName('sanguo')` file (map.ts); `getCurrentPosition` present in map.ts.
- The full phase loop is demonstrable in code: starter pick → travel → encounter → fight → battle log → Bắt → tier → capture success → collection line + companion switch → map position.
</verification>

## Artifacts this phase produces

| Symbol | Kind | File |
|--------|------|------|
| `heroesSubcommand` + `execute` + `handleStarterPick` + `handleZoneFilterSelect` | command module (TQC-12/13, D-14/D-15) | src/commands/sanguo/heroes.ts |
| `heroSubcommand` + `execute` + `handleCompanionPress` | command module (D-16) | src/commands/sanguo/hero.ts |
| `SanguoHeroesEmbedData` + `buildSanguoHeroesEmbed(data, t)` (collection + starter-picker states) | embed builder | src/ui/embeds/buildSanguoHeroesEmbed.ts |
| `SanguoHeroEmbedData` + `buildSanguoHeroEmbed(data, t)` | embed builder | src/ui/embeds/buildSanguoHeroEmbed.ts |
| `STARTER_PICK_PREFIX` + `buildStarterButtons` + `STARTER_SET_1`/`STARTER_SET_2` | starter buttons (D-14) | src/ui/components/sanguoStarterButtons.ts |
| `ZONE_MENU_ID` + `buildZoneFilterMenu` | zone filter select (D-15) | src/ui/components/sanguoHeroesZoneMenu.ts |
| `COMPANION_PREFIX` + `buildCompanionButton` | companion switch button (D-16) | src/ui/components/sanguoHeroCompanionButton.ts |
| heroes/hero subcommands + handler re-exports + current-position fix | command wiring (TQC-13 SC5) | src/commands/sanguo/map.ts |
| `sanguo:heroes:*`/`sanguo:hero:*` routes + extended `SanguoComponentHandlers` | interaction router | src/events/interactionCreate.ts |
| `heroes.*`/`hero.*`/`iv_grade.*`/`cmd.heroes.*`/`cmd.hero.*` keys (3 locales) | i18n | locales/{vi,en,zh-cn}/sanguo.json |
| heroes.test.ts (5) + hero.test.ts (4) + routing tests | test suites | src/commands/sanguo/__tests__/ |

<success_criteria>
- A new user can onboard: empty collection → starter picker → free pick → active companion — with the D-14 set rotation and zero Linh thạch touched.
- A returning user sees their collection (stars + IV grade, D-12 clean), filters by zone, and switches the active companion from /sanguo hero.
- /sanguo map shows the player's real current position (TQC-13 SC5).
- The vertical loop is closed end-to-end: starter → travel → encounter → battle → capture → collection; check-i18n/typecheck/lint all green.
</success_criteria>

<output>
Create `.planning/phases/10-battle-capture/10-07-SUMMARY.md` when done.
</output>

