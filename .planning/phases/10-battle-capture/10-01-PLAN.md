---
phase: 10-battle-capture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/services/sanguo/battleEngine.ts
  - src/services/sanguo/__tests__/battleEngine.test.ts
autonomous: true
requirements:
  - TQC-10

estimate:
  tokens: 44000
  raw_tokens: 22000
  tasks: 2
  confidence: low

assumption_delta: "no-change — orchestrator-confirmed: the Phase 10 copy-level signals ('failed attempts are ALSO recorded', 'can CHOOSE 1 free starter') are vocabulary false-positives, not identity-model or config-model transitions; no generalization occurred."

must_haves:
  truths:
    - "runBattle(seed, player, enemy) is a synchronous, I/O-free pure function: given the SAME seed and the SAME CombatantInput pair it ALWAYS returns deep-equal roundLogs/result (replay contract D-06 — the stored-record verification path is re-running the engine with the stored seed + input jsonb)."
    - "Battle seed is generated by crypto.randomInt at battle start (D-06) — the engine itself never draws randomness from the environment; all in-battle rolls ride a single xoroshiro128plus(seed) rng threaded as (rng, min, max)."
    - "combatStat(base, iv) = base + iv for the 6 IV stats; HP and MP are base-only — IV never adds HP (D-05 locked)."
    - "Turn order per round is MOV desc; MOV tie → AGI desc; still tie → the player-attacker acts first (D-05 tie-break ladder)."
    - "Attack type by class: vanguard/cavalry/archer → STR atk/def; spellcaster/schemer → INT atk/def; vu_co/thu_binh/cong_binh → MAX(STR,INT) atk/def (D-05)."
    - "damage = max(atk − def, 1); a miss deals 0; a crit doubles the damage; defender HP never drops below 0 (D-05)."
    - "Round cap is exactly 20: if both combatants survive 20 rounds, winner = higher total damage dealt; tie → higher remaining HP% (D-05)."
    - "hit/crit probabilities derive from AGI (attacker AGI raises both; defender AGI lowers both) via exported BATTLE_CONFIG constants so the Phase 10 balance pass can sanity-check them against the seeded AGI spread (D-05 principle, A9 drafts)."
    - "pure-rand is imported ONLY inside src/services/sanguo/battleEngine.ts — no other module in the phase touches it (D-06 battle-only mandate)."
  artifacts:
    - src/services/sanguo/battleEngine.ts — CombatantInput, TurnLog, BattleResult interfaces + combatStat() + runBattle(seed, player, enemy) + BATTLE_CONFIG + getAttackType()
    - src/services/sanguo/__tests__/battleEngine.test.ts — replay-determinism + formula edge tests
    - package.json — pure-rand@8.4.2 dependency (the ONLY new dependency of the milestone)
  key_links:
    - "runBattle's seed+input contract ↔ sanguo_battles.input jsonb (stored at battle start in 10-05) — the replay path re-runs the engine with the stored values; the input snapshot MUST contain both heroes' full base+IV+hpCurrent or replay diverges (Pitfall 1)."
    - "BATTLE_CONFIG hit/crit/round-cap constants ↔ 10-04 content seed AGI ranges — the balance pass re-checks the constants against the actual seeded stat spread before capture balancing."
  prohibitions:
    - "No DB import, no redis import, no discord.js import, no I/O anywhere in battleEngine.ts — engine is pure math only (analog: encounterService.ts header contract)."
    - "No Math.random and no environment-entropy draw anywhere in battleEngine.ts — every roll comes from the seeded rng only."
    - "No pure-rand import outside src/services/sanguo/battleEngine.ts (scoped grep gate below)."
  flagged_assumptions:
    - "FLAGGED ASSUMPTION (probe TQC-10 boundary, unresolved): round cap semantics at the exact boundary — the engine runs AT MOST 20 rounds; if a combatant reaches 0 HP at any point (including exactly on round 20's first action), the battle ends immediately with the other side the winner; the cap is never exceeded."
    - "FLAGGED ASSUMPTION (probe TQC-10 precision, unresolved): all damage/HP numbers are integer-valued (damage = max(atk−def, 1) on integers, HP clamped at 0); crit is exactly ×2 (integer multiplication); hit/crit rolls are uniformFloat64 draws in [0,1) compared strictly (roll < hitChance → hit); no rounding or tie-break drift is possible in the log."
    - "FLAGGED ASSUMPTION (D-05 residual tie, agent discretion): if after the round cap BOTH total damage AND remaining HP% tie, the battle resolves to the player (mirrors 'attacker first'); documented in the engine header."
    - "FLAGGED ASSUMPTION (A9 research): hit = clamp(0.85 + (agiA − agiD) × 0.003, 0.50, 0.99), crit = clamp(0.05 + (agiA − agiD) × 0.001, 0.02, 0.30) are the initial constants — exported via BATTLE_CONFIG and re-sanitized against the seeded AGI spread in 10-04 before capture balancing."
    - "FLAGGED ASSUMPTION (D-05 'attacker first' interpretation): in the solo-battle model the player is always the initiator (encounter and spar are both player-initiated), so the full tie-break resolves to the player; no spectator/third-party battle exists in Phase 10."

---

<objective>
What this plan accomplishes: the **seeded, replayable battle engine** — the phase's tracer and the only genuinely new code of the milestone. `battleEngine.ts` is a pure, I/O-free function `runBattle(seed, player, enemy) → BattleResult` implementing the locked D-05 formula (combatStat = base + IV, MOV-desc turn order with the AGI/attacker tie ladder, class-based attack type, damage = max(atk−def, 1), AGI-derived hit/crit, crit ×2, round cap 20 with total-damage → HP% resolution) with every in-battle roll riding `pure-rand`'s `xoroshiro128plus(seed)` (D-06). `pure-rand@8.4.2` is installed — the ONLY new dependency of the milestone (Package Legitimacy Audit: OK).

Purpose: prove the replay architecture end-to-end BEFORE any schema/service/UI builds on it. The replay contract (same seed + input → identical log) is the phase's core bet — if the seed/input contract is wrong, ten later layers (battle records, capture, collection) inherit the bug. Writing `runBattle` first and asserting `runBattle(seed, input)` twice deep-equals itself catches that dead-end after one commit.

Output: `battleEngine.ts` (pure seeded engine + exported constants), `battleEngine.test.ts` (replay + formula edge tests), `pure-rand@8.4.2` in package.json.
</objective>

<execution_context>
@C:/Users/901107/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/901107/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-battle-capture/10-CONTEXT.md
@.planning/phases/10-battle-capture/10-RESEARCH.md
@.planning/phases/10-battle-capture/10-PATTERNS.md
@src/services/sanguo/encounterService.ts
@src/services/sanguo/travelCheckInService.ts
@package.json
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 (tracer): pure-rand install + battleEngine.ts — the seeded replayable engine (D-05/D-06)</name>
  <files>package.json, package-lock.json, src/services/sanguo/battleEngine.ts, src/services/sanguo/__tests__/battleEngine.test.ts</files>
  <read_first>
    - src/services/sanguo/encounterService.ts (the pure-math module contract — header comment lines 3-24; cryptoUniform pattern lines 57-59; the rng-param injection convention for testability)
    - .planning/phases/10-battle-capture/10-RESEARCH.md §Pattern 1 (engine skeleton lines 238-265), §Common Operation 1 (pure-rand API lines 368-385 — use the impure `uniformInt(rng, min, max)` threading ONE mutable rng), §A9 (hit/crit drafts), §Pitfall 1 (replay divergence), §Pitfall 4 (pure-rand battle-only)
    - .planning/phases/10-battle-capture/10-PATTERNS.md §battleEngine.ts (exact skeleton lines 68-97)
    - .planning/phases/10-battle-capture/10-CONTEXT.md D-05 (locked formula) + D-06 (replay model) + §the agent's Discretion (formula numbers)
    - .planning/STATE.md (IV grade table line 79; IV stat meanings line 78 — STR=atk+def, AGI=accuracy+evasion, INT=magic atk+def, MOV=order, LEA/CHA unused Phase 10)
    - src/db/schema/heroes.ts (class enum values: vanguard/cavalry/archer/spellcaster/schemer/vu_co/thu_binh/cong_binh)
  </read_first>
  <behavior>
    - Test 1 (replay contract): runBattle(seed, input) twice → the second result deep-equals the first (roundLogs identical) for a fixed seed and fixed input.
    - Test 2 (seed sensitivity): runBattle(seedA, input) ≠ runBattle(seedB, input) for seedA ≠ seedB over the same input (at least one differing turn line across a sample of seeds).
    - Test 3 (combatStat): combatStat(base, iv) === base + iv for the 6 IV stats; HP/MP are NOT summed with IV (input carries hpCurrent; the engine never adds IV to HP).
    - Test 4 (turn order ladder): a higher-MOV combatant acts first each round; MOV tie → higher AGI first; MOV+AGI tie → the player (isPlayer) acts first.
    - Test 5 (class attack type): getAttackType('vanguard'|'cavalry'|'archer') = 'str'; 'spellcaster'|'schemer' = 'int'; 'vu_co'|'thu_binh'|'cong_binh' = 'max'.
    - Test 6 (damage floor): when atk ≤ def the dealt damage is exactly 1 (never 0 or negative); HP clamps at 0 (no negative HP in any turn log).
  </behavior>
  <action>
    Install the dependency first: `npm install pure-rand@8.4.2` (exact version — RESEARCH verified 8.4.2 on the registry 2026-07-10, legitimacy OK, no postinstall). This is the ONLY new dependency of the milestone.

    Create `src/services/sanguo/battleEngine.ts` — a pure module with the same header-contract discipline as encounterService.ts (no db/redis/discord imports; I/O is owned by callers). Use the type-only import form for pure-rand:
    - `import { xoroshiro128plus } from 'pure-rand/generator/xoroshiro128plus';` and `import { uniformFloat64 } from 'pure-rand/distribution/uniformFloat64';` (the impure threading API — ONE mutable rng per battle; replay creates a fresh `xoroshiro128plus(seed)` so the sequence is fully seed-determined).

    Export the following surface (signatures and semantics per RESEARCH Pattern 1 + PATTERNS §battleEngine.ts):
    - `export interface CombatantInput { heroId: string; base: { str; agi; int; mov; lea; cha; hp; mp }; iv: { str; agi; int; mov; lea; cha }; hpCurrent: number; class: 'vanguard'|'cavalry'|'archer'|'spellcaster'|'schemer'|'vu_co'|'thu_binh'|'cong_binh'; isPlayer: boolean; }` — all numbers are JS number (integer-valued); hpCurrent may be less than base.hp for the player (D-04 persistence), never more.
    - `export interface TurnLog { round: number; attacker: string; defender: string; hit: boolean; crit: boolean; dmg: number; defenderHpAfter: number; }` — attacker/defender are heroId strings; dmg 0 on miss.
    - `export interface BattleResult { roundLogs: TurnLog[]; winner: 'player' | 'enemy'; rounds: number; totalDamagePlayer: number; totalDamageEnemy: number; playerHpAfter: number; enemyHpAfter: number; }` — winner is the side (isPlayer flag), NOT the heroId.
    - `export function combatStat(base: number, iv: number): number { return base + iv; }` — D-05 locked.
    - `export function getAttackType(cls: CombatantInput['class']): 'str' | 'int' | 'max'` — the D-05 class mapping (STR: vanguard/cavalry/archer; INT: spellcaster/schemer; MAX(STR,INT): vu_co/thu_binh/cong_binh).
    - `export const BATTLE_CONFIG = { ROUND_CAP: 20, HIT_BASE: 0.85, HIT_AGI_FACTOR: 0.003, HIT_MIN: 0.5, HIT_MAX: 0.99, CRIT_BASE: 0.05, CRIT_AGI_FACTOR: 0.001, CRIT_MIN: 0.02, CRIT_MAX: 0.3 } as const;` — A9 drafts, exported for the 10-04 balance pass.
    - `export function runBattle(seed: number, player: CombatantInput, enemy: CombatantInput): BattleResult` — the engine body:
      - `const rng = xoroshiro128plus(seed);` — ONE mutable rng threaded through the whole battle.
      - Per round (1..ROUND_CAP): order the two combatants by MOV desc, tie → AGI desc, tie → the player (isPlayer) first. Each combatant in order: if the DEFENDER's HP is already 0, skip (the battle ends on the action that killed).
      - Attack resolution for an acting combatant A against defender D: `atk = combatStat(A.base[str|int|max], A.iv[str|int|max])` per getAttackType; `def = combatStat(D.base[<same stat>], D.iv[<same stat>])`; hitChance = clamp(HIT_BASE + (agiA − agiD) × HIT_AGI_FACTOR, HIT_MIN, HIT_MAX); roll hit = uniformFloat64(rng) < hitChance (strict <); on hit: critChance = clamp(CRIT_BASE + (agiA − agiD) × CRIT_AGI_FACTOR, CRIT_MIN, CRIT_MAX); crit = uniformFloat64(rng) < critChance; dmg = max(atk − def, 1) × (crit ? 2 : 1); defender HP = max(0, HP − dmg); push a TurnLog with defenderHpAfter.
      - Track totalDamagePlayer/totalDamageEnemy (sum of dmg dealt by each side) and hpAfter for both.
      - End conditions in priority order: (a) either side's HP reaches 0 → winner = the other side, battle ends immediately (rounds = the current round); (b) round counter exceeds ROUND_CAP with both alive → winner = higher totalDamage; tie → higher remaining HP% (playerHpAfter/base.hp vs enemyHpAfter/enemy.base.hp); full tie → player (documented in the header comment).
      - The engine NEVER reads Date/now/global state and NEVER draws entropy — the ONLY randomness source is the seeded rng.
    - Header comment must document: D-05 formula contract, D-06 replay model (`runBattle(seed, input)` re-run === stored log), the full-tie → player rule, and the pure-module contract (no I/O, no entropy).

    Create `src/services/sanguo/__tests__/battleEngine.test.ts` implementing the 6 behaviors above. Use vitest plain-assertion style (analog: `src/services/football/__tests__/oddsCalculator.test.ts` — pure fn tests, no mocks). Build a fixed input fixture (two CombatantInput objects with concrete base/iv/hpCurrent values chosen so the MOV/AGI tie ladder and the atk≤def floor case are exercised).
  </action>
  <verify>
    <automated>npx vitest run src/services/sanguo/__tests__/battleEngine.test.ts; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Command: `npx vitest run src/services/sanguo/__tests__/battleEngine.test.ts` exits 0 (all 6 behaviors green).
    - Command: `npm run typecheck` exits 0.
    - Source: `src/services/sanguo/battleEngine.ts` exports `runBattle`, `combatStat`, `getAttackType`, `BATTLE_CONFIG`, and the `CombatantInput`/`TurnLog`/`BattleResult` interfaces.
    - Source: `src/services/sanguo/battleEngine.ts` contains `ROUND_CAP: 20` and `const rng = xoroshiro128plus(seed)` and `uniformFloat64(rng` (the seeded-draw path).
    - Command (D-06 battle-only gate, scoped to the engine file): `grep -c "from 'pure-rand" src/services/sanguo/battleEngine.ts` outputs at least `2` (generator + distribution imports).
    - Command (battle-only gate, scoped OUTSIDE the engine — runs at phase end when 10-05/06/07 land; for now assert on the current tree): `Get-ChildItem -Recurse src/services/sanguo -Filter *.ts | Where-Object { $_.Name -ne 'battleEngine.ts' } | Select-String -Pattern "from 'pure-rand" | Measure-Object | Select-Object -ExpandProperty Count` outputs `0`.
    - Source: `src/services/sanguo/battleEngine.ts` contains no `import` of `../../db/`, no `redis`, and no `discord.js` (grep each: `grep -c "db/client" src/services/sanguo/battleEngine.ts` == 0; `grep -c "discord.js" src/services/sanguo/battleEngine.ts` == 0).
    - Source: `package.json` dependencies block contains `"pure-rand": "8.4.2"`.
  </acceptance_criteria>
  <done>pure-rand@8.4.2 installed; battleEngine.ts implements the full D-05 formula with the seeded xoroshiro128plus rng; the replay test (runBattle twice → deep-equal) passes; combatStat/getAttackType/BATTLE_CONFIG exported; typecheck green; engine is I/O-free and entropy-free.</done>
  <reversibility rating="costly">Implements D-06 (pure-rand dependency + seed/input replay contract) and the D-05 formula constants — retuning the formula or swapping the PRNG later touches every battle/capture layer; the dependency choice is locked by D-06.</reversibility>
</task>

<task type="expansion" tdd="true">
  <name>Task 2 (expansion): engine edge coverage — round-cap resolution, crit/miss paths, HP floor, replay across seed space</name>
  <files>src/services/sanguo/battleEngine.ts, src/services/sanguo/__tests__/battleEngine.test.ts</files>
  <read_first>
    - src/services/sanguo/battleEngine.ts (the file created in Task 1 — extend it, do not restructure the exported surface)
    - .planning/phases/10-battle-capture/10-RESEARCH.md §Pitfall 8 (round cap bounds the log: ≤20 lines, ≤ ~1,700 chars) + §A9 (constant sanity)
  </read_first>
  <behavior>
    - Test 7 (round-cap resolution): craft inputs whose damage totals keep both alive past round 20 → the battle ends at exactly 20 rounds with winner = higher totalDamage; a fixture where totalDamage ties → winner = higher remaining HP%.
    - Test 8 (crit path): a fixture with high attacker AGI + low defender AGI produces a crit turn whose dmg is exactly 2× a non-crit turn on identical stats; the TurnLog records crit: true.
    - Test 9 (miss path): a fixture with low attacker AGI vs high defender AGI produces a miss turn (hit: false, dmg: 0) and the defender HP is unchanged.
    - Test 10 (HP floor): a lethal blow sets defenderHpAfter to exactly 0 (never negative) and the battle ends that round with the attacker as winner.
    - Test 11 (replay determinism across the seed space): for a sample of ≥25 seeds over the same input, runBattle twice per seed → deep-equal roundLogs in every case; at least one turn line differs between the min and max seeds (seed sensitivity).
  </behavior>
  <action>
    Extend `battleEngine.ts` ONLY if a test exposes a formula defect (e.g., an off-by-one in the round counter or a missing HP clamp) — do not change the exported surface or BATTLE_CONFIG values. If the engine already satisfies all behaviors, leave the implementation untouched and add the tests.

    Extend `src/services/sanguo/__tests__/battleEngine.test.ts` with behaviors 7-11:
    - Round-cap fixtures: choose base/iv values so per-hit damage is tiny relative to HP (e.g., atk−def = 1) → both sides survive 20 rounds; assert `result.rounds === 20` and the winner matches the higher totalDamage. For the HP%-tie fixture, make both sides' damage totals equal and their remaining HP fractions differ by construction (different max HP with equal damage).
    - Crit fixture: attacker AGI 31 iv + base 90, defender AGI 0 → critChance clamps at CRIT_MAX; assert at least one `crit: true` TurnLog and that its dmg equals exactly 2× the corresponding non-crit dmg on identical atk/def.
    - Miss fixture: attacker AGI 0, defender AGI 31 iv + base 90 → hitChance clamps at HIT_MIN; the assertion is behavioral (some miss turns occur across seeds), never a deterministic count (the engine is seeded — pick a fixed seed that yields a miss and assert THAT seed's log contains `hit: false`).
    - HP-floor fixture: a single lethal blow (atk−def large) → the final TurnLog has defenderHpAfter 0 and result.rounds === 1.
    - Replay loop: `for (const seed of seeds) { const a = runBattle(seed, p, e); const b = runBattle(seed, p, e); expect(a).toEqual(b); }` plus a distinctness assertion across seeds (compare the JSON of the first turn line between two chosen seeds).
  </action>
  <verify>
    <automated>npx vitest run src/services/sanguo/__tests__/battleEngine.test.ts; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Command: `npx vitest run src/services/sanguo/__tests__/battleEngine.test.ts` exits 0 (behaviors 1-11 all green).
    - Source: `src/services/sanguo/__tests__/battleEngine.test.ts` contains a ≥25-seed replay loop asserting `expect(runBattle(seed, p, e)).toEqual(runBattle(seed, p, e))`.
    - Source: `src/services/sanguo/__tests__/battleEngine.test.ts` contains a round-cap fixture asserting `result.rounds === 20` and a lethal-blow fixture asserting `defenderHpAfter` is `0`.
    - Source: `src/services/sanguo/battleEngine.ts` still exports the SAME surface as Task 1 (`runBattle`, `combatStat`, `getAttackType`, `BATTLE_CONFIG`) — `grep -c "export function runBattle" src/services/sanguo/battleEngine.ts` outputs `1`.
    - Command: `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>The engine's edge contract is locked by tests: round cap 20 resolution (total damage → HP%), crit exactly ×2, miss = 0 damage, HP floor 0 with immediate battle end, and replay determinism across a 25-seed sample. No formula change was needed unless a test exposed a defect.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — pure function) | runBattle has no input boundary beyond its (seed, input) parameters; the engine executes no I/O and trusts nothing external. The relevant boundary is the CALLER (10-05 battleCheckInService), which is responsible for crypto seed generation and for never leaking the seeded rng outside this module. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01-01 | Tampering | runBattle randomness source | high | mitigate | The engine draws randomness ONLY from the seeded xoroshiro128plus(seed) rng — no Math.random, no entropy, no Date. Player-facing outcomes never ride this rng (D-06: capture/flee/IV stay crypto); the engine is structurally incapable of producing unpredictable rolls. |
| T-10-01-02 | Tampering | pure-rand scope leak | medium | mitigate | pure-rand imports exist ONLY in battleEngine.ts (scoped grep gate in acceptance criteria); 10-05 captureService re-affirms the same gate on its own files (Pitfall 4). |
| T-10-01-03 | Spoofing | Replay divergence (stored log ≠ re-run) | medium | mitigate | The seed+input contract is the replay path: the engine is pure and deterministic, so divergence can only come from an incomplete input snapshot — the 10-05 sanguo_battles.input jsonb MUST store both sides' full base+IV+hpCurrent (Pitfall 1, asserted in 10-05). |
| T-10-01-SC | Tampering | npm install (pure-rand@8.4.2) | — | not-triggered | RESEARCH Package Legitimacy Audit: pure-rand 8.4.2 [OK] — 65.5M wk downloads, ~7 yrs old, repo github.com/dubzzz/pure-rand, no postinstall script. No blocking human checkpoint required ([OK] verdict, not [ASSUMED]/[SUS]). |
</threat_model>

<verification>
- `npx vitest run src/services/sanguo/__tests__/battleEngine.test.ts` green (behaviors 1-11).
- `npm run typecheck` green.
- Scoped gates: pure-rand imports exist in battleEngine.ts (≥2) and nowhere else under src/services/sanguo; battleEngine.ts contains no db/client, redis, or discord.js import.
- `package.json` carries `"pure-rand": "8.4.2"`; package-lock.json updated by the install.
</verification>

## Artifacts this phase produces

| Symbol | Kind | File |
|--------|------|------|
| `CombatantInput` / `TurnLog` / `BattleResult` | interfaces (engine contract — the D-06 input/result shapes 10-02 schema and 10-05 service serialize into `sanguo_battles.input`/`result` jsonb) | src/services/sanguo/battleEngine.ts |
| `combatStat(base, iv)` | pure function (D-05 locked) | src/services/sanguo/battleEngine.ts |
| `getAttackType(cls)` | pure function (D-05 class → STR/INT/MAX mapping) | src/services/sanguo/battleEngine.ts |
| `BATTLE_CONFIG` (ROUND_CAP 20, HIT/CRIT constants) | exported config (A9 drafts — re-sanitized in 10-04 balance pass) | src/services/sanguo/battleEngine.ts |
| `runBattle(seed, player, enemy)` | pure seeded engine (D-05/D-06 replay contract) | src/services/sanguo/battleEngine.ts |
| battleEngine.test.ts — 11 behaviors incl. 25-seed replay loop | test suite | src/services/sanguo/__tests__/battleEngine.test.ts |
| `pure-rand@8.4.2` dependency | package.json dependency (only new dep) | package.json, package-lock.json |

<success_criteria>
- The seeded-replay architecture is proven: same seed + same input → identical log, verified by the 25-seed replay loop; different seeds diverge.
- The D-05 formula is fully implemented and locked by tests (combatStat, turn-order ladder, class attack type, damage floor, HP clamp, crit ×2, round cap resolution).
- pure-rand exists in package.json and is importable ONLY from battleEngine.ts (scoped gate) — the D-06 battle-only mandate holds from the very first commit.
- 10-02 (schema) can type the `sanguo_battles.input`/`result` jsonb against CombatantInput/BattleResult; 10-05 (services) can call runBattle with confidence that replay works.
</success_criteria>

<output>
Create `.planning/phases/10-battle-capture/10-01-SUMMARY.md` when done.
</output>
