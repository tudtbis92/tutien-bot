---
phase: 10-battle-capture
plan: 04
type: execute
wave: 2
depends_on:
  - 10-02
  - 10-03
files_modified:
  - scripts/data/sanguo-base-stats.json
  - scripts/seed-sanguo.ts
autonomous: true
requirements:
  - TQC-12

estimate:
  tokens: 48000
  raw_tokens: 24000
  tasks: 2
  confidence: low

assumption_delta: "no-change — orchestrator-confirmed: copy-level signals only."

must_haves:
  truths:
    - "Every one of the 132 seeded heroes has base STR/AGI/INT/MOV/LEA/CHA + HP + MP + rarity (1-5) + public tier (★1-5) values, written into the heroes table by the idempotent seed (D-02/D-08)."
    - "Rarity distribution matches the signed D-20 contract (60/25/10/4/1, ±2) — the economy re-sign priced capture against this distribution (10-03)."
    - "The public tier column is seeded INDEPENDENTLY of the hidden rarity column — the collection renders ★1-5 stars from tier; rarity never leaks to any UI (D-12/UI-SPEC resolution)."
    - "The starter roster is seeded with starter-appropriate stats: set 1 = Tào Tháo / Lưu Bị / Tôn Kiên, set 2 = Trương Giác / Viên Thiệu / Đổng Trác (names locked D-14) — all six exist with non-null base stats and rarity."
    - "Re-running the seed is idempotent: the heroes row count is unchanged on the second run and no researched value is clobbered with NULL (D-11 clobber-safe upsert)."
  artifacts:
    - scripts/data/sanguo-base-stats.json — { heroId: { str, agi, int, mov, lea, cha, hp, mp, rarity, tier } } for all 132 heroes + the 6 starter entries
    - scripts/seed-sanguo.ts — base-stats/rarity/tier upsert extension
  key_links:
    - "sanguo-base-stats.json heroId keys ↔ scripts/data/sanguo-classifications.json heroId set (the 132-hero canonical key space) — every key must exist, no orphans."
    - "rarity values in the JSON ↔ CAPTURE_BASE_BY_RARITY/FLEE_RATE_BY_RARITY in 10-03 constants ↔ captureService chance/flee math (10-05) — the same rarity number drives capture odds and content."
    - "tier values in the JSON ↔ 10-07 heroes collection line ({{stars}}) — the public display source that keeps rarity hidden."
    - "seed-sanguo.ts upsert columns ↔ 10-02 heroes.ts column names (str/agi/int/mov/lea/cha/hp/mp/rarity/tier) — exact-name contract."
  prohibitions:
    - "No heroId key outside the canonical 132-hero set (cross-checked against sanguo-classifications.json) — orphan keys silently seed nothing."
    - "No out-of-range values: base stats are positive integers, rarity 1-5, tier 1-5 (DB checks in 10-02 enforce this — the seed must not trip them)."
    - "No nameZh clobber regression: the upsert keeps the existing clobber-safe spread pattern (nameZh written only when a researched value exists)."
    - "No UI-facing rarity: the seed writes rarity to the DB column, nothing renders it (D-12)."
  flagged_assumptions:
    - "FLAGGED ASSUMPTION (A2 research, adopted — agent discretion per CONTEXT): base stats are class-template-generated (per-class stat templates + per-hero modifiers) with rarity by prominence tier, NOT hand-tuned per-hero research (TQC-09-grade fidelity was offered in RESEARCH Open Question 2 and defaulted to A2)."
    - "FLAGGED ASSUMPTION (A1 rarity distribution): the rarity weights 60/25/10/4/1 (±2) mirror the signed 10-03 contract — the 132 heroes are binned to rarity so the seeded distribution approximates the weights (small-population rounding accepted and documented in the file header)."
    - "FLAGGED ASSUMPTION (starter stats): the six starter heroes are seeded with starter-appropriate (early-game fair) base stats — slightly above the class-template median for their class so the first hero feels usable, documented in the JSON header."
    - "FLAGGED ASSUMPTION (boss stats): boss thường stat blocks are NOT in this file — they are zone-scaled templates (rarity 5, elevated HP/STR) authored at runtime in 10-05 src/constants/sanguoBoss.ts because encounter_runs bosses have hero_id NULL and no heroes row exists to carry them (A3)."
    - "FLAGGED ASSUMPTION (probe TQC-12 unclassified, unresolved): the starter-grant IV roll distribution is the same uniform crypto.randomInt(0,32)×6 as capture IVs (10-05) — the starter pick (10-07) reuses that exact roll, so a starter can roll any grade from Hôi cấp to Hoàng Kim."

---

<objective>
What this plan accomplishes: **the D-02/D-08 content pass** — base stats + hidden rarity + public tier for all 132 heroes, plus the 6 starter-roster entries, committed as `scripts/data/sanguo-base-stats.json` and wired into the idempotent `seed-sanguo.ts` upsert. Runs ONLY after the 10-02 migration (columns exist) and the 10-03 D-20 re-sign (rarity distribution is part of the signed economy contract — content ships after the gate, per D-20).

Purpose: every battle formula (10-01 combatStat), capture math (10-05), and collection line (10-07) consumes these numbers. The class-template approach (A2) keeps the pass deterministic without a per-hero research round; the rarity distribution ties content to the signed economy.

Output: committed base-stats JSON + extended seed script; `npm run seed:sanguo` writes the columns into the live dev DB idempotently.
</objective>

<execution_context>
@C:/Users/901107/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/901107/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-battle-capture/10-CONTEXT.md
@.planning/phases/10-battle-capture/10-RESEARCH.md
@.planning/phases/10-battle-capture/10-PATTERNS.md
@scripts/seed-sanguo.ts
@scripts/data/sanguo-classifications.json
@src/db/schema/heroes.ts
@src/constants/sanguoCapture.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit scripts/data/sanguo-base-stats.json — base stats + rarity + tier for 132 heroes + 6 starters</name>
  <files>scripts/data/sanguo-base-stats.json</files>
  <read_first>
    - scripts/data/sanguo-classifications.json (the canonical 132-hero key space + class/faction data the templates key off — READ ONLY, do not modify)
    - src/db/schema/heroes.ts (the column names the JSON keys must map to: str/agi/int/mov/lea/cha/hp/mp/rarity/tier)
    - .planning/phases/10-battle-capture/10-RESEARCH.md §A2 (template approach) + §A1 (rarity distribution 60/25/10/4/1) + §Open Question 2
    - .planning/phases/10-battle-capture/10-PATTERNS.md §scripts/data/sanguo-base-stats.json (shape lines 411-421 + the loadClassifications FATAL-guard analog)
    - src/constants/sanguoCapture.ts (RARITY_DISTRIBUTION — the signed distribution the seed must approximate)
  </read_first>
  <action>
    Create `scripts/data/sanguo-base-stats.json` — a flat object keyed by heroId (the natural keys from sanguo-classifications.json):
    - Every key MUST exist in the classifications file's hero set (cross-check programmatically before commit — see verify).
    - Value shape: `{ "str": <int>, "agi": <int>, "int": <int>, "mov": <int>, "lea": <int>, "cha": <int>, "hp": <int>, "mp": <int>, "rarity": <1-5>, "tier": <1-5> }` — base stats positive integers (HP/MP in the 50-300 band, the six stats in the 10-90 band — bands chosen so combatStat = base+IV (0-31) yields sane atk/def deltas under the 10-01 formula; exact values per class template).
    - Generation method (A2): per-class templates (vanguard = high str/hp, cavalry = high agi/mov, archer = high agi/int, spellcaster = high int/mp, schemer = high int/lea, vu_co/thu_binh/cong_binh = balanced max(str,int)) with per-hero ±modifiers by prominence; document the template table in the file's JSON header comment (allowed as a top-level `_comment` key or file-level comment if the tooling permits; otherwise document in the seed script).
    - Rarity: bin all 132 heroes to rarity 1-5 approximating 60/25/10/4/1 weights (≈79/33/13/5/2 for 132 — the two rarest bins are 5 and 2 heroes); heroes with major lore prominence (e.g., the big-name warlords/generals) land in rarity 3-5; distribution deviation ≤2 per bin documented in the header.
    - Tier (public ★1-5): seeded INDEPENDENTLY of rarity — tier reflects public display prominence (roughly correlated but NOT derived from rarity at render time; some high-rarity heroes may show ★3-4, some low-rarity ★2-3); no rule forces tier == rarity.
    - Starter entries: Tào Tháo (cao_cao), Lưu Bị (liu_bei), Tôn Kiên (sun_jian), Trương Giác (zhang_jue), Viên Thiệu (yuan_shao), Đổng Trác (dong_zhuo) — verify the actual heroId strings from sanguo-classifications.json and use those exact keys; starter-appropriate stats ≈ class-template median + small modifier.
  </action>
  <verify>
    <automated>npx tsx -e "import * as fs from 'node:fs'; const stats = JSON.parse(fs.readFileSync('scripts/data/sanguo-base-stats.json','utf8')); const cls = JSON.parse(fs.readFileSync('scripts/data/sanguo-classifications.json','utf8')); const clsIds = new Set(Object.keys(cls)); const statIds = Object.keys(stats); const missing = statIds.filter(id => !clsIds.has(id)); const orphan = [...clsIds].filter(id => !statIds.includes(id)); const badRange = statIds.filter(id => { const v = stats[id]; return !v || [v.str,v.agi,v.int,v.mov,v.lea,v.cha].some(n => !Number.isInteger(n) || n < 1 || n > 200) || !Number.isInteger(v.hp) || v.hp < 50 || v.hp > 300 || !Number.isInteger(v.mp) || v.mp < 10 || v.mp > 200 || ![1,2,3,4,5].includes(v.rarity) || ![1,2,3,4,5].includes(v.tier); }); if (missing.length || orphan.length || badRange.length) { console.error('MISSING:', missing, 'ORPHAN:', orphan, 'BADRANGE:', badRange); process.exit(1); } const dist = [0,0,0,0,0]; statIds.forEach(id => dist[stats[id].rarity-1]++); console.log('RARITY DIST:', dist.join('/')); if (dist[0] < 58 || dist[0] > 80 || dist[4] < 1) { console.error('RARITY DIST OUT OF BOUNDS'); process.exit(1); } console.log('BASE-STATS JSON VERIFIED');"
  </verify>
  <acceptance_criteria>
    - Command: the cross-check above prints `BASE-STATS JSON VERIFIED` and exits 0 (no missing/orphan keys, no out-of-range values).
    - Command: `node -e "JSON.parse(require('fs').readFileSync('scripts/data/sanguo-base-stats.json','utf8')); console.log('PARSE OK')"` prints `PARSE OK`.
    - Source: `scripts/data/sanguo-base-stats.json` contains keys for all six starter heroIds (cao_cao, liu_bei, sun_jian, zhang_jue, yuan_shao, dong_zhuo — verify exact strings against sanguo-classifications.json and use the real ones).
    - Source: every entry has all ten fields (`str`, `agi`, `int`, `mov`, `lea`, `cha`, `hp`, `mp`, `rarity`, `tier`); rarity is an integer 1-5 and tier an integer 1-5 (never strings).
  </acceptance_criteria>
  <done>The committed dataset covers all 132 heroes + 6 starters with in-range base stats, rarity (≈60/25/10/4/1) and independent public tier; the cross-check passes with no orphans or range violations.</done>
  <reversibility rating="costly">Implements D-02/D-08 content values — a reseed replaces numbers, but every battle/capture balance and the D-20 rarity distribution depend on this pass; changing the approach later (e.g., per-hero hand-tuning) means a new content round.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Extend seed-sanguo.ts — base-stats/rarity/tier upsert + starter roster content (idempotent)</name>
  <files>scripts/seed-sanguo.ts</files>
  <read_first>
    - scripts/seed-sanguo.ts (the file being modified — 567 lines; the hero upsert at ~lines 340-394 with the clobber-safe `...(zh ? { nameZh: zh } : {})` spread; the loadClassifications FATAL-guard pattern at lines 169-177)
    - scripts/data/sanguo-base-stats.json (the dataset created in Task 1 — the seed's source of truth for the new columns)
    - .planning/phases/10-battle-capture/10-PATTERNS.md §Idempotent seed upsert (lines 504-512) + §scripts/data/sanguo-base-stats.json (FATAL-guard analog)
  </read_first>
  <action>
    Extend `scripts/seed-sanguo.ts`:
    1. Load the new dataset with the same FATAL guard as loadClassifications: `sanguo-base-stats.json` missing → `console.error('[Seed] FATAL: scripts/data/sanguo-base-stats.json is required...'); process.exit(1)` (this file is REQUIRED — unlike the optional zh-names map, base stats gate every battle/capture).
    2. In the hero upsert (ON CONFLICT heroId DO UPDATE), extend the `set` clause with the ten new columns from the loaded map: `str`, `agi`, `int`, `mov`, `lea`, `cha`, `hp`, `mp`, `rarity`, `tier` — using the clobber-safe spread style: write each value from the dataset when the heroId has an entry; NEVER write NULL over an existing value (a dataset entry missing a heroId must leave the column untouched, mirroring the nameZh `...(zh ? {...} : {})` pattern — the dataset is complete per Task 1, so the spread is unconditional-but-guarded for safety).
    3. If the six starter heroes are NOT already in the 132-hero set, add them via the SAME upsert path (same heroId keys as the dataset) so the starter picker (10-07) finds their rows; if they are already seeded heroes, no extra insert is needed — only the dataset entries.
    4. Keep the script idempotent: re-running must not change the heroes row count (verify below) and must not clobber researched nameZh/classification values.
  </action>
  <verify>
    <automated>npm run seed:sanguo; npm run seed:sanguo; npx tsx -e "import { drizzle } from 'drizzle-orm/node-postgres'; import { Pool } from 'pg'; const db = drizzle({ client: new Pool({ connectionString: process.env.DATABASE_URL_DIRECT ?? process.env.DATABASE_URL, max: 1 }) }); const heroes = await db.execute('SELECT COUNT(*) AS c, COUNT(str) AS s, COUNT(rarity) AS r, COUNT(tier) AS t FROM heroes'); console.log(heroes.rows[0]); const starters = await db.execute(\"SELECT hero_id FROM heroes WHERE hero_id IN ('cao_cao','liu_bei','sun_jian','zhang_jue','yuan_shao','dong_zhuo')\"); console.log('STARTERS:', starters.rows.length);"
  </verify>
  <acceptance_criteria>
    - Command: `npm run seed:sanguo` exits 0 TWICE (idempotency run).
    - Command: the DB probe above prints the heroes count row — `c` is unchanged between the two seed runs and equals the pre-pass 132-hero count (or 132 + starter additions if starters were new rows), `s`/`r`/`t` (COUNT(str)/COUNT(rarity)/COUNT(tier)) all equal `c` (no NULL base stats/rarity/tier anywhere), and `STARTERS: 6`.
    - Source: `scripts/seed-sanguo.ts` contains the base-stats dataset load with the FATAL guard (`process.exit(1)` on missing file) and the ten new columns in the hero upsert `set` clause.
    - Command: `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>The seed writes base stats + rarity + tier for every hero idempotently (double-run stable, no NULL columns), the starter roster rows exist, and the FATAL guard protects the new dataset. Content is live in the dev DB and ready for 10-05 battle/capture and 10-07 collection.</done>
  <reversibility rating="costly">Implements the D-02 content-shipping pass — the seed data is the phase's balance foundation; re-running overwrites values, so any later rebalance is a new content pass (not a schema change).</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed script → heroes table | The idempotent upsert writes content columns into the live DB; a malformed dataset (out-of-range rarity/tier, orphan keys) silently degrades battle/capture/collection behavior. |
| content dataset ↔ signed economy | The rarity distribution in the dataset must match the D-20 contract — drift rebalances capture odds without a sign-off. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-04-01 | Tampering | Malformed base-stats dataset (out-of-range values, orphans) | medium | mitigate | The Task 1 cross-check rejects missing/orphan keys and out-of-range values BEFORE the seed runs; the DB checks (10-02 rarity_range/tier_range) are the second line of defense. |
| T-10-04-02 | Tampering | Rarity-distribution drift vs the signed D-20 contract | medium | mitigate | The verify prints the seeded distribution and bounds rarity-1 (58-80) and rarity-5 (≥1); the 10-03 doc stays the signed reference. |
| T-10-04-03 | Spoofing | NULL-clobber of researched values on reseed | medium | mitigate | The clobber-safe spread pattern (nameZh + new columns) never writes NULL over an existing value; idempotency verified by the double-run probe. |
| T-10-04-04 | Tampering | Rarity leaking to UI through the seed | low | mitigate | The seed writes rarity to its hidden column; no UI consumer exists (D-12) and 10-07 renders tier only. |
| T-10-04-SC | Tampering | npm/pip/cargo installs | — | not-triggered | This plan installs no packages. |
</threat_model>

<verification>
- `node -e` JSON parse + the tsx cross-check print `BASE-STATS JSON VERIFIED` (132+6 entries, in-range, no orphans).
- `npm run seed:sanguo` runs twice cleanly; the DB probe shows COUNT(str)=COUNT(rarity)=COUNT(tier)=heroes count and 6 starters.
- `npm run typecheck` green.
</verification>

## Artifacts this phase produces

| Symbol | Kind | File |
|--------|------|------|
| `scripts/data/sanguo-base-stats.json` — { heroId: {str,agi,int,mov,lea,cha,hp,mp,rarity,tier} } | content dataset (A2, 132 heroes + 6 starters) | scripts/data/sanguo-base-stats.json |
| Base-stats/rarity/tier upsert extension + FATAL guard + starter rows | seed-script logic | scripts/seed-sanguo.ts |

<success_criteria>
- All 132 heroes + 6 starters carry in-range base stats, hidden rarity (≈60/25/10/4/1), and independent public tier in the live DB.
- The seed is idempotent (double-run stable, no NULL clobber) and the dataset is cross-validated against the canonical heroId space.
- 10-05 (battle/capture) and 10-07 (collection) read real seeded numbers; the D-20 rarity contract holds.
</success_criteria>

<output>
Create `.planning/phases/10-battle-capture/10-04-SUMMARY.md` when done.
</output>
