---
phase: 10-battle-capture
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/constants/sanguoCapture.ts
  - docs/economy-budget.md
  - src/constants/__tests__/sanguoCapture.test.ts
autonomous: false
requirements:
  - TQC-11

estimate:
  tokens: 36000
  raw_tokens: 18000
  tasks: 3
  confidence: low

assumption_delta: "no-change — orchestrator-confirmed: copy-level signals only."

must_haves:
  truths:
    - "A capture-tier config module (src/constants/sanguoCapture.ts) models ALL 5 tiers: fee (bigint, matches users.balance) + capture-chance multiplier + requiresItem gate for tiers 4-5; Phase 10 activates tiers 1-3 (D-09 — engine + schema model all 5, tiers 4-5 unlock via special items in Phase 11/events)."
    - "The capture fee NEVER rides the customId or the interaction payload — only the tier number is sent; fee + multiplier resolve server-side from this config (anti-tamper, UI-SPEC component contract)."
    - "docs/economy-budget.md carries a new RE-SIGN block (2026-08-13, Phase 10 D-20) documenting the 5-tier capture-fee table and the recomputed E[net/hour] of the optimal loop with pull-driven encounter supply (≤20/hr): E[inflow]=0 (starter is the only, one-time faucet; no sanguo money-minting) and E[outflow]=E[attempts/hr]×E[fee/attempt] → E[net] <= 0 (D-19 hard constraint) and gross flow < ~416/hr magnitude bound."
    - "The free starter (10-07) is the ONLY faucet exception — capture fees are pure sinks via wallet.deductBalance (D-19 Phase 8 constraint restated in the re-sign)."
  artifacts:
    - src/constants/sanguoCapture.ts — CAPTURE_TIERS (5), CAPTURE_BASE_BY_RARITY, FLEE_RATE_BY_RARITY, PITY_INCREMENT, hpFactor(), RARITY_DISTRIBUTION
    - src/constants/__tests__/sanguoCapture.test.ts — tier/rarity constant sanity tests
    - docs/economy-budget.md — RE-SIGN block with the priced table + E[net/hour] computation + sign-off
  key_links:
    - "CAPTURE_TIERS config ↔ 10-05 captureService attempt tx — the fee resolved server-side from this config (never payload) ↔ wallet.deductBalance ledger rows ('sanguo_capture_t{n}' reasons)."
    - "CAPTURE_BASE_BY_RARITY + FLEE_RATE_BY_RARITY ↔ 10-04 content seed rarity distribution (60/25/10/4/1) — the chance/flee math reads the same rarity values the seed writes."
    - "The re-sign's E[net/hour] computation ↔ the A1 draft table — if the user adjusts tier values at the checkpoint, Task 3 recomputes with the final numbers."
  prohibitions:
    - "No fee amount literal in any customId, button label beyond the display copy, or interaction payload — server-side config only (anti-tamper)."
    - "No new faucet anywhere — the starter (10-07) is the ONLY faucet; the re-sign restates D-19 net-sink/neutral as a hard constraint."
    - "No UI exposure of tier multipliers, flee %, pity increment, or base capture rates — hidden mechanics (D-12); the config file is never rendered."
  flagged_assumptions:
    - "FLAGGED ASSUMPTION (probe TQC-11 boundary, unresolved): captureChance is clamped to [0,1] — a rolled value of exactly 0 always fails (strict roll < chance), and a chance of exactly 1 (clamped) always succeeds; the clamp happens AFTER pity addition."
    - "FLAGGED ASSUMPTION (probe TQC-11 adjacency, unresolved): the success boundary is strict — roll < chance succeeds, roll == chance fails; the flee boundary is likewise strict (roll < fleeRate); both roll and chance are stored in the audit row so SC2 is checkable."
    - "FLAGGED ASSUMPTION (probe TQC-11 precision, unresolved): the DISPLAYED percentage is floor(chance×100) while the ROLL uses the exact unrounded chance; the audit row stores the exact chance (displayed_chance) and the exact roll — display rounding never affects the outcome (Pitfall 2)."
    - "FLAGGED ASSUMPTION (A1 research): the starting 5-tier table is T1=10💎×1.0, T2=30💎×1.5, T3=80💎×2.0, T4=200💎×3.0 (item-gated), T5=500💎×5.0 (item-gated); base capture R1 .80/R2 .55/R3 .35/R4 .20/R5 .10; HP factor Pokemon-standard (3×max−2×cur)/(3×max); pity +5pp/attempt; flee R1 .10/R2 .20/R3 .35/R4 .55/R5 .75; rarity distribution 60/25/10/4/1 — the checkpoint below is the user's last cheap confirmation before these become the D-20 contract."

---

<objective>
What this plan accomplishes: **the D-18/D-20 economy re-sign — the BLOCKING gate before any Phase 10 content ships.** Task 1 is a checkpoint:decision confirming the 5-tier capture-fee table (D-09 one-way); Task 2 authors `src/constants/sanguoCapture.ts` (all capture math constants: 5 tiers with fee+multiplier+item-gate, base capture by rarity, flee rates, pity increment, HP factor, rarity distribution) — the single server-side source the captureService reads; Task 3 recomputes E[net/hour] of the optimal loop with pull-driven encounter supply (≤20/hr) and re-signs `docs/economy-budget.md` (D-20), asserting E[net] <= 0 (D-19 hard constraint) and gross flow < ~416/hr.

Purpose: the milestone's economic guardrail. RESEARCH A1 supplies draft values; the re-sign turns them into a signed contract the capture economy cannot silently rebalance away from. Content (10-04 seed, which prices nothing but carries the same rarity distribution) and the capture service (10-05, which spends the fees) both depend on this gate.

Output: capture constants module + its sanity tests + the re-signed economy budget.
</objective>

<execution_context>
@C:/Users/901107/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/901107/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-battle-capture/10-CONTEXT.md
@.planning/phases/10-battle-capture/10-RESEARCH.md
@.planning/phases/10-battle-capture/10-PATTERNS.md
@docs/economy-budget.md
@src/constants/gatherFees.ts
@src/services/wallet.ts
@src/db/schema/walletTransactions.ts
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <decision>Confirm the 5-tier capture-fee table + capture/flee/pity constants before they become the D-20 economy contract (D-09 one-way). Draft A1 (RESEARCH): tiers T1=10💎×1.0, T2=30💎×1.5, T3=80💎×2.0, T4=200💎×3.0 (item-gated), T5=500💎×5.0 (item-gated); base capture by rarity R1 .80 / R2 .55 / R3 .35 / R4 .20 / R5 .10; HP factor Pokemon-standard (3×HPmax − 2×HPcurrent)/(3×HPmax); pity +5pp per failed attempt; flee rates R1 .10 / R2 .20 / R3 .35 / R4 .55 / R5 .75; rarity distribution 60/25/10/4/1.</decision>
  <context>D-09 (5-tier fee model) and D-20 (re-sign gate) are one-way decisions: once the table is signed into docs/economy-budget.md and the captureService spends it, rebalancing requires a NEW economy sign-off. New users start at 0 Linh thạch (economy-budget.md:17) — funding flows from the main game (football winnings); sanguo is a pure sink. The draft values must satisfy E[net/hour] <= 0 AND feel fair (affordable first captures; rare chase drains). This checkpoint is the user's last cheap confirmation before the values become the contract.</context>
  <options>
    <option id="adopt-a1">
      <name>Adopt draft A1 as-is</name>
      <pros>Research-priced, internally consistent (rarity distribution ↔ base capture ↔ flee all move together); the re-sign computation in Task 3 verifies the constraint numerically before signing.</pros>
      <cons>Values are researcher estimates — first-pass fairness is unproven until live data lands (Phase 12 monitoring).</cons>
    </option>
    <option id="adjust-values">
      <name>Adjust specific values</name>
      <pros>User tunes fees/multipliers/rarity distribution to taste before the one-way sign-off.</pros>
      <cons>Task 2/3 use the adjusted values; the re-sign recomputes with them (same architecture, different numbers).</cons>
    </option>
  </options>
  <resume-signal>Select: adopt-a1 or adjust-values (if adjust-values, state the new tier fees/multipliers or rarity distribution)</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: src/constants/sanguoCapture.ts — the server-side capture config (all 5 tiers + rarity/flee/pity constants)</name>
  <files>src/constants/sanguoCapture.ts, src/constants/__tests__/sanguoCapture.test.ts</files>
  <read_first>
    - src/constants/gatherFees.ts (the fee-config analog — readonly bigint[] table + helper style)
    - .planning/phases/10-battle-capture/10-RESEARCH.md §A1 (draft values — use the checkpoint-confirmed table) + §Pattern 2 (chance formula) + §Common Operation 5 (IV grade)
    - .planning/phases/10-battle-capture/10-PATTERNS.md §src/constants/sanguoCapture.ts (exact shape lines 401-409)
    - src/db/schema/walletTransactions.ts (fee is bigint to match users.balance; reason ≤ 50 chars)
  </read_first>
  <behavior>
    - Test 1: CAPTURE_TIERS has exactly 5 entries; fees strictly ascending; multipliers strictly ascending; tiers 4-5 have a non-null requiresItem; tiers 1-3 have null requiresItem.
    - Test 2: CAPTURE_BASE_BY_RARITY and FLEE_RATE_BY_RARITY each have exactly 5 keys (1-5); base capture strictly decreasing with rarity; flee rate strictly increasing with rarity; all values within [0,1].
    - Test 3: PITY_INCREMENT is in (0, 0.25]; RARITY_DISTRIBUTION sums to 100.
    - Test 4: hpFactor(hpMax=100, hpCurrent=100) === 1/3... more usefully: hpFactor(100, 100) ≈ 0.333, hpFactor(100, 50) ≈ 0.667, hpFactor(100, 0) ≈ 1.0 (lower HP → higher factor), and hpFactor is 0 for hpMax <= 0.
  </behavior>
  <action>
    Create `src/constants/sanguoCapture.ts` using the checkpoint-confirmed table (A1 unless adjusted). Follow gatherFees.ts's readonly-const style:
    - `export interface CaptureTier { tier: number; fee: bigint; multiplier: number; requiresItem: string | null; }` — fee bigint matches users.balance (walletTransactions.ts:17); requiresItem is a Phase 11/event item code placeholder for tiers 4-5 (values: 'capture_tier4_key' / 'capture_tier5_key'), null for tiers 1-3.
    - `export const CAPTURE_TIERS: readonly CaptureTier[]` — the 5 entries (confirmed values; ascending fee and multiplier).
    - `export const CAPTURE_BASE_BY_RARITY: Readonly<Record<number, number>>` — base capture chance per rarity (1-5; strictly decreasing).
    - `export const FLEE_RATE_BY_RARITY: Readonly<Record<number, number>>` — flee chance per rarity (1-5; strictly increasing — rarer flees more, D-10).
    - `export const PITY_INCREMENT = 0.05;` — +5pp per failed attempt (D-11, agent discretion).
    - `export const RARITY_DISTRIBUTION: Readonly<Record<number, number>> = { 1: 60, 2: 25, 3: 10, 4: 4, 5: 1 };` — percent weights per rarity (A1; consumed by the 10-04 seed distribution and the re-sign).
    - `export function hpFactor(hpMax: number, hpCurrent: number): number` — the Pokemon-standard factor `(3×hpMax − 2×hpCurrent) / (3×hpMax)`, 0 for hpMax <= 0, clamped to [0,1] (RESEARCH:432) — lower HP raises the capture factor.
    - Header comment: D-09 (5-tier model), D-12 (never rendered — hidden mechanics), D-20 (values signed in docs/economy-budget.md), and the anti-tamper note (fee resolved server-side from this config; never in a customId).
    - Create `src/constants/__tests__/sanguoCapture.test.ts` with the 4 behaviors (pure constants + hpFactor function tests).
  </action>
  <verify>
    <automated>npx vitest run src/constants/__tests__/sanguoCapture.test.ts; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Command: `npx vitest run src/constants/__tests__/sanguoCapture.test.ts` exits 0 (behaviors 1-4 green).
    - Command: `npm run typecheck` exits 0.
    - Source: `src/constants/sanguoCapture.ts` exports `CAPTURE_TIERS`, `CAPTURE_BASE_BY_RARITY`, `FLEE_RATE_BY_RARITY`, `PITY_INCREMENT`, `RARITY_DISTRIBUTION`, `hpFactor`, `CaptureTier`.
    - Source: `grep -c "requiresItem" src/constants/sanguoCapture.ts` >= 2 — tiers 4-5 carry the item gate; the tier-4/5 entries' requiresItem is non-null (asserted by test 1).
    - Source: `src/constants/sanguoCapture.ts` header comment mentions the D-20 sign-off reference (docs/economy-budget.md) and the never-in-customId rule.
  </acceptance_criteria>
  <done>All capture constants are config-first: 5 tiers (3 active + 2 item-gated), rarity/flee tables, pity increment, HP factor, rarity distribution — sanity-tested and typecheck-green.</done>
  <reversibility rating="one-way">Authors the D-09/D-20 economic constants — once captureService spends these fees and the re-sign documents them, rebalancing requires a new economy sign-off (D-18 one-way gate).</reversibility>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] re-sign docs/economy-budget.md with the 5-tier table + E[net/hour] computation (D-20)</name>
  <files>docs/economy-budget.md</files>
  <read_first>
    - docs/economy-budget.md (the file being modified — 129 lines; the AMENDMENT pattern at line 6; the sign-off section at lines 108-128; the method spec at lines 71-72)
    - src/constants/sanguoCapture.ts (the confirmed values from Task 2 — the re-sign documents the SAME numbers the service spends)
    - .planning/phases/10-battle-capture/10-CONTEXT.md D-20 (the gate wording) + D-19 (net-sink/neutral hard constraint)
    - .planning/phases/10-battle-capture/10-RESEARCH.md §Open Question 1 (the re-sign method: E[net/hour] = E[inflow] − E[outflow] over the loop; realistic human cadence 5-10 encounters/hr vs theoretical 20)
  </read_first>
  <action>
    Edit `docs/economy-budget.md`:
    1. Under the existing AMENDMENT line 6 (or as a new RE-SIGN amendment directly after it), add a **RE-SIGN (2026-08-13, Phase 10 D-20)** amendment block documenting:
       - The confirmed 5-tier capture-fee table: fee (bigint) + multiplier per tier, tiers 4-5 item-gated (Phase 11/events unlock), values from src/constants/sanguoCapture.ts.
       - The supporting constants: base capture by rarity, flee rate by rarity, pity +5pp/attempt, HP factor formula, rarity distribution 60/25/10/4/1.
       - The recomputed **E[net/hour]** with pull-driven encounter supply (≤20/hr; realistic human cadence ~5-10/hr — document BOTH the theoretical 20/hr and the realistic cadence):
         - E[inflow] = 0 — the free starter is a ONE-TIME grant, not recurring; no sanguo mechanic mints Linh thạch (D-19).
         - E[outflow] = E[encounters resolved as capture attempts/hr] × E[fee/attempt] where E[fee/attempt] = Σ over the rarity-weighted attempt mix of (base attempt rate per rarity) × (tier multiplier blend, assumed tier 1 dominant for affordable early capture) — compute the actual number with the signed table.
         - State the conclusion: E[net] = 0 − E[outflow] < 0 → satisfies D-19 (net <= 0); gross per-hour flow stays below ~416/hr magnitude bound (state the computed gross).
       - A new sign-off line: "**RE-SIGNED (2026-08-13, Phase 10 D-20):** the capture-fee values above are the approved Phase 10 contract; any rebalancing requires a new sign-off (D-18 one-way gate)."
    2. Do NOT modify the convertibility matrix or the D-19 constraint section (they remain valid); the RE-SIGN block appends to the amendment chain.
    3. Update the one-line summary at the top only if needed to reflect the Phase 10 re-sign (keep the diff minimal).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const d=fs.readFileSync('docs/economy-budget.md','utf8');const re=/RE-SIGN \(2026-08-13, Phase 10 D-20\)/;const net=/E\[net\][^\n]*<= 0|E\[net\][^\n]*< 0|net[^\n]*satisfies D-19/i;const fee=/T[1-5][^\n]*💎/i;if(!re.test(d)||!net.test(d)||!fee.test(d)){console.error('RE-SIGN block incomplete');process.exit(1)}console.log('RE-SIGN VERIFIED')"</automated>
  </verify>
  <acceptance_criteria>
    - Command: the verification above prints `RE-SIGN VERIFIED` and exits 0 — the doc contains the D-20 RE-SIGN block, the E[net]<=0 conclusion, and the 5-tier 💎 fee table.
    - Source: `docs/economy-budget.md` contains the string `RE-SIGNED (2026-08-13, Phase 10 D-20)` (the sign-off line).
    - Source: `docs/economy-budget.md` RE-SIGN block states E[inflow] = 0 (starter is one-time) and the E[outflow] computation with the encounter-supply cadence assumption (≤20/hr theoretical, ~5-10/hr realistic).
    - Source: the fee values in the RE-SIGN block match `src/constants/sanguoCapture.ts` CAPTURE_TIERS (grep the doc for each fee/multiplier pair or assert by eye against Task 2's confirmed table).
  </acceptance_criteria>
  <done>docs/economy-budget.md carries the D-20 re-sign: the 5-tier capture-fee contract + supporting constants + E[net/hour] computation proving net-sink/neutral (D-19) and gross < ~416/hr, signed 2026-08-13. The BLOCKING gate is closed — Phase 10 content (10-04 seed) may now ship.</done>
  <reversibility rating="one-way">The D-20 re-sign is the milestone's one-way economy gate — rebalancing the fees/multipliers after this sign-off requires a NEW sign-off (D-18).</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| interaction payload → capture fee path | The tier number in a component customId crosses this boundary; the fee NEVER does — it resolves server-side from CAPTURE_TIERS inside the capture tx (10-05). |
| economy-budget doc ↔ capture constants | The signed contract must stay identical to the config the service spends — drift between doc and code is the rebalance-by-accident threat. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-03-01 | Tampering | Crafted customId fee tampering | high | mitigate | The fee is NEVER in the customId or payload — `sanguo:capture:tier:{n}` carries only the tier; fee + multiplier resolve from CAPTURE_TIERS server-side inside the 10-05 FOR UPDATE tx (UI-SPEC anti-tamper contract; Pitfall 3). |
| T-10-03-02 | Tampering | Economy rebalance-by-drift (doc ≠ constants) | medium | mitigate | Task 3's acceptance criteria asserts the doc table matches CAPTURE_TIERS; the constants file header references the doc sign-off — the two artifacts are cross-referenced. |
| T-10-03-03 | Spoofing | Faucet insertion (a new money-minting path) | high | mitigate | The re-sign restates D-19 net-sink/neutral as a hard constraint with E[inflow]=0; the starter (10-07) is the only documented faucet exception; Phase 12 audit consumes this doc as the baseline. |
| T-10-03-SC | Tampering | npm/pip/cargo installs | — | not-triggered | This plan installs no packages. |
</threat_model>

<verification>
- `npx vitest run src/constants/__tests__/sanguoCapture.test.ts` green; `npm run typecheck` green.
- Node probe prints `RE-SIGN VERIFIED` — the D-20 block, the E[net]<=0 conclusion, and the 💎 tier table are all present in docs/economy-budget.md.
- Checkpoint:decision (Task 1) resolved by the user before Tasks 2-3 execute — the plan is `autonomous: false`.
- The BLOCKING gate is closed before 10-04 (content seed) runs.
</verification>

## Artifacts this phase produces

| Symbol | Kind | File |
|--------|------|------|
| `CaptureTier` interface (tier/fee/multiplier/requiresItem) | type | src/constants/sanguoCapture.ts |
| `CAPTURE_TIERS` (5 tiers; 1-3 active, 4-5 item-gated) | config constant (D-09) | src/constants/sanguoCapture.ts |
| `CAPTURE_BASE_BY_RARITY` / `FLEE_RATE_BY_RARITY` (1-5) | config constants (D-08/D-10) | src/constants/sanguoCapture.ts |
| `PITY_INCREMENT` (0.05) / `RARITY_DISTRIBUTION` (60/25/10/4/1) | config constants (D-11/A1) | src/constants/sanguoCapture.ts |
| `hpFactor(hpMax, hpCurrent)` | pure function (Pokemon HP factor) | src/constants/sanguoCapture.ts |
| sanguoCapture.test.ts — 4 constant/function behaviors | test suite | src/constants/__tests__/sanguoCapture.test.ts |
| RE-SIGN (2026-08-13, Phase 10 D-20) block + E[net/hour] computation + sign-off line | economy gate document | docs/economy-budget.md |

<success_criteria>
- The 5-tier capture-fee contract is signed into the economy budget (D-20 closed) with E[net/hour] <= 0 under pull-driven supply and gross < ~416/hr.
- The constants the captureService will spend (10-05) are config-first, sanity-tested, and cross-referenced with the signed doc.
- No faucet exists outside the starter; D-12 hidden mechanics hold (no tier multiplier / flee % / pity visible anywhere).
</success_criteria>

<output>
Create `.planning/phases/10-battle-capture/10-03-SUMMARY.md` when done.
</output>
