---
phase: 10-battle-capture
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema/heroes.ts
  - src/db/schema/userHeroes.ts
  - src/db/schema/sanguoBattles.ts
  - src/db/schema/encounterRuns.ts
  - src/db/schema/captureAttempts.ts
  - src/db/schema/userSanguoState.ts
  - src/db/schema/index.ts
  - migrations/0019_*.sql
autonomous: true
requirements:
  - TQC-10
  - TQC-11
  - TQC-12
  - TQC-13

estimate:
  tokens: 38000
  raw_tokens: 19000
  tasks: 2
  confidence: low

assumption_delta: "no-change — orchestrator-confirmed: Phase 10 copy-level signals are vocabulary false-positives, not identity/config-model transitions."

must_haves:
  truths:
    - "heroes carries the 8 base-stat columns STR/AGI/INT/MOV/LEA/CHA + HP + MP and a rarity column (1-5) with a check constraint — combatStat = base + IV, HP/MP base-only (D-02/D-05/D-08)."
    - "heroes carries a public display tier column (★1-5) seeded INDEPENDENTLY of the hidden rarity column — the collection renders stars from tier, never from rarity (D-12 never-render + UI-SPEC resolution)."
    - "user_heroes carries hp_current (0 = fainted, set to the hero's base HP at capture/starter) and captured_zone (zone snapshot at capture for the /sanguo heroes zone filter) (D-04/A5)."
    - "sanguo_battles carries encounter_id (nullable FK), type ('encounter'|'spar'), seed (bigint with mode 'number'), input jsonb and result jsonb — the D-06 replay record (seed + full stat snapshot; replay = re-run engine(seed, input); the seed column uses mode 'number' so the stored value reads back as a JS number for runBattle(seed: number))."
    - "encounter_runs carries pity_count (smallint default 0) — the per-encounter bad-luck counter incremented per failed capture attempt, reset on success/flee/retreat (D-11)."
    - "A new capture_attempts audit table records EVERY attempt — user_id, encounter_id, tier, fee bigint, displayed_chance float8, roll float8, outcome ('success'|'fail'|'flee'), pity_before, created_at, indexed (user_id, created_at) — including failures (TQC-11/SC2, ASVS repudiation)."
    - "A new user_sanguo_state table holds one row per user: active_hero_id (FK user_heroes.id) + starter_views counter for the D-14 starter-set rotation (A4)."
    - "The migration (0019) applies cleanly via `npm run migrate` and the new columns/tables are observable in the live database (schema push gate)."
  artifacts:
    - src/db/schema/heroes.ts — 8 base-stat columns + rarity + tier + checks
    - src/db/schema/userHeroes.ts — hp_current + captured_zone
    - src/db/schema/sanguoBattles.ts — encounter_id/type/seed/input/result
    - src/db/schema/encounterRuns.ts — pity_count + extended status vocabulary comment
    - src/db/schema/captureAttempts.ts — NEW audit table
    - src/db/schema/userSanguoState.ts — NEW one-row-per-user state table
    - src/db/schema/index.ts — captureAttempts + userSanguoState re-exports
    - migrations/0019_*.sql — generated + applied
  key_links:
    - "heroes.rarity/tier columns ↔ 10-04 content seed (sanguo-base-stats.json fills them) ↔ captureService chance/flee math (10-05) — the column names are the contract."
    - "sanguo_battles.seed/input/result jsonb ↔ 10-01 CombatantInput/BattleResult types ↔ 10-05 battleCheckInService — the replay record stores the exact shapes the engine consumes."
    - "user_sanguo_state.activeHeroId ↔ 10-05 HP gate + 10-07 companion switch — single-writer FOR UPDATE on the same row."
    - "encounter_runs.pity_count + capture_attempts ↔ 10-05 captureService attempt tx — pity read/write and the audit row live in the SAME transaction as the fee + roll (Pitfall 2/3)."
  prohibitions:
    - "No IV-related changes to user_heroes' six iv_* columns or their 0-31 check constraints — they are final from Phase 8 (TQC-02)."
    - "No public exposure of rarity — the column exists for the engine/economy only; no UI reads it (D-12)."
    - "No migration hand-written — drizzle-kit generate produces 0019; hand-editing generated SQL is forbidden (Don't Hand-Roll table)."
    - "No status enum change on encounter_runs — status stays varchar(20); the Phase 10 statuses 'captured'|'fled'|'skipped'|'escaped' are documented in the comment, enforced in service code (A7)."
  flagged_assumptions:
    - "FLAGGED ASSUMPTION (A4 research, adopted): active-companion state = new user_sanguo_state table (user_id unique, active_hero_id FK user_heroes.id nullable, starter_views counter) — CONTEXT leaves 'the active-companion user-state shape' to agent discretion; the table form mirrors player_travel_state."
    - "FLAGGED ASSUMPTION (A5 research, adopted): user_heroes gains hp_current (smallint, 0 = fainted) + captured_zone (varchar(50)) — CONTEXT leaves the HP storage location to agent discretion; captured_zone is the zone snapshot enabling TQC-13 zone grouping without a heroes.zone column."
    - "FLAGGED ASSUMPTION (A6 research, adopted): sanguo_battles extension = encounter_id nullable FK + type varchar(20) ('encounter'|'spar') + seed bigint + input jsonb + result jsonb; statuses 'pending'|'completed' — CONTEXT explicitly leaves the seed+input schema to the agent."
    - "FLAGGED ASSUMPTION (A8 research, adopted): capture_attempts audit shape = user_id, encounter_id, tier smallint, fee bigint, displayed_chance float8, roll float8, outcome varchar(20), pity_before smallint, created_at, index (user_id, created_at) — mirrors wallet_transactions first-class-row philosophy for Phase 12 TQC-19 reports."
    - "FLAGGED ASSUMPTION (A7 research, adopted): encounter_runs.status extends to 'captured'|'fled'|'skipped'|'escaped' — kept as varchar (no enum migration) so the vocabulary is service-enforced and Phase 12 reports can distinguish win/loss attribution."
    - "FLAGGED ASSUMPTION (A11 research): the new heroes.tier public display column (★1-5) is distinct from the code-side spritesheet tier in heroEmoji(heroId, tier) — naming collision only, no functional coupling; heroEmoji keeps its default spritesheet tier."
    - "FLAGGED ASSUMPTION (probe TQC-10/11/12/13 schema boundary, unresolved): hp_current default is 0 at the DB level; the SERVICE (capture/starter insert paths) always writes the hero's base HP explicitly — the default is a safety net for direct inserts, never the expected end state."

---

<objective>
What this plan accomplishes: **migration 0019** — the schema foundation every Phase 10 service and surface builds on. Extends `heroes` (8 base-stat columns + rarity + public tier, D-02/D-08), `user_heroes` (hp_current + captured_zone, D-04/A5), `sanguo_battles` (encounter_id/type/seed/input/result — the D-06 replay record), `encounter_runs` (pity_count, D-11); creates `capture_attempts` (the TQC-11 audit table incl. failures) and `user_sanguo_state` (active companion + starter-views counter, A4/D-14); wires the new tables into the schema index; generates and applies migration 0019 via `drizzle-kit generate` + `npm run migrate` (the [BLOCKING] schema push gate).

Purpose: all battle/capture/collection code in 10-04..10-07 consumes these columns — locking the column names and types NOW prevents 10-05's capture tx and 10-07's collection query from churning schema in later waves.

Output: evolved schema files + applied migration 0019 (observable in the live dev DB).
</objective>

<execution_context>
@C:/Users/901107/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/901107/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-battle-capture/10-CONTEXT.md
@.planning/phases/10-battle-capture/10-RESEARCH.md
@.planning/phases/10-battle-capture/10-PATTERNS.md
@src/db/schema/heroes.ts
@src/db/schema/userHeroes.ts
@src/db/schema/sanguoBattles.ts
@src/db/schema/encounterRuns.ts
@src/db/schema/walletTransactions.ts
@src/db/schema/playerTravelState.ts
@src/db/schema/index.ts
@drizzle.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema evolution — heroes base stats/rarity/tier, user_heroes HP/zone, sanguo_battles replay record, encounter_runs pity, new capture_attempts + user_sanguo_state tables</name>
  <files>src/db/schema/heroes.ts, src/db/schema/userHeroes.ts, src/db/schema/sanguoBattles.ts, src/db/schema/encounterRuns.ts, src/db/schema/captureAttempts.ts, src/db/schema/userSanguoState.ts, src/db/schema/index.ts</files>
  <read_first>
    - src/db/schema/heroes.ts (the file being modified — 68 lines; add columns in the existing style, keep the heroId natural key + per-locale name columns)
    - src/db/schema/userHeroes.ts (the file being modified — 49 lines; the six iv_* columns + 0-31 checks at lines 27-42 are FINAL — do not touch them)
    - src/db/schema/sanguoBattles.ts (the file being modified — 23 lines; existing jsonb roundLogs pattern line 16)
    - src/db/schema/encounterRuns.ts (the file being modified — 35 lines; existing index pattern lines 27-31)
    - src/db/schema/walletTransactions.ts (the audit-table analog for captureAttempts — first-class rows + indexes)
    - src/db/schema/playerTravelState.ts (the one-row-per-user analog for userSanguoState — userId unique pattern)
    - src/db/schema/index.ts (the file being modified — add re-exports in the Phase 8 group)
    - .planning/phases/10-battle-capture/10-RESEARCH.md §A4-A8 (schema shapes the planner adopted) + §Architecture Patterns Pattern 3 (IV grade) + §Don't Hand-Roll (migrations)
    - .planning/phases/10-battle-capture/10-PATTERNS.md §Modified files (exact column additions table lines 425-440)
  </read_first>
  <action>
    Edit `src/db/schema/heroes.ts` — add AFTER the existing `class` column, BEFORE `familyId` (or after familyId — order is cosmetic; keep the content columns grouped):
    - `str: integer('str').notNull().default(0)`, `agi`, `int`, `mov`, `lea`, `cha` (six integer columns, same style), `hp: integer('hp').notNull().default(100)`, `mp: integer('mp').notNull().default(50)` — the 8 base-stat columns (D-02; numeric values are content-seeded in 10-04, defaults are placeholder-safe).
    - `rarity: smallint('rarity').notNull().default(1)` — D-08 hidden rarity (1-5); add to the table config: `check('rarity_range', sql`${table.rarity} >= 1 AND ${table.rarity} <= 5`)`.
    - `tier: smallint('tier').notNull().default(1)` — public display stars ★1-5 (UI-SPEC resolution — independent of rarity, never derived from it at render time); add `check('tier_range', sql`${table.tier} >= 1 AND ${table.tier} <= 5`)`.
    - Import `smallint` and `check` + `sql` alongside the existing `pgTable, serial, varchar, text, integer, pgEnum` import line.
    - Update the header comment: 8 base-stat columns + hidden rarity (D-08) + public tier (UI-SPEC resolution) + the D-12 never-render note for rarity.

    Edit `src/db/schema/userHeroes.ts` — add after the six iv_* columns, before capturedAt:
    - `hpCurrent: smallint('hp_current').notNull().default(0)` — 0 = fainted (D-04); the capture/starter insert paths write the hero's base HP explicitly (flagged assumption; DB default is a safety net only).
    - `capturedZone: varchar('captured_zone', { length: 50 })` — nullable zone snapshot at capture (A5; NULL for starter grants — starter heroes are not zone-captured).
    - Do NOT touch the iv_* columns or the six iv_*_range checks (TQC-02 final).

    Edit `src/db/schema/sanguoBattles.ts` — add after `roundLogs`:
    - `encounterId: integer('encounter_id').references(() => encounterRuns.id)` — nullable FK (NULL for spar battles); import `encounterRuns` from './encounterRuns.js'.
    - `type: varchar('type', { length: 20 }).notNull().default('encounter')` — values 'encounter'|'spar' (D-17 spar is player-initiated practice).
    - `seed: bigint('seed', { mode: 'number' })` — nullable until a battle is run; the D-06 replay seed (crypto.randomInt(< 2^48) — a safe JS integer; `mode: 'number'` is REQUIRED by drizzle (bigint without a mode fails typecheck) and keeps the value a JS `number` so 10-05 can pass it straight to `runBattle(seed: number)`).
    - `input: jsonb('input').$type<Record<string, unknown>>()` and `result: jsonb('result').$type<Record<string, unknown>>()` — the full stat snapshot + battle result (A6; cast through Record to keep the schema decoupled from the engine types).
    - Update the header comment: replay contract = re-run runBattle(seed, input) === stored roundLogs/result (D-06).

    Edit `src/db/schema/encounterRuns.ts`:
    - Add `pityCount: smallint('pity_count').notNull().default(0)` after `status` — the per-encounter bad-luck counter (D-11).
    - Update the `status` column comment: Phase 10 vocabulary 'pending'|'captured'|'fled'|'skipped'|'escaped' (A7 — kept varchar, service-enforced).
    - Keep the existing `encounter_runs_user_status_idx` index untouched (F2 re-fetch).

    Create `src/db/schema/captureAttempts.ts` (A8) — mirror walletTransactions.ts structure:
    - `captureAttempts = pgTable('capture_attempts', { id: serial('id').primaryKey(), userId: integer('user_id').notNull().references(() => users.id), encounterId: integer('encounter_id').notNull().references(() => encounterRuns.id), tier: smallint('tier').notNull(), fee: bigint('fee').notNull(), displayedChance: doublePrecision('displayed_chance').notNull(), roll: doublePrecision('roll').notNull(), outcome: varchar('outcome', { length: 20 }).notNull(), pityBefore: smallint('pity_before').notNull(), createdAt: timestamp('created_at', { withTimezone: true }).notNull().defaultNow() }, (table) => [ index('capture_attempts_user_created_idx').on(table.userId, table.createdAt) ])`.
    - outcome values 'success'|'fail'|'flee' (documented in the header comment) — the SC2 audit proof: the exact chance is stored as displayedChance, the roll as roll, and the row exists for EVERY attempt including failures (TQC-11, ASVS repudiation).
    - Imports: `pgTable, serial, integer, smallint, bigint, doublePrecision, varchar, timestamp, index` from 'drizzle-orm/pg-core'; `users` and `encounterRuns` from their modules.
    - Export `CaptureAttempt = typeof captureAttempts.$inferSelect; NewCaptureAttempt = typeof captureAttempts.$inferInsert;`

    Create `src/db/schema/userSanguoState.ts` (A4) — mirror playerTravelState.ts:
    - `userSanguoState = pgTable('user_sanguo_state', { id: serial('id').primaryKey(), userId: integer('user_id').notNull().references(() => users.id).unique(), activeHeroId: integer('active_hero_id').references(() => userHeroes.id), starterViews: integer('starter_views').notNull().default(0), createdAt: timestamp('created_at', { withTimezone: true }).notNull().defaultNow(), updatedAt: timestamp('updated_at', { withTimezone: true }).notNull().defaultNow() })`.
    - activeHeroId nullable — no hero before the starter pick; starterViews is the D-14 rotation counter (incremented per /sanguo heroes invocation while the collection is empty; 4th call+ → starter set 2).
    - Imports: `pgTable, serial, integer, timestamp` from 'drizzle-orm/pg-core'; `users`, `userHeroes` from their modules.
    - Export `UserSanguoState = typeof userSanguoState.$inferSelect; NewUserSanguoState = typeof userSanguoState.$inferInsert;`

    Edit `src/db/schema/index.ts` — add to the Phase 8 schema group:
    - `export * from './captureAttempts.js';` and `export * from './userSanguoState.js';` — the drizzle-kit generate at Task 2 reads index.ts, so both MUST be exported here BEFORE generate runs.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Command: `npm run typecheck` exits 0.
    - Source: `src/db/schema/heroes.ts` contains `str: integer('str')`, `agi`, `int`, `mov`, `lea`, `cha`, `hp: integer('hp')`, `mp: integer('mp')`, `rarity: smallint('rarity')`, `tier: smallint('tier')` and the two check constraints `rarity_range` and `tier_range`.
    - Source: `src/db/schema/userHeroes.ts` contains `hp_current` and `captured_zone`; the six `iv_*` columns and all six `iv_*_range` checks remain present (grep `grep -c "iv_str_range" src/db/schema/userHeroes.ts` == 1).
    - Source: `src/db/schema/sanguoBattles.ts` contains `encounter_id`, `type: varchar('type'`, `seed: bigint('seed', { mode: 'number' })`, `input: jsonb('input')`, `result: jsonb('result')`.
    - Source: `src/db/schema/encounterRuns.ts` contains `pity_count`; `encounter_runs_user_status_idx` still present (grep == 1).
    - Source: `src/db/schema/captureAttempts.ts` defines table `capture_attempts` with `displayed_chance`, `roll`, `outcome`, `pity_before`, and index `capture_attempts_user_created_idx`.
    - Source: `src/db/schema/userSanguoState.ts` defines table `user_sanguo_state` with `active_hero_id` FK to user_heroes and `starter_views` default 0, `userId` unique.
    - Source: `src/db/schema/index.ts` contains both `captureAttempts` and `userSanguoState` re-export lines.
  </acceptance_criteria>
  <done>All six schema files carry the Phase 10 columns/tables; index.ts exports the new tables; typecheck green; IV columns untouched.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] generate + apply migration 0019 (schema push gate)</name>
  <files>migrations/0019_*.sql</files>
  <read_first>
    - src/db/schema/index.ts (the aggregate drizzle-kit reads — verify Task 1's re-exports are present BEFORE generating)
    - migrations/0018_sanguo_travel_map.sql (the generated-migration style to expect from drizzle-kit)
    - drizzle.config.ts (the generate/migrate config — schema path, out dir, db url env)
    - .planning/phases/10-battle-capture/10-RESEARCH.md §Don't Hand-Roll (migrations: drizzle-kit generate + npm run migrate — established 0000-0018 chain)
  </read_first>
  <action>
    Run the push AFTER all Task 1 schema modifications are complete and typecheck green:
    1. `npx drizzle-kit generate` — produces `migrations/0019_*.sql` from the schema index. Review the generated SQL once: it MUST contain (a) ALTER TABLE heroes ADD COLUMN str/agi/int/mov/lea/cha/hp/mp/rarity/tier (+ the two check constraints), (b) ALTER TABLE user_heroes ADD COLUMN hp_current/captured_zone, (c) ALTER TABLE sanguo_battles ADD COLUMN encounter_id/type/seed/input/result, (d) ALTER TABLE encounter_runs ADD COLUMN pity_count, (e) CREATE TABLE capture_attempts, (f) CREATE TABLE user_sanguo_state. Do NOT hand-edit the generated SQL (Don't Hand-Roll).
    2. `npm run migrate` — applies 0019 to the dev DB (DATABASE_URL/DATABASE_URL_DIRECT from .env). If drizzle-kit migrate prompts interactively, set the non-interactive flag (`drizzle-kit migrate --force` is NOT valid — instead ensure DATABASE_URL is set and run with `--config=drizzle.config.ts` non-interactively; if the environment blocks it, mark the task for manual intervention per the schema-push gate).
    3. Verify the push observably: run a tsx one-liner against the dev DB asserting each new artifact exists — `npx tsx -e "import { drizzle } from 'drizzle-orm/node-postgres'; import { Pool } from 'pg'; const db = drizzle({ client: new Pool({ connectionString: process.env.DATABASE_URL_DIRECT ?? process.env.DATABASE_URL, max: 1 }) }); const q = async (sql: string) => (await db.execute(sql)).rows; (async () => { const cols = await q(\"SELECT column_name FROM information_schema.columns WHERE table_name = 'heroes'\"); const tables = await q(\"SELECT table_name FROM information_schema.tables WHERE table_schema = 'public'\"); const names = [cols, tables].flat().map(r => Object.values(r)[0]); for (const need of ['str','agi','int','mov','lea','cha','hp','mp','rarity','tier','hp_current','captured_zone','encounter_id','seed','input','result','pity_count','capture_attempts','user_sanguo_state']) { if (!names.includes(need)) { console.error('MISSING: ' + need); process.exit(1); } } console.log('MIGRATION 0019 VERIFIED'); })();"`
  </action>
  <verify>
    <automated>npm run migrate; npx tsx -e "import { drizzle } from 'drizzle-orm/node-postgres'; import { Pool } from 'pg'; const db = drizzle({ client: new Pool({ connectionString: process.env.DATABASE_URL_DIRECT ?? process.env.DATABASE_URL, max: 1 }) }); const q = async (sql: string) => (await db.execute(sql)).rows; (async () => { const cols = await q(\"SELECT column_name FROM information_schema.columns WHERE table_name = 'heroes'\"); const tables = await q(\"SELECT table_name FROM information_schema.tables WHERE table_schema = 'public'\"); const names = [cols, tables].flat().map(r => Object.values(r)[0]); for (const need of ['str','agi','int','mov','lea','cha','hp','mp','rarity','tier','hp_current','captured_zone','encounter_id','seed','input','result','pity_count','capture_attempts','user_sanguo_state']) { if (!names.includes(need)) { console.error('MISSING: ' + need); process.exit(1); } } console.log('MIGRATION 0019 VERIFIED'); })();"; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Command: `npm run migrate` exits 0.
    - Source: a file `migrations/0019_*.sql` exists (glob `Get-ChildItem migrations -Filter "0019_*.sql" | Measure-Object | Select-Object -ExpandProperty Count` == 1).
    - Command: the information_schema verification above prints `MIGRATION 0019 VERIFIED` and exits 0 (all 19 column/table names present in the live DB).
    - Command: `npm run typecheck` exits 0.
    - Source: the generated 0019 SQL was NOT hand-edited — `git diff --stat migrations/0019_*.sql` shows the file as added (untouched by manual edits beyond generate output).
  </acceptance_criteria>
  <done>Migration 0019 generated from the schema index and applied to the dev DB; the information_schema probe confirms every new column and table is live; typecheck green. The schema push gate is satisfied — later plans' services query real columns, not type-config ghosts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| schema files → drizzle-kit generate → live DB | The migration path: generated SQL is applied to PostgreSQL. The DB is the single source of truth for battle/capture state; the schema push gate exists because typecheck passes against config types even when the DB lacks the columns. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-02-01 | Tampering | Hand-edited migration SQL | medium | mitigate | drizzle-kit generate is the only author of 0019; the acceptance criteria asserts the file is added-by-generate (git diff shows no manual edit). Hand-written SQL is forbidden (Don't Hand-Roll). |
| T-10-02-02 | Tampering | IV range constraint loss | medium | mitigate | The six iv_* columns and their 0-31 checks are untouched by this migration (grep gate in Task 1 acceptance); capture IV rolls (10-05) must stay within 0-31 or the DB check rejects the row — a correctness backstop, not a bypass. |
| T-10-02-03 | Tampering | Audit-table absence (failed attempts invisible) | high | mitigate | capture_attempts is a first-class table with an (user_id, created_at) index — Phase 12 TQC-19 reports and the SC2 audit-proof depend on its existence; the schema-push verification probes it explicitly. |
| T-10-02-04 | Spoofing | Rarity column leakage surface | medium | mitigate | rarity is a plain hidden column with NO UI read path planned (D-12); the 10-07 collection queries tier only. The check constraint bounds it 1-5 for engine correctness. |
| T-10-02-SC | Tampering | npm/pip/cargo installs | — | not-triggered | This plan installs no packages (pure-rand installs in 10-01). |
</threat_model>

<verification>
- `npm run typecheck` green after Task 1.
- `npm run migrate` green; information_schema probe prints `MIGRATION 0019 VERIFIED` (all 19 names live).
- `migrations/0019_*.sql` exists and is generated (not hand-edited).
- The schema push gate is closed: the phase CANNOT pass verification without the applied migration — 10-05's capture tx and 10-07's collection query run against real columns.
</verification>

## Artifacts this phase produces

| Symbol | Kind | File |
|--------|------|------|
| `str/agi/int/mov/lea/cha/hp/mp` + `rarity` (hidden) + `tier` (public ★1-5) columns + `rarity_range`/`tier_range` checks | schema columns | src/db/schema/heroes.ts |
| `hp_current` + `captured_zone` columns | schema columns (D-04/A5) | src/db/schema/userHeroes.ts |
| `encounter_id` FK + `type` + `seed` bigint + `input`/`result` jsonb | schema columns (D-06 replay record) | src/db/schema/sanguoBattles.ts |
| `pity_count` column + extended status vocabulary | schema column (D-11/A7) | src/db/schema/encounterRuns.ts |
| `captureAttempts` table (user_id, encounter_id, tier, fee, displayed_chance, roll, outcome, pity_before, created_at + index) | new table (TQC-11 audit) | src/db/schema/captureAttempts.ts |
| `userSanguoState` table (user_id unique, active_hero_id FK, starter_views) | new table (A4/D-14) | src/db/schema/userSanguoState.ts |
| `captureAttempts`/`userSanguoState` re-exports | schema index additions | src/db/schema/index.ts |
| `0019_*.sql` | applied migration (schema push gate) | migrations/0019_*.sql |

<success_criteria>
- Every Phase 10 column/table the later plans name is defined and LIVE in the dev DB (verified by information_schema probe, not just typecheck).
- The IV columns remain Phase-8-final; rarity stays hidden (no UI consumer); tier is the public display source.
- The replay record (seed/input/result), the pity counter, the audit table, and the active-companion state are all first-class schema — later waves build on stable names.
</success_criteria>

<output>
Create `.planning/phases/10-battle-capture/10-02-SUMMARY.md` when done.
</output>
