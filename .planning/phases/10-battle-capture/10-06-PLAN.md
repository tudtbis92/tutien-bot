---
phase: 10-battle-capture
plan: 06
type: execute
wave: 3
depends_on:
  - 10-05
files_modified:
  - src/ui/embeds/buildSanguoBattleLogEmbed.ts
  - src/ui/embeds/buildSanguoCaptureEmbed.ts
  - src/ui/components/sanguoBattleButtons.ts
  - src/ui/components/sanguoCaptureButtons.ts
  - src/commands/sanguo/battle.ts
  - src/commands/sanguo/travel.ts
  - src/commands/sanguo/map.ts
  - src/events/interactionCreate.ts
  - locales/vi/sanguo.json
  - locales/en/sanguo.json
  - locales/zh-cn/sanguo.json
  - src/commands/sanguo/__tests__/battle.test.ts
autonomous: true
requirements:
  - TQC-10
  - TQC-11

estimate:
  tokens: 58000
  raw_tokens: 29000
  tasks: 2
  confidence: low

assumption_delta: "no-change — orchestrator-confirmed: copy-level signals only."

must_haves:
  truths:
    - "The encounter embed (Phase 9) now renders with a 'Chiến đấu' (sanguo:battle:start) + 'Bỏ qua' (sanguo:battle:skip) button row — the D-25 ack button is REPLACED by battle entry (D-01); the old ACK_BTN_ID route is removed from the router and handleAckPress is retired (its FOR UPDATE resume semantics live in the battle/capture resolution path)."
    - "Pressing sanguo:battle:start runs startEncounterBattle and renders the FULL turn-by-turn battle log in ONE embed (D-07): ≤20 turn lines ≤ ~80 chars each (≤ ~1,700 chars < 4,096), SEASON color for encounters; win → a 'Bắt' button (sanguo:capture:open) opens the capture view; loss → 'escaped' resolution embed with travel-resume ETA (humanizeEta)."
    - "Spar (/sanguo battle) renders the same battle log in NEUTRAL color with the spar hint (no stakes, no HP loss, no reward — D-17) and NO capture button."
    - "The capture view renders the SINGLE mechanic number — capture.chance 'Tỷ lệ bắt: **{percent}%**' with percent = floor(chance×100) — plus exactly 3 tier buttons (Mức {tier} — {fee} 💎, fee from server config) + a retreat button in ONE ActionRow (4 components ≤ 5); tiers 4-5 NEVER render without a held special item (D-09/UI-SPEC overflow contract)."
    - "The capture attempt flow: tier press → deferReply → attemptCapture → success = SUCCESS-color 'Đã thu phục' embed with NO buttons; fail + no flee = WARNING-color 'Bắt thất bại' embed with retry + retreat buttons (retry = new fee); flee = DANGER-color 'đã bỏ chạy' embed; retreat = 'Bạn bỏ qua {hero}. Hành trình tiếp tục.' — every editReply passes components: [] when clearing (CR-09-03/04 live-verified)."
    - "Blocked/error states render documented copy: fainted active hero → battle.blocked_fainted (DANGER) with the 'đổi hero đồng hành trước' prompt; no pending encounter → battle.no_encounter; insufficient fee → capture.insufficient with the required amount (fee validated server-side before the roll)."
    - "All new UI strings live in the sanguo namespace (battle.* / capture.* / cmd.battle.*) with identical key sets across vi/en/zh-cn (npm run check-i18n parity); embed colors come ONLY from theme.ts COLORS (SEASON/GOLD/SUCCESS/DANGER/WARNING/NEUTRAL); every embed has embedFooter(shardId) + setTimestamp()."
    - "The interaction router dispatches sanguo:battle:* (prefix) and sanguo:capture:* (prefix for tier:{n}, === for fixed ids) BEFORE the chat-input gate; tier numbers are parseInt+isNaN-guarded then validated 1-5 server-side (V5 input validation, ASVS)."
  artifacts:
    - src/ui/embeds/buildSanguoBattleLogEmbed.ts — buildSanguoBattleLogEmbed(data, t) + formatTurnLine
    - src/ui/embeds/buildSanguoCaptureEmbed.ts — capture view + result-state builder
    - src/ui/components/sanguoBattleButtons.ts — BATTLE_START_ID/BATTLE_SKIP_ID/CAPTURE_OPEN_ID + builders
    - src/ui/components/sanguoCaptureButtons.ts — CAPTURE_TIER_PREFIX/CAPTURE_RETRY_ID/CAPTURE_RETREAT_ID + builders
    - src/commands/sanguo/battle.ts — battleSubcommand + execute (spar) + handleBattleStart/handleCaptureOpen/handleCaptureTierPress/handleCaptureRetryPress/handleCaptureRetreatPress
    - src/commands/sanguo/travel.ts — ack row → battle row (D-01)
    - src/commands/sanguo/map.ts — battle subcommand appended
    - src/events/interactionCreate.ts — sanguo:battle:*/sanguo:capture:* routes; ACK route removed
    - locales/{vi,en,zh-cn}/sanguo.json — battle.*/capture.*/cmd.battle.* keys
  key_links:
    - "Button customIds ↔ interactionCreate routes ↔ battle.ts handlers — the D-01 inversion: sanguo:battle:start replaces ACK_BTN_ID at both the embed row AND the router (Pitfall 7: the old route must be GONE, not dormant)."
    - "Capture tier buttons ↔ attemptCapture(server fee) — the customId carries ONLY the tier (anti-tamper); the fee displayed in the label comes from CAPTURE_TIERS at render time, the fee charged comes from CAPTURE_TIERS at attempt time (same config)."
    - "battle log embed ↔ 10-05 BattleOutcome roundLogs — the embed maps TurnLog[] to ≤20 turn lines; the round cap (10-01) bounds the description (overflow contract)."
    - "travel.ts resolution path ↔ 10-05 skipEncounter/resolution — skip/retreat/loss all resume travel via the service (encounterActive cleared + updatedAt pinned), never by editing state in the command layer."
  prohibitions:
    - "No raw IV numbers, no rarity number, no formula/flee/pity/multiplier rendering anywhere (D-12 hard rule) — only the single capture % and the IV grade in the collection (10-07)."
    - "No fee amount in any customId or payload — display copy only, server config is the source (anti-tamper, Pitfall 3)."
    - "No handleAckPress remnant: the ack route, the ack button builder usage on the encounter embed, and the ack copy path are replaced by battle entry (Pitfall 7)."
    <!-- planner-discipline-allow: ACK_BTN_ID -->
    <!-- planner-discipline-allow: buildAckButton -->
    - "No 5th component in the capture row: exactly 3 tier buttons + 1 retreat in ONE ActionRow (UI-SPEC overflow contract); retry swaps the row content, never appends."
    - "No hardcoded hex colors — theme.ts COLORS only (UI-SPEC color contract)."
  flagged_assumptions:
    - "FLAGGED ASSUMPTION (UI-SPEC gap, agent discretion): the battle-win 'Bắt' button is `sanguo:capture:open` (label capture.open_button 'Bắt') — the UI-SPEC customId table omits it but D-10 mandates 'battle win → Bắt button → capture view'; it is part of the sanguo:capture:* namespace."
    - "FLAGGED ASSUMPTION (probe TQC-11 precision, unresolved, UI layer): the capture view displays floor(chance×100)% exactly as computed by captureService at render time; the attempt re-computes the chance inside the tx — a small % drift between render and press is possible if the player waits (HP/pity unchanged mid-window in practice since the battle is over), and the ATTEMPT always uses the recomputed exact chance (Pitfall 2)."
    - "FLAGGED ASSUMPTION (UI-SPEC backstop, battle-log loading): the seeded engine computes ≤ 20 rounds synchronously (µs) and the handler replies within the 3s window via deferReply → editReply (CR-09-06) — verified by a held-out interaction test, not a skeleton (no skeleton exists on Discord)."
    - "FLAGGED ASSUMPTION (UI-SPEC backstop, capture-view loading): the tier press follows the same defer pattern and the attempt tx completes within the window — held-out interaction test."
    - "FLAGGED ASSUMPTION (A10 environment): E2E interaction tests that hit the travel→encounter chain need REDIS_URL reachable; unit tests mock the services."
    - "FLAGGED ASSUMPTION (D-12 UI): the capture view shows ONLY the % and tier fees — the flee chance, pity counter, and multipliers are never rendered (hidden mechanics)."

---

<objective>
What this plan accomplishes: **the battle/capture Discord surface** — battle log embed (D-07 single embed, ≤20 turn lines), capture view + result embeds (single mechanic number: the %), the button set (fight/skip on the encounter embed replacing the D-25 ack — D-01; Bắt/tier/retry/retreat in the capture flow — D-09/D-10/D-18), the `/sanguo battle` spar command (D-17, NEUTRAL), the interaction router routes (`sanguo:battle:*` / `sanguo:capture:*`, ack route removed), the travel.ts ack→battle inversion, and the full `battle.*`/`capture.*` i18n key sets (3 locales).

Purpose: this is the player's first visible battle/capture experience — the phase's 'is it fun?' checkpoint. It consumes the 10-05 services (never re-implements state) and obeys every UI-SPEC budget (ActionRow widths, description length, colors, D-12 never-render, CR-09 component hygiene).

Output: battle/capture embeds + buttons + spar command + routing + i18n keys + command tests.
</objective>

<execution_context>
@C:/Users/901107/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/901107/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-battle-capture/10-CONTEXT.md
@.planning/phases/10-battle-capture/10-RESEARCH.md
@.planning/phases/10-battle-capture/10-PATTERNS.md
@.planning/phases/10-battle-capture/10-UI-SPEC.md
@src/commands/sanguo/travel.ts
@src/commands/sanguo/map.ts
@src/events/interactionCreate.ts
@src/ui/components/sanguoTravelButtons.ts
@src/ui/embeds/buildSanguoEncounterEmbed.ts
@src/ui/embeds/buildSanguoAckEmbed.ts
@src/ui/embeds/buildErrorEmbed.ts
@src/ui/theme.ts
@src/services/sanguo/battleCheckInService.ts
@src/services/sanguo/captureService.ts
@src/constants/sanguoCapture.ts
@locales/vi/sanguo.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: battle log + capture embeds + button builders + i18n battle/capture keys</name>
  <files>src/ui/embeds/buildSanguoBattleLogEmbed.ts, src/ui/embeds/buildSanguoCaptureEmbed.ts, src/ui/components/sanguoBattleButtons.ts, src/ui/components/sanguoCaptureButtons.ts, locales/vi/sanguo.json, locales/en/sanguo.json, locales/zh-cn/sanguo.json</files>
  <read_first>
    - src/ui/embeds/buildSanguoEncounterEmbed.ts + src/ui/embeds/buildSanguoAckEmbed.ts (the embed-builder pattern: data interface + builder(data, t), theme colors, footer/timestamp)
    - src/ui/components/sanguoTravelButtons.ts (the customId const + builder pattern)
    - src/ui/theme.ts (COLORS keys — SEASON/GOLD/SUCCESS/DANGER/WARNING/NEUTRAL; embedFooter)
    - src/services/sanguo/battleEngine.ts (TurnLog/BattleResult shapes the log embed consumes)
    - src/services/sanguo/captureService.ts (CaptureAttemptResult — the result-state embed consumes outcome/chance)
    - src/constants/sanguoCapture.ts (CAPTURE_TIERS — tier button labels show fee from config)
    - locales/vi/sanguo.json (existing key nesting style; cmd.* section)
    - .planning/phases/10-battle-capture/10-UI-SPEC.md §Component & CustomId Contract + §Copywriting Contract + §Color + §Typography budgets
    - .planning/phases/10-battle-capture/10-PATTERNS.md §embed builders (lines 291-326) + §buttons (lines 328-363)
  </read_first>
  <action>
    Create `src/ui/embeds/buildSanguoBattleLogEmbed.ts`:
    - `export interface SanguoBattleLogEmbedData { playerName: string; enemyName: string; enemyEmoji?: string; roundLogs: TurnLog[]; winner: 'player' | 'enemy'; rounds: number; playerHpAfter: number; enemyHpAfter: number; spar: boolean; shardId?: number }`.
    - `buildSanguoBattleLogEmbed(data, t: TFunction): EmbedBuilder` — `.setColor(spar ? COLORS.NEUTRAL : COLORS.SEASON)` (NEVER hardcode hex; spar = NEUTRAL no-stakes D-17, encounter = SEASON); `.setTitle(t('sanguo:battle.log_title', { player, enemy }))`; description = the turn-by-turn log — ONE line per turn ≤ ~80 chars: `t('sanguo:battle.turn', { n, attacker, defender, dmg })` or the crit variant `t('sanguo:battle.turn_crit', ...)` when crit — joined with '\n' (D-07: single embed, description-only, NEVER fields-per-round — 25-field cap); footer embedFooter(shardId) + timestamp.
    - Resolution footer/description: win → `t('sanguo:battle.win')` + spar hint line `t('sanguo:battle.spar_hint')` when spar; loss → `t('sanguo:battle.loss', { enemy })` (encounter flees; spar loss same line).
    - `export function formatTurnLine(log: TurnLog, t: TFunction): string` — the ≤80-char line builder (unit-testable).

    Create `src/ui/embeds/buildSanguoCaptureEmbed.ts`:
    - `export interface SanguoCaptureEmbedData { heroName: string; heroEmoji?: string; percent: number; // floor(chance×100) — THE single mechanic number; state: 'view' | 'success' | 'fail' | 'flee' | 'retreat'; boss: boolean; shardId?: number }`.
    - `buildSanguoCaptureEmbed(data, t)` — color by state: view = boss ? COLORS.GOLD : COLORS.SEASON; success = COLORS.SUCCESS; fail = COLORS.WARNING (setback, retry open — never DANGER unless flee); flee = COLORS.DANGER; retreat = COLORS.NEUTRAL.
    - View state: title `t('sanguo:capture.title', { hero_emoji, hero })`; description `t('sanguo:capture.chance', { percent })` (the ONLY mechanic number — D-12); tier fees render in the BUTTON labels (not the embed body) per the copywriting contract.
    - Result states: success → `t('sanguo:capture.success_title', { hero_emoji, hero })`; fail → title `t('sanguo:capture.fail_title')` + body `t('sanguo:capture.fail_body')`; flee → `t('sanguo:capture.flee_title', { hero })`; retreat → title + body `t('sanguo:capture.retreat_body', { hero })`.

    Create `src/ui/components/sanguoBattleButtons.ts`:
    - `export const BATTLE_START_ID = 'sanguo:battle:start';` `export const BATTLE_SKIP_ID = 'sanguo:battle:skip';` `export const CAPTURE_OPEN_ID = 'sanguo:capture:open';` (the D-10 'Bắt' button — flagged assumption).
    - `buildBattleStartButton(t)` — label `t('sanguo:battle.fight_button')`, Primary; `buildBattleSkipButton(t)` — label `t('sanguo:battle.skip_button')`, Secondary (same row as start: 2 comps).
    - `buildCaptureOpenButton(t)` — label `t('sanguo:capture.open_button')`, Primary (battle-win row).

    Create `src/ui/components/sanguoCaptureButtons.ts`:
    - `export const CAPTURE_TIER_PREFIX = 'sanguo:capture:tier';` `export const CAPTURE_RETRY_ID = 'sanguo:capture:retry';` `export const CAPTURE_RETREAT_ID = 'sanguo:capture:retreat';`
    - `buildCaptureTierButtons(t, tiers: { tier: number; fee: string }[]): ButtonBuilder[]` — exactly the ACTIVE tiers (Phase 10: 1-3 from CAPTURE_TIERS with null requiresItem); customId `${CAPTURE_TIER_PREFIX}:${tier}` (tier ONLY — the fee NEVER rides the customId, anti-tamper); label `t('sanguo:capture.tier_button', { tier, fee })` ('Mức 1 — 10 💎').
    - `buildCaptureRetryButton(t)` — label `t('sanguo:capture.retry_button')`, Primary; `buildCaptureRetreatButton(t)` — label `t('sanguo:capture.retreat_button')`, Secondary (retry row = retry + retreat, 2 comps).

    Edit `locales/vi/sanguo.json`, `locales/en/sanguo.json`, `locales/zh-cn/sanguo.json` — add `cmd.battle.description` and top-level `battle.*` + `capture.*` sections with IDENTICAL key structures (check-i18n parity; VI canonical, EN/ZH translate per the existing sanguo tone):
    - battle: fight_button 'Chiến đấu', skip_button 'Bỏ qua', log_title '⚔️ Giao chiến: {{player}} vs {{enemy}}', turn 'Lượt {{n}} — {{attacker}} → {{defender}}: **{{dmg}}** sát thương', turn_crit 'Lượt {{n}} — 💥 {{attacker}} chí mạng {{defender}}: **{{dmg}}** sát thương', win '🏆 Chiến thắng!', loss '💔 Thất bại — {{enemy}} đã rời đi', spar_hint 'Tập luyện — không mất HP thật, không có phần thưởng.', blocked_fainted 'Hero đồng hành **{{name}}** đã kiệt sức. Đổi hero bằng /sanguo hero trước khi chiến đấu.', no_encounter 'Không có kỳ ngộ nào đang chờ.', error 'Có lỗi khi giao chiến. Hãy thử lại.'.
    - capture: title '🪤 Thu phục {{hero_emoji}} {{hero}}', chance 'Tỷ lệ bắt: **{{percent}}%**', open_button 'Bắt', tier_button 'Mức {{tier}} — {{fee}} 💎', retry_button 'Bắt lại', retreat_button 'Bỏ qua', success_title '🎉 Đã thu phục {{hero_emoji}} {{hero}}!', fail_title '❌ Bắt thất bại', fail_body 'Bạn có thể **Bắt lại** (tính phí mới) hoặc **Bỏ qua**.', flee_title '🏃 {{hero}} đã bỏ chạy!', retreat_body 'Bạn bỏ qua {{hero}}. Hành trình tiếp tục.', insufficient 'Không đủ Linh thạch (cần {{fee}} 💎). Hãy kiểm tra số dư và thử lại.', error 'Có lỗi khi thu phục. Hãy thử lại.'.
    - cmd.battle.description 'Tập luyện với một hero ngẫu nhiên'.
  </action>
  <verify>
    <automated>npm run check-i18n; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Command: `npm run check-i18n` exits 0 (vi/en/zh-cn key parity).
    - Command: `npm run typecheck` exits 0.
    - Source: `buildSanguoBattleLogEmbed.ts` exports `buildSanguoBattleLogEmbed` + `formatTurnLine`; uses `COLORS.SEASON`/`COLORS.NEUTRAL` (no hex literals — grep `grep -cE "0x[0-9A-Fa-f]{6}" src/ui/embeds/buildSanguoBattleLogEmbed.ts` == 0) and no `.addFields` for turn lines.
    - Source: `buildSanguoCaptureEmbed.ts` uses the five COLORS keys (SEASON/GOLD/SUCCESS/WARNING/DANGER/NEUTRAL) and renders the percent ONLY via the `capture.chance` key (no raw mechanic numbers elsewhere).
    - Source: `sanguoBattleButtons.ts` exports `BATTLE_START_ID`/`BATTLE_SKIP_ID`/`CAPTURE_OPEN_ID`; `sanguoCaptureButtons.ts` exports `CAPTURE_TIER_PREFIX`/`CAPTURE_RETRY_ID`/`CAPTURE_RETREAT_ID` — no `fee` in any customId (grep `grep -c "tier.*fee\|fee.*tier" src/ui/components/sanguoCaptureButtons.ts` == 0).
    - Config: all three locale files contain `battle.log_title`, `battle.turn`, `battle.win`, `battle.loss`, `capture.chance`, `capture.tier_button`, `capture.retreat_button`, `cmd.battle.description`.
    - Source: `buildCaptureTierButtons` renders tiers 1-3 only from CAPTURE_TIERS entries with `requiresItem === null` (tiers 4-5 hidden — D-09).
  </acceptance_criteria>
  <done>Battle log + capture embeds, fight/skip/Bắt/tier/retry/retreat buttons, and the full battle/capture i18n key sets (3 locales) exist with correct colors, budgets, and hidden-mechanics compliance; check-i18n + typecheck green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: /sanguo battle spar command + encounter-battle/capture button handlers + travel.ts ack→battle inversion + interactionCreate routes</name>
  <files>src/commands/sanguo/battle.ts, src/commands/sanguo/travel.ts, src/commands/sanguo/map.ts, src/events/interactionCreate.ts, src/commands/sanguo/__tests__/battle.test.ts</files>
  <read_first>
    - src/commands/sanguo/travel.ts (the exact analog: subcommand builder lines 39-47, execute pattern 304-354, button-handler pattern 520-577 handleAckPress — the FOR UPDATE resume semantics REPLACED by the battle resolution; dispatchCheckIn encounter branch lines 273-293 — the ack row becomes the battle row)
    - src/commands/sanguo/map.ts (subcommand composition lines 29-38, 71-81 — append battle)
    - src/events/interactionCreate.ts (SanguoComponentHandlers interface lines 28-32; the ACK_BTN_ID route lines 106-120 — REPLACE with sanguo:battle:*/sanguo:capture:* routes; the parseInt+isNaN pattern lines 221-227)
    - src/ui/embeds/buildSanguoEncounterEmbed.ts + buildSanguoAckEmbed.ts + buildErrorEmbed.ts (embeds consumed by the handlers)
    - src/services/sanguo/battleCheckInService.ts + src/services/sanguo/captureService.ts (the services the handlers call)
    - src/utils/commandContext.ts (fetchCommandContext — user.id = users.id)
    - .planning/phases/10-battle-capture/10-PATTERNS.md §battle.ts (lines 234-289) + §interactionCreate (lines 514-516) + §Error handling (lines 489-498)
    - .planning/phases/10-battle-capture/10-UI-SPEC.md §Component & CustomId Contract (exact customIds) + §Loading backstops (defer pattern)
  </read_first>
  <behavior>
    - Test 1: interactionCreate routes `sanguo:battle:start`/`sanguo:battle:skip` (prefix `startsWith('sanguo:battle:')`), `sanguo:capture:open` (===), `sanguo:capture:tier:{n}` (prefix), `sanguo:capture:retry`/`retreat` (===) to the sanguo command module handlers BEFORE the chat-input gate; the ACK_BTN_ID route is REMOVED (no dispatch branch remains).
    - Test 2: handleBattleStart → startEncounterBattle → renders buildSanguoBattleLogEmbed (SEASON); on win adds the Bắt button row; on loss renders the loss resolution (no buttons).
    - Test 3: handleCaptureOpen renders the capture view: chance % from captureService, 3 tier buttons + retreat in ONE ActionRow (4 comps).
    - Test 4: handleCaptureTierPress parses the tier from `sanguo:capture:tier:{n}` (parseInt + isNaN guard), calls attemptCapture with it; success → SUCCESS embed components: []; fail-no-flee → WARNING embed with retry + retreat row; flee → DANGER embed components: []; NO_PENDING_ENCOUNTER → battle.no_encounter embed; INSUFFICIENT_BALANCE → capture.insufficient embed (all with components: [] — CR-09-04).
    - Test 5: handleCaptureRetryPress re-renders the capture view with the recomputed % (new attempt); handleCaptureRetreatPress resolves skipEncounter and renders the retreat embed.
    - Test 6: /sanguo battle execute (spar) → startSparBattle → NEUTRAL battle log embed + spar hint, NO capture button; HERO_FAINTED → battle.blocked_fainted DANGER embed.
    - Test 7: the travel.ts encounter branch renders the fight/skip button row (NOT the ack row); buildAckRow is gone from the encounter path.
  </behavior>
  <action>
    Create `src/commands/sanguo/battle.ts`:
    - `export const battleSubcommand = new SlashCommandSubcommandBuilder().setName('battle').setDescription('Tập luyện với một hero ngẫu nhiên')` + localized descriptions (inside the eslint-disable i18next/no-literal-string block, mirroring travel.ts:39-47). NO options.
    - `execute(interaction)` (spar, D-17): `fetchCommandContext` → not-registered guard → `startSparBattle(user.id)` → `buildSanguoBattleLogEmbed({ ..., spar: true })` NEUTRAL; catch: HERO_FAINTED → `buildErrorEmbed(t('sanguo:battle.blocked_fainted', { name }))` DANGER (name from the active companion), NO_SPAR_POOL/generic → `buildErrorEmbed(t('sanguo:battle.error'))`; always `components: []`.
    - `handleBattleStart(interaction: ButtonInteraction)` — `deferUpdate()` → resolve user row (users.discordId → users.id, NEVER char.id) → `resolveLocale` + `getT` → `startEncounterBattle(user.id)` → render the battle log embed; win: `components: [row(buildCaptureOpenButton(t))]`; loss: `components: []`; HERO_FAINTED → battle.blocked_fainted; NO_PENDING_ENCOUNTER → battle.no_encounter.
    - `handleBattleSkip(interaction)` — `deferUpdate()` → `skipEncounter(user.id)` → render the skip/retreat consequence (capture.retreat_body embed, NEUTRAL) `components: []` (D-18: wild departs, travel resumes).
    - `handleCaptureOpen(interaction)` — `deferUpdate()` → render the capture view: chance for the pending encounter via a captureService read helper (`getCaptureView(userId)` — add it in 10-05 captureService or read the last battle result + rarity in the handler through captureService) → embed + `buildCaptureTierButtons(t, activeTiers)` + retreat in ONE row.
    - `handleCaptureTierPress(interaction)` — `deferUpdate()` → parse tier: `interaction.customId.slice(CAPTURE_TIER_PREFIX.length + 1)` → `parseInt` + `isNaN` guard → `attemptCapture(user.id, tier)` in try/catch: success → SUCCESS capture embed `components: []`; fail-no-flee → WARNING embed + retry/retreat row; flee → DANGER embed `components: []`; INSUFFICIENT_BALANCE → capture.insufficient embed; NO_PENDING_ENCOUNTER → battle.no_encounter; TIER_LOCKED → battle.error (unreachable for the 3 rendered tiers but guarded).
    - `handleCaptureRetryPress(interaction)` — re-render the capture view with the recomputed chance (the pending encounter survived the fail) — `deferUpdate()` → same capture-view render as handleCaptureOpen.
    - `handleCaptureRetreatPress(interaction)` — `deferUpdate()` → `skipEncounter(user.id)` → retreat consequence embed `components: []`.

    Edit `src/commands/sanguo/travel.ts` (D-01 ack→battle inversion):
    - Replace `buildAckRow(t)` usage in `dispatchCheckIn`'s encounter/encounterPending branch with the battle row: `[ActionRow(buildBattleStartButton(t), buildBattleSkipButton(t))]` (2 comps, one row).
    - Remove `buildAckButton`/`buildAckRow` from the encounter path and REMOVE `handleAckPress` from travel.ts (its FOR UPDATE resume semantics are already covered by the 10-05 resolution path — Pitfall 7).

    Edit `src/commands/sanguo/map.ts`:
    - Append `.addSubcommand(battleSubcommand)` to the `data` builder; import from './battle.js'.
    - Dispatch `if (subcommand === 'battle') { await battleExecute(interaction); return; }`.
    - Replace the ack re-export with the battle/capture handlers: `export { handleBattleStart, handleBattleSkip, handleCaptureOpen, handleCaptureTierPress, handleCaptureRetryPress, handleCaptureRetreatPress } from './battle.js';` (remove handleAckPress).

    Edit `src/events/interactionCreate.ts`:
    - Extend `SanguoComponentHandlers` with the battle/capture handler signatures (ButtonInteraction → Promise<void>).
    - REPLACE the `ACK_BTN_ID` route block (lines ~106-120) with: `customId.startsWith('sanguo:battle:')` → dispatch handleBattleStart/handleBattleSkip by `===` (BATTLE_START_ID / BATTLE_SKIP_ID); `customId === CAPTURE_OPEN_ID` → handleCaptureOpen; `customId.startsWith(CAPTURE_TIER_PREFIX)` → parse tier with the parseInt/isNaN guard → handleCaptureTierPress; `customId === CAPTURE_RETRY_ID` → handleCaptureRetryPress; `customId === CAPTURE_RETREAT_ID` → handleCaptureRetreatPress — every branch wrapped in try/catch + `logger.error('InteractionCreate', ...)` (component-routing style).
    - Remove the ACK_BTN_ID import if unused; import the new customId consts from the battle/capture button modules.

    Create `src/commands/sanguo/__tests__/battle.test.ts` with the 7 behaviors — mock db/client, commandContext, and vi.mock the battleCheckInService/captureService modules; assert embed colors (SEASON/NEUTRAL/SUCCESS/WARNING/DANGER), component rows (tier count 3 + retreat in ONE row; retry row), components: [] on terminal states, tier parseInt guard, and that the ACK route is gone from the router mock.
  </action>
  <verify>
    <automated>npx vitest run src/commands/sanguo/__tests__/battle.test.ts; npm run check-i18n; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Command: `npx vitest run src/commands/sanguo/__tests__/battle.test.ts` exits 0 (behaviors 1-7 green).
    - Command: `npm run check-i18n` exits 0; `npm run typecheck` exits 0.
    - Source: `src/events/interactionCreate.ts` contains `startsWith('sanguo:battle:')` and `startsWith(CAPTURE_TIER_PREFIX)` routes; does NOT contain an `ACK_BTN_ID` dispatch branch (grep `grep -c "ACK_BTN_ID" src/events/interactionCreate.ts` == 0).
    - Source: `src/commands/sanguo/travel.ts` encounter branch renders `buildBattleStartButton` + `buildBattleSkipButton` and no longer renders `buildAckButton` (grep `grep -c "buildAckButton" src/commands/sanguo/travel.ts` == 0).
    - Source: `src/commands/sanguo/map.ts` contains `.addSubcommand(battleSubcommand)` and re-exports the battle/capture handlers (no handleAckPress re-export).
    - Source: `src/commands/sanguo/battle.ts` exports `battleSubcommand`, `execute`, `handleBattleStart`, `handleBattleSkip`, `handleCaptureOpen`, `handleCaptureTierPress`, `handleCaptureRetryPress`, `handleCaptureRetreatPress`; the tier parse uses `parseInt` with an `isNaN` guard.
    - Source: `src/commands/sanguo/battle.ts` capture row uses exactly 3 tier buttons + 1 retreat in ONE ActionRow (no 5th component).
    - Grep (identity): `grep -cE "startEncounterBattle\(char\.id|startSparBattle\(char\.id|attemptCapture\(char\.id" src/commands/sanguo/battle.ts` outputs `0` — all service calls key on users.id.
  </acceptance_criteria>
  <done>The spar command and the full encounter-battle/capture button flow are wired: routers dispatch the new customIds (ack route removed), travel.ts renders fight/skip instead of ack, the capture row obeys the 4-component budget, and every terminal state clears components. Tests + check-i18n + typecheck green.</done>
  <reversibility rating="one-way">Implements D-01 — the ack contract inversion (ack button/route/handler removed, battle entry in its place). Locked user decision; the removal is structural (grep gates assert the old route is GONE).</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → interaction router | Component customIds and their suffixes (tier numbers) cross here — advisory strings, never authoritative (V5 input validation). |
| router → battle.ts handlers | Parsed tier/action dispatch — validated again server-side (tier 1-5) before any service call. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-06-01 | Tampering | Crafted customId fee tampering (sanguo:capture:tier:{n}) | high | mitigate | The tier is parseInt+isNaN-guarded then server-validated (1-5); the fee NEVER rides the customId — attemptCapture resolves it from CAPTURE_TIERS inside the tx (UI-SPEC anti-tamper; 10-05 T-10-05-02). |
| T-10-06-02 | Tampering | Stale component persistence on editReply (PATCH merge) | high | mitigate | Every editReply passes `components: []` when clearing (CR-09-03/04 live-verified); the acceptance criteria assert terminal states clear components. |
| T-10-06-03 | Spoofing | Ack-contract inversion regression (old route dormant) | medium | mitigate | The ACK route is REMOVED, not disabled: grep gate asserts zero `ACK_BTN_ID` dispatch in interactionCreate.ts and zero `buildAckButton` in travel.ts (Pitfall 7). |
| T-10-06-04 | Information disclosure | D-12 leakage (IV/rarity/formula rendered) | high | mitigate | The capture view renders ONLY the single % (capture.chance); the log renders turn lines only; no raw IV/rarity/flee/pity/multiplier anywhere (UI-SPEC never-render contract). |
| T-10-06-05 | Tampering | Component count overflow (5th button) | low | mitigate | The capture row is exactly 3 tier buttons + 1 retreat (4 ≤ 5); retry swaps the row content (UI-SPEC overflow contract; Discord rejects >5 with a payload error). |
| T-10-06-SC | Tampering | npm/pip/cargo installs | — | not-triggered | This plan installs no packages. |
</threat_model>

<verification>
- `npx vitest run src/commands/sanguo/__tests__/battle.test.ts` green; `npm run check-i18n` green; `npm run typecheck` green.
- Grep gates: zero `ACK_BTN_ID` in interactionCreate.ts; zero `buildAckButton` in travel.ts; zero `startEncounterBattle(char.id`/`attemptCapture(char.id` in battle.ts.
- UI-SPEC budget spot-checks: capture row = 4 comps (3 tiers + retreat); battle log description ≤ ~1,700 chars by the round cap; all colors from theme.ts.
- The D-01 inversion is complete end-to-end: encounter embed → fight button → battle log → capture view → tier/retry/retreat → travel resume.
</verification>

## Artifacts this phase produces

| Symbol | Kind | File |
|--------|------|------|
| `SanguoBattleLogEmbedData` + `buildSanguoBattleLogEmbed(data, t)` + `formatTurnLine(log, t)` | embed builder + helpers (D-07 single embed) | src/ui/embeds/buildSanguoBattleLogEmbed.ts |
| `SanguoCaptureEmbedData` + `buildSanguoCaptureEmbed(data, t)` (view/success/fail/flee/retreat states) | embed builder | src/ui/embeds/buildSanguoCaptureEmbed.ts |
| `BATTLE_START_ID`/`BATTLE_SKIP_ID`/`CAPTURE_OPEN_ID` + `buildBattleStartButton`/`buildBattleSkipButton`/`buildCaptureOpenButton` | buttons (D-01/D-10) | src/ui/components/sanguoBattleButtons.ts |
| `CAPTURE_TIER_PREFIX`/`CAPTURE_RETRY_ID`/`CAPTURE_RETREAT_ID` + `buildCaptureTierButtons`/`buildCaptureRetryButton`/`buildCaptureRetreatButton` | buttons (D-09/D-10/D-18) | src/ui/components/sanguoCaptureButtons.ts |
| `battleSubcommand` + `execute` + `handleBattleStart`/`handleBattleSkip`/`handleCaptureOpen`/`handleCaptureTierPress`/`handleCaptureRetryPress`/`handleCaptureRetreatPress` | command module (spar + battle/capture handlers) | src/commands/sanguo/battle.ts |
| ack row → fight/skip row; handleAckPress removed | D-01 inversion | src/commands/sanguo/travel.ts |
| battle subcommand appended + handler re-exports (ack re-export removed) | command wiring | src/commands/sanguo/map.ts |
| `sanguo:battle:*`/`sanguo:capture:*` routes (ACK route removed) + extended `SanguoComponentHandlers` | interaction router | src/events/interactionCreate.ts |
| `battle.*`/`capture.*`/`cmd.battle.*` keys (3 locales) | i18n | locales/{vi,en,zh-cn}/sanguo.json |
| battle.test.ts — 7 routing/handler behaviors | test suite | src/commands/sanguo/__tests__/battle.test.ts |

<success_criteria>
- A user can now fight an encounter (fight/skip buttons on the encounter embed), watch the seeded battle log, press Bắt, pick a tier, and see the capture outcome — all inline, all server-authoritative, all within the UI-SPEC budgets.
- Spar practice works standalone with NEUTRAL styling and zero stakes.
- The D-01 ack inversion is complete and regression-gated (old route/button gone).
- i18n parity holds; hidden mechanics (D-12) hold — only the single capture % is shown.
</success_criteria>

<output>
Create `.planning/phases/10-battle-capture/10-06-SUMMARY.md` when done.
</output>

