---
phase: 06-Farming-Logic-Captcha-Handling
plan: 04
type: execute
wave: 3
depends_on: ["06-03"]
files_modified: [src/workers/selfBotWorker.ts, src/workers/selfBotMaster.ts, src/shard.ts, src/commands/game/farming.ts, .planning/phases/06-Farming-Logic-Captcha-Handling/06-VALIDATION.md]
autonomous: false
requirements: [FARM-04, FARM-05]

must_haves:
  truths:
    - "Self-bot stops farming and notifies user via DM when captcha detected."
    - "User can check bot status and resume farming via slash commands."
    - "Notification pipeline uses ShardingManager.broadcast to reach the user's shard."
    - "06-VALIDATION.md defines the full verification criteria for the phase."
  artifacts:
    - path: "src/workers/selfBotWorker.ts"
      provides: "Captcha detection patterns"
    - path: "src/commands/game/farming.ts"
      provides: "/farming status and /farming resume commands"
    - path: ".planning/phases/06-Farming-Logic-Captcha-Handling/06-VALIDATION.md"
      provides: "Phase validation document"
---

<objective>
Implement robust captcha detection, notification pipeline, and user control commands, and finalize phase validation.

Purpose: Protect accounts from security checks and provide users with transparency and control over their farming bots.
Output: Detection logic, DM notification system, slash commands, and validation document.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-Farming-Logic-Captcha-Handling/06-03-SUMMARY.md
@src/workers/selfBotWorker.ts
@src/workers/selfBotMaster.ts
@src/shard.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Robust Captcha Detection (DM & Channel, Text/Embed/Attachment)</name>
  <files>src/workers/selfBotWorker.ts</files>
  <behavior>
    - Scans messages globally, filtering only for messages sent by the official `OWO_BOT_ID` ('408785106942164992').
    - Monitors both the designated `channelId` and Direct Messages (DMs).
    - Checks `message.content` for trigger phrases ('Are you a human?', 'solve the captcha', 'verify you are human') and website links ('owobot.com/captcha').
    - Checks `message.embeds` (titles, descriptions, URLs) and `message.attachments` (presence of captcha images).
    - On match: Immediately stops loops, destroys the Discord client, and sends 'CAPTCHA_DETECTED' status.
  </behavior>
  <action>
    - Refine the `messageCreate` listener in `WorkerManager` to prioritize global captcha detection from `OWO_BOT_ID`.
    - Implement content, embed, and attachment captcha matching based on 06-CONTEXT.md.
    - On match: `client.destroy()`, remove from `clients` map, and call `this.sendStatus(botId, 'CAPTCHA_DETECTED')`.
  </action>
  <verify>
    <automated># Manual verification: Simulate DM captcha, Embed captcha, and Attachment captcha from OwO Bot ID and check if bot halts instantly</automated>
  </verify>
  <done>Robust captcha detection (DMs, embeds, attachments) and emergency stop implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Notification Pipeline (Master -> Shard -> User)</name>
  <files>src/shard.ts</files>
  <action>
    - In `src/shard.ts`, implement the listener for `NOTIFY_CAPTCHA` messages from the ShardingManager (sent by `SelfBotMaster` per Plan 01).
    - Locate the user via `client.users.fetch(userId)`.
    - Send a DM notification using i18n (e.g., `farming.captcha_detected`).
    - Include a link to the channel or instructions to use `/farming resume`.
  </action>
  <verify>
    <automated>grep -q "NOTIFY_CAPTCHA" src/shard.ts</automated>
  </verify>
  <done>Users are notified via DM when their bot encounters a captcha.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Status and Resume Commands</name>
  <files>src/commands/game/farming.ts</files>
  <action>
    - Implement `/farming status`: Display account status (active, stopped, captcha_waiting), proxy info, and active toggles from `settings`.
    - Implement `/farming resume`: 
      - Check if status is `captcha_waiting` or `stopped`.
      - Update status to `active` in DB.
      - Emit `FARMING_ACCOUNT_UPDATED` to the ShardingManager (handled in `bot.ts`).
  </action>
  <verify>
    <automated># Manual verification: Check output of /farming status and effect of /farming resume</automated>
  </verify>
  <done>User control commands (/status, /resume) are operational.</done>
</task>

<task type="auto">
  <name>Task 4: Create Validation Document</name>
  <files>.planning/phases/06-Farming-Logic-Captcha-Handling/06-VALIDATION.md</files>
  <action>
    - Create the `06-VALIDATION.md` file following the Nyquist Dimension 8 requirement.
    - Define concrete verification commands for all truths in must_haves across all plans.
    - Include edge case test scenarios (proxy failure, multiple bots, concurrent captchas).
  </action>
  <verify>
    <automated>test -f .planning/phases/06-Farming-Logic-Captcha-Handling/06-VALIDATION.md</automated>
  </verify>
  <done>Phase validation document created.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Master → Shard | Broadcasted IPC messages for user notification. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07 | Spoofing | Captcha Patterns | mitigate | Only listen for messages from the official OwO bot ID (verified via config). |
| T-06-08 | Denial of Service | Notification Flood | mitigate | Send notification once per captcha event; record state in DB to prevent re-notifying until resumed. |
</threat_model>

<verification>
Check DM receipt on simulated captcha. Verify `/farming resume` triggers bot restart in Master logs.
</verification>

<success_criteria>
1. Captcha detection stops farming immediately.
2. User receives a DM via the main bot.
3. `/farming status` displays correct configuration and status.
4. `/farming resume` successfully restarts a stopped/captcha'd bot.
5. `06-VALIDATION.md` is present.
</success_criteria>

<output>
Create `.planning/phases/06-Farming-Logic-Captcha-Handling/06-04-SUMMARY.md` when done
</output>
