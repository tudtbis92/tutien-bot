---
status: diagnosed
phase: 02.1-gather-craft-seed-data
source: [git:85b8e6c feat(02.1): gacha gather, seed data, /recipes command]
started: 2026-04-14T14:00:00Z
updated: 2026-04-14T14:45:00Z
---

## Current Test

[testing paused — 2 items outstanding]

## Tests

### 1. Cold Start Smoke Test
expected: Kill any running bot process. Start fresh with `npm run dev` (or equivalent). Bot boots without errors, migration 0002 runs (or was already applied), seed script completes (or is idempotent on re-run), and the bot comes online in Discord without crash logs.
result: pass

### 2. /gather — Basic Single Gather
expected: Run `/gather` (no amount specified, defaults to 1). Bot deducts a linh thạch fee matching your current realm tier, rolls a random item from the pool, and replies with an embed showing the item received (name, tier, quantity) and the fee paid. No item_id parameter exists anymore.
result: issue
reported: "player có linh thạch = 0, vẫn thực hiện gather thành công; UI show tên item: items.raw.khi_tu_thach (i18n key not resolved); nội dung trong UI thu thập thành công bị lặp lại, cần show thêm nội dung Linh thạch còn lại: remain_amount (- consume gather)"
severity: major

### 3. /gather — Multi-Gather (amount)
expected: Run `/gather amount:5`. Bot deducts 5× the single fee, rolls 5 independent drops, and shows a summary embed listing all 5 items received (possibly grouped if duplicates). Total fee and items are both displayed.
result: blocked
blocked_by: prior-phase
reason: "block do test trước, sẽ bị bug tương tự, đợi fix xong test 2 rồi test lại"

### 4. /gather — Insufficient Balance
expected: Run `/gather` with a character whose linh thạch balance is below the realm fee. Bot replies with an error message saying you don't have enough linh thạch (no item given, no deduction).
result: issue
reported: "fail, vẫn gather thành công"
severity: major

### 5. /gather — Higher Realm Gets Higher-Tier Items
expected: With a high-realm character (Kim Đan or above), run `/gather` multiple times. Higher-tier items (tier 3–5) should appear in drops. A Luyện Khí character running the same command should only receive tier 1–2 items.
result: blocked
blocked_by: prior-phase
reason: "block bởi test 2. test lại sau khi fix"

### 6. /recipes — Lists Unlocked Recipes
expected: Run `/recipes`. Bot shows an embed listing recipes unlocked for your current profession and level. Each entry shows: recipe name, profession, min level required, ingredients needed, and the crafted item output. Only recipes at or below your current profession level appear.
result: issue
reported: "prof level = 0 vẫn hiển thị tất cả recipe; chưa có button di chuyển qua lại các trang recipe nếu có nhiều hơn 1 trang; tên các item vẫn lỗi không translate i18n: items.crafted.tu_khi_binh #6, items.raw.khi_tu_thach ×3, items.raw.tran_van_thach ×2"
severity: major

### 7. /recipes — Pagination
expected: With enough recipes unlocked (or by checking a profession with 4–5 recipes), run `/recipes`. If recipes span more than one page, Previous/Next buttons appear and navigate correctly between pages.
result: blocked
blocked_by: prior-phase
reason: "test lại sau khi fix test 6"

### 8. /recipes — No Recipes Unlocked
expected: Run `/recipes` on a brand-new character with profession level 0. Bot replies with a message indicating no recipes are unlocked yet (no crash, no empty embed error).
result: issue
reported: "fail, đã mô tả trong test 6 — prof level = 0 vẫn hiển thị tất cả recipe thay vì báo chưa unlock"
severity: major

### 9. /craft — Crafts with Seed Data
expected: Gather the required raw materials for a tier-1 recipe. Run `/craft` with that recipe. Bot consumes the ingredients from inventory and adds the crafted item. The crafted item is a "crafted" type, not a raw material. Transaction is atomic (ingredients consumed + item added together).
result: [pending]

### 10. Seed Data Integrity — Items in Pool
expected: Run `/gather` multiple times (10+). Observe that items from different professions appear (Luyện Đan herbs, Luyện Kim ores, Phù Sư materials, etc.). Items have Vietnamese names reflecting their profession theme (e.g. herbs for Luyện Đan, metals for Luyện Kim).
result: [pending]

### 10. Seed Data Integrity — Items in Pool
expected: Run `/gather` multiple times (10+). Observe that items from different professions appear (Luyện Đan herbs, Luyện Kim ores, Phù Sư materials, etc.). Items have Vietnamese names reflecting their profession theme (e.g. herbs for Luyện Đan, metals for Luyện Kim).
result: [pending]

## Summary

total: 10
passed: 1
issues: 4
pending: 2
skipped: 0
blocked: 3

## Gaps

- truth: "Bot rejects /gather when player balance is below the realm fee"
  status: failed
  reason: "User reported: player có linh thạch = 0, vẫn thực hiện gather thành công"
  severity: major
  test: 2
  root_cause: "gather.ts uses char.tuVi (cultivation points) for both the balance check (line 106) and the fee deduction (line 157) instead of users.balance (linh thạch currency). The two are separate: users.balance = linh thạch (money), characters.tuVi = cultivation points. A player with 0 linh thạch but non-zero tu vi passes the check (tuVi > 0 >= fee) and tu vi is deducted instead of linh thạch — gather succeeds for free. The comment on line 154 even acknowledges the confusion: '// Deduct tu vi (linh thạch is the currency)'."
  artifacts:
    - path: "src/commands/game/gather.ts"
      issue: "Line 106: if (char.tuVi < totalFee) — should check users.balance. Line 157: update(characters).set({ tuVi: ... }) — should update users.balance. fetchCommandContext() does not expose users.balance."
    - path: "src/utils/commandContext.ts"
      issue: "fetchCommandContext only fetches users.locale, not users.balance — gather needs balance added to context or fetched separately"
  missing:
    - "Fetch users.balance in gather.ts (join or separate query alongside char fetch)"
    - "Change balance check from char.tuVi to userBalance"
    - "Change transaction deduction from update(characters).set({tuVi}) to update(users).set({balance})"
    - "Change error message to show linh thạch balance, not tu vi"
    - "Change remainingBalance calculation to userBalance - totalFee"
  debug_session: ".planning/debug/gather-balance-check.md"

- truth: "Gather result embed shows resolved item name (e.g. 'Khí Tu Thạch'), not raw i18n key"
  status: failed
  reason: "User reported: UI show tên item: items.raw.khi_tu_thach (i18n key displayed instead of translation)"
  severity: major
  test: 2
  root_cause: "The entire items.raw.* and items.crafted.* translation key subtrees are missing from all locale files. locales/vi/game.json, en/game.json, zh-cn/game.json have no 'items' section. When t('game:items.raw.khi_tu_thach') is called, i18next gets a key-miss and returns the key string itself. The runtime call chain (DB key → t() → embed) is architecturally correct — only locale data is missing."
  artifacts:
    - path: "locales/vi/game.json"
      issue: "Missing entire items section (items.raw.* + items.crafted.*)"
    - path: "locales/en/game.json"
      issue: "Missing entire items section"
    - path: "locales/zh-cn/game.json"
      issue: "Missing entire items section"
  missing:
    - "Add items.raw.* and items.crafted.* translation keys to all 3 locale files (50 raw + 44 crafted slugs)"
  debug_session: ".planning/debug/item-i18n-not-resolved.md"

- truth: "Gather result embed shows remaining linh thạch balance after fee deduction"
  status: failed
  reason: "User reported: nội dung trong UI thu thập thành công bị lặp lại, cần show thêm nội dung Linh thạch còn lại: remain_amount (- consume gather)"
  severity: minor
  test: 2
  root_cause: "gather.ts currently calculates remainingBalance = char.tuVi - totalFee (wrong currency). Additionally the embed shows item name in both title and description (duplication). Fix: use userBalance - totalFee for remaining display; remove item name from description in gather embed type."
  artifacts:
    - path: "src/commands/game/gather.ts"
      issue: "remainingBalance computed from tuVi instead of users.balance; not passed to embed at all"
    - path: "src/ui/embeds/buildItemEmbed.ts"
      issue: "Gather type shows item name in both title (via gather.success) and description (explicit bold text) — duplication. No remainingBalance field."
  missing:
    - "Pass remainingBalance (= userBalance - totalFee) to buildItemEmbed"
    - "Add remainingBalance field to ItemResultData interface"
    - "Remove description duplication for gather type in buildItemEmbed"
    - "Display remaining linh thạch as embed field"
  debug_session: ""

- truth: "Bot rejects /gather when player balance is 0 (insufficient for any realm fee)"
  status: failed
  reason: "User reported: fail, vẫn gather thành công"
  severity: major
  test: 4
  root_cause: "Same root cause as Test 2 — gather.ts uses char.tuVi instead of users.balance for both check and deduction. Covered by same fix."
  artifacts:
    - path: "src/commands/game/gather.ts"
      issue: "char.tuVi used instead of users.balance"
  missing:
    - "Covered by gather currency fix above"
  debug_session: ".planning/debug/gather-balance-check.md"

- truth: "/recipes only shows recipes where min_profession_level <= character's current profession level"
  status: failed
  reason: "User reported: prof level = 0 vẫn hiển thị tất cả recipe"
  severity: major
  test: 6
  root_cause: "The DB query in recipes.ts never filters by min_profession_level. charProfLevel is computed but only used to render ✅/🔒 icons — it never gates which recipes are fetched or displayed. No application-side filter exists. The 'no recipes unlocked' empty-state path is also missing for the level-filter case."
  artifacts:
    - path: "src/commands/game/recipes.ts"
      issue: "Lines 96–105: DB query missing min_profession_level filter. Lines 107–112: empty-state check does not account for level-filtered results."
  missing:
    - "After DB fetch, filter allRecipes by getProfessionLevel(char.professionPoints, r.professionType) >= r.minProfessionLevel"
    - "Check filtered set (not allRecipes) for empty state and return 'no recipes unlocked' embed"
  debug_session: ".planning/debug/recipes-prof-level-filter.md"

- truth: "/recipes shows Previous/Next pagination buttons when recipes span multiple pages"
  status: failed
  reason: "User reported: chưa có button di chuyển qua lại các trang recipe nếu có nhiều hơn 1 trang"
  severity: major
  test: 6
  root_cause: "Pagination buttons were never implemented. recipes.ts sends embed-only (no components:). buildRecipesEmbed returns EmbedBuilder only. interactionCreate.ts has no recipes_* button handler. Pagination math (totalPages, page slicing) is correct but the entire Discord interactive component layer is absent."
  artifacts:
    - path: "src/commands/game/recipes.ts"
      issue: "execute() sends embeds only, no ActionRowBuilder or button customId encoding"
    - path: "src/ui/embeds/buildRecipesEmbed.ts"
      issue: "Returns EmbedBuilder only, no ActionRowBuilder"
    - path: "src/events/interactionCreate.ts"
      issue: "No handler for recipes_prev_* / recipes_next_* customIds"
  missing:
    - "Create buildRecipesPage() returning { embed, row } with ◀/▶ buttons (customId: recipes_prev_{page}_{profession|none})"
    - "Update execute() to send components: [row] alongside embed"
    - "Add recipes_prev_* / recipes_next_* handler in interactionCreate.ts"
  debug_session: ".planning/debug/recipes-pagination-missing.md"

- truth: "Item names in /recipes embed are translated (e.g. 'Tụ Khí Bình', 'Khí Tu Thạch'), not raw i18n keys"
  status: failed
  reason: "User reported: tên các item vẫn lỗi không translate i18n: items.crafted.tu_khi_binh #6, items.raw.khi_tu_thach ×3, items.raw.tran_van_thach ×2"
  severity: major
  test: 6
  root_cause: "Same root cause as Test 2 i18n issue — locale files missing items.raw.* and items.crafted.* sections. Covered by same locale fix."
  artifacts:
    - path: "locales/vi/game.json"
      issue: "Missing items section"
  missing:
    - "Covered by item-i18n fix above"
  debug_session: ".planning/debug/item-i18n-not-resolved.md"
