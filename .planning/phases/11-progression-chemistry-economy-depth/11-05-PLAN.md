---
phase: 11-progression-chemistry-economy-depth
plan: 05
type: execute
wave: 4
depends_on:
  - 11-01
  - 11-02
files_modified:
  - src/services/sanguo/battleEngine.ts
  - src/services/sanguo/__tests__/battleEngine.test.ts
  - src/services/sanguo/chemistryService.ts
  - src/services/sanguo/__tests__/chemistryService.test.ts
  - src/constants/sanguoChemistry.ts
autonomous: true
requirements:
  - TQC-17

estimate:
  tokens: 48000
  raw_tokens: 24000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "battleEngine extends NON-BREAKING: CombatantInput gains an OPTIONAL level?: number (D-08) — absent → levelGain 0 → the Phase 10 runBattle(seed, player, enemy) contract and its replay tests are UNCHANGED; the eff() helper becomes base + IV + (level−1) × STAT_GAIN_PER_LEVEL (D-05 + D-08)."
    - "runLegionBattle(seed, input) is a NEW pure export alongside runBattle: input = { mains[3]: buffed CombatantInput + level + skillIds, supports[9]: { heroId, class, lea, special }, boss: CombatantInput } (D-17); the 3 mains take turns (MOV desc → AGI desc → player-first tie), damage = max(atk−def, 1), crit ×2, round cap 20 → winner by total damage, tie → remaining HP%; the same seeded xoroshiro128plus threading (D-06 replay contract)."
    - "The 2-slot skill/MP system is in-engine (D-29): normal attacks generate +12 MP; special attacks consume MP (cost 15/25/40 by rarity from the skill catalog); insufficient MP → the hero falls back to a normal attack (skills.no_mp line); MP starts at base mp per battle; support heroes' special skills trigger in-battle buff effects on a LEA-driven chance (D-18): triggerChance = clamp(0.15 × (1 + (lea − 10) × 0.02), 0.05, 0.35), effects attack_up +20% atk 1 turn / hp_regen 15% of main max HP / mp_regen +10 MP — ALL rolls ride the seeded rng (pure engine, replay-faithful)."
    - "chemistryService is a PURE module (no db/redis/discord imports, no Math.random — D-06 discipline): mainChemistryPoints(main, supports) sums 3/2/1 (family+spouse 3, faction 2, role 1 — D-19) over main↔support pairs ONLY (D-17), maps to a CHEMISTRY_TIERS tier via sanguoChemistry.ts, and computes the multiplicative buff applied to the main's final combatStat ((base + IV + levelGain) × (1 + buff%)) — the buffed stats are PRE-BAKED into the mains' CombatantInput BEFORE runLegionBattle so sanguo_battles.input stays replay-faithful (Pitfall 6)."
    - "Strict class-match (D-20) is enforced at assembly time (legionService, 11-07) — a wrong-class hero contributes zero chemistry and zero support effect; the chemistry computation itself is class-agnostic (it receives the already-validated slots)."
  artifacts:
    - src/services/sanguo/battleEngine.ts — optional level on CombatantInput, runLegionBattle export, MP/skills/support-effect logic, BattleConfig extension
    - src/services/sanguo/chemistryService.ts — mainChemistryPoints, chemistryBuff, supportTriggerChance
    - src/constants/sanguoChemistry.ts — CHEMISTRY_POINTS/CHEMISTRY_TIERS (from 11-02, consumed here)
    - __tests__ for both (replay deep-equals + chemistry table tests)
  key_links:
    - "runLegionBattle input snapshot ↔ sanguo_battles.input jsonb (11-06 wires the storage) — exactly the values passed to the engine must be stored (Pitfall 6 replay contract)."
    - "chemistryService buff ↔ mains' CombatantInput — pre-baked before the engine, never a live DB read inside runLegionBattle (anti-pattern 'baking chemistry into the engine')."
    - "skill catalog (sanguo_skills seed, 11-02) ↔ engine skillIds — the mpCost/mpGain/effectType/effectValue the engine resolves by id from the snapshot."
    - "STAT_GAIN_PER_LEVEL + TIER_MULTIPLIERS (sanguoProgression.ts) ↔ eff() + boss input builder — the level and evolution terms the whole Phase 11 power curve rides on."
  prohibitions:
    - "No breaking change to runBattle(seed, player, enemy) — the Phase 10 test suite (battleEngine.test.ts:40-55 replay deep-equals) must pass UNCHANGED; the level field is OPTIONAL with default 1 → levelGain 0."
    - "No Math.random / no Date / no db imports in battleEngine or chemistryService (pure-module contract, battleEngine.ts:24-27)."
    - "No chemistry points/buff% rendered anywhere — hidden mechanics (D-12); the engine input carries buffed numbers, the UI carries only tier label + link count (11-07)."
    - "No pure-rand outside battleEngine.ts — support-effect rolls ride the seeded engine rng, never crypto (they are part of the replay)."
  flagged_assumptions:
    - "FLAGGED ASSUMPTION (probe TQC-17 unclassified, unresolved): the '3v1' legion battle is the DESIGN TARGET; the engine accepts mains array length 1-3 (incomplete legions fight with what they have — legion.incomplete caution, R-11 UI-SPEC); the forced-legion routing (11-06) requires >= 1 main assembled, never 0."
    - "FLAGGED ASSUMPTION (A4 carry): skill rarity weights + MP costs + effect values are seed content (11-02) — the engine reads them from the snapshot's skillIds, so rebalancing is a seed edit, no engine change."
    - "FLAGGED ASSUMPTION (support-trigger determinism, RESEARCH OQ4): support-effect outcomes are part of the replay (like hit/crit) — the trigger rolls ride the seeded rng; documented in the engine header."
    - "FLAGGED ASSUMPTION (A6 carry): MP resets to base mp at battle start (per-battle resource); HP persists (D-04)."

---

<objective>
What this plan accomplishes: **the combat core of Phase 11 — the battleEngine extension (TQC-17) that turns the Phase 10 1v1 solo engine into the legion 3+9 battle engine, plus the pure chemistry module (D-19).** Task 1 extends battleEngine.ts: optional level term (D-08), the new runLegionBattle export (D-17), the 2-slot skill/MP economy (D-29), and the support-effect trigger system (D-18) — all riding the seeded rng (D-06). Task 2 builds chemistryService: the pure link→points→tier→buff computation that pre-bakes the mains' buffed stats into the engine input.

Purpose: the replayable engine is the balance anchor (D-05/D-06 contract). Building the extension as pure functions with the Phase 10 replay tests still green proves the non-breaking contract before any boss routing (11-06) or assembly UI (11-07) depends on it.

Output: runLegionBattle + MP/skills/support effects + chemistryService, all replay-tested and typecheck-green.
</objective>

<execution_context>
@C:/Users/901107/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/901107/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-progression-chemistry-economy-depth/11-CONTEXT.md
@.planning/phases/11-progression-chemistry-economy-depth/11-RESEARCH.md
@.planning/phases/11-progression-chemistry-economy-depth/11-PATTERNS.md
@.planning/phases/11-progression-chemistry-economy-depth/11-01-PLAN.md
@.planning/phases/11-progression-chemistry-economy-depth/11-02-PLAN.md
@src/services/sanguo/battleEngine.ts
@src/services/sanguo/__tests__/battleEngine.test.ts
@src/constants/sanguoChemistry.ts
@src/constants/sanguoProgression.ts
@src/db/schema/sanguoSkills.ts
@src/db/schema/heroes.ts
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (TRACER): battleEngine extension — optional level + runLegionBattle + MP/skills + support effects, Phase 10 tests unchanged</name>
  <files>src/services/sanguo/battleEngine.ts, src/services/sanguo/__tests__/battleEngine.test.ts</files>
  <read_first>
    - src/services/sanguo/battleEngine.ts IN FULL (the D-05/D-06 contract — CombatantInput lines 39-71, eff() 132-134, runBattle 185-273, BATTLE_CONFIG 119-129, pure-module header 24-27)
    - src/services/sanguo/__tests__/battleEngine.test.ts (the pinned Phase 10 replay tests — they MUST pass unchanged)
    - src/constants/sanguoProgression.ts (STAT_GAIN_PER_LEVEL — the level term) + sanguoChemistry.ts (CHEMISTRY_POINTS/TIERS for Task 2's import)
    - .planning/phases/11-progression-chemistry-economy-depth/11-RESEARCH.md §Pattern 3 (skills/MP/support effects) + §Common Operation 1 (CombatantInput level extension) + §Common Operation 5 (supportTriggerChance)
    - .planning/phases/11-progression-chemistry-economy-depth/11-PATTERNS.md §battleEngine.ts (lines 303-312, exact non-breaking shape)
  </read_first>
  <action>
    Extend `src/services/sanguo/battleEngine.ts` — NON-BREAKING (Phase 10 tests unchanged):
    1. Add `/** Phase 11 (D-08): optional level — absent → levelGain 0 → Phase 10 behavior unchanged. */ level?: number;` to CombatantInput (line 71 area). Update the eff() helper (line 132-134) to `base + IV + ((c.level ?? 1) − 1) × STAT_GAIN_PER_LEVEL` (import STAT_GAIN_PER_LEVEL from sanguoProgression.ts). combatStat() stays base+IV (backward-compat export).
    2. Add the MP/skill fields to CombatantInput (optional, Phase-11-only): `mpCurrent?: number; skillNormal?: { id: string; mpGain: number } | null; skillSpecial?: { id: string; mpCost: number; effectType: 'damage' | 'attack_up' | 'hp_regen' | 'mp_regen'; effectValue: number } | null;` — the engine resolves these from the snapshot (Pitfall 6 — the snapshot carries exactly what the engine needs, never a DB read).
    3. New export `runLegionBattle(seed: number, input: LegionBattleInput): LegionBattleResult` where `LegionBattleInput = { mains: Array<CombatantInput & { level: number }>; supports: Array<{ heroId: string; class: CombatantInput['class']; lea: number; special: { id: string; effectType: string; effectValue: number } }>; boss: CombatantInput }` and `LegionBattleResult = { roundLogs: TurnLog[]; winner: 'player' | 'enemy'; rounds: number; totalDamagePlayer: number; totalDamageEnemy: number; playerHpAfter: number; enemyHpAfter: number }` — mirror the runBattle shape (battleEngine.ts:85-93). Thread ONE seeded xoroshiro128plus via uniformFloat64(rng).
    4. Legion turn logic: per round, the 3 mains act in MOV desc → AGI desc order (player-first tie — the Phase 10 compareCombatants:148-156 pattern extended to 3), each main attacks the boss: roll hit (HIT_BASE + AGI_FACTOR, clamped) then crit (CRIT_BASE + AGI_FACTOR, ×2); skill resolution — if the main has MP >= skillSpecial.mpCost and the attack roll succeeds, use the special (consume MP, apply effectValue damage multiplier on the class stat pair — getAttackType()); else use the normal attack (generate +12 MP via skillNormal.mpGain). Then the boss attacks one main (target: lowest current HP — deterministic, replay-faithful). Support effects (D-18): per round, per support, roll `uniformFloat64(rng) < clamp(0.15 × (1 + (lea − 10) × 0.02), 0.05, 0.35)` → apply the support's special effect to one main (attack_up: +20% atk for 1 turn on a random main — deterministic pick via the same rng; hp_regen: heal 15% of that main's base HP; mp_regen: +10 MP). Insufficient-MP fallback emits a skills.no_mp-style log line (the log line is UI-rendered — the engine emits the outcome). Round cap 20 → winner by total damage, tie → remaining HP% (Phase 10 rules).
    5. Update BATTLE_CONFIG only if a new constant is genuinely needed (supportTriggerChance lives in chemistryService, not here); the header docblock gains the D-17/D-29/D-18 + replay note (support effects are part of the replay, RESEARCH OQ4).
    6. Tests: keep ALL Phase 10 tests untouched + ADD legion tests: (a) replay deep-equals — same seed + input → identical roundLogs; (b) a main with insufficient MP falls back to a normal attack and gains MP; (c) a support with high LEA triggers its effect within a seeded battle (deterministic given the seed); (d) winner-by-damage at round cap; (e) the level term changes a L1 vs L50 main's damage (levelGain = (L−1)×2).
  </action>
  <verify>
    <automated>npx vitest run src/services/sanguo/__tests__/battleEngine.test.ts; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Command: the FULL battleEngine test file exits 0 — the Phase 10 replay tests pass UNCHANGED (non-breaking proof) + the new legion behaviors green.
    - Command: `npm run typecheck` exits 0.
    - Source: `grep -c "level?" src/services/sanguo/battleEngine.ts` >= 1 (optional field); `grep -c "runLegionBattle" src/services/sanguo/battleEngine.ts` >= 2 (export + definition).
    - Source: `grep -c "Math.random\|new Date\|from '.*db" src/services/sanguo/battleEngine.ts` === 0 (pure-module contract holds).
    - Source: no import of pure-rand OUTSIDE this file is introduced by the extension (grep the new code — seeded rng threaded through uniformFloat64 only).
  </acceptance_criteria>
  <done>runLegionBattle works: 3 mains vs 1 boss with MP economy, special skills, and LEA-driven support effects — deterministic, replayable, and the Phase 10 1v1 contract is untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: chemistryService — pure main↔support link → points → tier → buff (pre-baked engine input)</name>
  <files>src/services/sanguo/chemistryService.ts, src/services/sanguo/__tests__/chemistryService.test.ts</files>
  <read_first>
    - src/constants/sanguoChemistry.ts (CHEMISTRY_POINTS/CHEMISTRY_TIERS from 11-02 — the source of truth)
    - src/db/schema/heroes.ts (factionId/role/class/familyId columns the link resolution reads — lines 44-97) + heroRelations.ts (spouse pairs — type='spouse' only, lines 12-33)
    - .planning/phases/11-progression-chemistry-economy-depth/11-RESEARCH.md §Pattern 2 (points/tiers/buffs) + §Common Operation 4 (mainChemistryPoints)
    - .planning/phases/11-progression-chemistry-economy-depth/11-PATTERNS.md §chemistryService.ts (lines 97-119, pure-module contract + D-06 discipline)
  </read_first>
  <behavior>
    - Test 1 (mainChemistryPoints): a main linked to supports with exact family_id matches scores 3 per family link; a spouse support scores 3 (spouseOfMain flag); a same-faction support scores 2; a same-role support scores 1; a no-match support scores 0 — sums correctly (3 family + 2 faction + 1 role + 0 = 6 for a 4-support fixture).
    - Test 2 (tier mapping): point sums map to CHEMISTRY_TIERS — 12→S (+10%), 8→A (+8%), 5→B (+6%), 3→C (+4%), 1→D (+2%), 0→no tier/buff 0 (bonus-only). Buff is MULTIPLICATIVE on the final combatStat: (base + IV + levelGain) × (1 + buff) — a 100-stat main at S tier → 110.
    - Test 3 (supportTriggerChance): lea 10 → 0.15, lea 40 → 0.24, lea 60 → 0.30, clamped to [0.05, 0.35] at the extremes.
  </behavior>
  <action>
    Create `src/services/sanguo/chemistryService.ts` (pure module — D-19, D-06 discipline):
    - `export interface ChemistryLinkInput { factionId: number; role: string; familyId: number | null }` and `export interface SupportLinkInput extends ChemistryLinkInput { spouseOfMain: boolean }`.
    - `export function mainChemistryPoints(main: ChemistryLinkInput, supports: SupportLinkInput[]): number` — sum over the 9 supports: spouseOfMain OR exact family_id match → +CHEMISTRY_POINTS.family (3); faction match → +2; role match → +1; else 0. Main↔support pairs ONLY (D-17 — supports never link to each other).
    - `export function chemistryTier(points: number): { label: string | null; buff: number }` — walk CHEMISTRY_TIERS (S min 12 … D min 1, min 0 → { label: null, buff: 0 }) — bonus-only (EA FC 0-chemistry).
    - `export function applyChemistryBuff(stat: number, buff: number): number` — Math.round(stat × (1 + buff)) (integer combat stats; the buff applies to the FINAL combatStat (base + IV + levelGain), so chemistry scales with level — RESEARCH Pattern 2).
    - `export function supportTriggerChance(lea: number): number` — `Math.min(0.35, Math.max(0.05, 0.15 * (1 + (lea - 10) * 0.02)))` (D-18, RESEARCH Common Operation 5 — the value 11-05 Task 1's engine support rolls use).
    - Note in the header: chemistry is computed BEFORE the engine (pre-baked buffed mains into CombatantInput), never a live DB read inside runLegionBattle (Pitfall 6); points/buff% NEVER render (D-12 — only tier label + link count via 11-07).
    - Tests: the 3 behaviors.
  </action>
  <verify>
    <automated>npx vitest run src/services/sanguo/__tests__/chemistryService.test.ts; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Command: vitest green for the 3 chemistry behaviors; `npm run typecheck` green.
    - Source: `grep -c "from '.*db\|from '.*redis\|from 'discord" src/services/sanguo/chemistryService.ts` === 0 (pure module — D-06).
    - Source: mainChemistryPoints references CHEMISTRY_POINTS (import, not literal 3/2/1 — single source).
    - Source: supportTriggerChance clamps to [0.05, 0.35] (grep 'Math.min(0.35' + 'Math.max(0.05').
  </acceptance_criteria>
  <done>The chemistry contract is pure and tested: links → points → tier → multiplicative buff, ready for the legion assembly (11-07) and the boss input builder (11-06) to consume.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| legion input snapshot → engine | The engine is PURE — it trusts nothing external; the caller must bake chemistry + skills into the snapshot (a live DB read inside the engine would break replay). |
| engine output → battle log UI | TurnLog/skill-effect lines cross to the renderer; hidden mechanics must not leak through the log shape. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-05-01 | Tampering | Replay contract break (input snapshot ≠ engine input) | high | mitigate | runLegionBattle's input IS the snapshot (Pitfall 6 — chemistry pre-baked, skills by id); replay deep-equals tests pin the contract (Phase 10 Test 8 pattern). |
| T-11-05-02 | Tampering | Breaking the Phase 10 runBattle contract via the level field | high | mitigate | level is OPTIONAL (default 1 → levelGain 0); the full Phase 10 test file must pass unchanged — non-breaking proof is an acceptance criterion. |
| T-11-05-03 | Spoofing | Chemistry buffs applied twice (render-time + engine-time) | medium | mitigate | Buff is applied ONCE, pre-baked into the mains' input; the engine never recomputes chemistry (structure, not convention). |
| T-11-05-04 | Information disclosure | Chemistry points/buff% in the battle log | high | mitigate | The log shape (TurnLog) carries no chemistry fields; the buffed numbers exist only inside the engine input (D-12). |
| T-11-05-SC | Tampering | npm/pip/cargo installs | — | not-triggered | This plan installs no packages. |
</threat_model>

<verification>
- `npx vitest run src/services/sanguo/__tests__/battleEngine.test.ts src/services/sanguo/__tests__/chemistryService.test.ts` green — including the UNCHANGED Phase 10 replay tests.
- `npm run typecheck` green.
- Pure-module greps pass (no db/redis/discord/Math.random/Date in either new-code surface).
- The chemistry + engine modules are ready for 11-06 (boss routing) and 11-07 (legion assembly) to consume.
</verification>

## Artifacts this phase produces

| Symbol | Kind | File |
|--------|------|------|
| `CombatantInput.level?` (optional, D-08) | interface extension | src/services/sanguo/battleEngine.ts |
| `CombatantInput.mpCurrent?` / `skillNormal?` / `skillSpecial?` | interface extension (D-29) | src/services/sanguo/battleEngine.ts |
| `runLegionBattle(seed, input)` → LegionBattleResult | pure engine export (D-17) | src/services/sanguo/battleEngine.ts |
| `LegionBattleInput` / `LegionBattleResult` | types (snapshot contract) | src/services/sanguo/battleEngine.ts |
| MP economy (+12 normal / −cost special, fallback) + support-effect rolls | engine logic (D-29/D-18) | src/services/sanguo/battleEngine.ts |
| `mainChemistryPoints(main, supports)` → points | pure fn (D-19) | src/services/sanguo/chemistryService.ts |
| `chemistryTier(points)` → {label, buff} | pure fn (D-19) | src/services/sanguo/chemistryService.ts |
| `applyChemistryBuff(stat, buff)` → rounded stat | pure fn (multiplicative) | src/services/sanguo/chemistryService.ts |
| `supportTriggerChance(lea)` → clamped chance | pure fn (D-18, LEA-driven) | src/services/sanguo/chemistryService.ts |
| battleEngine.test.ts legion suite + chemistryService.test.ts | test suites | src/services/sanguo/__tests__/ |

<success_criteria>
- TQC-17 engine half: runLegionBattle is pure, seeded, replayable — 3 mains + 9 supports vs 1 boss with MP/skills/support effects.
- The Phase 10 runBattle contract is untouched (all old tests green).
- The chemistry model is pure + tested: family+spouse > faction > role → S/A/B/C/D → +10..+2% multiplicative, bonus-only.
- No hidden mechanic (points/buff%) can reach the UI through the engine/log shape.
</success_criteria>

<output>
Create `.planning/phases/11-progression-chemistry-economy-depth/11-05-SUMMARY.md` when done.
</output>
