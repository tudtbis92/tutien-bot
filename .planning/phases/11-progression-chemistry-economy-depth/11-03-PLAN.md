---
phase: 11-progression-chemistry-economy-depth
plan: 03
type: execute
wave: 3
depends_on:
  - 11-01
  - 11-02
files_modified:
  - src/services/sanguo/soulgemService.ts
  - src/services/sanguo/__tests__/soulgemService.test.ts
  - src/commands/sanguo/hero.ts
  - src/commands/sanguo/__tests__/hero.test.ts
  - src/events/interactionCreate.ts
  - src/ui/components/sanguoHeroCopyMenu.ts
  - src/ui/components/sanguoHeroPageButtons.ts
  - src/ui/components/sanguoConvertButton.ts
  - src/ui/components/sanguoLevelButton.ts
  - src/ui/components/sanguoEvolveButton.ts
  - src/ui/components/sanguoRerollSlotMenu.ts
  - src/ui/components/sanguoRerollButton.ts
  - src/ui/embeds/buildSanguoHeroEmbed.ts
  - src/ui/embeds/buildSanguoProgressionResultEmbed.ts
  - src/i18n/index.ts
  - locales/vi/sanguo.json
  - locales/en/sanguo.json
  - locales/zh-cn/sanguo.json
autonomous: true
requirements:
  - TQC-14
  - TQC-15

estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A user with ≥2 copies of the same hero can convert a duplicate copy to per-hero hồn ngọc via /sanguo hero: the copy selector (D-04, paged at 25) lists every copy; the convert action consumes the SELECTED copy and credits the per-(user, hero) pool with the flat-by-tier value t0=1 / t1=5 / t2=10 / t3=20 (D-03), x2 when a booster_x2 item is owned and consumed atomically in the SAME tx (D-12, Pitfall 2 anti-clone)."
    - "Conversion, leveling, evolution, and skill re-roll are hồn ngọc sinks ONLY — never wallet.deductBalance (D-01/D-02); every mutation runs in ONE FOR UPDATE tx locking the pool row + the target copy + user_sanguo_state, with the new deductHonNgoc(tx, userId, heroId, amount) WHERE-guard primitive throwing INSUFFICIENT_HON_NGOC on zero rows (mirrors wallet.ts:53-62)."
    - "Leveling is an explicit action: LEVEL_COST from sanguoProgression.ts charged per level, max level 100 (D-01/D-05); evolution requires the level gate (L20→t1, L50→t2) + EVOLUTION_COSTS hồn ngọc, raises base stats via TIER_MULTIPLIERS + swaps the emoji to the t1/t2 spritesheet variant via heroEmoji(heroId, tier) (D-06/D-07); IVs stay capture-locked (never re-rolled)."
    - "Skill re-roll (D-32) costs REROLL_COST=10 hồn ngọc per slot, one slot at a time (normal/special), replacing only the selected slot's skill on the selected copy."
    - "Converting the ACTIVE companion copy is blocked or auto-switches the companion to the earliest remaining copy inside the tx — a dangling activeHeroId (NO_ACTIVE_HERO at battleCheckInService.ts:131) can never be created (Pitfall 3)."
    - "UI-SPEC lifted: copy-selector surfaces handle zero-one-many (1 copy → direct actions, no select; many → paged select at 25 with page buttons + counter in a ≤1,024-char field); hero-detail adds the 🎯 Kỹ năng field (2 slots with MP costs) + action row; hồn ngọc balances/costs and MP costs render, stat deltas never (D-12)."
  artifacts:
    - src/services/sanguo/soulgemService.ts — deductHonNgoc, convertDuplicate, levelUp, evolveHero, rerollSkill
    - src/services/sanguo/__tests__/soulgemService.test.ts
    - src/commands/sanguo/hero.ts — copy selector + convert/level/evolve/reroll action handlers
    - 6 new UI components + 2 extended/new embeds (copy menu, page buttons, convert/level/evolve buttons, reroll slot menu + button, hero embed copy/skill fields, progression result embed)
    - interactionCreate.ts — sanguo:hero:copy/page/convert/level/evolve/reroll routing blocks
    - i18n: convert.*/level.*/evolve.*/reroll.*/skills.* sections + hero.* additive keys, 3 locales
  key_links:
    - "deductHonNgoc ↔ user_hero_soulgems row — the WHERE-guard (amount >= cost + rowCount) is the single anti-double-spend control for ALL four progression actions (Pitfall 1)."
    - "convert tx ↔ user_sanguo_items booster row — booster consumption + yield computation in ONE tx with FOR UPDATE + quantity>=1 guard (Pitfall 2 anti-clone)."
    - "copy selector customId ↔ resolveOwnedHero — the pressed userHeroId is re-validated inside the tx (ownership re-gate on every press, never trusts the payload)."
    - "heroEmoji(heroId, tier) ↔ evolve action — the D-07 emoji swap renders the t1/t2 spritesheet variant after evolution."
  prohibitions:
    - "No wallet.deductBalance anywhere in soulgemService — hồn ngọc is a SEPARATE account-bound per-hero resource, never a users.balance flow (D-02)."
    - "No hồn ngọc → Linh thạch conversion path (D-02 account-bound, never converts back)."
    - "No re-roll of IVs by level/evolve — IVs are capture-locked (D-02 Phase 10, D-07)."
    - "No per-copy pool — user_hero_soulgems is per-(user, heroId) species, shared across copies (D-02 'spendable on Tào Tháo copies')."
    - "No stat deltas / base stats / tier multipliers rendered — level-up result shows level only (D-12, UI-SPEC copy rule)."
    - "No daily conversion cap — D-03 amends TQC-14's 'daily conversion cap' wording away (flat-by-tier is the diminishing-returns mechanism)."
  flagged_assumptions:
    - "FLAGGED ASSUMPTION (probe TQC-14 boundary, unresolved): conversion requires ≥2 owned copies — exactly 2 copies converting one leaves exactly 1 copy; the guard is >= 2 (A10); converting with 1 copy → convert.insufficient."
    - "FLAGGED ASSUMPTION (probe TQC-14 precision, unresolved): hồn ngọc math is INTEGER throughout (tier values 1/5/10/20 × booster 2 → 2/10/20/40, never fractional); no float arithmetic in the pool."
    - "FLAGGED ASSUMPTION (probe TQC-15 adjacency, unresolved): evolution thresholds are INCLUSIVE — a hero at exactly L20 may evolve to t1, exactly L50 to t2; t3 requires L80+ AND an event item (D-09) — unreachable in v3, the evolve button renders disabled with evolve.t3_gated."
    - "FLAGGED ASSUMPTION (probe TQC-15 empty, unresolved): LEVEL_COST(1) = 1 — the first level costs 1 hồn ngọc; a level-100 hero pressing level → level.max error; a copy with 0 hồn ngọc in pool → level.insufficient."
    - "FLAGGED ASSUMPTION (probe TQC-15 ordering, unresolved): copy selector options ordered by capturedAt asc (earliest copy first — matches resolveOwnedHero's earliest-captured preference, D-04), then userHeroes.id asc as tiebreak; the copy index #N is stable within a page."

---

<objective>
What this plan accomplishes: **the phase's TRACER — the hồn ngọc progression vertical slice, wired end-to-end: /sanguo hero copy selector (D-04) → convert duplicate (D-03) → per-hero hồn ngọc pool (D-02) → level (D-05) → evolve (D-06/D-07) → skill re-roll (D-32).** Task 1 builds soulgemService (deductHonNgoc + convertDuplicate with atomic booster consumption) + the copy selector + convert UI; Task 2 adds levelUp + evolveHero + their buttons/embeds; Task 3 adds rerollSkill + the reroll UI + the i18n sections. The tracer proves the FOR UPDATE single-writer pattern + WHERE-guard primitive + copy-selector interaction on the phase's thinnest path before the shop (11-04) and legion (11-05+) expand on the same patterns.

Purpose: TQC-14 (dupe → hồn ngọc, account-bound) + TQC-15 (evolution L20/L50) are the first vertical loop of Phase 11; the copy selector is the interaction surface every progression action shares.

Output: working convert/level/evolve/reroll over the copy selector, hồn ngọc pool persisted, i18n green.
</objective>

<execution_context>
@C:/Users/901107/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/901107/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-progression-chemistry-economy-depth/11-CONTEXT.md
@.planning/phases/11-progression-chemistry-economy-depth/11-RESEARCH.md
@.planning/phases/11-progression-chemistry-economy-depth/11-PATTERNS.md
@.planning/phases/11-progression-chemistry-economy-depth/11-UI-SPEC.md
@.planning/phases/11-progression-chemistry-economy-depth/11-01-PLAN.md
@.planning/phases/11-progression-chemistry-economy-depth/11-02-PLAN.md
@src/services/wallet.ts
@src/services/sanguo/battleCheckInService.ts
@src/commands/sanguo/hero.ts
@src/commands/sanguo/heroes.ts
@src/events/interactionCreate.ts
@src/ui/embeds/buildSanguoHeroEmbed.ts
@src/db/schema/userHeroes.ts
@src/db/schema/userHeroSoulgems.ts
@src/db/schema/soulgemTransactions.ts
@src/db/schema/userSanguoState.ts
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (TRACER): soulgemService.convertDuplicate + deductHonNgoc + copy selector in /sanguo hero + convert UI + routing</name>
  <files>src/services/sanguo/soulgemService.ts, src/services/sanguo/__tests__/soulgemService.test.ts, src/commands/sanguo/hero.ts, src/commands/sanguo/__tests__/hero.test.ts, src/events/interactionCreate.ts, src/ui/components/sanguoHeroCopyMenu.ts, src/ui/components/sanguoHeroPageButtons.ts, src/ui/components/sanguoConvertButton.ts, src/ui/embeds/buildSanguoHeroEmbed.ts, src/ui/embeds/buildSanguoProgressionResultEmbed.ts</files>
  <read_first>
    - src/services/wallet.ts lines 47-82 (the deductBalance WHERE-guard pattern deductHonNgoc mirrors)
    - src/services/sanguo/battleCheckInService.ts lines 224-236 (FOR UPDATE single-writer tx pattern) + lines 35-37 (error convention)
    - src/commands/sanguo/hero.ts (resolveOwnedHero lines 142-170 — the D-04 extension point; renderHeroDetail lines 178-219; handleCompanionPress lines 269-348)
    - src/events/interactionCreate.ts lines 119-259 (customId prefix/exact routing pattern — the template for the new sanguo:hero:copy/page/convert blocks)
    - src/ui/embeds/buildSanguoHeroEmbed.ts (D-12 safe data-interface pattern lines 18-64)
    - src/db/schema/userHeroSoulgems.ts + soulgemTransactions.ts + userSanguoState.ts (the tables the tx locks)
    - .planning/phases/11-progression-chemistry-economy-depth/11-RESEARCH.md §Pattern 5 (hồn ngọc single-writer txs + convert flow) + §Common Operation 2 (deductHonNgoc)
    - .planning/phases/11-progression-chemistry-economy-depth/11-UI-SPEC.md §Component & CustomId Contract (copy/page/convert rows) + §Copywriting (convert.*/hero.copy_* keys)
  </read_first>
  <action>
    Create `src/services/sanguo/soulgemService.ts` with the wallet-mirroring primitive + the conversion tx (TQC-14):
    - `type Tx = Parameters<Parameters<typeof db.transaction>[0]>[0];` and `export async function deductHonNgoc(tx: Tx, userId: number, heroId: number, amount: number): Promise<void>` — UPDATE user_hero_soulgems SET amount = amount − $amount WHERE userId = $userId AND heroId = $heroId AND amount >= $amount, returning the new amount; throw new Error('INSUFFICIENT_HON_NGOC') on zero rows (exact wallet.ts:53-62 shape).
    - `export async function convertDuplicate(userId: number, userHeroId: number): Promise<{ yield: number; boosterUsed: boolean }>` — ONE db.transaction: (1) FOR UPDATE lock the target user_heroes copy (ownership re-gate: WHERE id = $userHeroId AND userId = $userId, else NOT_OWNED) + FOR UPDATE lock the user_hero_soulgems pool row (upsert-on-missing pattern from hero.ts:307-320) + lock user_sanguo_state; (2) count owned copies of the SAME heroId — if < 2 throw new Error('NOT_ENOUGH_COPIES'); (3) if the copy is the active companion (user_sanguo_state.activeHeroId === userHeroId), auto-switch activeHeroId to the earliest remaining copy (lowest userHeroes.id) in the same tx (Pitfall 3 — never leave a dangling activeHeroId); (4) read booster ownership: user_sanguo_items join sanguo_items WHERE code='booster_x2' quantity >= 1 — if owned, FOR UPDATE that inventory row, yield = TIER_VALUE[tier] × 2, decrement quantity (delete row at 0 — quantity_positive check); else yield = TIER_VALUE[tier]; (5) DELETE the selected copy row; (6) upsert user_hero_soulgems.amount += yield; (7) INSERT a soulgem_transactions row { userId, heroId, type: 'convert', amount: +yield, balanceAfter }; (8) return { yield, boosterUsed }. TIER_VALUE = { 0: 1, 1: 5, 2: 10, 3: 20 } (D-03 flat-by-tier; the tier read from the copy's user_heroes.tier column). NO daily cap (D-03 amendment).
    - Extend `src/commands/sanguo/hero.ts`: after renderHeroDetail resolves the target copy, if the hero has >1 owned copies render the `sanguoHeroCopyMenu` (StringSelectMenu, paged at 25, option label hero.copy_line `#{{i}} — Lv{{level}} • {{grade}}` + setEmoji(heroEmoji(heroId)) CR-09-02, own ActionRow CR-09-01) + page buttons row (sanguo:hero:page:{dir}:{offset}) + action row (convert/level/evolve/reroll buttons carrying the chosen userHeroId); hero with exactly 1 copy → action buttons directly, no select (zero-one-many). Add handler functions handleCopyPress (re-render with action buttons targeting the chosen copy), handleCopyPage, handleConvertPress — the convert handler calls soulgemService.convertDuplicate then renders the progression-result embed (buildSanguoProgressionResultEmbed, SUCCESS color, convert.done copy with the yield + booster hint convert.booster_hint).
    - Extend `src/events/interactionCreate.ts`: add customId routing blocks for `sanguo:hero:copy:` (parseInt + isNaN guard), `sanguo:hero:page:`, `sanguo:convert:go:` following the sanguo:capture:tier: template (lines 212-230); extend the SanguoComponentHandlers interface.
    - Create the UI components (customId contract constants + builders per sanguoCaptureButtons.ts pattern): sanguoHeroCopyMenu.ts (sanguo:hero:copy:{userHeroId}), sanguoHeroPageButtons.ts (sanguo:hero:page:{dir}:{offset}, emoji-only ⬅️/➡️), sanguoConvertButton.ts (sanguo:convert:go:{userHeroId}, Secondary — destructive-class, yield in label via convert.button key).
    - Extend buildSanguoHeroEmbed.ts with the copy-list field (≤2 fields: list + page counter, ≤1,024 chars) + the 🎯 Kỹ năng field (2 slots, skills.field labels + skills.line with MP cost) — data interface carries visible fields only (D-12).
    - Create buildSanguoProgressionResultEmbed.ts — shared result states (convert/level/evolve/reroll), SUCCESS/DANGER colors from theme.ts COLORS only.
    - Tests: soulgemService.test.ts (convert yield + booster x2 atomicity + NOT_ENOUGH_COPIES + active-companion auto-switch + INSUFFICIENT_HON_NGOC on concurrent deduction — injectable rng not needed, deterministic); hero.test.ts extension (copy select renders for >1 copies, skipped for 1).
  </action>
  <verify>
    <automated>npx vitest run src/services/sanguo/__tests__/soulgemService.test.ts src/commands/sanguo/__tests__/hero.test.ts; npm run typecheck; npm run check-i18n</automated>
  </verify>
  <acceptance_criteria>
    - Command: `npx vitest run src/services/sanguo/__tests__/soulgemService.test.ts src/commands/sanguo/__tests__/hero.test.ts` exits 0.
    - Command: `npm run typecheck` + `npm run check-i18n` exit 0.
    - Source: `grep -c "deductHonNgoc" src/services/sanguo/soulgemService.ts` >= 1; the function contains `amount >=` in its WHERE (WHERE-guard) — `grep -c "INSUFFICIENT_HON_NGOC" src/services/sanguo/soulgemService.ts` >= 1.
    - Source: `grep -c "wallet" src/services/sanguo/soulgemService.ts` === 0 — hồn ngọc is never a users.balance flow (D-02); `grep -v '^#' src/services/sanguo/soulgemService.ts | grep -c "deductBalance"` === 0.
    - Source: convertDuplicate guards >= 2 copies (grep 'NOT_ENOUGH_COPIES' >= 1) and the booster consumption sits INSIDE the same tx as the yield computation (single db.transaction wrapping both — Pitfall 2).
    - Source: interactionCreate.ts has the 3 new routing blocks (sanguo:hero:copy / sanguo:hero:page / sanguo:convert:go).
    - Source: locales/{vi,en,zh-cn}/sanguo.json all contain the convert.*, hero.copy_select/copy_line keys (check-i18n parity enforced).
  </acceptance_criteria>
  <done>The tracer is live: a user with duplicate copies can open /sanguo hero, pick a copy, convert it to per-hero hồn ngọc (booster 2× atomic), and see the yield — pool persisted, ledger row written, no wallet call anywhere.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: levelUp + evolveHero (L20→t1 / L50→t2) + level/evolve buttons + result embed states</name>
  <files>src/services/sanguo/soulgemService.ts, src/services/sanguo/__tests__/soulgemService.test.ts, src/commands/sanguo/hero.ts, src/events/interactionCreate.ts, src/ui/components/sanguoLevelButton.ts, src/ui/components/sanguoEvolveButton.ts, src/ui/embeds/buildSanguoProgressionResultEmbed.ts</files>
  <read_first>
    - src/constants/sanguoProgression.ts (LEVEL_COST, MAX_LEVEL, TIER_MULTIPLIERS, EVOLUTION_COSTS — from 11-02)
    - src/assets/sanguoEmojis.ts line ~1230 (heroEmoji(heroId, tier, star) — the tier-aware emoji for the D-07 swap)
    - .planning/phases/11-progression-chemistry-economy-depth/11-RESEARCH.md §Pattern 1 (evolution costs + tier multipliers) + §Common Operation 2 (level-up tx shape)
    - .planning/phases/11-progression-chemistry-economy-depth/11-UI-SPEC.md §Copywriting (level.*/evolve.* keys incl. level.max, evolve.requirement, evolve.t3_gated)
  </read_first>
  <behavior>
    - Test 1 (levelUp): a L1 copy with a pool of 10 hồn ngọc levels to L2 and charges LEVEL_COST(1)=1 (pool 9); pool exactly equal to the cost succeeds; pool 1 short → INSUFFICIENT_HON_NGOC with NO level change (whole tx rollback).
    - Test 2 (levelUp): a L100 copy → LEVEL_MAX error, no deduction.
    - Test 3 (evolveHero): a L20 t0 copy with 20+ hồn ngọc evolves to t1 and charges 20; a L19 copy → LEVEL_REQUIRED error; a L50 t1 copy evolves to t2 for 50; a t2 copy pressing evolve → T3_GATED error (L80+ AND event item, D-09 unreachable); tier writes to user_heroes.tier.
  </behavior>
  <action>
    Extend `soulgemService.ts`:
    - `export async function levelUp(userId: number, userHeroId: number): Promise<{ newLevel: number; cost: number }>` — ONE tx: FOR UPDATE lock the copy (ownership re-gate) + pool row; read the copy's current level; if level >= MAX_LEVEL (100) throw new Error('LEVEL_MAX'); cost = LEVEL_COST(level); deductHonNgoc(tx, userId, heroId, cost); UPDATE user_heroes SET level = level + 1; INSERT soulgem_transactions { type: 'level', amount: −cost, balanceAfter }; return { newLevel: level + 1, cost }. Level is per-copy (each copy has its own level column — D-34).
    - `export async function evolveHero(userId: number, userHeroId: number): Promise<{ newTier: number; cost: number }>` — ONE tx: lock copy + pool; read tier + level; tier 0→1 requires level >= 20 (evolve to t1, cost EVOLUTION_COSTS[1] = 20); tier 1→2 requires level >= 50 (cost 50); tier 2→3 throws new Error('T3_GATED') (D-09 — L80+ AND event item, unreachable in v3); deductHonNgoc; UPDATE user_heroes SET tier = tier + 1; INSERT soulgem_transactions { type: 'evolve', amount: −cost }; return { newTier, cost }. Evolution does NOT block leveling and never re-rolls IVs (D-06/D-07 — IVs capture-locked).
    - Extend `hero.ts` handlers: handleLevelPress + handleEvolvePress calling the services, rendering the result embed with level.up / evolve.done copy (evolve.done shows the NEW emoji via heroEmoji(heroId, newTier) — the D-07 t1/t2 spritesheet swap). The evolve button renders DISABLED until L20/L50 + sufficient hồn ngọc with the evolve.requirement label (UI-SPEC).
    - Create sanguoLevelButton.ts (sanguo:level:go:{userHeroId}, Primary, level.button cost in label) + sanguoEvolveButton.ts (sanguo:evolve:go:{userHeroId}, Primary, disabled-state handling). Extend interactionCreate.ts with the sanguo:level:go / sanguo:evolve:go routing blocks. Extend the result embed with level/evolve state flags (level-up shows level ONLY, never stat deltas — D-12).
    - Tests: the 3 behaviors in soulgemService.test.ts.
  </action>
  <verify>
    <automated>npx vitest run src/services/sanguo/__tests__/soulgemService.test.ts; npm run typecheck; npm run check-i18n</automated>
  </verify>
  <acceptance_criteria>
    - Command: vitest green for the 3 new behaviors; `npm run typecheck` + `npm run check-i18n` green.
    - Source: levelUp contains the MAX_LEVEL guard (`LEVEL_MAX`) and LEVEL_COST call; evolveHero contains the L20/L50 thresholds + `T3_GATED`.
    - Source: `grep -c "heroEmoji" src/commands/sanguo/hero.ts` >= 1 in the evolve flow (tier-aware emoji swap, D-07).
    - Source: locales have level.*/evolve.* keys (level.max, evolve.requirement, evolve.t3_gated) in all 3 locales.
    - Source: the result embed for level-up carries NO stat field (grep the embed data interface for absence of stat keys — D-12).
  </acceptance_criteria>
  <done>A user can level a copy (hồn ngọc curve, max 100) and evolve it at L20→t1 / L50→t2 with the emoji swap + base-stat multipliers applied — all hồn ngọc-charged, IVs untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: rerollSkill (D-32) + reroll slot UI + i18n sections (convert/level/evolve/reroll/skills)</name>
  <files>src/services/sanguo/soulgemService.ts, src/services/sanguo/__tests__/soulgemService.test.ts, src/commands/sanguo/hero.ts, src/events/interactionCreate.ts, src/ui/components/sanguoRerollSlotMenu.ts, src/ui/components/sanguoRerollButton.ts, src/i18n/index.ts, locales/vi/sanguo.json, locales/en/sanguo.json, locales/zh-cn/sanguo.json</files>
  <read_first>
    - src/db/schema/sanguoSkills.ts (the skill catalog table — the reroll replacement pool reads it)
    - src/constants/sanguoProgression.ts (REROLL_COST = 10)
    - .planning/phases/11-progression-chemistry-economy-depth/11-RESEARCH.md §Pattern 3 (skill pools + rarity weights) + D-32 (TM-style reroll, one slot at a time)
    - .planning/phases/11-progression-chemistry-economy-depth/11-UI-SPEC.md §Component & CustomId Contract (reroll rows) + §Copywriting (reroll.*/skills.* keys)
    - src/i18n/index.ts (sanguo namespace registration pattern)
  </read_first>
  <behavior>
    - Test 1 (rerollSkill): a copy with 10 hồn ngọc re-rolls the normal slot → charges REROLL_COST, the slot's skill_id changes (drawn from the class pool weighted by rarity via crypto — injectable rng for determinism), soulgem_transactions type 'reroll' written; pool 9 → INSUFFICIENT_HON_NGOC, skill unchanged.
    - Test 2 (rerollSkill): the special slot re-roll draws ONLY from the class's special pool; the normal slot re-roll draws ONLY from the normal pool (slot isolation — D-32 one slot at a time).
  </behavior>
  <action>
    Extend `soulgemService.ts`:
    - `export async function rerollSkill(userId: number, userHeroId: number, slot: 'normal' | 'special', rng: () => number = cryptoUniform): Promise<{ newSkillCode: string }>` — ONE tx: FOR UPDATE lock copy + pool; ownership re-gate; deductHonNgoc(tx, userId, heroId, REROLL_COST); pick the replacement from the class's skill pool for that slot (sanguo_skills WHERE class = copy's class AND slot = $slot, weighted by rarity weight common 80/rare 20 for normal, common 60/rare 30/epic 10 for special — RESEARCH Pattern 3; weighted pick via the cumulative walk at encounterService.ts:121-127 shape); UPDATE user_heroes SET skill_normal_id / skill_special_id = picked id (whichever slot); INSERT soulgem_transactions { type: 'reroll', amount: −10 }. crypto RNG ONLY (player-facing roll — milestone mandate); pure-rand never here.
    - Extend `hero.ts`: handleRerollPress flow — the reroll slot select (sanguoRerollSlotMenu, sanguo:reroll:slot:{userHeroId}) then the confirm button (sanguoRerollButton, sanguo:reroll:go:{userHeroId}:{slot}, Secondary — destructive-class: old skill lost, reroll.done states the replacement). Add the routing blocks to interactionCreate.ts (parseInt + isNaN guard on the slot).
    - Author the i18n sections in all 3 locales: convert.*, level.*, evolve.*, reroll.*, skills.* (skill names + MP labels), + additive hero.copy_select/copy_line/convert_button/level_button/evolve_button/reroll_button/field_skills — every key from the UI-SPEC Copywriting table, parity via check-i18n. Register any new namespace section in src/i18n/index.ts if the pattern requires.
    - Tests: the 2 behaviors (injectable rng for determinism).
  </action>
  <verify>
    <automated>npx vitest run src/services/sanguo/__tests__/soulgemService.test.ts; npm run check-i18n; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Command: vitest green for the reroll behaviors; `npm run check-i18n` + `npm run lint` green (eslint i18next/no-literal-string — zero hardcoded strings in the new UI).
    - Source: rerollSkill uses cryptoUniform (grep 'cryptoUniform' >= 1) and NO pure-rand import in soulgemService.ts.
    - Source: slot isolation — the WHERE includes the slot filter (grep 'slot' in the pool query).
    - Source: locales/{vi,en,zh-cn}/sanguo.json contain the FULL UI-SPEC key set for convert/level/evolve/reroll/skills + hero additive keys (check-i18n parity).
  </acceptance_criteria>
  <done>The full hồn ngọc progression loop is live: convert → pool → level → evolve → reroll, all account-bound per-hero, all hồn ngọc sinks, all i18n-complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| component customId → progression tx | The pressed userHeroId crosses this boundary; the cost/yield NEVER does — LEVEL_COST/EVOLUTION_COSTS/REROLL_COST resolve server-side inside the tx from constants (anti-tamper). |
| tx → pool/ledger | hồn ngọc mutations must be atomic — a level press that charges but doesn't write, or converts but clones the booster, corrupts the economy. |
| user_heroes copy rows | The active-companion identity must survive a copy deletion — a dangling activeHeroId breaks the next battle entry. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-03-01 | Tampering | Hồn ngọc double-spend (concurrent level/convert/evolve/reroll presses) | high | mitigate | Single-writer FOR UPDATE on pool + copy rows; deductHonNgoc WHERE-guard (amount >= cost + rowCount → INSUFFICIENT_HON_NGOC, whole tx rolls back) — mirrors wallet.ts:53-62 (Pitfall 1). |
| T-11-03-02 | Tampering | Booster cloning (2× applied twice) | high | mitigate | Booster consumption + yield computation in the SAME conversion tx; FOR UPDATE on the user_sanguo_items row; quantity >= 1 guard; delete at 0 (quantity_positive check) — Pitfall 2. |
| T-11-03-03 | Tampering | Crafted customId cost tampering (sanguo:convert:go / level / evolve / reroll) | high | mitigate | Costs NEVER in customIds — only userHeroId/slot ride the payload; prices resolve server-side inside the tx (UI-SPEC anti-tamper contract). |
| T-11-03-04 | Tampering | Last-copy / active-companion conversion → orphaned activeHeroId | high | mitigate | Conversion guards >= 2 copies + auto-switches the companion to the earliest remaining copy in the tx (Pitfall 3); regression test on battle entry after conversion. |
| T-11-03-05 | Information disclosure | Stat deltas / base stats / tier multipliers in level/evolve results | high | mitigate | Level-up result shows level only; embed data interfaces carry visible fields only (D-12 structural rule); no constants import in any UI file. |
| T-11-03-06 | Repudiation | Progression history (convert/level/evolve/reroll) | low | mitigate | soulgem_transactions ledger row per mutation (userId/heroId/type/amount/balanceAfter) — Phase 12 TQC-19 audit + /profile future. |
| T-11-03-SC | Tampering | npm/pip/cargo installs | — | not-triggered | This plan installs no packages. |
</threat_model>

<verification>
- `npx vitest run src/services/sanguo/__tests__/soulgemService.test.ts src/commands/sanguo/__tests__/hero.test.ts` green.
- `npm run typecheck`, `npm run check-i18n`, `npm run lint` green.
- Wallet-grep discipline: zero `deductBalance`/wallet imports in soulgemService.ts (D-02 structural proof).
- The tracer slice is E2E-verifiable: convert a dupe, spend the pool, evolve — all through the copy selector.
</verification>

## Artifacts this phase produces

| Symbol | Kind | File |
|--------|------|------|
| `deductHonNgoc(tx, userId, heroId, amount)` | wallet-mirror primitive (WHERE-guard) | src/services/sanguo/soulgemService.ts |
| `convertDuplicate(userId, userHeroId)` → {yield, boosterUsed} | tx (≥2 copies, booster atomic, companion auto-switch) | src/services/sanguo/soulgemService.ts |
| `levelUp(userId, userHeroId)` → {newLevel, cost} | tx (LEVEL_COST, MAX_LEVEL) | src/services/sanguo/soulgemService.ts |
| `evolveHero(userId, userHeroId)` → {newTier, cost} | tx (L20/L50 gates, T3_GATED) | src/services/sanguo/soulgemService.ts |
| `rerollSkill(userId, userHeroId, slot, rng)` → {newSkillCode} | tx (REROLL_COST, class-pool weighted, crypto) | src/services/sanguo/soulgemService.ts |
| Copy selector flow: handleCopyPress / handleCopyPage / handleConvertPress / handleLevelPress / handleEvolvePress / handleRerollPress | command handlers | src/commands/sanguo/hero.ts |
| sanguoHeroCopyMenu / sanguoHeroPageButtons / sanguoConvertButton / sanguoLevelButton / sanguoEvolveButton / sanguoRerollSlotMenu / sanguoRerollButton | UI components (customId contracts) | src/ui/components/ |
| buildSanguoHeroEmbed copy-list + skills fields; buildSanguoProgressionResultEmbed | embeds (extended/new) | src/ui/embeds/ |
| customId routing: sanguo:hero:copy/page/convert/level/evolve/reroll | event routing | src/events/interactionCreate.ts |
| i18n sections: convert.*/level.*/evolve.*/reroll.*/skills.* + hero additive keys (3 locales) | i18n | locales/{vi,en,zh-cn}/sanguo.json |
| soulgemService.test.ts (convert/level/evolve/reroll behaviors) | test suite | src/services/sanguo/__tests__/ |

<success_criteria>
- TQC-14: duplicate conversion works, tier-scaled flat (1/5/10/20), account-bound per-hero, booster 2× atomic — no daily cap (D-03 amendment).
- TQC-15: evolution at L20→t1 / L50→t2 with hồn ngọc cost; t3 gated (unreachable in v3).
- D-02/D-01 hold structurally: no wallet call in soulgemService; no hồn ngọc → Linh thạch path.
- The copy selector (D-04) works for all four progression actions with zero-one-many + paging handled.
</success_criteria>

<output>
Create `.planning/phases/11-progression-chemistry-economy-depth/11-03-SUMMARY.md` when done.
</output>
