---
phase: 11-progression-chemistry-economy-depth
plan: 06
type: execute
wave: 5
depends_on:
  - 11-04
  - 11-05
files_modified:
  - src/services/sanguo/skillService.ts
  - src/services/sanguo/encounterLevelService.ts
  - src/services/sanguo/travelCheckInService.ts
  - src/services/sanguo/battleCheckInService.ts
  - src/services/sanguo/captureService.ts
  - src/services/sanguo/__tests__/skillService.test.ts
  - src/services/sanguo/__tests__/encounterLevelService.test.ts
  - src/services/sanguo/__tests__/travelCheckInService.test.ts
  - src/services/sanguo/__tests__/battleCheckInService.test.ts
  - src/services/sanguo/__tests__/captureService.test.ts
  - src/constants/sanguoBoss.ts
  - src/ui/embeds/buildSanguoBossEncounterEmbed.ts
  - src/ui/embeds/buildSanguoBattleLogEmbed.ts
  - src/ui/embeds/buildSanguoCaptureEmbed.ts
  - locales/vi/sanguo.json
  - locales/en/sanguo.json
  - locales/zh-cn/sanguo.json
autonomous: false
requirements:
  - TQC-16
  - TQC-17

estimate:
  tokens: 58000
  raw_tokens: 29000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The boss encounter is REDESIGNED (D-24, WINDOWS.md #5 closed): the boss is a REAL hero drawn from the zone pool (like a normal encounter) whose FIGHT uses t2 base stats × IV all-31 × L50 (D-24/D-35); encounter_runs.hero_id is NON-NULL for bosses; src/constants/sanguoBoss.ts BOSS_TEMPLATES + bossTemplateFor() are REMOVED (superseded, not dormant) and the captureService BOSS_CAPTURE_UNAVAILABLE guard is REMOVED — a real heroes row exists so the boss is capturable."
    - "Wild heroes spawn at a random level (D-33): band roll L1-10 60% / L11-20 30% / L21-30 9.9% / L31-50 0.1% via crypto (cryptoUniform band then crypto.randomInt uniform-within-band), written to encounter_runs.level at spawn; the captured hero KEEPS the encounter level (D-34). The boss fights at fixed L50 (D-35)."
    - "Hero skills are rolled AT ENCOUNTER SPAWN from the class-based pools weighted by rarity (D-30: normal common 80/rare 20, special common 60/rare 30/epic 10), written to encounter_runs.skill_normal_id/skill_special_id, and CARRY to capture (D-31); replayable battles include the rolled skills in the sanguo_battles.input snapshot (D-06 replay contract)."
    - "Boss battle entry is a FORCED legion battle (D-25, no solo option): battleCheckInService routes boss encounters to runLegionBattle (11-05) with the user's active legion (user_legions + user_legion_slots); win → guaranteed item drop via dropService (11-04, D-14) + capture available; loss → boss departs, travel resumes (D-25); no assembled legion → legion.not_assembled."
    - "Boss capture uses the SAME signed 5-tier capture-fee model with the rarity-5 10% base chance (D-26 — NO new fee schedule, no D-18 re-sign); on success the CAPTURED copy is a FRESH roll — random IV ×6, random tier t0 95% / t1 4.98% / t2 0.02%, FIXED level 20, skills from encounter_runs (D-28/D-36) — stored via user_heroes.tier; fight difficulty ≠ prize is the user-design contract."
  artifacts:
    - src/services/sanguo/skillService.ts — rollNormalSkill/rollSpecialSkill (crypto-weighted)
    - src/services/sanguo/encounterLevelService.ts — rollWildLevel (band roll, D-33)
    - src/services/sanguo/travelCheckInService.ts — spawn integration (level + skills + boss real-hero pick)
    - src/services/sanguo/battleCheckInService.ts — boss → legion routing (buildEnemyInput zone general + runLegionBattle)
    - src/services/sanguo/captureService.ts — boss capture roll branch (guard removed)
    - src/constants/sanguoBoss.ts — REMOVED/replaced with a zone-general constants stub
    - 3 embeds (boss encounter GOLD variant, battle-log 3v1 extension, capture boss variant) + i18n capture/encounter additive keys
  key_links:
    - "encounter_runs.level/skill columns ↔ spawn roll (travelCheckInService) ↔ capture insert (captureService) — the spawn-time values flow through the whole chain (D-31/D-34)."
    - "battleCheckInService boss branch ↔ user_legions/user_legion_slots (11-07 builds the assembly; this plan consumes the persisted active legion) ↔ runLegionBattle (11-05)."
    - "boss win ↔ dropService.rollBossDrop (11-04) — the guaranteed item, wired in the win branch (D-14)."
    - "capture success boss branch ↔ user_heroes.tier + IV + L20 insert ↔ the collection render (11-07 heroes filters)."
  prohibitions:
    - "No new fee schedule for boss capture — CAPTURE_TIERS + CAPTURE_BASE_BY_RARITY reused verbatim (D-26); any new fee would require a D-18 re-sign."
    - "No dormant code: sanguoBoss.ts BOSS_TEMPLATES/bossTemplateFor + BOSS_CAPTURE_UNAVAILABLE are REMOVED, not commented out (D-24 one-way supersession — Pitfall 7)."
    - "No captured boss copy keeps the L50 fight level — the captured level is FIXED L20 (D-36); the t0 95 / t1 4.98 / t2 0.02 weights NEVER render (D-28 hidden mechanics)."
    - "No skill rarity weights or wild-level percentages rendered (D-30/D-33 hidden mechanics) — the rolled level renders as Lv{{n}}, skills render by name + MP cost only."
    - "No crypto leak: pure-rand stays inside battleEngine (11-05); spawn/skill/capture rolls are cryptoUniform ONLY."
  flagged_assumptions:
    - "SCOPE NOTE (dense-plan heuristic accepted — checker W-2): files_modified = 16 paths vs the 15/file-plan heuristic. Per-task CODE-file counts stay ≤ 10: Task 2 = 6 paths; Task 3 = 8 code paths + the locales/{vi,en,zh-cn} triplet counted as ONE i18n parity unit (check-i18n enforces the 3 files together — not 3 independent implementation units). Estimate 58k tokens (low confidence) is under the smart-zone budget; no scope is dropped vs the artifact table. If the executor hits context pressure in Task 3, the embeds+i18n block (surfaces only, zero service logic) is the cleanest defer point."
    - "FLAGGED ASSUMPTION (probe TQC-17 unclassified, unresolved): the forced-legion routing requires >= 1 main assembled; a user with an incomplete legion (n/3 mains) fights with what they have (R-11 legion.incomplete caution) — 0 mains → legion.not_assembled, no battle entry."
    - "FLAGGED ASSUMPTION (A12 carry): IV stays a battle-time roll (Phase 10 CR-02 behavior); only level + skills move to spawn-time — the CR-02 stale-button guard carries."
    - "FLAGGED ASSUMPTION (boss-win capture regression): boss capture success MUST grant a user_heroes row with the captured tier/level/skills — regression test pins it (Pitfall 7)."
    - "FLAGGED ASSUMPTION (boss sub-roll frequency, D-27): the ~5-10% boss sub-roll replacing a successful hero roll is KEPT from Phase 9 D-14 — no scheduled boss windows."

---

<objective>
What this plan accomplishes: **the boss redesign vertical — closing WINDOWS.md #5 (D-24..D-28/D-35/D-36).** Task 1 is a checkpoint:decision on the one-way boss supersession (remove the sanguoBoss template path + BOSS_CAPTURE_UNAVAILABLE guard vs. keep-dormant). Task 2 builds the encounter-spawn layer: skillService (class-pool roll at spawn, D-30), encounterLevelService (wild level band roll, D-33), and the travelCheckInService integration (boss sub-roll → REAL zone general pick, level + skills written to encounter_runs, D-24/D-27/D-31). Task 3 builds the boss fight + capture routing: battleCheckInService forces the legion battle (buildEnemyInput → t2×IV100×L50 zone general, runLegionBattle, D-25), wires the guaranteed drop (dropService, D-14), extends captureService with the boss capture roll (random IV/tier/L20, D-28/D-36), removes the superseded code, and renders the boss encounter/battle-log/capture surfaces (GOLD variant + 3v1 log + item-drop line).

Purpose: the boss is now a real, capturable hero — the Phase 10 stub becomes a full legion-battle prize loop, and the fight difficulty ≠ prize contract (D-28) is the phase's most distinctive economy design.

Output: a capturable random-zone-general boss fought as a forced 3v1 legion battle, with spawn-rolled levels/skills flowing to capture.
</objective>

<execution_context>
@C:/Users/901107/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/901107/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-progression-chemistry-economy-depth/11-CONTEXT.md
@.planning/phases/11-progression-chemistry-economy-depth/11-RESEARCH.md
@.planning/phases/11-progression-chemistry-economy-depth/11-PATTERNS.md
@.planning/phases/11-progression-chemistry-economy-depth/11-UI-SPEC.md
@.planning/phases/11-progression-chemistry-economy-depth/11-05-PLAN.md
@src/constants/sanguoBoss.ts
@src/services/sanguo/travelCheckInService.ts
@src/services/sanguo/battleCheckInService.ts
@src/services/sanguo/captureService.ts
@src/services/sanguo/encounterService.ts
@src/constants/sanguoCapture.ts
@src/db/schema/encounterRuns.ts
@src/db/schema/heroes.ts
@src/db/schema/heroZoneRates.ts
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <decision>Confirm the one-way boss redesign supersession (D-24, WINDOWS.md #5): the boss becomes a REAL hero from the zone pool (fight = t2 base × IV all-31 × L50, D-24/D-35; capturable — encounter_runs.hero_id non-null), which REQUIRES removing src/constants/sanguoBoss.ts BOSS_TEMPLATES + bossTemplateFor() and the captureService.ts:150 BOSS_CAPTURE_UNAVAILABLE guard — SUPERSEDED AND REMOVED, not dormant.</decision>
  <context>D-24 is a one-way user design decision: the Phase 10 zone-scaled template approach and the boss-capture stub are DELETED, replaced by the random-zone-general flow. Keeping both paths would let the old template path resurface through battleCheckInService's buildEnemyInput (battleCheckInService.ts:146-165 currently calls bossTemplateFor(encounter.zone)) — the exact regression Pitfall 7 flags. The checkpoint mirrors the Phase 10 ack→battle inversion precedent (one-way supersession, removal not dormancy). Cost of reversal: re-adding the template path is a small diff, but the user design (D-24/D-28 fight-difficulty ≠ prize) is locked.</context>
  <options>
    <option id="adopt-d24">
      <name>Full removal (locked D-24): delete BOSS_TEMPLATES/bossTemplateFor + BOSS_CAPTURE_UNAVAILABLE, route bosses as real zone-general heroes</name>
      <pros>Honors the locked redesign; no dormant dead code; boss capture resolves the Phase 10 stub; the fight-difficulty ≠ prize contract (D-28) lands as designed.</pros>
      <cons>One-way — the old template path is gone; boss difficulty is now a function of the zone general's real stats at t2×IV100×L50 (balance pass in 11-08 tunes it).</cons>
    </option>
    <option id="keep-dormant">
      <name>Keep the template path dormant (comment out, not remove)</name>
      <pros>Preserves the old code for reference.</pros>
      <cons>VIOLATES the locked D-24 supersession + Pitfall 7 (dormant superseded code re-surfaces in regression); dead code that the 11-08 balance pass cannot trust — not recommended.</cons>
    </option>
  </options>
  <resume-signal>Select: adopt-d24 (recommended, honors locked D-24) or keep-dormant (deviation — requires a CONTEXT amendment)</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Encounter spawn — skillService + encounterLevelService + travelCheckInService integration (boss real-hero pick, level + skills written)</name>
  <files>src/services/sanguo/skillService.ts, src/services/sanguo/encounterLevelService.ts, src/services/sanguo/travelCheckInService.ts, src/services/sanguo/__tests__/skillService.test.ts, src/services/sanguo/__tests__/encounterLevelService.test.ts, src/services/sanguo/__tests__/travelCheckInService.test.ts</files>
  <read_first>
    - src/services/sanguo/travelCheckInService.ts lines 147-174 (the spawn-site integration — makeDefaultRollMinute: boss sub-roll + encounter_runs insert)
    - src/services/sanguo/encounterService.ts lines 57-59 (cryptoUniform) + lines 121-127 (weighted cumulative walk)
    - src/db/schema/encounterRuns.ts (level + skillNormalId + skillSpecialId columns from 11-01)
    - src/db/schema/heroZoneRates.ts + heroes.ts (the zone pool the boss general is picked from — D-24 'drawn from the zone pool')
    - .planning/phases/11-progression-chemistry-economy-depth/11-RESEARCH.md §Pattern 3 (skill roll weights) + §Common Operation 3 (rollWildLevel + rollSpecialSkill) + D-33 band math
    - .planning/phases/11-progression-chemistry-economy-depth/11-PATTERNS.md §skillService/encounterLevelService (lines 185-197)
  </read_first>
  <behavior>
    - Test 1 (rollWildLevel): injected rng 0.0-0.599 → L1-10, 0.6-0.899 → L11-20, 0.9-0.998 → L21-30, 0.999 → L31-50 (band boundaries via crypto.randomInt uniform-within-band — test with the pure band function + injected rng for the band, then assert the range).
    - Test 2 (rollNormalSkill/rollSpecialSkill): with a fixture class pool + injected rng, the pick is rarity-weighted (common 80/20, special 60/30/10); the special roll draws ONLY from the special pool, normal ONLY from normal.
    - Test 3 (spawn integration): a non-boss encounter run insert carries level + skill ids; a boss sub-roll inserts a REAL heroId (non-null) with L50 + rolled skills — hero_id no longer NULL for bosses (D-24).
  </behavior>
  <action>
    Create the two pure spawn modules + integrate into travelCheckInService:
    1. `src/services/sanguo/encounterLevelService.ts` — `export function rollWildLevel(rng: () => number = cryptoUniform): number` — band = Math.floor(rng() × 1000); band < 600 → crypto.randomInt(1, 11); < 900 → crypto.randomInt(11, 21); < 999 → crypto.randomInt(21, 31); else crypto.randomInt(31, 51) (D-33 — L1-10 60%, L11-20 30%, L21-30 9.9%, L31-50 0.1%, '30+' caps at L50). Pure module: no db imports, crypto default + injectable rng for tests.
    2. `src/services/sanguo/skillService.ts` — `export function rollSkill(classPool: SanguoSkill[], rng: () => number = cryptoUniform): SanguoSkill` — rarity-weighted cumulative walk (RARITY_WEIGHT = { common: 80, rare: 20 } for normal; { common: 60, rare: 30, epic: 10 } for special — passed per pool context), + `export async function rollSkillsForSpawn(classId: string, rng?: () => number): Promise<{ normalId: number | null; specialId: number | null }>` — queries sanguo_skills WHERE class = $classId AND slot = 'normal'/'special' (weights from the rarity column mapping). Crypto default (D-30 player-facing roll); injectable rng for tests.
    3. Extend `travelCheckInService.ts` makeDefaultRollMinute (lines 147-174): (a) boss sub-roll (shouldRollBoss — D-27 ~5-10% kept) now picks a REAL hero from the zone pool (the same pickEncounterHero weighted pick at encounterService.ts:91-96 — a zone general), NOT heroId null; (b) wild heroes: level = rollWildLevel(), skills = rollSkillsForSpawn(hero.class); (c) boss: level = 50 (D-35 fixed), skills = rollSkillsForSpawn(boss.class); (d) write level + skillNormalId + skillSpecialId into the encounter_runs insert. The F2 re-fetch + caps logic stay untouched.
    - Tests: the 3 behaviors.
  </action>
  <verify>
    <automated>npx vitest run src/services/sanguo/__tests__/encounterLevelService.test.ts src/services/sanguo/__tests__/skillService.test.ts src/services/sanguo/__tests__/travelCheckInService.test.ts; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Command: vitest green for the 3 spawn behaviors; `npm run typecheck` green.
    - Source: `grep -c "cryptoUniform\|crypto.randomInt" src/services/sanguo/encounterLevelService.ts src/services/sanguo/skillService.ts` >= 1 each (crypto player-facing rolls; no pure-rand import in either).
    - Source: travelCheckInService boss branch no longer sets heroId = null (grep the boss path — heroId comes from the zone pick); encounter_runs insert includes level + skillNormalId + skillSpecialId.
    - Source: the boss insert uses level 50 (D-35); wild inserts use the rollWildLevel result (D-33).
  </acceptance_criteria>
  <done>Encounters spawn with levels + rolled skills; bosses are real zone-general heroes (hero_id non-null) at L50 — the spawn layer feeds both solo (level term) and legion (boss) battles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Boss fight routing (forced legion) + guaranteed drop wiring + boss capture roll + superseded-code removal + boss surfaces</name>
  <files>src/services/sanguo/battleCheckInService.ts, src/services/sanguo/captureService.ts, src/constants/sanguoBoss.ts, src/services/sanguo/__tests__/battleCheckInService.test.ts, src/services/sanguo/__tests__/captureService.test.ts, src/ui/embeds/buildSanguoBossEncounterEmbed.ts, src/ui/embeds/buildSanguoBattleLogEmbed.ts, src/ui/embeds/buildSanguoCaptureEmbed.ts, locales/vi/sanguo.json, locales/en/sanguo.json, locales/zh-cn/sanguo.json</files>
  <read_first>
    - src/services/sanguo/battleCheckInService.ts lines 146-165 (buildEnemyInput — the bossTemplateFor call to REPLACE), 224-313 (FOR UPDATE tx + storeBattle snapshot + HP write-back), 35-37 (error convention)
    - src/services/sanguo/captureService.ts lines 118-291 (capture tx + success insert lines 218-234 + the BOSS_CAPTURE_UNAVAILABLE guard at 150)
    - src/constants/sanguoBoss.ts (the module to REMOVE per D-24)
    - src/constants/sanguoCapture.ts (CAPTURE_TIERS/CAPTURE_BASE_BY_RARITY — reused verbatim, D-26)
    - src/services/sanguo/dropService.ts (11-04 — the guaranteed drop wiring target)
    - src/ui/embeds/buildSanguoEncounterEmbed.ts + buildSanguoBattleLogEmbed.ts + buildSanguoCaptureEmbed.ts (the embed extension bases)
    - .planning/phases/11-progression-chemistry-economy-depth/11-RESEARCH.md §Pattern 4 (boss redesign + capture math) + §Common Operation 6 (boss capture roll)
    - .planning/phases/11-progression-chemistry-economy-depth/11-UI-SPEC.md §Color (GOLD 0xF59E0B boss variant) + §Copywriting (capture.item_drop, capture.captured_copy, encounter.boss_line, skills.battle_* lines)
  </read_first>
  <behavior>
    - Test 1 (boss routing): a pending BOSS encounter with an assembled legion routes to runLegionBattle (runBattleFn injected → asserts the legion input shape: mains[3] with buffed stats + level, supports[9], boss with t2×IV{31×6}×L50); a boss encounter with NO legion → legion.not_assembled, no engine call; a WILD encounter still routes to runBattle (solo unchanged — D-23).
    - Test 2 (boss win + drop): a legion WIN calls dropService.rollBossDrop (injected) → guaranteed item granted + capture view opens; a LOSS → boss departs (travel resumes), no drop.
    - Test 3 (boss capture): success on a boss encounter inserts user_heroes with RANDOM IV ×6, tier from the t0 95/t1 4.98/t2 0.02 roll (injected rng: 0.94→t0, 0.95→t1, 0.9998→t2), FIXED level 20 (D-36), skills from encounter_runs (D-31); the BOSS_CAPTURE_UNAVAILABLE guard is GONE (boss capture no longer throws).
  </behavior>
  <action>
    Extend the routing + capture services and remove the superseded code:
    1. `src/services/sanguo/battleCheckInService.ts` — REPLACE the boss branch in buildEnemyInput (lines 146-165): DELETE the bossTemplateFor(encounter.zone) call; the boss enemy input = the encounter's REAL heroes row with base stats × TIER_MULTIPLIERS[2] (t2, 1.25) × IV all-31 × level 50 (D-24/D-35 — eff() adds the level term from 11-05). Add the boss routing branch in the battle flow: boss encounter → read the user's active legion (user_legions join user_legion_slots join user_heroes — assembled in 11-07); if no mains → throw new Error('legion.not_assembled'); call runLegionBattle(seed, legionInput) where legionInput = { mains[3] (buffed via chemistryService.applyChemistryBuff — 11-05 — + level + skillIds), supports[9] (heroId/class/lea/special), boss }; store the FULL legion input in sanguo_battles.input jsonb (Pitfall 6 replay contract). WIN → dropService.rollBossDrop(userId) (guaranteed ≥1, D-14) + open capture; LOSS → boss departs, travel resumes (D-25). Wild encounters stay SOLO runBattle (D-23) with the encounter level added to both inputs.
    2. `src/services/sanguo/captureService.ts` — REMOVE the BOSS_CAPTURE_UNAVAILABLE guard (line 150 — no longer reachable since boss encounters carry a real heroId); add the boss-success branch (D-28/D-36): same fees/chance (CAPTURE_TIERS + rarity-5 10% base, D-26); on success insert user_heroes with iv = 6× crypto.randomInt(0, 32), tier = cryptoUniform() < 0.95 ? 0 : < 0.9998 ? 1 : 2, level = 20 (FIXED, never the L50 fight level), skillNormalId/skillSpecialId FROM encounter_runs (D-31), capturedZone = the boss zone. The user_heroes.tier column (11-01) stores the captured roll.
    3. `src/constants/sanguoBoss.ts` — REMOVE the module per the checkpoint (delete BOSS_TEMPLATES + bossTemplateFor; if the checkpoint chose full removal, delete the file + remove its import at battleCheckInService.ts:11). Do NOT leave a dormant copy.
    4. Surfaces: buildSanguoBossEncounterEmbed.ts (GOLD 0xF59E0B, named general, encounter.boss_line with the Lv50 + forced-legion copy); extend buildSanguoBattleLogEmbed.ts for the 3v1 log (≤20 rounds, ≤~2,000 chars, skills.battle_mp_gain/battle_special/battle_support/no_mp lines per UI-SPEC budgets); extend buildSanguoCaptureEmbed.ts + result builder for the boss variant (capture.item_drop line after a win + capture.captured_copy reveal showing stars/grade/Lv20 — the D-28 weights NEVER render). Add the i18n keys: capture.item_drop/captured_copy, encounter.level/boss_line, skills.battle_* (3 locales).
    5. Tests: the 3 behaviors (injectable deps per battleCheckInService.ts:51-63 BattleDeps + captureService CaptureDeps patterns).
  </action>
  <verify>
    <automated>npx vitest run src/services/sanguo/__tests__/battleCheckInService.test.ts src/services/sanguo/__tests__/captureService.test.ts; npm run typecheck; npm run check-i18n</automated>
  </verify>
  <acceptance_criteria>
    - Command: vitest green for the 3 routing/capture behaviors; typecheck + check-i18n green.
    - Source: `grep -c "bossTemplateFor\|BOSS_TEMPLATES\|BOSS_CAPTURE_UNAVAILABLE" src/` === 0 — the superseded code is fully removed, not dormant (Pitfall 7; import at battleCheckInService.ts:11 gone).
    - Source: battleCheckInService boss branch calls runLegionBattle + dropService (grep 'runLegionBattle' + 'rollBossDrop' >= 1 each); wild branch still calls runBattle.
    - Source: captureService boss success inserts level 20 (grep 'level: 20' or the constant) + the t0/t1/t2 roll via cryptoUniform (grep '0.9998\|0.95' — the D-28 weights); the tier/level/skills are written to user_heroes.
    - Source: `grep -c "0.95\|4.98\|0.02" src/ui/` === 0 — the capture-result weight distribution NEVER renders (D-28 hidden mechanics).
  </acceptance_criteria>
  <done>The boss is a real capturable hero: forced 3v1 legion battle at t2×IV100×L50, guaranteed item on win, capture with the D-28 random-roll prize (tier t0 95/4.98/0.02, L20) — WINDOWS.md #5 closed.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| encounter spawn → battle/capture chain | Spawn-time values (level, skills, boss identity) cross into the battle and capture txs — they must be written once and carried (D-31/D-34), not re-rolled. |
| legion slots → boss battle input | The pressed/persisted legion heroIds cross into the engine input — ownership + class-match must hold (validated in 11-07; the 11-06 builder re-reads from the DB). |
| boss win → drop + capture | The win branch grants an item and opens capture — both must be gated on the actual engine WIN (server-authoritative, not client-claimed). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-06-01 | Spoofing | Boss capture regression (WINDOWS.md #5 left open) | high | mitigate | Remove the BOSS_CAPTURE_UNAVAILABLE guard + bossTemplateFor path (checkpoint-confirmed, D-24); regression test: boss capture grants a user_heroes row with captured tier/level/skills (Pitfall 7). |
| T-11-06-02 | Tampering | Skill/level re-roll at battle time (spawn roll duplicated) | high | mitigate | Level + skills are written ONCE at spawn (encounter_runs) and carried to battle + capture — the CR-02 stale-button guard covers stale encounter state (A12). |
| T-11-06-03 | Tampering | Boss capture charged with a non-boss fee / new schedule | high | mitigate | Boss capture reuses CAPTURE_TIERS + rarity-5 base verbatim (D-26) — no new fee code path; the fee resolves server-side from sanguoCapture.ts. |
| T-11-06-04 | Information disclosure | D-28 weights (95/4.98/0.02), skill rarity weights, wild-level % leaking to UI | high | mitigate | Never-render enforced structurally (embed data interfaces carry only stars/grade/Lv20/name); grep gate asserts zero weight literals in src/ui/. |
| T-11-06-05 | Spoofing | Boss-win drop granted without a real engine win | high | mitigate | dropService called only in the WIN branch inside the FOR UPDATE tx that stores the battle result (server-authoritative; battleCheckInService.ts:224-313 pattern). |
| T-11-06-SC | Tampering | npm/pip/cargo installs | — | not-triggered | This plan installs no packages. |
</threat_model>

<verification>
- `npx vitest run src/services/sanguo/__tests__/battleCheckInService.test.ts src/services/sanguo/__tests__/captureService.test.ts src/services/sanguo/__tests__/travelCheckInService.test.ts src/services/sanguo/__tests__/skillService.test.ts src/services/sanguo/__tests__/encounterLevelService.test.ts` green.
- `npm run typecheck`, `npm run check-i18n` green.
- Grep gate: zero references to bossTemplateFor/BOSS_TEMPLATES/BOSS_CAPTURE_UNAVAILABLE anywhere in src/.
- Checkpoint:decision (Task 1) resolved before Tasks 2-3 execute — plan is `autonomous: false`.
</verification>

## Artifacts this phase produces

| Symbol | Kind | File |
|--------|------|------|
| `rollWildLevel(rng)` → level 1-50 (60/30/9.9/0.1 bands) | pure fn (D-33, crypto) | src/services/sanguo/encounterLevelService.ts |
| `rollSkill(classPool, rng)` / `rollSkillsForSpawn(classId)` | weighted pick (D-30, crypto) | src/services/sanguo/skillService.ts |
| travelCheckInService: boss real-hero pick + level/skills written to encounter_runs | spawn integration (D-24/D-27/D-31/D-35) | src/services/sanguo/travelCheckInService.ts |
| battleCheckInService: boss → runLegionBattle routing + dropService wiring + legion input snapshot | routing (D-23/D-25) | src/services/sanguo/battleCheckInService.ts |
| captureService: boss capture roll branch (random IV/tier/L20, skills carried) | capture (D-26/D-28/D-36) | src/services/sanguo/captureService.ts |
| sanguoBoss.ts — REMOVED (BOSS_TEMPLATES/bossTemplateFor deleted) | superseded code removal (D-24) | src/constants/sanguoBoss.ts |
| buildSanguoBossEncounterEmbed (GOLD, Lv50 forced-legion line) | embed | src/ui/embeds/ |
| buildSanguoBattleLogEmbed 3v1 + MP/special/support-effect lines | embed extension | src/ui/embeds/ |
| buildSanguoCaptureEmbed boss variant (item drop + captured-copy reveal) | embed extension | src/ui/embeds/ |
| i18n: capture.item_drop/captured_copy, encounter.level/boss_line, skills.battle_* (3 locales) | i18n | locales/{vi,en,zh-cn}/sanguo.json |
| spawn/routing/capture test suites | tests | src/services/sanguo/__tests__/ |

<success_criteria>
- WINDOWS.md #5 is closed: the boss is a real capturable hero fought as a forced 3v1 legion battle at t2×IV100×L50.
- Wild heroes spawn at random levels with class-pool skills that carry to capture (D-33/D-34/D-31).
- Boss capture grants a random-tier/IV/L20 copy (D-28/D-36) with no fee-schedule change (D-26).
- No superseded code remains; no hidden weight renders.
</success_criteria>

<output>
Create `.planning/phases/11-progression-chemistry-economy-depth/11-06-SUMMARY.md` when done.
</output>
