---
phase: 11-progression-chemistry-economy-depth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/economy-budget.md
  - src/db/schema/sanguoSkills.ts
  - src/db/schema/userHeroSoulgems.ts
  - src/db/schema/userLegions.ts
  - src/db/schema/userHeroes.ts
  - src/db/schema/sanguoItems.ts
  - src/db/schema/encounterRuns.ts
  - src/db/schema/formations.ts
  - src/db/schema/index.ts
  - migrations/0020_*.sql
autonomous: false
requirements:
  - TQC-14
  - TQC-15
  - TQC-16
  - TQC-17

estimate:
  tokens: 44000
  raw_tokens: 22000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "docs/economy-budget.md carries a new AMENDMENT (2026-08-14, Phase 11) restating the Linh thạch sink set: the ONLY new Linh thạch sinks are the shop (heal_pill 50💎, booster_x2 100💎 — prices as confirmed at the checkpoint) and formation purchases (200-500💎), all via wallet.deductBalance; evolution/leveling/re-roll are HỒN NGỌC sinks and the superseded 'Linh thạch → evolution' convertibility row is replaced by 'Linh thạch → hồn ngọc: only via the booster (bounded, one-way)'."
    - "The amendment recomputes E[net/hour] with the Phase 11 shop prices: E[inflow]=0 (booster is a Linh thạch SINK, not a source), E[outflow] = shop/formation spend at realistic cadence → E[net] <= 0 (D-19 hard constraint) and gross flow < ~416/hr magnitude bound — the Phase 11 sink table + the E[net/hour] computation are both present."
    - "Migration 0020 (via drizzle-kit generate + npm run migrate, never hand-written SQL) creates 5 new tables: sanguo_skills, user_hero_soulgems, user_legions, user_legion_slots, soulgem_transactions — and extends user_heroes (tier smallint 0-3 + skill_normal_id + skill_special_id FKs), sanguo_items (price_linh/price_event/sale_state/drop_weight), encounter_runs (level + skill ids), formations (emoji) — every column the Phase 11 services read exists in the live DB."
    - "The live database schema matches the TypeScript schema (no false-positive green): the [BLOCKING] migrate step ran after all schema edits; a probe against information_schema confirms the new tables + columns exist."
  artifacts:
    - docs/economy-budget.md — Phase 11 AMENDMENT block (sink set restatement + E[net/hour] recompute + sign-off)
    - src/db/schema/sanguoSkills.ts — NEW skill catalog table (mechanics; names = i18n keys)
    - src/db/schema/userHeroSoulgems.ts — NEW per-hero hồn ngọc pool (userId+heroId unique, amount >= 0 check)
    - src/db/schema/userLegions.ts — NEW active-legion state + user_legion_slots (12 rows, unique(userId, slotOrder))
    - src/db/schema/soulgemTransactions.ts — NEW hồn ngọc audit ledger (mirrors wallet_transactions)
    - migrations/0020_*.sql — generated migration (5 new tables + 4 schema extends)
  key_links:
    - "sanguo_items.price_linh ↔ wallet.deductBalance — shop purchases charge the signed Phase 11 prices (11-04 shopService) inside one tx."
    - "user_heroes.tier ↔ captureService boss branch (11-06) — the captured boss roll stores its tier here (D-28/D-10); battleEngine evolution multipliers read it."
    - "sanguo_skills ↔ skillService spawn roll (11-06) + user_heroes skill columns (D-30/D-31) — the catalog the encounter spawn rolls from and the copy stores."
    - "economy-budget.md amendment ↔ seed prices (11-02) — the checkpoint-confirmed numbers are the single source the seed writes (Pitfall 8)."
  prohibitions:
    - "No wallet.deductBalance for evolution/leveling/re-roll — those are HỒN NGỌC sinks (D-01/D-06); the amendment restates this (anti-pattern 'Evolution via wallet')."
    - "No hồn ngọc → Linh thạch conversion path anywhere (D-02 account-bound, never converts back)."
    - "No new faucet: the starter (10-07) is the ONLY faucet; boss drops are items only, never money (D-19)."
    - "No hand-written migration SQL — drizzle-kit generate only (established migrations 0000-0019 pattern)."
    - "No capture_key monetization via Linh thạch (D-15 one-way) — sale_state='locked' is the schema default; selling later needs a re-sign."
  flagged_assumptions:
    - "FLAGGED ASSUMPTION (economy amendment, A5): the checkpoint presents the research-proposed shop prices (heal 50💎 / booster 100💎 / formations 200-300-500💎) and boss drop weights (heal 70% / booster 25% / key4 4.9% / key5 0.1%) as adopt-a5; the amendment recomputes E[net/hour] with the CONFIRMED numbers — if the user adjusts, the seed (11-02) writes the adjusted values."
    - "FLAGGED ASSUMPTION (probe TQC-16 empty, unresolved): the Event shop tab renders the locked capture_key row + shop.event_empty note — never an empty blank surface by construction (UI-SPEC covered row)."
    - "FLAGGED ASSUMPTION (booster bridge, A11): the booster (Linh thạch-priced, 2× one conversion) is a documented, bounded Linh thạch→hồn ngọc bridge — net Linh thạch flow stays <= 0; flagged for Phase 12 TQC-19 monitoring."

---

<objective>
What this plan accomplishes: **the two BLOCKING gates of Phase 11 — the economy-budget amendment (D-18 one-way, mirroring Phase 10's 10-03) and the schema migration 0020 that every Phase 11 service reads.** Task 1 is a checkpoint:decision amending + re-signing `docs/economy-budget.md`: the convertibility matrix's "Linh thạch → evolution" row is superseded by D-01/D-06 (evolution/level/reroll = hồn ngọc sinks), the ONLY new Linh thạch sinks are the shop (heal 50💎 / booster 100💎) + formation purchases (200-500💎), boss drops stay items-only, and E[net/hour] is recomputed <= 0 with gross < ~416/hr. Task 2 authors migration 0020 — 5 new tables (sanguo_skills, user_hero_soulgems, user_legions, user_legion_slots, soulgem_transactions) + 4 schema extends (user_heroes tier/skills, sanguo_items price-currency model, encounter_runs level/skills, formations emoji) — with the [BLOCKING] migrate step (schema-gate).

Purpose: the milestone's economic guardrail + the data foundation. The economy amendment decides what the seed (11-02) writes and what the shop service (11-04) charges; the migration decides where every Phase 11 service stores state. Both are one-way (D-18 re-sign gate; D-10/D-16 schema contract) — the checkpoint is the user's last cheap confirmation.

Output: the re-signed economy budget + migration 0020 applied to the live DB.
</objective>

<execution_context>
@C:/Users/901107/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/901107/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-progression-chemistry-economy-depth/11-CONTEXT.md
@.planning/phases/11-progression-chemistry-economy-depth/11-RESEARCH.md
@.planning/phases/11-progression-chemistry-economy-depth/11-PATTERNS.md
@docs/economy-budget.md
@src/db/schema/userHeroes.ts
@src/db/schema/sanguoItems.ts
@src/db/schema/encounterRuns.ts
@src/db/schema/formations.ts
@src/db/schema/heroes.ts
@src/db/schema/index.ts
@src/db/schema/userSanguoItems.ts
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <decision>Amend + re-sign docs/economy-budget.md for Phase 11 (D-18 one-way gate). The amendment (a) supersedes the convertibility row "Linh thạch → evolution" — evolution/leveling/re-roll are HỒN NGỌC sinks (D-01/D-06), never Linh thạch; (b) restates the ONLY new Linh thạch sinks: shop heal_pill **50💎**, booster_x2 **100💎**, formations **200/300/500💎** — all via wallet.deductBalance; (c) records boss drop weights heal 70% / booster 25% / key4 4.9% / key5 0.1% (items only, never money); (d) recomputes E[net/hour] = E[inflow] − E[outflow] <= 0 and gross < ~416/hr with the confirmed prices.</decision>
  <context>D-18 is a one-way economy gate: once the amendment is signed and the seed (11-02) writes the prices, rebalancing requires a NEW sign-off. RESEARCH Open Question 1 found docs/economy-budget.md's convertibility matrix still says "Linh thạch → evolution" (Phase 8-era wording) — this conflicts with CONTEXT D-01/D-06 which lock evolution/leveling/reroll as hồn ngọc sinks. The amendment makes D-19 compliance STRONGER (hồn ngọc is minted only from dupes, never from Linh thạch) and is required before any Phase 11 content ships. The booster (100💎 → 2× on one conversion) is a documented bounded bridge — E[inflow] stays 0 (booster is a sink), so E[net] <= 0 holds trivially; the binding constraint is the ~416/hr GROSS bound at realistic cadence (5-10 encounters/hr).</context>
  <options>
    <option id="adopt-a5">
      <name>Adopt research prices/drop-weights as-is</name>
      <pros>Research-priced, internally consistent (heal 50 / booster 100 / formations 200-300-500; drops 70/25/4.9/0.1); the amendment computation verifies E[net]<=0 + gross<416 before signing.</pros>
      <cons>Values are agent-discretion proposals — first-pass fairness unproven until live data (Phase 12 TQC-19 monitoring).</cons>
    </option>
    <option id="adjust-prices">
      <name>Adjust specific prices or drop weights</name>
      <pros>User tunes shop prices / formation prices / drop weights before the one-way sign-off.</pros>
      <cons>The 11-02 seed and 11-04 shopService use the adjusted values; the amendment recomputes with them.</cons>
    </option>
  </options>
  <resume-signal>Select: adopt-a5 or adjust-prices (if adjust-prices, state the new prices/drop-weights)</resume-signal>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Migration 0020 — 5 new tables + 4 schema extends + drizzle-kit generate + migrate (schema-gate)</name>
  <files>src/db/schema/sanguoSkills.ts, src/db/schema/userHeroSoulgems.ts, src/db/schema/userLegions.ts, src/db/schema/soulgemTransactions.ts, src/db/schema/userHeroes.ts, src/db/schema/sanguoItems.ts, src/db/schema/encounterRuns.ts, src/db/schema/formations.ts, src/db/schema/index.ts, migrations/0020_*.sql</files>
  <read_first>
    - src/db/schema/userHeroes.ts (the file being extended — IV checks pattern lines 44-49, NO-unique comment lines 9-10)
    - src/db/schema/sanguoItems.ts (the file being extended — base_price bigint line 20, sql`0` bigint-default comment lines 19-20)
    - src/db/schema/encounterRuns.ts (hero_id nullable comment lines 28-30, F2 index lines 42-45)
    - src/db/schema/formations.ts (base_price bigint line 17, formation_slots class line 30, user_formations lines 39-48)
    - src/db/schema/index.ts (the barrel — export pattern, ~50 lines)
    - .planning/phases/11-progression-chemistry-economy-depth/11-PATTERNS.md §Schema EXTENDs (lines 334-344, exact column shapes)
    - .planning/phases/11-progression-chemistry-economy-depth/11-RESEARCH.md §Pattern 5 (soulgem_transactions audit table) + §Assumptions A7-A9 (schema shapes)
  </read_first>
  <action>
    Create the 4 new schema files + extend 4 existing + update the barrel in `src/db/schema/`:
    1. NEW `src/db/schema/sanguoSkills.ts` — `sanguoSkills` pgTable: id serial pk, code varchar(50) unique (natural key, matches the seed upsert), class varchar(20) (8-class enum values), slot varchar(10) 'normal'|'special', rarity varchar(10) 'common'|'rare'|'epic', mpCost smallint notNull default 0, mpGain smallint notNull default 0, effectType varchar(20) 'damage'|'attack_up'|'hp_regen'|'mp_regen', effectValue smallint notNull default 0, emoji varchar(100) (content-driven, never theme constants — RESEARCH don't-hand-roll). Export SanguoSkill/NewSanguoSkill types.
    2. NEW `src/db/schema/userHeroSoulgems.ts` — `userHeroSoulgems` pgTable (analog: userSanguoItems.ts): userId integer FK users NOT NULL, heroId integer FK heroes NOT NULL (the heroes.id species — shared across copies per D-02 "spendable on Tào Tháo copies"), amount integer NOT NULL DEFAULT 0, updatedAt timestamp withTimezone defaultNow; uniqueIndex(userId, heroId); check(amount >= 0). Export types.
    3. NEW `src/db/schema/userLegions.ts` — TWO tables: `userLegions` (id serial pk, userId integer FK users NOT NULL, formationId integer FK formations NOT NULL, updatedAt timestamp defaultNow, unique userId — one active legion per user, A9) + `userLegionSlots` (id serial pk, userId FK NOT NULL, slotOrder smallint NOT NULL, userHeroId integer FK userHeroes NULL — empty slots allowed (D-20 bonus-only), check(slotOrder >= 0 AND slotOrder <= 11), uniqueIndex(userId, slotOrder)). Export all types.
    4. NEW `src/db/schema/soulgemTransactions.ts` — `soulgemTransactions` pgTable (audit ledger mirroring walletTransactions): id serial pk, userId FK NOT NULL, heroId integer FK heroes NOT NULL, type varchar(20) 'convert'|'level'|'evolve'|'reroll' NOT NULL, amount integer NOT NULL (signed: +convert yield / −spend), balanceAfter integer NOT NULL, createdAt timestamp defaultNow; index(userId). Export types.
    5. EXTEND `src/db/schema/userHeroes.ts` — add `tier: smallint('tier').notNull().default(0)` + check('user_heroes_tier_range', tier >= 0 AND tier <= 3) (D-10 single source of truth for player evolution AND captured boss tier); add `skillNormalId: integer('skill_normal_id').references(() => sanguoSkills.id)` + `skillSpecialId: integer('skill_special_id').references(() => sanguoSkills.id)` (D-31 per-copy columns — exactly 2 slots, TM-swap semantics, zero joins). Import sanguoSkills. Keep the NO-unique (userId, heroId) comment (dupes are the D-03 conversion fuel).
    6. EXTEND `src/db/schema/sanguoItems.ts` — REPLACE `basePrice` with the multi-currency model (D-16): `priceLinh: bigint('price_linh', { mode: 'bigint' }).notNull().default(sql`0`)`, `priceEvent: bigint('price_event', { mode: 'bigint' }).notNull().default(sql`0`)`, `saleState: varchar('sale_state', { length: 10 }).notNull().default('locked')` ('sold'|'locked' — capture_key stays locked, D-15), `dropWeight: numeric('drop_weight', { precision: 5, scale: 2 }).notNull().default('0')` (percent 0-100, 4.9 needs scale 2). Remove the basePrice column entirely (the 11-02 seed rewrites the catalog). Keep the sql`0` bigint-default comment pattern.
    7. EXTEND `src/db/schema/encounterRuns.ts` — flip the hero_id comment (bosses now carry a REAL hero per D-24); add `level: smallint('level').notNull().default(1)` (D-33), `skillNormalId: integer('skill_normal_id').references(() => sanguoSkills.id)` + `skillSpecialId: integer('skill_special_id').references(() => sanguoSkills.id)` (D-31 spawn roll). Keep the F2 index.
    8. EXTEND `src/db/schema/formations.ts` — add `emoji: varchar('emoji', { length: 100 })` (UI-SPEC content-driven emoji; nullable). base_price stays (200-500💎 seed values in 11-02).
    9. EXTEND `src/db/schema/index.ts` — re-export the 4 new tables (sanguoSkills, userHeroSoulgems, userLegions, userLegionSlots, soulgemTransactions) following the existing barrel pattern.
    10. Run `npx drizzle-kit generate` (creates migrations/0020_*.sql) then `npm run migrate` — the [BLOCKING] schema-push step (project pattern: generate + migrate, never hand-written SQL; satisfy the schema-gate push requirement).
  </action>
  <verify>
    <automated>npm run typecheck; node -e "const {Client}=require('pg');const c=new Client({connectionString:process.env.DATABASE_URL||'postgres://localhost:5432/tutien'});" 2>$null; npx drizzle-kit check</automated>
  </verify>
  <acceptance_criteria>
    - Command: `npm run typecheck` exits 0 — all 4 new schema files + 4 extends compile against drizzle 0.45.2.
    - Command: `npm run migrate` exits 0 — migration 0020 applied to the live DB (localhost:5432 LISTENING per RESEARCH environment table).
    - Command: node probe against information_schema confirms tables `sanguo_skills`, `user_hero_soulgems`, `user_legions`, `user_legion_slots`, `soulgem_transactions` exist AND columns `user_heroes.tier`, `user_heroes.skill_normal_id`, `sanguo_items.price_linh`, `sanguo_items.sale_state`, `sanguo_items.drop_weight`, `encounter_runs.level`, `formations.emoji` exist (probe ALL tables, not just heroes — Phase 10 migration-verification lesson).
    - Source: `src/db/schema/userHeroes.ts` exports `tier` with the 0-3 range check; `sanguoItems.ts` has NO `basePrice` column and HAS `priceLinh`/`priceEvent`/`saleState`/`dropWeight`.
    - Source: `migrations/0020_*.sql` exists (generated, never hand-written).
    - Source: `src/db/schema/index.ts` re-exports all 5 new table symbols.
  </acceptance_criteria>
  <done>Migration 0020 is applied: 5 new tables + 4 schema extends live in PostgreSQL, typecheck-green, and every column the Phase 11 services read exists in the live DB (no false-positive verification).</done>
  <reversibility rating="one-way">D-10 (user_heroes.tier single source of truth) + D-16 (multi-currency price model) are schema contracts every Phase 11 service reads — re-shaping them later is a migration + query-rewrite cost; the checkpoint in Task 1 confirmed the surrounding economy first.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| economy-budget doc ↔ seed/shop config | The signed amendment must stay identical to the prices the 11-02 seed writes and the 11-04 shopService charges — drift between doc and code is the rebalance-by-accident threat (Pitfall 8). |
| schema (TS) ↔ live DB | TypeScript types come from config; the live DB is the runtime truth — migrate must run before verification or build/types pass falsely (schema-gate). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01-01 | Tampering | Economy rebalance-by-drift (doc ≠ prices) | medium | mitigate | The amendment documents the exact values the seed writes (Pitfall 8: prices are seed data, single source); acceptance criteria cross-check the doc against 11-02's values. |
| T-11-01-02 | Tampering | hồn ngọc spent via wallet (evolution via Linh thạch) | high | mitigate | The amendment restates D-01/D-06 (evolution/level/reroll = hồn ngọc, never wallet.deductBalance) as a hard prohibition; the D-19 compliance check in 11-08 verifies no wallet call reaches the progression services. |
| T-11-01-03 | Spoofing | New faucet insertion / hồn ngọc → Linh thạch path | high | mitigate | D-02 account-bound rule restated in the amendment; boss drops items-only (never money); the starter remains the only faucet. |
| T-11-01-04 | Tampering | Schema/DB drift after migration | medium | mitigate | [BLOCKING] migrate step inside the plan; acceptance probe covers ALL new tables + columns (Phase 10 lesson — a heroes-only probe cannot see cross-table columns). |
| T-11-01-SC | Tampering | npm/pip/cargo installs | — | not-triggered | This plan installs no packages (RESEARCH: no new dependencies). |
</threat_model>

<verification>
- `npm run typecheck` green after the schema edits.
- `npm run migrate` applies 0020 — the information_schema probe (all tables) confirms the 5 new tables + 4 column sets exist.
- Checkpoint:decision (Task 1) resolved by the user before Task 2 executes — plan is `autonomous: false`.
- The D-18 amendment is signed before 11-02 (seed) and 11-04 (shop) run.
</verification>

## Artifacts this phase produces

| Symbol | Kind | File |
|--------|------|------|
| `sanguoSkills` table + `SanguoSkill`/`NewSanguoSkill` types | schema (NEW) | src/db/schema/sanguoSkills.ts |
| `userHeroSoulgems` table + types (userId+heroId unique, amount>=0) | schema (NEW) | src/db/schema/userHeroSoulgems.ts |
| `userLegions` + `userLegionSlots` tables + types (12 slots, unique(userId,slotOrder)) | schema (NEW) | src/db/schema/userLegions.ts |
| `soulgemTransactions` table + types (audit ledger) | schema (NEW) | src/db/schema/soulgemTransactions.ts |
| `userHeroes.tier` (0-3 check) + `skillNormalId`/`skillSpecialId` FKs | schema (EXTEND) | src/db/schema/userHeroes.ts |
| `sanguoItems.priceLinh`/`priceEvent`/`saleState`/`dropWeight` (basePrice removed) | schema (EXTEND, D-16) | src/db/schema/sanguoItems.ts |
| `encounterRuns.level` + skill FKs; hero_id comment flip (D-24) | schema (EXTEND) | src/db/schema/encounterRuns.ts |
| `formations.emoji` column | schema (EXTEND) | src/db/schema/formations.ts |
| Barrel re-exports (5 new tables) | schema index | src/db/schema/index.ts |
| migrations/0020_*.sql | migration (generated) | migrations/0020_*.sql |
| Phase 11 AMENDMENT block + E[net/hour] recompute + sign-off line | economy gate doc | docs/economy-budget.md |

<success_criteria>
- The economy amendment is signed (D-18): the only new Linh thạch sinks are shop + formations (via wallet.deductBalance); evolution/level/reroll are hồn ngọc; E[net] <= 0 and gross < ~416/hr recomputed with confirmed prices.
- Migration 0020 is applied to the live DB — every table/column the Phase 11 services read exists; no false-positive verification (schema-gate closed).
- D-19 holds by construction: no wallet path to hồn ngọc, no hồn ngọc → Linh thạch, boss drops items only.
</success_criteria>

<output>
Create `.planning/phases/11-progression-chemistry-economy-depth/11-01-SUMMARY.md` when done.
</output>
