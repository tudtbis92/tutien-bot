---
phase: 11-progression-chemistry-economy-depth
plan: 08
type: execute
wave: 7
depends_on:
  - 11-06
  - 11-07
files_modified:
  - src/constants/sanguoProgression.ts
  - src/constants/sanguoChemistry.ts
  - docs/economy-budget.md
  - locales/vi/sanguo.json
  - locales/en/sanguo.json
  - locales/zh-cn/sanguo.json
  - src/services/sanguo/__tests__/balancePass.test.ts
autonomous: true
requirements:
  - TQC-14
  - TQC-15
  - TQC-16
  - TQC-17

estimate:
  tokens: 48000
  raw_tokens: 24000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The balance pass (Pitfall 4) simulates the seeded stat ranges (scripts/data/sanguo-base-stats.json: 6 stats in the 10-90 band, HP 50-300) at representative legion levels (L50/L60/L70 × t0/t1/t2 mains × chemistry tiers) against the boss wall (t2 base × IV all-31 × L50, D-35) using runLegionBattle (11-05) — the boss is BEATABLE-BUT-HARD (win rate in a tuned band, e.g. ~40-70% for a maxed L60+ legion), never trivially won and never unwinnable; the fight formula (D-05) is NOT modified — only the constants (LEVEL_COST slope, STAT_GAIN_PER_LEVEL, TIER_MULTIPLIERS, CHEMISTRY_TIERS buffs) are tuned if the simulation demands. The simulation's mains MUST bake TIER_MULTIPLIERS[tier] into base stats exactly as the 11-06 legion builder does (PLAN-FIX P0-2) — the simulation and production must share the same power curve."
    - "PLAN-FIX P1-4 (progression pacing): the balance pass ALSO runs a hồn ngọc pacing sanity check — compute time-to-L20 (t0→t1 gate) and time-to-L50 (t1→t2 gate) from realistic hồn ngọc income (e.g., ~20-40 encounters/hr × capture rate → dupes/hr × flat-by-tier value t0=1/t1=5, minus booster uptake), against the cumulative LEVEL_COST curve (L1→20 ≈ 24, L1→50 ≈ 264) + EVOLUTION_COSTS (20/50). The intent: the L50 boss wall must be reachable within a reasonable play session (e.g., days-weeks of active play), not months — if the pacing implies an unbounded grind, tune the level-curve slope or tier conversion values as a documented constant change (no economy re-sign needed — hồn ngọc is not Linh thạch, D-02). This closes the coverage gap where combat was calibrated but the cost-to-reach-the-wall was never modeled."
    - "docs/economy-budget.md gains a Phase 11 COMPLIANCE VERIFICATION block (post-content): the actual seeded shop prices (heal 50💎 / booster 100💎 / formations 200-500💎) + drop weights (70/25/4.9/0.1) are checked against the 11-01 amendment's E[net/hour] computation — E[net] <= 0 (D-19 hard constraint) and gross < ~416/hr at realistic cadence 5-10/hr; any drift between the doc and the live seed values is flagged, not silently accepted (Pitfall 8)."
    - "The FULL i18n surface is parity-complete: every new Phase 11 key (shop.*, bag.*, legion.*, convert.*, level.*, evolve.*, reroll.*, skills.*, cmd.* + additive hero/heroes/capture/encounter keys) exists in locales/vi|en|zh-cn/sanguo.json — `npm run check-i18n` exits 0 and `npm run lint` (eslint i18next/no-literal-string) exits 0 across all new commands/embeds/components."
    - "The full test suite is green: `npm test` (vitest run) covers soulgem/chemistry/shop/bag/drop/legion/level/engine behaviors + the Phase 10 suites unchanged; `npm run typecheck` green; `npm run lint` green — the phase's integration is verified as a whole, not per-plan."
  artifacts:
    - src/services/sanguo/__tests__/balancePass.test.ts — the legion-vs-boss simulation test (deterministic via seeded runLegionBattle)
    - docs/economy-budget.md — Phase 11 COMPLIANCE VERIFICATION block
    - src/constants/sanguoProgression.ts / sanguoChemistry.ts — tuned values (IF the balance pass requires; otherwise unchanged)
  key_links:
    - "balance pass simulation ↔ runLegionBattle (11-05) + TIER_MULTIPLIERS/STAT_GAIN_PER_LEVEL/CHEMISTRY_TIERS — the tuned constants change the boss-wall outcome."
    - "compliance verification ↔ 11-01 amendment + 11-02 seed — doc prices must equal live seed prices (single source, Pitfall 8)."
    - "full regression ↔ all 7 prior plans — the phase seals only when every suite green together."
  prohibitions:
    - "No modification of the D-05 fight formula (damage = max(atk−def,1), crit ×2, round cap 20) — the balance pass tunes CONSTANTS only (RESEARCH OQ2: 'tune tier multipliers or STAT_GAIN_PER_LEVEL — NOT the fight formula (D-05 locked)')."
    - "No economy re-sign bypass: if the compliance verification finds E[net] > 0 or gross >= ~416/hr, the fix is a NEW amendment sign-off (D-18), not a silent constant tweak."
    - "No new i18n keys added at this stage without parity in all 3 locales (check-i18n is the gate)."
    - "No UI leak of tuned hidden numbers — the constants stay D-12 hidden (they are balance knobs, not render values)."
  flagged_assumptions:
    - "FLAGGED ASSUMPTION (Pitfall 4, boss-wall calibration): if the L50+ legion wall proves unwinnable at the proposed constants, the pass tunes STAT_GAIN_PER_LEVEL or TIER_MULTIPLIERS (or chemistry buffs) — with the drift re-verified against the economy amendment (hồn ngọc costs unchanged; only combat power shifts)."
    - "FLAGGED ASSUMPTION (PLAN-FIX P1-4 pacing): the pacing sanity check (time-to-L20/L50 from realistic hồn ngọc income vs the cumulative level curve) is a design-sanity bound — if the reachable-time is off, tune the LEVEL_COST slope or tier conversion values as a documented constant change; hồn ngọc is not Linh thạch (D-02), so no economy re-sign is needed."
    - "FLAGGED ASSUMPTION (A5 gross bound): the compliance verification recomputes gross spend with the LIVE seeded prices — the ~416/hr magnitude reference is a design-sanity bound, not a hard cap; documented band conclusions, not a pass/fail cliff."
  ui_backstops:
    - statement: "Loading state for shop/bag/copy-selector/legion-assembly/hero-actions/boss-capture surfaces — all component presses follow deferReply → editReply within the 3s window (CR-09-06, no duplicate defer); verify handler latency with held-out interaction tests per surface."
      verification: backstop
    - statement: "Loading state for battle-log/heroes-collection (carried from Phase 10) — deferReply latency held-out tests carry."
      verification: backstop
    - statement: "UI-SPEC 31 covered considerations lifted across 11-03..11-08 plans (empty/populated/overflow/zero-one-many/long-text/partial per surface) — no silent drops; every row has a resolution."
      verification: backstop

---

<objective>
What this plan accomplishes: **the phase's closing pass — balance calibration (Pitfall 4), economy compliance verification (D-19 against the 11-01 amendment), i18n parity completion, and the full regression that seals the phase.** Task 1 builds the balance-pass simulation (legion-vs-boss wall over the seeded stat ranges at L50/60/70 × t0/t1/t2 × chemistry tiers via the 11-05 engine) and tunes the progression/chemistry constants if the wall is mis-calibrated; Task 2 verifies the economy amendment's numbers against the live seed (E[net] <= 0, gross < ~416/hr), completes any i18n parity gaps, and runs the full suite (test/typecheck/lint) as the phase gate.

Purpose: RESEARCH Pitfall 4 + Open Question 2 require the balance pass before content ships; the economy amendment (11-01) must be verified against the actual seeded values (Pitfall 8). This plan closes both loops and proves the phase as a whole.

Output: tuned constants (if needed), the compliance block in the economy budget, i18n parity, and a green full-suite regression.
</objective>

<execution_context>
@C:/Users/901107/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/901107/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-progression-chemistry-economy-depth/11-CONTEXT.md
@.planning/phases/11-progression-chemistry-economy-depth/11-RESEARCH.md
@.planning/phases/11-progression-chemistry-economy-depth/11-PATTERNS.md
@.planning/phases/11-progression-chemistry-economy-depth/11-UI-SPEC.md
@.planning/phases/11-progression-chemistry-economy-depth/11-01-PLAN.md
@.planning/phases/11-progression-chemistry-economy-depth/11-02-PLAN.md
@docs/economy-budget.md
@scripts/data/sanguo-base-stats.json
@src/constants/sanguoProgression.ts
@src/constants/sanguoChemistry.ts
@src/services/sanguo/battleEngine.ts
@src/services/sanguo/chemistryService.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Balance pass — legion-vs-boss simulation over the seeded stat ranges + constant tuning (Pitfall 4)</name>
  <files>src/services/sanguo/__tests__/balancePass.test.ts, src/constants/sanguoProgression.ts, src/constants/sanguoChemistry.ts</files>
  <read_first>
    - scripts/data/sanguo-base-stats.json (the seeded stat ranges — 6 stats 10-90 band, HP 50-300 — the simulation input)
    - src/constants/sanguoProgression.ts + sanguoChemistry.ts (the constants the pass may tune)
    - src/services/sanguo/battleEngine.ts (runLegionBattle — the simulation engine) + chemistryService.ts (applyChemistryBuff)
    - .planning/phases/11-progression-chemistry-economy-depth/11-RESEARCH.md §Pitfall 4 (the balance-pass method: 'simulates the seeded stat ranges at representative legion levels (L50/L60/L70 × t0/t1/t2 mains × chemistry tiers) and tunes the constants before content ships') + §Open Question 2 (tune constants, NOT the D-05 formula)
  </read_first>
  <behavior>
    - Test 1 (boss-wall outcome): a simulated L60 t2 legion with S-tier chemistry (all 3 mains, 9 supports class-matched) vs the boss (t2 × IV all-31 × L50) wins a deterministic seeded battle (fixed seeds) — asserting the wall is BEATABLE at the intended progression depth.
    - Test 2 (tuning invariants): if the wall is unwinnable at the proposed constants, adjusting STAT_GAIN_PER_LEVEL or TIER_MULTIPLIERS by one step flips a sampled L60/L70 legion to a win — proving the constants (not the formula) are the tuning surface.
    - Test 3 (economy invariance): hồn ngọc costs (LEVEL_COST/EVOLUTION_COSTS/REROLL_COST) are NOT touched by the balance pass — combat-power tuning only (the amendment's E[net/hour] math is hồn ngọc-agnostic).
    - Test 4 (PLAN-FIX P1-4 pacing): the hồn ngọc pacing sanity check — cumulative cost to the L20→t1 gate (LEVEL_COST sum + EVOLUTION_COSTS[1]) and the L50→t2 gate are reachable within the design time band (single-digit-hours for the first gate; multi-session/days for the second), given realistic dupe income — NOT an unbounded multi-month grind.
  </behavior>
  <action>
    Create `src/services/sanguo/__tests__/balancePass.test.ts` (the Pitfall 4 simulation):
    - Build a deterministic simulation helper: sample representative heroes from the seeded base-stats ranges (STR/AGI/INT/MOV/LEA/CHA 10-90, HP 50-300), construct mains at levels {50, 60, 70} × tiers {t0, t1, t2} × chemistry tiers {none, D, B, S} (applyChemistryBuff from 11-05), the boss = t2 base × IV all-31 × L50 (D-35) — and run runLegionBattle with FIXED seeds (deterministic assertions).
    - Assert the outcome distribution: at L60+ t2 with S chemistry the legion wins (wall beatable-but-hard); a L50 t0 lone legion loses (the wall holds — D-35 'proving L50+ legion strength'); the win band is neither trivial (< 5% loss) nor unwinnable (> 95% loss at maxed L70 t2 S). **PLAN-FIX P0-2: the simulation builds each main's CombatantInput with base × TIER_MULTIPLIERS[tier] baked in — IDENTICAL to the 11-06 legion builder — so the tuned constants calibrate the same power curve production fights on; a mismatch here would make the pass green while production is broken.**
    - **PLAN-FIX P1-4: add a pacing sanity assertion — cumulative LEVEL_COST(L1→20) + EVOLUTION_COSTS[1] ≈ 24 + 20 = 44 hồn ngọc for the first evolution gate; model realistic hồn ngọc income (e.g., dupes/hr = encounters/hr × capture-rate × dupe-share; t0=1/t1=5) and assert the L20→t1 gate is reachable in a single-digit-hours play session and the L50→t2 gate in a multi-session (days) span — NOT a multi-month unbounded grind. If the pacing is off, tune LEVEL_COST slope or tier conversion values as a documented constant change (no economy re-sign — hồn ngọc is not Linh thạch, D-02).**
    - IF the simulation fails (unwinnable wall), tune the CONSTANTS in sanguoProgression.ts/sanguoChemistry.ts (STAT_GAIN_PER_LEVEL 2 → 3, or TIER_MULTIPLIERS up, or CHEMISTRY_TIERS buffs up) — NEVER the D-05 formula (RESEARCH OQ2). Re-run the suite; document the tuning in the test's docblock + the 11-08 summary.
    - Tests: the 3 behaviors (fixed seeds; the pass runs as part of the suite).
  </action>
  <verify>
    <automated>npx vitest run src/services/sanguo/__tests__/balancePass.test.ts src/services/sanguo/__tests__/battleEngine.test.ts; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Command: the balance-pass + battle-engine suites exit 0 (deterministic — fixed seeds).
    - Source: balancePass.test.ts asserts the L60+ t2 S-tier legion BEATS the boss and the L50 t0 legion LOSES (the wall holds) — the two anchors of the calibration.
    - Source: if constants were tuned, the diff is confined to sanguoProgression.ts/sanguoChemistry.ts numeric values — `git diff --stat` shows NO change to battleEngine.ts (D-05 formula locked).
    - Source: hồn ngọc costs (LEVEL_COST/EVOLUTION_COSTS/REROLL_COST) unchanged by the pass (grep the final values match 11-02's tests).
    - Source: balancePass.test.ts includes the pacing assertion (PLAN-FIX P1-4 — grep 'pacing' or the LEVEL_COST-sum + EVOLUTION_COSTS math) so the boss-wall calibration is validated against the cost-to-reach-it, not just the combat outcome.
  </acceptance_criteria>
  <done>The boss wall is calibrated: beatable-but-hard at the intended L50+ legion depth, reachable within a sane play-time (pacing check), with any constant tuning documented and the D-05 formula untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Economy compliance verification (D-19 vs the amendment) + i18n parity + full phase regression</name>
  <files>docs/economy-budget.md, locales/vi/sanguo.json, locales/en/sanguo.json, locales/zh-cn/sanguo.json</files>
  <read_first>
    - docs/economy-budget.md (the file being extended — the 11-01 amendment block + the D-19 constraint section + the ~416/hr magnitude bound method at lines 100-104)
    - .planning/phases/11-progression-chemistry-economy-depth/11-01-PLAN.md (the amendment it must verify against)
    - scripts/data/sanguo-items.json + sanguo-formations.json (the LIVE seeded prices/weights the verification reads)
    - .planning/phases/11-progression-chemistry-economy-depth/11-RESEARCH.md §Pitfall 8 (price drift vs the amendment)
  </read_first>
  <action>
    1. Verify the economy compliance against the 11-01 amendment + 11-02 seed: read the LIVE seeded values (sanguo_items.price_linh/sale_state/drop_weight + formations.base_price) and recompute E[net/hour] of the optimal loop at realistic cadence 5-10 encounters/hr — E[inflow] = 0 (starter only faucet; boss drops items never money — D-19), E[outflow] = shop + formation spend → assert E[net] <= 0 and gross < ~416/hr. Drift between the doc and the live seed → flag + reconcile (the seed is the single source; fix the doc or re-sign — D-18).
    2. Append a **COMPLIANCE VERIFICATION (2026-08-14, Phase 11)** block to docs/economy-budget.md after the 11-01 amendment: the verified sink set (shop 50/100💎, formations 200-500💎, all via wallet.deductBalance), the confirmed boss drop weights (items only), the recomputed E[net/hour] conclusion (<= 0, D-19) + gross band, and a sign-off line that any future rebalancing requires a new sign-off (D-18).
    3. i18n parity completion: run `npm run check-i18n` — fix any missing Phase 11 keys across the 3 locales (additive keys from UI-SPEC Copywriting table: shop.*/bag.*/legion.*/convert.*/level.*/evolve.*/reroll.*/skills.*/cmd.* + hero/heroes/capture/encounter additives). No new keys without 3-locale parity.
    4. Full regression: `npm test` (entire vitest suite — all Phase 10 + Phase 11 suites together), `npm run typecheck`, `npm run lint` — the phase gate.
  </action>
  <verify>
    <automated>npm test; npm run typecheck; npm run lint; npm run check-i18n</automated>
  </verify>
  <acceptance_criteria>
    - Command: `npm test` exits 0 (full suite — Phase 10 + Phase 11); `npm run typecheck` + `npm run lint` + `npm run check-i18n` all exit 0.
    - Command: node probe on docs/economy-budget.md prints `COMPLIANCE VERIFIED` (the block contains 'E[net]' with a <= 0 conclusion + the 'satisfies D-19' wording — mirror the 10-03 verify probe style).
    - Source: docs/economy-budget.md contains the 'COMPLIANCE VERIFICATION (2026-08-14, Phase 11)' block + the 'requires a new sign-off (D-18 one-way gate)' line.
    - Source: the compliance block's prices match the live seed (grep the doc for 50/100/200-500 against sanguo-items.json/formations values).
  </acceptance_criteria>
  <done>The phase seals: economy compliance verified against the amendment (E[net] <= 0, gross < ~416/hr), i18n parity complete across 3 locales, and the entire test suite green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| economy doc ↔ live seed values | The signed amendment must equal the seeded prices the shop charges — drift is the rebalance-by-accident threat (Pitfall 8). |
| balance constants ↔ engine behavior | Tuning combat power shifts the boss wall; an over-tune can silently make the economy grind trivial or impossible. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-08-01 | Tampering | Economy rebalance-by-drift (amendment ≠ seed prices) | medium | mitigate | The compliance block verifies doc ↔ live seed; drift is reconciled explicitly (seed = single source) or re-signed (D-18). |
| T-11-08-02 | Tampering | Balance-tune leaking into the D-05 formula | high | mitigate | The pass tunes constants ONLY (RESEARCH OQ2); acceptance asserts battleEngine.ts has zero diff (formula locked). |
| T-11-08-03 | Tampering | Hidden balance numbers reaching the UI during tuning | high | mitigate | Tuned constants stay in the hidden modules (D-12); no UI file imports them; the never-render list holds through the pass. |
| T-11-08-04 | Spoofing | A green-but-incomplete suite sealing the phase | medium | mitigate | `npm test` runs the FULL suite (not per-plan) as the phase gate; check-i18n + lint are separate mandatory gates. |
| T-11-08-SC | Tampering | npm/pip/cargo installs | — | not-triggered | This plan installs no packages. |
</threat_model>

<verification>
- `npm test` (full suite), `npm run typecheck`, `npm run lint`, `npm run check-i18n` all green — the phase gate.
- The balance-pass simulation is deterministic (fixed seeds) and asserts the beatable-but-hard wall.
- The compliance block prints `COMPLIANCE VERIFIED` via the node probe.
- No diff to battleEngine.ts (D-05 formula locked).
</verification>

## Artifacts this phase produces

| Symbol | Kind | File |
|--------|------|------|
| balancePass.test.ts (legion-vs-boss simulation, fixed seeds) | test suite (Pitfall 4) | src/services/sanguo/__tests__/balancePass.test.ts |
| Tuned constants (IF the pass requires) — STAT_GAIN_PER_LEVEL / TIER_MULTIPLIERS / CHEMISTRY_TIERS buffs | constants tuning | src/constants/sanguoProgression.ts, sanguoChemistry.ts |
| COMPLIANCE VERIFICATION (2026-08-14, Phase 11) block + D-18 sign-off line | economy doc | docs/economy-budget.md |
| i18n parity completion (any missing Phase 11 keys, 3 locales) | i18n | locales/{vi,en,zh-cn}/sanguo.json |

<success_criteria>
- The boss wall is calibrated (beatable-but-hard at L60+ t2 S-legions; the L50 wall holds) with the D-05 formula untouched.
- The economy amendment's numbers are verified against the live seed: E[net] <= 0 (D-19), gross < ~416/hr — documented in the compliance block.
- i18n parity + the full test suite + typecheck + lint all green — Phase 11 seals as a whole.
</success_criteria>

<output>
Create `.planning/phases/11-progression-chemistry-economy-depth/11-08-SUMMARY.md` when done.
</output>
