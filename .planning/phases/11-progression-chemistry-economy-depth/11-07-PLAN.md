---
phase: 11-progression-chemistry-economy-depth
plan: 07
type: execute
wave: 6
depends_on:
  - 11-05
  - 11-06
files_modified:
  - src/services/sanguo/legionService.ts
  - src/services/sanguo/__tests__/legionService.test.ts
  - src/commands/sanguo/legion.ts
  - src/commands/sanguo/heroes.ts
  - src/commands/sanguo/__tests__/legion.test.ts
  - src/commands/sanguo/__tests__/heroes.test.ts
  - src/commands/sanguo/map.ts
  - src/events/interactionCreate.ts
  - src/ui/components/sanguoLegionFormationMenu.ts
  - src/ui/components/sanguoLegionSlotMenu.ts
  - src/ui/components/sanguoLegionHeroMenu.ts
  - src/ui/components/sanguoLegionSaveButton.ts
  - src/ui/components/sanguoHeroesFactionMenu.ts
  - src/ui/components/sanguoHeroesIvMenu.ts
  - src/ui/embeds/buildSanguoLegionEmbed.ts
  - src/ui/embeds/buildSanguoHeroesEmbed.ts
  - locales/vi/sanguo.json
  - locales/en/sanguo.json
  - locales/zh-cn/sanguo.json
autonomous: true
requirements:
  - TQC-17

estimate:
  tokens: 54000
  raw_tokens: 27000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A user can assemble a legion via /sanguo legion: pick an OWNED formation (free starter at onboarding + purchased ones from 11-04 — D-21), assign 3 mains + 9 support from the collection with STRICT class-match validation (D-20: a hero contributes only in a slot matching its class; wrong class → legion.class_mismatch, no write), and save the active legion to user_legions + user_legion_slots (D-22)."
    - "Legion assembly is ownership-gated (V4): every pressed userHeroId is re-validated server-side (belongs to the user) before any write — a crafted sanguo:legion:hero:{slot}:{id} with another user's hero → NOT_OWNED, no state change."
    - "Save is allowed with < 3 mains (incomplete legion caution, R-11 — legion.incomplete WARNING copy); empty support slots are allowed (bonus-only chemistry — empty = no buff, no penalty, D-19 Phase 8); the legion embed shows chemistry lines per main (legion.chemistry_line: tier label + link COUNT only — never points/buff% — D-12/D-19)."
    - "The legion embed + chemistry lines are computed via chemistryService (11-05): per-main link points from the 9 supports (family+spouse 3pt / faction 2pt / role 1pt) → tier S/A/B/C/D; supports' special skills render by name (skills are part of the support loadout — D-18) but trigger chances/weights never render."
    - "/sanguo heroes gains the SC5 filters: faction (heroes.faction_filter, stable faction codes + 'Tất cả' reset) + IV grade (heroes.iv_filter, iv_grade keys — grade NEVER raw IV, D-12) in addition to the existing zone filter — 3 filter selects, each in its own ActionRow; the embed title count reflects the filtered total."
    - "UI-SPEC lifted: legion assembly = formation select (row 1) → slot-pick select (row 2, 12 slots) → hero-pick select (row 3, class-filtered + paged at 25) → save button (row 4) — 4 ActionRows ≤ 5; slots render legion.slot_empty when empty; the 2-field legion view (3 mains + 9 support lines, ≤ 60 chars each) fits the ≤ 25-field budget."
  artifacts:
    - src/services/sanguo/legionService.ts — listOwnedFormations, getActiveLegion, assignHero, clearSlot, saveLegion (ownership + class-match validation)
    - src/commands/sanguo/legion.ts — /sanguo legion + assembly handlers (formation/slot/hero/save)
    - src/commands/sanguo/heroes.ts — faction + IV filter extension (queryOwnedHeroes + 2 select menus)
    - 6 new UI components + 2 embeds (legion + heroes filters)
    - interactionCreate routing: sanguo:legion:formation/slot/hero/save, sanguo:heroes:faction/iv
    - i18n: legion.*/heroes.* sections + cmd.legion, 3 locales
  key_links:
    - "legionService.saveLegion ↔ user_legions/user_legion_slots (11-01 schema) — the persisted active legion the boss routing (11-06) reads."
    - "class-match validation ↔ formation_slots.class (11-01) ↔ heroes.class — the D-20 strict rule enforced server-side at assign time."
    - "chemistry lines ↔ chemistryService (11-05) — the legion embed computes per-main tier from the CURRENT slots on every render (pure, no engine call)."
    - "heroes filters ↔ user_heroes captured_zone + heroes.faction_id + iv_grade keys — SC5 team-building surface."
  prohibitions:
    - "No chemistry points or buff % rendered — tier label + link count only (D-12/D-19, UI-SPEC legion.chemistry_line)."
    - "No raw IV in the heroes IV filter — values are iv_grade keys only (D-12)."
    - "No saving a wrong-class hero into a slot (D-20 strict — server-side validation, not UI-only)."
    - "No per-slot duplicate hero across the legion (a hero cannot be in two slots — one copy = one slot)."
    - "No wallet call in legion assembly — formation PURCHASE is the shop sink (11-04); assigning/saving is free."
  flagged_assumptions:
    - "SCOPE NOTE (dense-plan heuristic accepted — checker W-2): files_modified = 19 paths vs the 15/file-plan heuristic. Per-task CODE-file counts stay ≤ 10: Task 1 = 2 paths; Task 2 = 8 code paths + the locales triplet as ONE i18n parity unit; Task 3 = 5 code paths + the locales triplet. Estimate 54k tokens (low confidence) is under the smart-zone budget; no scope is dropped vs the artifact table. If the executor hits context pressure in Task 2, the 4-row assembly UI block is the cleanest defer point."
    - "FLAGGED ASSUMPTION (probe TQC-17 empty, unresolved): a user with NO owned formation (impossible in practice — the starter formation is granted free on the FIRST legion use via this plan's first-use upsert, not at the Phase 10 onboarding starter-hero flow) → legion.error defensive copy; the starter grant is wired in the 11-02 seed (formations catalog, basePrice 0) + this plan's first-use upsert on user_formations — the upsert rides the PLAN-FIX P0-1 `user_formations_unique_user_formation` index (11-01)."
    - "FLAGGED ASSUMPTION (probe TQC-17 ordering, unresolved): hero-pick select options ordered by userHeroes.id asc (earliest captured first — matches the collection convention); slot order is the fixed 0-11 formation layout (3 mains first, then 9 supports)."
    - "FLAGGED ASSUMPTION (class-match edge): a hero whose class matches NO slot of the chosen formation cannot be placed at all (legion.class_mismatch on every slot) — the hero-pick menu simply excludes it (D-20 strict)."

---

<objective>
What this plan accomplishes: **the team-building surface (TQC-17 assembly half + ROADMAP SC5) — /sanguo legion (formation pick, 3+9 class-matched assignment, save) and the /sanguo heroes faction/IV filters.** Task 1 builds legionService (ownership + strict class-match validation + persistence to user_legions/user_legion_slots); Task 2 builds the /sanguo legion command + the 4-row assembly UI (formation → slot → hero → save) + the chemistry-line embed; Task 3 extends /sanguo heroes with the faction + IV-grade filters (SC5).

Purpose: the legion the boss routing (11-06) consumes must be assemblable; the chemistry depth (11-05) must be visible as tier labels; SC5's full collection filters enable team building. The ownership + class-match server-side validation is the security-critical piece (V4).

Output: a working legion assembly flow + SC5 heroes filters, i18n complete.
</objective>

<execution_context>
@C:/Users/901107/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/901107/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-progression-chemistry-economy-depth/11-CONTEXT.md
@.planning/phases/11-progression-chemistry-economy-depth/11-RESEARCH.md
@.planning/phases/11-progression-chemistry-economy-depth/11-PATTERNS.md
@.planning/phases/11-progression-chemistry-economy-depth/11-UI-SPEC.md
@.planning/phases/11-progression-chemistry-economy-depth/11-05-PLAN.md
@src/services/sanguo/chemistryService.ts
@src/commands/sanguo/hero.ts
@src/commands/sanguo/heroes.ts
@src/db/schema/userLegions.ts
@src/db/schema/formations.ts
@src/db/schema/userHeroes.ts
@src/db/schema/heroes.ts
@src/db/schema/heroFactions.ts
@src/events/interactionCreate.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: legionService — ownership-gated + class-matched 3+9 assembly persistence</name>
  <files>src/services/sanguo/legionService.ts, src/services/sanguo/__tests__/legionService.test.ts</files>
  <read_first>
    - src/db/schema/userLegions.ts (user_legions + user_legion_slots from 11-01 — 12 slots, unique(userId, slotOrder))
    - src/db/schema/formations.ts (formation_slots.class per slotOrder — the D-20 match target)
    - src/commands/sanguo/hero.ts lines 286-323 (the FOR UPDATE ownership tx + one-row upsert pattern legionService mirrors)
    - src/services/sanguo/chemistryService.ts (11-05 — the per-main tier the embed consumes)
    - .planning/phases/11-progression-chemistry-economy-depth/11-RESEARCH.md §Pattern 2 (class-match strictness) + A9 (legion schema)
    - .planning/phases/11-progression-chemistry-economy-depth/11-PATTERNS.md §legionService.ts (lines 159-183, ownership tx + class-match validation)
  </read_first>
  <behavior>
    - Test 1 (assignHero): assigning a hero whose heroes.class matches formation_slots.class for the slot succeeds (row upserted in user_legion_slots); a wrong-class hero → legion.class_mismatch, no write (D-20).
    - Test 2 (assignHero ownership): assigning a userHeroId belonging to ANOTHER user → NOT_OWNED, no write (V4 — crafted id).
    - Test 3 (assignHero duplicates): assigning the same copy to a second slot → HERO_ALREADY_ASSIGNED (one copy = one slot).
    - Test 4 (saveLegion): persists the formation + 12 slots (empty allowed) atomically; a second save replaces the previous assignment cleanly (single active legion per user).
  </behavior>
  <action>
    Create `src/services/sanguo/legionService.ts` (D-20/D-22, A9):
    - `export async function listOwnedFormations(userId: number)` — user_formations join formations (ordered by id), for the formation select menu.
    - `export async function getActiveLegion(userId: number)` — user_legions + user_legion_slots join user_heroes + heroes (names/classes/emojis), for the render + the 11-06 boss-routing read.
    - `export async function assignHero(userId: number, formationId: number, slotOrder: number, userHeroId: number)` — ONE tx: (1) verify the formation is owned (user_formations WHERE userId + formationId else NOT_OWNED); (2) FOR UPDATE lock the target copy (user_heroes WHERE id + userId else NOT_OWNED — V4 ownership re-gate); (3) resolve the slot's required class (formation_slots WHERE formationId + slotOrder); (4) compare with the copy's heroes.class — mismatch → throw new Error('legion.class_mismatch') BEFORE any write (D-20 strict); (5) check the copy is not already in another slot of this legion (user_legion_slots WHERE userId + userHeroId) → throw new Error('HERO_ALREADY_ASSIGNED'); (6) upsert the slot row (user_legion_slots, onConflictDoUpdate on unique(userId, slotOrder)). slotOrder bounds 0-11 validated.
    - `export async function clearSlot(userId: number, slotOrder: number)` — delete the slot row (empty slots allowed — bonus-only chemistry).
    - `export async function saveLegion(userId: number, formationId: number)` — ONE tx: verify ownership; upsert user_legions (unique userId — one active legion); the 12 slot rows persist as assigned (empty stays empty); returns the saved formation. Incomplete (n/3 mains) is ALLOWED with the caution (R-11 — validated in the command layer).
    - Tests: the 4 behaviors.
  </action>
  <verify>
    <automated>npx vitest run src/services/sanguo/__tests__/legionService.test.ts; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Command: vitest green for the 4 legion behaviors; `npm run typecheck` green.
    - Source: assignHero contains the class-match comparison (grep 'class' + 'legion.class_mismatch' >= 1 each) BEFORE the slot write (assert by ordering in the action).
    - Source: `grep -c "NOT_OWNED" src/services/sanguo/legionService.ts` >= 1 (ownership re-gate on the pressed userHeroId + formation).
    - Source: no wallet import in legionService.ts (grep 'wallet' === 0 — assembly is free, D-19).
  </acceptance_criteria>
  <done>The legion service enforces ownership + strict class-match + one-copy-one-slot, persisting the active legion for the boss routing (11-06).</done>
</task>

<task type="auto">
  <name>Task 2: /sanguo legion command + 4-row assembly UI + chemistry-line embed + routing</name>
  <files>src/commands/sanguo/legion.ts, src/commands/sanguo/map.ts, src/events/interactionCreate.ts, src/ui/components/sanguoLegionFormationMenu.ts, src/ui/components/sanguoLegionSlotMenu.ts, src/ui/components/sanguoLegionHeroMenu.ts, src/ui/components/sanguoLegionSaveButton.ts, src/ui/embeds/buildSanguoLegionEmbed.ts, locales/vi/sanguo.json, locales/en/sanguo.json, locales/zh-cn/sanguo.json</files>
  <read_first>
    - src/commands/sanguo/hero.ts (renderHeroDetail + handleCompanionPress — the render/press trio pattern to copy)
    - src/commands/sanguo/heroes.ts lines 395-454 (the StringSelectMenu handler pattern — hero-pick menu variant)
    - src/ui/embeds/buildSanguoHeroesEmbed.ts + buildSanguoHeroEmbed.ts (embed + data-interface patterns)
    - src/services/sanguo/chemistryService.ts (11-05 — the tier computation for the chemistry lines)
    - .planning/phases/11-progression-chemistry-economy-depth/11-UI-SPEC.md §Component & CustomId Contract (legion rows) + §Spacing (legion 4-row budget) + §Copywriting (legion.*/cmd.legion keys)
  </read_first>
  <action>
    Create `src/commands/sanguo/legion.ts` (D-22, UI-SPEC R-10 interaction contract):
    - Subcommand builder (cmd.legion.description localizations) + execute: fetchCommandContext → notRegistered guard → render buildSanguoLegionEmbed (SEASON color, legion.title with the formation name, 2 fields: 3 mains + 9 support lines via legion.main_slot/support_slot/slot_empty) with the 4 ActionRows: formation select (sanguoLegionFormationMenu, sanguo:legion:formation:{formationId}, own ActionRow), slot-pick select (sanguoLegionSlotMenu, sanguo:legion:slot:{slotIndex}, 12 options with class labels, own row), hero-pick select (sanguoLegionHeroMenu, sanguo:legion:hero:{slotIndex}:{userHeroId}, class-matched owned heroes ONLY paged at 25, own row), save button (sanguoLegionSaveButton, sanguo:legion:save, Primary — enabled with < 3 mains, the caution copy legion.incomplete in the embed).
    - Handlers: handleFormationPress (re-render the 12-slot view for that formation), handleSlotPress (re-render the hero-pick menu for that slot, class-filtered), handleHeroPress (assignHero → re-render with the updated assignment + chemistry lines), handleSavePress (saveLegion → SUCCESS embed legion.saved). Chemistry lines: for each main, compute per-main chemistry via chemistryService (mainChemistryPoints + chemistryTier) against the current 9 support slots → legion.chemistry_line ({{hero}}: **{{tier}}** • {{n}} liên kết) + legion.link_line per matched link (type labels family/spouse/faction/role — presence implies type, never points).
    - Register in map.ts (addSubcommand + dispatch + SanguoComponentHandlers re-export); extend interactionCreate.ts routing (sanguo:legion:formation/slot/hero/save — parseInt + isNaN guards on slotIndex/userHeroId).
    - Create the 4 components + buildSanguoLegionEmbed.ts (data interface carries hero names/emojis/tier labels/link counts ONLY — no chemistry numbers, D-12).
    - i18n: legion.* + cmd.legion sections in all 3 locales (full UI-SPEC key set: title/main_slot/support_slot/slot_empty/formation_select/slot_pick/hero_pick/save_button/saved/chemistry_line/link_line/tier_s..d/incomplete/not_assembled/class_mismatch/error).
  </action>
  <verify>
    <automated>npm run typecheck; npm run check-i18n; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Command: `npm run typecheck` + `npm run check-i18n` + `npm run lint` exit 0 (eslint i18next/no-literal-string — zero hardcoded strings).
    - Source: legion.ts renders the 4 ActionRows (formation/slot/hero/save — grep the 4 component imports).
    - Source: the hero-pick menu query filters by the slot's required class (grep the class filter in the handler).
    - Source: buildSanguoLegionEmbed data interface has NO chemistry points/buff% fields (grep the interface — tier label + link count only, D-12).
    - Source: locales have the legion.* key set in all 3 locales (check-i18n parity).
  </acceptance_criteria>
  <done>A user can assemble + save a 3+9 legion through the 4-row UI with chemistry tier lines — the surface the boss routing consumes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: /sanguo heroes SC5 filters — faction + IV grade (+ existing zone) + filter menus</name>
  <files>src/commands/sanguo/heroes.ts, src/commands/sanguo/__tests__/heroes.test.ts, src/ui/components/sanguoHeroesFactionMenu.ts, src/ui/components/sanguoHeroesIvMenu.ts, src/ui/embeds/buildSanguoHeroesEmbed.ts, locales/vi/sanguo.json, locales/en/sanguo.json, locales/zh-cn/sanguo.json</files>
  <read_first>
    - src/commands/sanguo/heroes.ts (queryOwnedHeroes lines 138-153 + the zone filter select — the SC5 extension point; handleZoneFilterSelect lines 395-454)
    - src/ui/embeds/buildSanguoHeroesEmbed.ts (the embed to extend with 2 more filters)
    - src/db/schema/heroFactions.ts (stable faction reference — the faction filter values) + userHeroes.ts (captured_zone) + heroes.ts (faction_id)
    - .planning/phases/11-progression-chemistry-economy-depth/11-UI-SPEC.md §Component & CustomId Contract (heroes:faction/iv rows) + §Copywriting (heroes.faction_filter/iv_filter/filter_all)
  </read_first>
  <behavior>
    - Test 1 (queryOwnedHeroes filters): the query applies zone AND faction AND IV-grade filters combined (a crafted filter set returns only matching copies); each filter alone works; 'Tất cả' (filter_all) resets that filter.
    - Test 2 (IV-grade filter): the filter matches the computed iv_grade key (grade — the SAME function the collection render uses; never raw IV values in the filter surface, D-12).
  </behavior>
  <action>
    Extend `src/commands/sanguo/heroes.ts` (SC5, ROADMAP SC5):
    - Extend queryOwnedHeroes (lines 138-153) to accept optional factionCode + ivGrade filters: faction via heroes.faction_id join heroFactions (stable code match), IV grade via the existing ivGradeKey() computation filtered server-side (the grade key the render uses — grade, never raw IV, D-12). Zone filter (existing) unchanged. The three filters COMBINE (AND).
    - Add the two select menus: sanguoHeroesFactionMenu (sanguo:heroes:faction, values = stable faction codes from heroFactions + a 'Tất cả' filter_all option) + sanguoHeroesIvMenu (sanguo:heroes:iv, values = the iv_grade keys + filter_all) — each in its own ActionRow (3 filter rows total with the existing zone menu).
    - Extend buildSanguoHeroesEmbed.ts data interface: active filter state (zone/faction/iv) so the embed reflects what's applied; the title count = the filtered total (existing pattern).
    - i18n: heroes.faction_filter / heroes.iv_filter / heroes.filter_all additive keys (3 locales).
    - Tests: the 2 behaviors.
  </action>
  <verify>
    <automated>npx vitest run src/commands/sanguo/__tests__/heroes.test.ts; npm run typecheck; npm run check-i18n</automated>
  </verify>
  <acceptance_criteria>
    - Command: vitest green for the filter behaviors; typecheck + check-i18n green.
    - Source: queryOwnedHeroes accepts the two new filter params (grep the signature — faction + ivGrade) and combines them AND-wise.
    - Source: the IV filter uses iv_grade keys (grep 'ivGradeKey' or the grade-key import) — no raw IV in the menu options (D-12).
    - Source: interactionCreate routes sanguo:heroes:faction + sanguo:heroes:iv (grep the blocks).
    - Source: locales have heroes.faction_filter/iv_filter/filter_all in all 3 locales.
  </acceptance_criteria>
  <done>The collection has the full SC5 filter set — zone + faction + IV grade — enabling team building for the legion.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| legion assembly customIds → legion tx | Pressed userHeroId + formationId + slotIndex cross this boundary; costs NEVER (assembly is free); ownership + class-match validated server-side (V4). |
| legion slots → boss battle input (11-06) | The persisted assignments become combatants — a wrong-class or foreign hero in the snapshot would corrupt the battle. |
| filter customIds → collection query | Filter values are server-validated against reference tables (faction codes, iv_grade keys) — crafted values return empty, never leak raw IV. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-07-01 | Tampering | Crafted legion assembly (foreign hero / wrong-class hero in a slot) | high | mitigate | Server-side ownership re-gate (NOT_OWNED) + strict class-match (legion.class_mismatch) BEFORE any write (legionService assignHero — D-20, V4). |
| T-11-07-02 | Tampering | One copy assigned to multiple slots (duplicate combatants) | medium | mitigate | HERO_ALREADY_ASSIGNED guard in the assign tx (one copy = one slot). |
| T-11-07-03 | Information disclosure | Chemistry points/buff% in the legion embed | high | mitigate | Embed data interface carries tier label + link count ONLY (D-12/D-19); the chemistry numbers live in the service, never the render. |
| T-11-07-04 | Information disclosure | Raw IV via the heroes IV filter | high | mitigate | The filter surface uses iv_grade keys (the same grade function the render uses) — raw IV values never appear (D-12). |
| T-11-07-05 | Tampering | Filter-bypass (crafted faction code returns hidden data) | low | mitigate | Filter values validated against heroFactions/iv_grade reference sets server-side; invalid → empty result, no crash (V5 input validation). |
| T-11-07-SC | Tampering | npm/pip/cargo installs | — | not-triggered | This plan installs no packages. |
</threat_model>

<verification>
- `npx vitest run src/services/sanguo/__tests__/legionService.test.ts src/commands/sanguo/__tests__/legion.test.ts src/commands/sanguo/__tests__/heroes.test.ts` green.
- `npm run typecheck`, `npm run check-i18n`, `npm run lint` green.
- Grep gates: zero wallet imports in legionService; zero chemistry numbers / raw IV in the embed data interfaces.
- The 11-06 boss routing can read a saved active legion.
</verification>

## Artifacts this phase produces

| Symbol | Kind | File |
|--------|------|------|
| `listOwnedFormations(userId)` / `getActiveLegion(userId)` | queries | src/services/sanguo/legionService.ts |
| `assignHero(userId, formationId, slotOrder, userHeroId)` | ownership + class-match tx (D-20/V4) | src/services/sanguo/legionService.ts |
| `clearSlot(userId, slotOrder)` / `saveLegion(userId, formationId)` | persistence (D-22) | src/services/sanguo/legionService.ts |
| /sanguo legion command + 4-row assembly handlers | command (D-22, R-10) | src/commands/sanguo/legion.ts |
| sanguoLegionFormationMenu / SlotMenu / HeroMenu / SaveButton | UI components | src/ui/components/ |
| sanguoHeroesFactionMenu / sanguoHeroesIvMenu | UI components (SC5) | src/ui/components/ |
| buildSanguoLegionEmbed (chemistry tier lines) / buildSanguoHeroesEmbed (3-filter state) | embeds | src/ui/embeds/ |
| queryOwnedHeroes faction + IV-grade filters (AND-combined) | collection query extension (SC5) | src/commands/sanguo/heroes.ts |
| customId routing: sanguo:legion:*, sanguo:heroes:faction/iv | event routing | src/events/interactionCreate.ts |
| i18n: legion.* + heroes.faction_filter/iv_filter/filter_all (3 locales) | i18n | locales/{vi,en,zh-cn}/sanguo.json |
| legionService + heroes filter test suites | tests | src/services/sanguo/__tests__/, src/commands/sanguo/__tests__/ |

<success_criteria>
- TQC-17 assembly half: 3+9 legion assemblable + persisted, class-matched, ownership-gated.
- SC5: /sanguo heroes filters by zone + faction + IV grade (grade keys only).
- Chemistry tier labels + link counts render; points/buff% never; no raw IV in filters (D-12).
- The saved legion feeds the 11-06 boss battle routing.
</success_criteria>

<output>
Create `.planning/phases/11-progression-chemistry-economy-depth/11-07-SUMMARY.md` when done.
</output>
