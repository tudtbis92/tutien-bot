---
phase: 11-progression-chemistry-economy-depth
plan: 04
type: execute
wave: 4
depends_on:
  - 11-01
  - 11-02
files_modified:
  - src/services/sanguo/shopService.ts
  - src/services/sanguo/bagService.ts
  - src/services/sanguo/dropService.ts
  - src/services/sanguo/__tests__/shopService.test.ts
  - src/services/sanguo/__tests__/bagService.test.ts
  - src/services/sanguo/__tests__/dropService.test.ts
  - src/commands/sanguo/shop.ts
  - src/commands/sanguo/bag.ts
  - src/commands/sanguo/map.ts
  - src/events/interactionCreate.ts
  - src/ui/components/sanguoShopTabs.ts
  - src/ui/components/sanguoShopBuyButtons.ts
  - src/ui/components/sanguoBagUseButtons.ts
  - src/ui/embeds/buildSanguoShopEmbed.ts
  - src/ui/embeds/buildSanguoBagEmbed.ts
  - locales/vi/sanguo.json
  - locales/en/sanguo.json
  - locales/zh-cn/sanguo.json
autonomous: true
requirements:
  - TQC-16

estimate:
  tokens: 52000
  raw_tokens: 26000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A user can open /sanguo shop and see TWO currency tabs (💎 Linh thạch / 🎁 Sự kiện — D-16 tab buttons, one ActionRow); the Linh thạch tab lists the purchasable items (heal_pill 50💎, booster_x2 100💎) + formations (200-500💎) with the price in the buy-button label; the Event tab shows the locked capture_key row with shop.capture_key_locked (D-15 — shown not sold, no buy button)."
    - "Buying an item routes through wallet.deductBalance in ONE tx (D-03 Phase 8 / D-19): price + sale_state resolve server-side from sanguo_items inside the tx (price NEVER in the customId — anti-tamper); capture_key with saleState !== 'sold' → ITEM_NOT_FOR_SALE (D-15). Formation purchases deduct formations.base_price and grant user_formations ownership (D-21)."
    - "A user can open /sanguo bag and see owned items (user_sanguo_items) with a Dùng button per item (≤3 catalog-bound): healing item restores the active companion's HP (or a selected copy via the D-04 selector — bag.no_target when no target), booster shows an owned hint (applies at the conversion site, 11-03), capture_key gates the T4/T5 capture buttons (already wired in sanguoCapture.ts)."
    - "Boss thường wins grant a GUARANTEED item drop (≥1, D-14): dropService picks rarity-weighted from the D-11 pool (heal_pill 70% / booster_x2 25% / capture_tier4_key 4.9% / capture_tier5_key 0.1% — checkpoint-confirmed) via cryptoUniform; items only, never money (D-19 — no users.balance touch)."
    - "UI-SPEC lifted: shop/bag surfaces handle empty (bag.empty with next-step copy; shop always >= 2 purchasable rows by catalog bound + defensive shop.empty), populated rows per shop.item_line/bag.line, overflow bounded by the ≤3-item catalog; prices render as glyph + bold number in labels (never a color accent)."
  artifacts:
    - src/services/sanguo/shopService.ts — buyItem, buyFormation (wallet sinks)
    - src/services/sanguo/bagService.ts — listBag, useItem (heal)
    - src/services/sanguo/dropService.ts — rollBossDrop (crypto-weighted, guaranteed >=1)
    - src/commands/sanguo/shop.ts + bag.ts (subcommands + handlers, map.ts registration)
    - 3 new UI components + 2 new embeds + interactionCreate routing (sanguo:shop:*, sanguo:bag:*)
    - i18n: shop.*/bag.*/cmd.shop/cmd.bag sections, 3 locales
  key_links:
    - "shopService.buyItem ↔ wallet.deductBalance — every Linh thạch sink through the WHERE-guarded wallet (D-19); ledger reason 'sanguo_shop_{code}'."
    - "dropService ↔ battleCheckInService boss-win branch (11-06 wires the CALL; this plan builds the service) — the guaranteed drop after a legion boss win (D-14)."
    - "sanguoItems.saleState ↔ shop buy buttons — capture_key renders no buy button; saleState 'locked' throws ITEM_NOT_FOR_SALE server-side (D-15)."
    - "bag use ↔ user_sanguo_items quantity_positive check — heal decrements quantity, delete at 0 (userSanguoItems.ts:20)."
  prohibitions:
    - "No price in any customId — sanguo:shop:buy:{itemCode} carries the code ONLY (anti-tamper, Pitfall 3 Phase 10 carry); prices resolve server-side inside the tx."
    - "No capture_key sale for Linh thạch (D-15 one-way): saleState stays 'locked'; selling later needs an economy re-sign."
    - "No money from boss drops — items only (D-19); dropService never touches users.balance."
    - "No new faucet: shop is a pure sink (booster is a documented bounded bridge, A11 — flagged for Phase 12 monitoring)."
    - "No event-tab purchases in v3 — price_event stays 0/unused (event items are Phase 12+ content); the tab renders the locked key row + shop.event_empty."
  flagged_assumptions:
    - "FLAGGED ASSUMPTION (probe TQC-16 adjacency, unresolved): healing a copy at full HP → bag.no_target ('Không có hero nào để dùng vật phẩm này') — heal applies only when hpCurrent < base HP; the item is NOT consumed on a failed target (tx rolls back)."
    - "FLAGGED ASSUMPTION (probe TQC-16 empty, unresolved): the bag renders bag.empty with the next step (shop / boss thường) — never a blank surface (UI-SPEC covered row)."
    - "FLAGGED ASSUMPTION (probe TQC-16 ordering, unresolved): shop rows ordered by section (items then formations) then price asc; bag rows ordered by item id asc — stable ordering (11-02 carry)."
    - "FLAGGED ASSUMPTION (A11 booster volume): the booster's Linh thạch→hồn ngọc bridge is documented in the economy amendment; Phase 12 TQC-19 should flag booster purchase volume — no runtime cap in v3 (D-11/D-12)."
    - "FLAGGED ASSUMPTION (drop weights): rollBossDrop uses the checkpoint-confirmed weights (70/25/4.9/0.1); if the user adjusted at the 11-01 checkpoint, the seed (11-02) already carries the final drop_weight columns — the service reads them from sanguo_items, never hardcodes."
    - "FLAGGED ASSUMPTION (D-21 partial delivery, scope reduction — needs a CONTEXT amendment): D-21's formation sourcing reads 'purchasable (Linh thạch from shop / event items) + boss drops' and 'buy/sell logic lands here'. v3 delivers the free starter (11-02 seed) + shop purchase (buyFormation) ONLY — formation SELL is deferred (no sell flow this phase) and the 'formations via boss drops' channel is NOT delivered (dropService covers sanguo_items only: WHERE drop_weight > 0, no formation rows in the pool — RESEARCH A5 weights sum to 100 across the 4 items). This is an explicit scope reduction vs the D-21 wording; record it in the CONTEXT amendment (deferral to a later phase) before the phase seals — not a silent drop."

---

<objective>
What this plan accomplishes: **the shop + bag + boss-drop economy services (TQC-16) — the Linh thạch sink surface of Phase 11.** Task 1 builds shopService (multi-currency buy via wallet.deductBalance, formation purchase) + /sanguo shop with the two currency tabs; Task 2 builds bagService (heal use targeting the active companion/copy) + /sanguo bag; Task 3 builds dropService (guaranteed ≥1 rarity-weighted boss item drop) — the service the 11-06 boss-win branch calls. All three are wallet-disciplined sinks (D-19): items cost Linh thạch, drops never mint money, capture_key is shown-but-locked.

Purpose: the second vertical loop of Phase 11 (spend → own → use), closing the economy under D-19. The patterns proven here (wallet-sink tx + anti-tamper buy + inventory upsert) are the same ones the legion/formation surfaces reuse.

Output: working /sanguo shop + /sanguo bag + the drop service, i18n complete.
</objective>

<execution_context>
@C:/Users/901107/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/901107/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-progression-chemistry-economy-depth/11-CONTEXT.md
@.planning/phases/11-progression-chemistry-economy-depth/11-RESEARCH.md
@.planning/phases/11-progression-chemistry-economy-depth/11-PATTERNS.md
@.planning/phases/11-progression-chemistry-economy-depth/11-UI-SPEC.md
@.planning/phases/11-progression-chemistry-economy-depth/11-01-PLAN.md
@.planning/phases/11-progression-chemistry-economy-depth/11-02-PLAN.md
@src/services/wallet.ts
@src/services/sanguo/captureService.ts
@src/constants/sanguoCapture.ts
@src/db/schema/sanguoItems.ts
@src/db/schema/userSanguoItems.ts
@src/db/schema/formations.ts
@src/commands/sanguo/heroes.ts
@src/commands/sanguo/map.ts
@src/events/interactionCreate.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: shopService (buyItem + buyFormation via wallet) + /sanguo shop + currency tabs + buy UI</name>
  <files>src/services/sanguo/shopService.ts, src/services/sanguo/__tests__/shopService.test.ts, src/commands/sanguo/shop.ts, src/commands/sanguo/map.ts, src/events/interactionCreate.ts, src/ui/components/sanguoShopTabs.ts, src/ui/components/sanguoShopBuyButtons.ts, src/ui/embeds/buildSanguoShopEmbed.ts</files>
  <read_first>
    - src/services/wallet.ts lines 47-82 (deductBalance WHERE-guard — the sink mechanism)
    - src/services/sanguo/captureService.ts lines 127-196 (the wallet-sink tx + audit pattern shopService mirrors)
    - src/db/schema/sanguoItems.ts (price_linh/price_event/sale_state/drop_weight columns from 11-01) + userSanguoItems.ts (upsert pattern lines 20-24) + formations.ts (base_price + user_formations)
    - src/commands/sanguo/heroes.ts (subcommand builder + handler pattern) + map.ts (registration pattern lines 14-57)
    - .planning/phases/11-progression-chemistry-economy-depth/11-RESEARCH.md §Common Operation 7 (shop buy via wallet) + A5 prices
    - .planning/phases/11-progression-chemistry-economy-depth/11-UI-SPEC.md §Component & CustomId Contract (shop rows) + §Copywriting (shop.*/cmd.shop keys)
  </read_first>
  <behavior>
    - Test 1 (buyItem): buying heal_pill with sufficient balance → deductBalance called with the price, user_sanguo_items quantity +1, ledger reason 'sanguo_shop_heal_pill'; insufficient balance → INSUFFICIENT_BALANCE, no inventory change.
    - Test 2 (buyItem): buying capture_key (saleState 'locked') → ITEM_NOT_FOR_SALE, no deduction (D-15).
    - Test 3 (buyFormation): buying a 200💎 formation with sufficient balance → deductBalance + user_formations row (unique userId+formationId upsert, no duplicate ownership); already-owned → ALREADY_OWNED error.
  </behavior>
  <action>
    Create `src/services/sanguo/shopService.ts` (TQC-16, D-16):
    - `export async function buyItem(userId: number, itemCode: string): Promise<{ name: string; qty: number; price: bigint }>` — ONE tx: select the item by code; if !item || item.saleState !== 'sold' throw new Error('ITEM_NOT_FOR_SALE'); price = item.priceLinh; await deductBalance(tx, userId, price, { reason: 'sanguo_shop_' + itemCode, metadata: { itemId: item.id } }); insert user_sanguo_items quantity 1 onConflictDoUpdate quantity + 1 (userSanguoItems.ts:20-24 pattern); return name/price for the UI. Price NEVER in the customId — resolved here inside the tx.
    - `export async function buyFormation(userId: number, formationCode: string): Promise<{ name: string; price: bigint }>` — ONE tx: select the formation by code (formations table); if already owned (user_formations unique userId+formationId) throw new Error('ALREADY_OWNED'); deductBalance(tx, userId, formation.basePrice, { reason: 'sanguo_shop_formation_' + code }); insert user_formations row. (D-21 buy/sell — buy lands here; formation SELL and the 'formations via boss drops' sourcing channel are deferred out of v3 — see the flagged assumption; not silently dropped.)
    - Create `src/commands/sanguo/shop.ts`: subcommand builder (setDescriptionLocalizations, eslint-disable i18next/no-literal-string for static Discord strings — heroes.ts:58 pattern), execute renders buildSanguoShopEmbed (tab-state-aware 💎/🎁), handlers handleTabPress (sanguo:shop:tab:{linh|event} re-render for the chosen tab) + handleBuyPress (sanguo:shop:buy:{itemCode} → shopService.buyItem → SUCCESS embed shop.bought or shop.insufficient DANGER). Register in map.ts (addSubcommand + dispatch + SanguoComponentHandlers re-export).
    - Create sanguoShopTabs.ts (2 toggle buttons one row: Primary active / Secondary inactive, shop.tab_linh / shop.tab_event) + sanguoShopBuyButtons.ts (one button per purchasable row, shop.buy_button price in label; capture_key row renders NO button — locked line shop.capture_key_locked). buildSanguoShopEmbed.ts — tab-state-aware, SEASON color, sections shop.section_items + shop.section_formations, rows per shop.item_line.
    - Extend interactionCreate.ts: sanguo:shop:tab: + sanguo:shop:buy: routing (parseInt/isNaN guard on nothing — codes are strings, validate against the catalog in the service).
    - Tests: the 3 behaviors.
  </action>
  <verify>
    <automated>npx vitest run src/services/sanguo/__tests__/shopService.test.ts; npm run typecheck; npm run check-i18n</automated>
  </verify>
  <acceptance_criteria>
    - Command: vitest green for the 3 shop behaviors; `npm run typecheck` + `npm run check-i18n` green.
    - Source: `grep -c "deductBalance" src/services/sanguo/shopService.ts` >= 1 (wallet sink) and the ledger reasons use 'sanguo_shop_' prefix.
    - Source: buyItem contains the `saleState !== 'sold'` guard (ITEM_NOT_FOR_SALE) — capture_key cannot be bought (D-15).
    - Source: `grep -c "price" src/ui/components/sanguoShopBuyButtons.ts` — the label carries the price for DISPLAY, but the customId is 'sanguo:shop:buy:{itemCode}' with NO price segment (anti-tamper — assert by inspecting the customId template string).
    - Source: map.ts registers the shop subcommand + re-exports its handlers.
  </acceptance_criteria>
  <done>A user can open /sanguo shop, switch currency tabs, and buy heal/booster items + formations with Linh thạch through wallet.deductBalance — capture_key visibly locked.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: bagService (heal use) + /sanguo bag + use UI + booster hint</name>
  <files>src/services/sanguo/bagService.ts, src/services/sanguo/__tests__/bagService.test.ts, src/commands/sanguo/bag.ts, src/commands/sanguo/map.ts, src/events/interactionCreate.ts, src/ui/components/sanguoBagUseButtons.ts, src/ui/embeds/buildSanguoBagEmbed.ts</files>
  <read_first>
    - src/db/schema/userSanguoItems.ts (quantity_positive check + unique(userId, itemId) — the bag query + decrement target)
    - src/db/schema/userHeroes.ts (hp_current column — the heal target) + userSanguoState.ts (activeHeroId)
    - src/commands/sanguo/hero.ts (copy-selector reuse for the heal-target flow — D-13)
    - .planning/phases/11-progression-chemistry-economy-depth/11-RESEARCH.md A6 (HP persists / D-04) + D-13 (bag + heal targeting)
    - .planning/phases/11-progression-chemistry-economy-depth/11-UI-SPEC.md §Component & CustomId Contract (bag row) + §Copywriting (bag.* keys)
  </read_first>
  <behavior>
    - Test 1 (useItem heal): a fainted (hp_current 0) active companion heals to base HP; a damaged copy heals fully (hp_current = base hp); a full-HP copy → NO_TARGET error, item NOT consumed.
    - Test 2 (useItem heal): consuming decrements quantity (delete the row at 0 — quantity_positive check); item not owned → ITEM_NOT_OWNED.
    - Test 3 (listBag): rows ordered by item id asc with quantity; empty bag → empty result (command renders bag.empty).
  </behavior>
  <action>
    Create `src/services/sanguo/bagService.ts` (D-13):
    - `export async function listBag(userId: number)` — select user_sanguo_items join sanguo_items ordered by item id asc, return { itemCode, name, quantity } per row (the ≤3 catalog-bound list).
    - `export async function useHeal(userId: number, itemCode: string, targetUserHeroId: number | null): Promise<{ healedHeroId: number; hpAfter: number }>` — ONE tx: FOR UPDATE lock the user_sanguo_items row (WHERE userId + item join code; quantity >= 1 else ITEM_NOT_OWNED); resolve the target — targetUserHeroId if given (ownership re-gate) else the active companion from user_sanguo_state; if none → NO_TARGET; read the copy's hp_current + heroes base hp; if hp_current >= base hp → throw new Error('NO_TARGET') (full-HP — adjacency flagged assumption, item NOT consumed); UPDATE user_heroes SET hp_current = base hp; decrement quantity (delete at 0); return { healedHeroId, hpAfter }. Heal restores to FULL base HP (simplest D-04 recovery path; the copy-selector flow from hero.ts re-renders after).
    - Create `src/commands/sanguo/bag.ts`: subcommand builder + execute renders buildSanguoBagEmbed (bag.title with count, rows per bag.line); handleUsePress (sanguo:bag:use:{itemCode}) — heal targets the active companion directly (bag.use_button); booster owned → render the hint (convert.booster_hint) not an apply site (D-13); capture_key renders no use button. Register in map.ts + interactionCreate routing block.
    - Create sanguoBagUseButtons.ts (≤3 buttons catalog-bound) + buildSanguoBagEmbed.ts (SEASON color, bag.empty defensive copy).
    - Tests: the 3 behaviors.
  </action>
  <verify>
    <automated>npx vitest run src/services/sanguo/__tests__/bagService.test.ts; npm run typecheck; npm run check-i18n</automated>
  </verify>
  <acceptance_criteria>
    - Command: vitest green for the 3 bag behaviors; typecheck + check-i18n green.
    - Source: useHeal contains the hp_current >= base-hp guard (NO_TARGET, no consumption on full HP) and the quantity decrement with delete-at-0.
    - Source: `grep -c "deductBalance" src/services/sanguo/bagService.ts` === 0 (bag use is NOT a money flow — D-19).
    - Source: map.ts registers the bag subcommand; interactionCreate has the sanguo:bag:use routing block.
    - Source: locales have bag.* keys in all 3 locales (bag.empty includes the next step copy per UI-SPEC).
  </acceptance_criteria>
  <done>A user can open /sanguo bag, see owned items with quantities, and heal the active companion (or a selected copy) — the D-04 soft-lock recovery path works.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: dropService — guaranteed boss item drop, rarity-weighted (crypto)</name>
  <files>src/services/sanguo/dropService.ts, src/services/sanguo/__tests__/dropService.test.ts</files>
  <read_first>
    - src/db/schema/sanguoItems.ts (drop_weight column — the weights live in seed, never hardcoded in the service)
    - src/services/sanguo/encounterService.ts lines 121-127 (cumulative-walk weighted pick — the shape rollBossDrop reuses)
    - .planning/phases/11-progression-chemistry-economy-depth/11-RESEARCH.md §Pattern 4 (drop weights 70/25/4.9/0.1) + D-14 (guaranteed >=1 item, items only)
    - .planning/phases/11-progression-chemistry-economy-depth/11-PATTERNS.md §src/services/sanguo/dropService.ts (lines 153-155, no direct analog — weighted pick + boss branch patterns)
  </read_first>
  <behavior>
    - Test 1 (rollBossDrop): with the seeded weights (heal 70 / booster 25 / key4 4.9 / key5 0.1) + injected rng, returns exactly one item code; an rng of 0.0 → heal_pill; 0.70 → booster_x2; 0.95 → capture_tier4_key; 0.999 → capture_tier5_key (boundary precision).
    - Test 2 (rollBossDrop): the cumulative walk never returns undefined (weights sum to 100 from the DB); the returned code exists in the catalog.
  </behavior>
  <action>
    Create `src/services/sanguo/dropService.ts` (D-14):
    - `export async function rollBossDrop(userId: number, rng: () => number = cryptoUniform): Promise<{ itemCode: string; quantity: number }>` — ONE tx: select the drop-eligible items (sanguo_items WHERE drop_weight > 0) ordered by id; cumulative-walk the drop_weight values (encounterService.ts:121-127 shape) with the injected rng (cryptoUniform default — player-facing roll, crypto mandate); upsert user_sanguo_items quantity +1 (onConflictDoUpdate pattern); return { itemCode, quantity: 1 }. Guaranteed ≥1 item — the function ALWAYS returns (D-14); the call site is the boss-win branch (11-06 wires it). Items only — the tx never touches users.balance (D-19). Drop weights read from the DB columns (seeded in 11-02) — never hardcoded (Pitfall 8).
    - Tests: the 2 behaviors (injectable rng for boundary determinism — crypto default for production).
  </action>
  <verify>
    <automated>npx vitest run src/services/sanguo/__tests__/dropService.test.ts; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Command: vitest green for the drop behaviors (boundary rolls at 0.0/0.70/0.95/0.999 land on the expected codes).
    - Source: `grep -c "cryptoUniform" src/services/sanguo/dropService.ts` >= 1 (crypto default) and NO pure-rand import.
    - Source: `grep -v '^#' src/services/sanguo/dropService.ts | grep -c "deductBalance\|creditBalance"` === 0 — drops never touch money (D-19).
    - Source: the drop weights are read from sanguo_items.drop_weight (grep 'drop_weight' >= 1), not hardcoded percent literals in the service.
  </acceptance_criteria>
  <done>The drop service exists and is boundary-tested — 11-06 wires it into the boss-win branch for the guaranteed item drop.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| component customId → shop/bag tx | The item code crosses this boundary; the PRICE never does — it resolves server-side from sanguo_items inside the tx (anti-tamper). |
| tx → wallet ledger | Every Linh thạch sink must write a ledger row (SC1 reconcilability) — a buy that deducts without a ledger row breaks the audit trail. |
| drop roll → inventory | Boss drops must never mint money; the item pool + weights are the only payout surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-04-01 | Tampering | Crafted buy customId with a fabricated price | high | mitigate | sanguo:shop:buy:{itemCode} carries ONLY the code; price/saleState resolve server-side inside the tx from sanguo_items (UI-SPEC anti-tamper; Pitfall 3 Phase 10 carry). |
| T-11-04-02 | Spoofing | Buying capture_key for Linh thạch (D-15 bypass) | high | mitigate | saleState 'locked' → ITEM_NOT_FOR_SALE inside the tx (server-side, not UI); the Event tab shows the locked row with no buy button (UI-SPEC). |
| T-11-04-03 | Tampering | Boss drop minting money (D-19 violation) | high | mitigate | dropService never imports wallet; regression test asserts zero deductBalance/creditBalance (grep gate); drops upsert inventory rows only. |
| T-11-04-04 | Tampering | Heal-use cloning (item consumed twice) | medium | mitigate | FOR UPDATE on the inventory row + quantity >= 1 guard + delete-at-0 inside the same tx as the HP write (bagService). |
| T-11-04-05 | Tampering | Drop weight drift vs the signed amendment | medium | mitigate | Weights are seed data (11-02 writes the checkpoint-confirmed values); the service reads drop_weight from the DB — single source (Pitfall 8). |
| T-11-04-SC | Tampering | npm/pip/cargo installs | — | not-triggered | This plan installs no packages. |
</threat_model>

<verification>
- `npx vitest run src/services/sanguo/__tests__/shopService.test.ts src/services/sanguo/__tests__/bagService.test.ts src/services/sanguo/__tests__/dropService.test.ts` green.
- `npm run typecheck`, `npm run check-i18n` green.
- Wallet discipline: shopService has deductBalance; bagService + dropService have ZERO wallet calls (grep gates in acceptance criteria).
- The 11-06 plan wires dropService into the boss-win branch.
</verification>

## Artifacts this phase produces

| Symbol | Kind | File |
|--------|------|------|
| `buyItem(userId, itemCode)` → {name, qty, price} | wallet-sink tx (anti-tamper, saleState gate) | src/services/sanguo/shopService.ts |
| `buyFormation(userId, formationCode)` → {name, price} | wallet-sink tx (ALREADY_OWNED guard) | src/services/sanguo/shopService.ts |
| `listBag(userId)` / `useHeal(userId, itemCode, target)` | inventory tx (heal restore, NO_TARGET) | src/services/sanguo/bagService.ts |
| `rollBossDrop(userId, rng)` → {itemCode, quantity} | crypto-weighted guaranteed drop (D-14) | src/services/sanguo/dropService.ts |
| /sanguo shop + /sanguo bag subcommands + handlers | commands (map.ts registered) | src/commands/sanguo/shop.ts, bag.ts |
| sanguoShopTabs / sanguoShopBuyButtons / sanguoBagUseButtons | UI components | src/ui/components/ |
| buildSanguoShopEmbed / buildSanguoBagEmbed | embeds (SEASON color) | src/ui/embeds/ |
| customId routing: sanguo:shop:tab/buy, sanguo:bag:use | event routing | src/events/interactionCreate.ts |
| i18n sections: shop.*/bag.*/cmd.shop/cmd.bag (3 locales) | i18n | locales/{vi,en,zh-cn}/sanguo.json |
| shopService/bagService/dropService test suites | tests | src/services/sanguo/__tests__/ |

<success_criteria>
- TQC-16: /sanguo shop + bag work; every Linh thạch sink goes through wallet.deductBalance with a ledger row.
- Boss drops are guaranteed ≥1 item, rarity-weighted, items only (D-14/D-19).
- capture_key is shown-but-locked (D-15); the Event tab is non-empty by construction (UI-SPEC).
- The D-04 soft-lock recovery path (heal) works from the bag.
</success_criteria>

<output>
Create `.planning/phases/11-progression-chemistry-economy-depth/11-04-SUMMARY.md` when done.
</output>
