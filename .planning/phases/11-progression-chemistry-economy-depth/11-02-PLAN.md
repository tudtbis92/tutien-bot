---
phase: 11-progression-chemistry-economy-depth
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - src/constants/sanguoProgression.ts
  - src/constants/sanguoChemistry.ts
  - src/constants/__tests__/sanguoProgression.test.ts
  - src/constants/__tests__/sanguoChemistry.test.ts
  - scripts/data/sanguo-skills.json
  - scripts/data/sanguo-items.json
  - scripts/data/sanguo-formations.json
  - scripts/seed-sanguo.ts
  - src/ui/theme.ts
autonomous: true
requirements:
  - TQC-14
  - TQC-15
  - TQC-16
  - TQC-17

estimate:
  tokens: 42000
  raw_tokens: 21000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "src/constants/sanguoProgression.ts carries the signed balance contract (hidden, D-12 never-rendered): LEVEL_COST(level) = 1 + Math.floor((level−1)²/200) identical across tiers (D-05), STAT_GAIN_PER_LEVEL = 2 on the 6 battle stats (D-08), tier multipliers t0 1.0 / t1 1.1 / t2 1.25 / t3 1.5 (D-07), evolution costs 20/50/100 hồn ngọc (t0→t1→t2→t3, D-06/D-09), skill re-roll cost 10 hồn ngọc/slot (D-32)."
    - "src/constants/sanguoChemistry.ts carries the EA FC-grounded chemistry contract (hidden): family + spouse 3pt, faction 2pt, role 1pt (D-19 hierarchy); tiers S≥12→+10% / A 8-11→+8% / B 5-7→+6% / C 3-4→+4% / D 1-2→+2% / 0→no tier (bonus-only, no penalty); main↔support pairs only (D-17); strict class-match (D-20)."
    - "scripts/data/sanguo-items.json replaces the seed-sanguo.ts placeholder SANGUO_ITEMS (heal_pill 10n / xian_tea 25n / qinglong_dan 120n) with the D-11 catalog: heal_pill (50💎 sold, dropWeight 70), booster_x2 (100💎 sold, effect x2 next conversion D-12, dropWeight 25), capture_key (sale_state locked — shown not sold, D-15; dropWeight 0 — NOT in the drop pool) + capture_tier4_key / capture_tier5_key (sale_state locked, drop-only, dropWeight 4.9 / 0.1 — the T4/T5 requiresItem gates from sanguoCapture.ts) — the checkpoint-confirmed drop weights 70/25/4.9/0.1."
    - "scripts/data/sanguo-skills.json + sanguo-formations.json seed the class-based skill pools (normal + special per class, rarity common/rare/epic, D-30) and the formation catalog (starter free + 2 purchasable 200-500💎, D-21) — all three datasets FATAL-required by the extended seed-sanguo.ts (mirror sanguo-base-stats.json loader)."
    - "src/ui/theme.ts gains EXACTLY ONE addition: EMOJI.HON_NGOC: '🧿' (verified distinct from 💎 LINH_THACH and 💠 linh_khi_tinh in src/db/seed.ts:68 — UI-SPEC flag) — no other theme change."
  artifacts:
    - src/constants/sanguoProgression.ts — LEVEL_COST, STAT_GAIN_PER_LEVEL, TIER_MULTIPLIERS, EVOLUTION_COSTS, REROLL_COST
    - src/constants/sanguoChemistry.ts — CHEMISTRY_POINTS, CHEMISTRY_TIERS, (support trigger in 11-04/05)
    - src/constants/__tests__/sanguoProgression.test.ts + sanguoChemistry.test.ts
    - scripts/data/sanguo-skills.json, sanguo-items.json, sanguo-formations.json
    - scripts/seed-sanguo.ts — 3 FATAL datasets + D-11 item catalog + idempotent upserts
  key_links:
    - "sanguoProgression.ts LEVEL_COST ↔ soulgemService levelUp (11-03) — the cost curve the level button label and the deductHonNgoc charge read."
    - "sanguoItems price/sale_state ↔ shopService buyItem (11-04) — ITEM_NOT_FOR_SALE on capture_key (saleState !== 'sold')."
    - "sanguoSkills.ts schema (11-01) ↔ sanguo-skills.json seed ↔ skillService spawn roll (11-06) — the catalog rows the encounter roll picks from."
    - "formations emoji column + sanguo-formations.json ↔ legion assembly embed (11-07) — content-driven emoji, never theme constants."
  prohibitions:
    - "No balance numbers in ANY UI surface or customId — progression/chemistry constants are hidden mechanics (D-12), only spendable costs render (hồn ngọc 🧿 / Linh thạch 💎 / MP)."
    - "No theme.ts change beyond EMOJI.HON_NGOC — COLORS stays locked (UI-SPEC); no new color keys, no new hex values."
    - "No skill names in seed JSON as display strings — skill names are i18n keys (sanguo:skills.*), the seed stores mechanics + emoji only."
    - "No item emoji as theme constants — content-driven emoji columns in the seed tables (RESEARCH don't-hand-roll)."
  flagged_assumptions:
    - "FLAGGED ASSUMPTION (A1/A2): the level curve 1+⌊(L−1)²/200⌋ (L1→21 ≈ 27, L1→51 ≈ 264, L1→100 ≈ 1741) + STAT_GAIN_PER_LEVEL 2 + tier multipliers 1.0/1.1/1.25/1.5 are the balance contract — pure constant knobs; the 11-08 balance pass re-sanitizes them against the seeded stat ranges (Pitfall 4)."
    - "FLAGGED ASSUMPTION (A3): chemistry thresholds S≥12/A≥8/B≥5/C≥3/D≥1 and buffs +10/+8/+6/+4/+2% are proposals pending the balance pass; only the tier LABEL set S/A/B/C/D is UI-SPEC-locked."
    - "FLAGGED ASSUMPTION (A4): skill rarity weights normal 80/20, special 60/30/10 + MP +12/hit, special costs 15/25/40 are seed content — rebalancing is a seed edit, no economy re-sign needed (intra-collection loop)."
    - "FLAGGED ASSUMPTION (probe TQC-16 ordering, unresolved): bag/shop row order is stable — shop rows ordered by section (items then formations) then price asc; bag rows ordered by item id asc (UI-SPEC covered row)."

---

<objective>
What this plan accomplishes: **the balance/content layer — the two hidden-mechanics constants modules every Phase 11 service consumes (sanguoProgression.ts: level curve, stat gain, tier multipliers, evolution + reroll costs; sanguoChemistry.ts: link points, tier thresholds, buff %) and the three content seed datasets (skills, items, formations) that replace the Phase 8 placeholder SANGUO_ITEMS.** Task 1 authors the constants + sanity tests (pure functions, research-anchored values); Task 2 authors the three seed JSON datasets + extends seed-sanguo.ts with FATAL-required loaders and idempotent upserts, plus the single theme.ts addition (EMOJI.HON_NGOC).

Purpose: the balance contract and the content the shop (11-04), soulgem services (11-03), and skill spawn (11-06) all read. Constants before services (services import them); seed before shop/bag (the catalog must exist for purchases/drops to resolve).

Output: the two constants modules (+ tests) and the three content datasets seeded into the live DB.
</objective>

<execution_context>
@C:/Users/901107/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/901107/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-progression-chemistry-economy-depth/11-CONTEXT.md
@.planning/phases/11-progression-chemistry-economy-depth/11-RESEARCH.md
@.planning/phases/11-progression-chemistry-economy-depth/11-PATTERNS.md
@.planning/phases/11-progression-chemistry-economy-depth/11-01-PLAN.md
@src/constants/sanguoCapture.ts
@scripts/seed-sanguo.ts
@scripts/data/sanguo-base-stats.json
@src/ui/theme.ts
@src/db/schema/sanguoSkills.ts
@src/db/schema/sanguoItems.ts
@src/db/schema/formations.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: src/constants/sanguoProgression.ts + sanguoChemistry.ts — the hidden balance contract (TDD)</name>
  <files>src/constants/sanguoProgression.ts, src/constants/sanguoChemistry.ts, src/constants/__tests__/sanguoProgression.test.ts, src/constants/__tests__/sanguoChemistry.test.ts</files>
  <read_first>
    - src/constants/sanguoCapture.ts (the established hidden-constants module — signed-contract header + typed const tables + never-render rule, lines 1-25/43-49)
    - .planning/phases/11-progression-chemistry-economy-depth/11-RESEARCH.md §Pattern 1 (level curve + stat gain + tier multipliers table) + §Pattern 2 (chemistry points/tiers/buffs table)
    - .planning/phases/11-progression-chemistry-economy-depth/11-CONTEXT.md D-05/D-07/D-08/D-09/D-32 (progression) + D-19/D-20 (chemistry)
    - .planning/phases/11-progression-chemistry-economy-depth/11-PATTERNS.md §src/constants/sanguoProgression.ts + sanguoChemistry.ts (lines 201-213)
  </read_first>
  <behavior>
    - Test 1 (progression): LEVEL_COST(1) === 1, LEVEL_COST(2) === 1, LEVEL_COST(21) === 3, LEVEL_COST(51) === 13, LEVEL_COST(100) === 50 (formula 1 + Math.floor((L−1)²/200)); cost is identical for the same level regardless of tier (D-05).
    - Test 2 (progression): STAT_GAIN_PER_LEVEL === 2; TIER_MULTIPLIERS has exactly 4 entries {0:1.0, 1:1.1, 2:1.25, 3:1.5} strictly increasing; EVOLUTION_COSTS = {1:20, 2:50, 3:100} (t0→t1 20, t1→t2 50, t2→t3 100); REROLL_COST === 10.
    - Test 3 (chemistry): CHEMISTRY_POINTS = { family:3, spouse:3, faction:2, role:1 }; CHEMISTRY_TIERS thresholds strictly descending with buff strictly ascending — S min 12 buff 0.10, A min 8 buff 0.08, B min 5 buff 0.06, C min 3 buff 0.04, D min 1 buff 0.02, and a 0-point entry with buff 0 (bonus-only).
  </behavior>
  <action>
    Create `src/constants/sanguoProgression.ts` and `src/constants/sanguoChemistry.ts` following sanguoCapture.ts's header/const style:
    - `sanguoProgression.ts`: `export const LEVEL_COST = (level: number): number => 1 + Math.floor((level - 1) ** 2 / 200);` (valid for level 1..99 — the cost TO reach level+1; D-05 identical across tiers); `export const STAT_GAIN_PER_LEVEL = 2 as const;` (D-08 — each of the 6 IV stats gains +2 per level, HP/MP base-only); `export const TIER_MULTIPLIERS: Readonly<Record<number, number>> = { 0: 1.0, 1: 1.1, 2: 1.25, 3: 1.5 };` (D-07 applied to all 8 base stats); `export const EVOLUTION_COSTS: Readonly<Record<number, number>> = { 1: 20, 2: 50, 3: 100 };` (hồn ngọc cost to evolve INTO tier 1/2/3 — D-06/D-09); `export const REROLL_COST = 10 as const;` (D-32 per slot); `export const MAX_LEVEL = 100 as const;` (D-01). Header: D-12 never-rendered (hidden mechanics), D-19 net-sink (hồn ngọc only — never wallet), the values are the signed balance contract.
    - `sanguoChemistry.ts`: `export const CHEMISTRY_POINTS: Readonly<Record<'family'|'spouse'|'faction'|'role', number>> = { family: 3, spouse: 3, faction: 2, role: 1 };` (D-19 hierarchy: family+spouse tier-1 strongest, faction mid, role weakest — Phase 8 lock); `export const CHEMISTRY_TIERS: readonly { min: number; label: string | null; buff: number }[]` = S min 12 +10%, A min 8 +8%, B min 5 +6%, C min 3 +4%, D min 1 +2%, and a min-0 entry { min: 0, label: null, buff: 0 } (bonus-only — EA FC 0-chemistry grounding). Header: D-12 never-rendered (points and buff% NEVER reach a UI surface — only the tier label + link count render), D-20 strict class-match note.
    - Create both test files with the 3 behaviors above (pure constants/functions).
  </action>
  <verify>
    <automated>npx vitest run src/constants/__tests__/sanguoProgression.test.ts src/constants/__tests__/sanguoChemistry.test.ts; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Command: `npx vitest run src/constants/__tests__/sanguoProgression.test.ts src/constants/__tests__/sanguoChemistry.test.ts` exits 0 (behaviors 1-3 green).
    - Command: `npm run typecheck` exits 0.
    - Source: `src/constants/sanguoProgression.ts` exports `LEVEL_COST`, `STAT_GAIN_PER_LEVEL`, `TIER_MULTIPLIERS`, `EVOLUTION_COSTS`, `REROLL_COST`, `MAX_LEVEL`.
    - Source: `src/constants/sanguoChemistry.ts` exports `CHEMISTRY_POINTS`, `CHEMISTRY_TIERS` with the exact thresholds/buffs above.
    - Source: both files' headers mention the D-12 never-render rule; no import of wallet/db anywhere in the constants (pure modules).
  </acceptance_criteria>
  <done>Both hidden-mechanics constants modules exist, sanity-tested, typecheck-green — the exact numbers every Phase 11 service imports.</done>
</task>

<task type="auto">
  <name>Task 2: Content seed — sanguo-skills.json + sanguo-items.json + sanguo-formations.json + seed-sanguo.ts extension + theme HON_NGOC</name>
  <files>scripts/data/sanguo-skills.json, scripts/data/sanguo-items.json, scripts/data/sanguo-formations.json, scripts/seed-sanguo.ts, src/ui/theme.ts</files>
  <read_first>
    - scripts/seed-sanguo.ts (the file being extended — placeholder SANGUO_ITEMS at lines 312-337; FATAL loader pattern at 228-236; idempotent upsert at 352-368)
    - scripts/data/sanguo-base-stats.json (the dataset shape analog)
    - src/db/schema/sanguoItems.ts (new price/sale_state/drop_weight columns from 11-01)
    - src/db/schema/sanguoSkills.ts + formations.ts (the tables the seed writes)
    - src/ui/theme.ts (EMOJI block — the HON_NGOC addition point; verify 💠 clash against src/db/seed.ts:68)
    - .planning/phases/11-progression-chemistry-economy-depth/11-RESEARCH.md §Common Operation 8 (seed extension pattern) + §Pattern 3 (skill pools/weights) + Assumptions A4/A5
    - .planning/phases/11-progression-chemistry-economy-depth/11-PATTERNS.md §scripts/data + seed-sanguo.ts (lines 283-293)
  </read_first>
  <action>
    Author the three seed datasets + extend seed-sanguo.ts:
    1. `scripts/data/sanguo-skills.json` — catalog of class-based skills. Each entry: { skillId (code string, e.g. 'vu_co_attack_up'), class (8-class enum), slot ('normal'|'special'), rarity ('common'|'rare'|'epic'), mpCost (special: 15 common / 25 rare / 40 epic), mpGain (normal: 12), effectType ('damage'|'attack_up'|'hp_regen'|'mp_regen'), effectValue (damage: multiplier on the class stat pair, attack_up: 20, hp_regen: 15 (% of main max HP), mp_regen: 10), emoji }. Every class MUST have a normal pool (damage skills, common 80% / rare 20% weight) AND a special pool (damage + at least one support-type special for vu_co per D-18's "buff sỹ khí" example, common 60 / rare 30 / epic 10). Names are i18n keys (sanguo:skills.*) — the seed carries mechanics + emoji ONLY.
     2. `scripts/data/sanguo-items.json` — D-11 catalog REPLACING the placeholder SANGUO_ITEMS (heal_pill 10n / xian_tea 25n / qinglong_dan 120n — REMOVE those placeholder codes): heal_pill { priceLinh: 50, saleState: 'sold', dropWeight: 70, emoji, per-locale names (nameVi/nameEn/nameZh — content-in-DB D-07 Phase 8) }, booster_x2 { priceLinh: 100, saleState: 'sold', dropWeight: 25, itemType: 'booster', ... }, capture_key { priceLinh: 0, saleState: 'locked', dropWeight: 0, ... } (generic locked key — shown not sold, D-15, and NOT in the drop pool: dropService's WHERE drop_weight > 0 excludes it) — plus capture_tier4_key { priceLinh: 0, saleState: 'locked', dropWeight: 4.9, ... } / capture_tier5_key { priceLinh: 0, saleState: 'locked', dropWeight: 0.1, ... } (the D-20 requiresItem codes 'capture_tier4_key'/'capture_tier5_key' from sanguoCapture.ts — the T4/T5 gated keys are the DROP-ONLY sourcing, D-14/D-15). Prices = checkpoint-confirmed values (50/100; formations 200-300-500 live in sanguo-formations.json).
    3. `scripts/data/sanguo-formations.json` — catalog: starter formation (code 'can_ban', basePrice 0 — FREE at onboarding D-21, slotCount 12) + 2 purchasable (200💎 / 300💎 or 500💎 per the checkpoint) with per-formation slot layout (12 slots: 3 mains + 9 support with class requirements per slot — class varchar matching formation_slots.class) + emoji. Prices from the 11-01 checkpoint.
    4. Extend `scripts/seed-sanguo.ts` — add three FATAL-required loaders (loadSkills/loadItems/loadFormations mirroring loadBaseStats at 228-236, process.exit(1) on missing); upsert skills on code, items on code, formations on code, formation_slots on (formationId, slotOrder), all with onConflictDoUpdate clobber-safe spreads (lines 352-368 pattern); REMOVE the placeholder SANGUO_ITEMS array + its upsert; delete rows whose codes are no longer in the catalog (xian_tea/qinglong_dan) to avoid orphan rows.
    5. `src/ui/theme.ts` — add EXACTLY `EMOJI.HON_NGOC: '🧿'` to the EMOJI block (verified distinct from 💎 LINH_THACH and 💠 linh_khi_tinh). No other theme change (UI-SPEC: COLORS unchanged, zero new color keys).
  </action>
  <verify>
    <automated>npm run seed:sanguo; npm run check-i18n</automated>
  </verify>
  <acceptance_criteria>
    - Command: `npm run seed:sanguo` exits 0 and re-runs idempotently (run twice — row counts stable).
    - Command: node probe confirms `sanguo_items` has the D-11 catalog (heal_pill/booster_x2/capture_key/capture_tier4_key/capture_tier5_key) with sale_state 'locked' on all three keys + drop weights 70 (heal_pill) / 25 (booster_x2) / 4.9 (capture_tier4_key) / 0.1 (capture_tier5_key) / 0 (capture_key — generic key excluded from the drop pool); `sanguo_skills` has per-class normal+special entries; `formations` has 3 formations (starter basePrice 0 + 2 priced).
    - Command: `npm run check-i18n` exits 0 (no hardcoded string drift — seed data is content-in-DB, not i18n).
    - Source: `scripts/seed-sanguo.ts` no longer contains the placeholder codes xian_tea/qinglong_dan.
    - Source: `src/ui/theme.ts` diff is exactly +1 line (EMOJI.HON_NGOC) — no COLORS change.
  </acceptance_criteria>
  <done>The content layer is seeded: skills/items/formations catalogs live in the DB with the checkpoint-confirmed prices + D-11 item semantics, and theme.ts carries the hồn ngọc glyph.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed JSON ↔ live DB | Untrusted-at-write content enters the DB — the FATAL loader pattern (exit on missing dataset) prevents a silently-empty catalog (shop/bag/drop would resolve nothing). |
| constants ↔ UI | Hidden-mechanics numbers cross into render if a UI reads the wrong export — D-12 contract holds by structure (only spendable costs render). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-02-01 | Tampering | Price/drop-weight drift vs the signed amendment | medium | mitigate | The seed writes the checkpoint-confirmed values (11-01); Pitfall 8 single-source rule; the 11-08 compliance check re-verifies doc ↔ seed. |
| T-11-02-02 | Spoofing | Orphan/corrupt catalog rows (placeholder items surviving the swap) | medium | mitigate | Task 2 deletes codes no longer in the catalog (xian_tea/qinglong_dan); FATAL loader fails loudly on missing datasets. |
| T-11-02-03 | Information disclosure | Balance numbers (curve/multipliers/chemistry %) leaking to UI | high | mitigate | Constants modules are hidden (D-12); the never-render rule is structural (only tier label + link count + spendable costs reach embeds); no UI file imports these constants. |
| T-11-02-SC | Tampering | npm/pip/cargo installs | — | not-triggered | This plan installs no packages. |
</threat_model>

<verification>
- `npx vitest run src/constants/__tests__/sanguoProgression.test.ts src/constants/__tests__/sanguoChemistry.test.ts` green; `npm run typecheck` green.
- `npm run seed:sanguo` idempotent (double-run stable); DB probe confirms the catalogs.
- `npm run check-i18n` green (content-in-DB vs i18n split intact).
</verification>

## Artifacts this phase produces

| Symbol | Kind | File |
|--------|------|------|
| `LEVEL_COST(level)` = 1 + Math.floor((level−1)²/200) | pure function (hidden, D-05) | src/constants/sanguoProgression.ts |
| `STAT_GAIN_PER_LEVEL` (2) / `TIER_MULTIPLIERS` (1.0/1.1/1.25/1.5) | constants (D-08/D-07) | src/constants/sanguoProgression.ts |
| `EVOLUTION_COSTS` (20/50/100) / `REROLL_COST` (10) / `MAX_LEVEL` (100) | constants (D-06/D-32/D-01) | src/constants/sanguoProgression.ts |
| `CHEMISTRY_POINTS` (3/3/2/1) / `CHEMISTRY_TIERS` (S..D + 0) | constants (D-19, hidden) | src/constants/sanguoChemistry.ts |
| sanguoProgression.test.ts + sanguoChemistry.test.ts | test suites | src/constants/__tests__/ |
| sanguo-skills.json (class pools, rarity weights, MP/effects) | seed dataset (D-30) | scripts/data/sanguo-skills.json |
| sanguo-items.json (D-11 catalog + prices + drop weights) | seed dataset (D-16) | scripts/data/sanguo-items.json |
| sanguo-formations.json (starter + 2 purchasable + slots) | seed dataset (D-21) | scripts/data/sanguo-formations.json |
| seed-sanguo.ts: loadSkills/loadItems/loadFormations + upserts + placeholder removal | seed extension | scripts/seed-sanguo.ts |
| `EMOJI.HON_NGOC: '🧿'` | theme constant (only addition) | src/ui/theme.ts |

<success_criteria>
- The balance contract (progression + chemistry) is constant-first, hidden (D-12), sanity-tested.
- The D-11 item catalog + skill pools + formation catalog are seeded idempotently with checkpoint-confirmed prices.
- capture_key is 'locked' by construction; heal/booster are 'sold' at 50/100💎; no placeholder items remain.
</success_criteria>

<output>
Create `.planning/phases/11-progression-chemistry-economy-depth/11-02-SUMMARY.md` when done.
</output>
