---
phase: 05.1-proxy-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/db/schema/farming.ts, src/services/farming/proxyService.ts, src/services/farming/__tests__/proxyService.test.ts, src/workers/selfBotMaster.ts]
autonomous: true
requirements: [FARM-08]

must_haves:
  truths:
    - "Database schema includes proxies table and proxyId field in farming_accounts"
    - "Proxy assignment respects the 1:3 account-to-proxy ratio"
    - "Assignment prioritizes proxies with the lowest current usage"
    - "SelfBotMaster fetches and uses the assigned proxy URL from the proxies table"
  artifacts:
    - path: "src/db/schema/farming.ts"
      provides: "Proxy schema and relationship to accounts"
    - path: "src/services/farming/proxyService.ts"
      provides: "CRUD and assignment logic for proxies"
    - path: "src/workers/selfBotMaster.ts"
      provides: "Integration of proxy pool with worker processes"
---

<objective>
Implement the database schema for the proxy pool, the core ProxyService responsible for auto-assignment and usage tracking, and integrate it with the worker master.

Purpose: Provide a centralized way to manage proxies and ensure safe distribution among farming accounts to prevent Discord flagging.
Output: New proxies table, updated farming_accounts schema, ProxyService logic, and updated SelfBotMaster.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05.1-proxy-management/05.1-CONTEXT.md
@.planning/phases/05.1-proxy-management/05.1-RESEARCH.md
@src/db/schema/farming.ts
@src/workers/selfBotMaster.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Database Schema & Migration (per D-01, D-03)</name>
  <files>src/db/schema/farming.ts, migrations/</files>
  <behavior>
    - Add `proxyStatusEnum` with ['active', 'dead', 'maintenance'].
    - Define `proxies` table with: id (serial PK), url (text), location (text), provider (text), status (enum), usageCount (integer, default 0), createdAt, updatedAt.
    - Add `proxyId` (integer) to `farmingAccounts` as a foreign key to `proxies.id`.
    - Export `Proxy` and `NewProxy` types.
  </behavior>
  <action>
    Modify `src/db/schema/farming.ts` to implement the proxy pool schema as defined in RESEARCH.md.
    Then, run `npx drizzle-kit generate` to create the migration files.
    Finally, run `npm run migrate` to apply changes to the database.
  </action>
  <verify>
    <automated>npx drizzle-kit check</automated>
  </verify>
  <done>proxies table exists in DB and farming_accounts has proxy_id column.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement ProxyService (per D-02)</name>
  <files>src/services/farming/proxyService.ts</files>
  <behavior>
    - `getAvailableProxy()`: Returns an active proxy with usageCount < 3, ordered by usageCount ASC.
    - `assignProxy(userId)`: Finds an available proxy, updates farming_accounts, and increments proxy usageCount in a transaction.
    - `unassignProxy(userId)`: If user has a proxyId, decrements proxy usageCount and sets account proxyId to null.
  </behavior>
  <action>
    Create `src/services/farming/proxyService.ts`. Use Drizzle ORM transactions to ensure atomic updates to both `farming_accounts` and `proxies` tables.
    IMPORTANT: You MUST use `.for('update', { skipLocked: true })` in the Drizzle query when selecting a proxy to prevent concurrency race conditions.
    Implement the 1:3 limit strictly.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>ProxyService methods implemented and compiling.</done>
</task>

<task type="auto">
  <name>Task 3: Unit Tests for Assignment Logic</name>
  <files>src/services/farming/__tests__/proxyService.test.ts</files>
  <action>
    Create unit tests using `vitest` to verify:
    1. A proxy cannot be assigned more than 3 times.
    2. The system picks the proxy with the least usage first.
    3. Transactional integrity (mocking the DB client if necessary).
  </action>
  <verify>
    <automated>npx vitest src/services/farming/__tests__/proxyService.test.ts</automated>
  </verify>
  <done>Tests pass for assignment limits and prioritization.</done>
</task>

<task type="auto">
  <name>Task 4: Update SelfBotMaster to use Proxy Pool</name>
  <files>src/workers/selfBotMaster.ts</files>
  <behavior>
    - Join `farmingAccounts` with `proxies` table in `rebalance()` method.
    - Prioritize `proxies.url` over `farmingAccounts.proxyUrl`.
  </behavior>
  <action>
    Modify `src/workers/selfBotMaster.ts`'s `rebalance` method. Use a left join between `farmingAccounts` and `proxies` to fetch the assigned proxy URL. Update the bot configuration to use this URL.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>SelfBotMaster now consumes proxies from the managed pool.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Database → ProxyService | Assumed trusted internal data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.1-01 | Tampering | DB Schema | mitigate | Use Drizzle migrations for controlled changes |
| T-05.1-02 | Denial of Service | Proxy Pool | mitigate | Enforce 1:3 ratio to prevent overloading any single proxy |
</threat_model>

<verification>
Check DB schema: `psql $DATABASE_URL -c "\d proxies"` and `psql $DATABASE_URL -c "\d farming_accounts"`.
Verify ProxyService logic via unit tests.
Verify SelfBotMaster.ts joins with proxies table.
</verification>

<success_criteria>
- Schema migration completed successfully.
- ProxyService methods handle assignment and usage tracking correctly.
- Tests confirm the 3-account-per-proxy limit.
</success_criteria>

<output>
Create `.planning/phases/05.1-proxy-management/05.1-01-SUMMARY.md` when done
</output>
