---
phase: "02.2"
plan: "05"
type: execute
wave: 2
depends_on: ["02.2-01"]
files_modified:
  - locales/vi/football.json
  - locales/en/football.json
  - locales/zh-cn/football.json
  - src/i18n/index.ts
  - src/commands/predictions/predictions.ts
  - src/commands/predictions/config.ts
  - src/commands/predictions/index.ts
  - .env.example
autonomous: true
requirements:
  - PRED-11
  - PRED-12
must_haves:
  truths:
    - "All prediction-related strings available in VI, EN, ZH-CN locale files"
    - "i18n namespace 'football' registered in i18n init"
    - "/predictions command shows user's active bets and history"
    - "/config predictions off disables all predictions for the guild"
    - "/config predictions on enables all predictions for the guild"
    - "/config predictions league <league> toggles specific league on/off"
    - "API_FOOTBALL_KEYS env var documented in .env.example"
  artifacts:
    - path: "locales/vi/football.json"
      provides: "Vietnamese translations for all prediction strings"
      contains: '"predictions":'
    - path: "locales/en/football.json"
      provides: "English translations for all prediction strings"
      contains: '"predictions":'
    - path: "locales/zh-cn/football.json"
      provides: "Chinese translations for all prediction strings"
      contains: '"predictions":'
    - path: "src/commands/predictions/predictions.ts"
      provides: "/predictions slash command for bet status and history"
      exports: ["default"]
    - path: "src/commands/predictions/config.ts"
      provides: "/config predictions admin command with on/off/league subcommands"
      exports: ["default"]
  key_links:
    - from: "src/i18n/index.ts"
      to: "locales/*/football.json"
      via: "namespace registration"
      pattern: "ns:.*'football'"
    - from: "src/commands/predictions/predictions.ts"
      to: "src/db/schema/footballBets.ts"
      via: "query user's bets"
      pattern: "db\\.select.*from.*footballBets"
    - from: "src/commands/predictions/config.ts"
      to: "src/db/schema/predictionChannels.ts"
      via: "UPSERT enabled for (guildId, channelId, league_id)"
      pattern: "db\\.insert.*predictionChannels"
---

<objective>
Add i18n translations for all prediction strings, create /predictions and /config slash commands, and document required environment variables.

Purpose: Complete the feature with localized strings, user-facing commands for checking bets, and admin configuration for prediction channels.
Output: 3 locale files, i18n namespace registration, 2 slash commands, .env.example updated.
</objective>

<execution_context>
@C:/Users/901107/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/901107/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02.2-add-football-prediction-event-system/02.2-CONTEXT.md
@.planning/phases/02.2-add-football-prediction-event-system/02.2-RESEARCH.md
@locales/vi/game.json — existing i18n structure, key naming conventions
@locales/en/game.json
@locales/zh-cn/game.json
@src/i18n/index.ts — namespace registration
@src/commands/game/start.ts — command structure pattern (SlashCommandBuilder, export default)
@src/commands/game/profile.ts — command with embed reply pattern
@src/db/schema/footballBets.ts — bet data for /predictions display
@src/db/schema/predictionChannels.ts — per-channel prediction config
@src/db/schema/predictionChannels.ts — per-channel per-league toggle (league_id=0 = global, league_id>0 = per-league)
@src/db/client.ts
@src/utils/commandContext.ts

<interfaces>
From src/i18n/index.ts — namespace registration:
```typescript
ns: ['common', 'game', 'combat', 'marketplace', 'admin'],
```
Need to add 'football' to this array.

From locales/vi/game.json — key structure pattern:
```json
{
  "start": {
    "title": "...",
    "description": "...",
    "not_registered": "..."
  },
  "profile": { ... }
}
```

From src/commands/game/start.ts — command pattern:
```typescript
import { SlashCommandBuilder, ChatInputCommandInteraction } from 'discord.js';
export const data = new SlashCommandBuilder()...;
export async function execute(interaction: ChatInputCommandInteraction) { ... }
// NOTE: Named exports only — no default export. loadCommands() uses { data, execute } named imports.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create i18n locale files for football prediction namespace</name>
  <files>locales/vi/football.json, locales/en/football.json, locales/zh-cn/football.json, src/i18n/index.ts</files>
  <read_first>
    - locales/vi/game.json — key naming conventions, string patterns
    - locales/en/game.json — English baseline
    - locales/zh-cn/game.json — Chinese translations pattern
    - src/i18n/index.ts — ns array to update
  </read_first>
  <action>
    Update `src/i18n/index.ts`:
    - Add 'football' to the `ns` array: `ns: ['common', 'game', 'combat', 'marketplace', 'admin', 'football']`.

    Create `locales/vi/football.json` with keys organized by feature:
    - `predictions.title`: "⚽ Dự Đoán Bóng Đá"
    - `predictions.not_registered`: "Bạn cần tạo nhân vật trước khi đặt cược."
    - `predictions.match_not_found`: "Không tìm thấy trận đấu này."
    - `predictions.already_started`: "Trận đấu đã bắt đầu. Không thể đặt cược."
    - `predictions.insufficient_balance`: "Không đủ linh thạch. Cần tối thiểu {{amount}}."
    - `predictions.bet_too_low`: "Cược tối thiểu là {{min}} linh thạch."
    - `predictions.bet_too_high`: "Cược tối đa là {{max}} linh thạch."
    - `predictions.result_selected`: "Bạn đã chọn: {{prediction}}. Xác nhận cược để hoàn tất."
    - `predictions.score_selected`: "Bạn đã chọn tỉ số: {{score}}. Xác nhận cược để hoàn tất."
    - `predictions.bet_placed`: "Đặt cược thành công! {{prediction}} với {{wager}} linh thạch. Thanh toán tiềm năng: {{payout}}."
    - `predictions.bet_edited`: "Dự đoán đã được cập nhật. Cược cũ đã hoàn tiền."
    - `predictions.void_refund`: "Trận đấu bị hủy. Cược của bạn đã được hoàn tiền: {{amount}} linh thạch."
    - `predictions.bet_won`: "Chúc mừng! Bạn thắng cược {{wager}} → nhận {{payout}} linh thạch."
    - `predictions.bet_lost`: "Rất tiếc, cược của bạn không trúng. Thua {{wager}} linh thạch."
    - `predictions.list_title`: "Các trận đang mở cược"
    - `predictions.no_active`: "Hiện không có trận nào đang mở cược."
    - `predictions.history_title`: "Lịch sử cược của bạn"
    - `predictions.no_history`: "Bạn chưa đặt cược trận nào."
    - `predictions.status.pending`: "Đang chờ"
    - `predictions.status.won`: "Thắng"
    - `predictions.status.lost`: "Thua"
    - `predictions.status.void`: "Hủy"
    - `embed.choose_result`: "Chọn kết quả trận đấu..."
    - `embed.home_win`: "Chủ nhà thắng"
    - `embed.draw`: "Hòa"
    - `embed.away_win`: "Khách thắng"
    - `embed.confirm`: "Xác nhận dự đoán"
    - `embed.odds_label`: "Tỷ lệ cược"
    - `embed.live_score`: "TRỰC TIẾP"
    - `config.channel_set`: "Đã cài kênh dự đoán: {{channel}}."
    - `config.enabled`: "Đã bật dự đoán bóng đá cho kênh này."
    - `config.disabled`: "Đã tắt dự đoán bóng đá cho kênh này."
    - `config.league_toggled`: "{{league}}: {{status}}"
    - `config.league_status.on`: "ĐÃ BẬT"
    - `config.league_status.off`: "ĐÃ TẮT"
    - `config.admin_only`: "Chỉ admin server mới có thể thay đổi cài đặt dự đoán."

    Create `locales/en/football.json` with English equivalents:
    - Mirror all keys above with English translations.
    - "predictions.title": "⚽ Football Predictions"
    - "predictions.not_registered": "You need to create a character before placing bets."
    - etc.

    Create `locales/zh-cn/football.json` with Chinese equivalents:
    - Mirror all keys above with Chinese translations.
    - "predictions.title": "⚽ 足球预测"
    - "predictions.not_registered": "你需要先创建角色才能下注。"
    - etc.
  </action>
  <verify>
    <automated>node -e "['vi','en','zh-cn'].forEach(l => { const d = require('./locales/'+l+'/football.json'); console.log(l, Object.keys(d).length, 'keys'); })" — each file has 20+ keys</automated>
  </verify>
  <acceptance_criteria>
    - locales/vi/football.json exists with valid JSON, 20+ keys
    - locales/en/football.json exists with valid JSON, same keys as vi
    - locales/zh-cn/football.json exists with valid JSON, same keys as vi
    - All 3 files have matching key structure (same keys, different values)
    - src/i18n/index.ts ns array includes 'football'
    - Key categories present: predictions.*, embed.*, config.*, predictions.status.*
    - No hardcoded user-facing strings in any football.json value
  </acceptance_criteria>
  <done>All 3 locale files created with matching keys, i18n namespace registered</done>
</task>

<task type="auto">
  <name>Task 2: Create /predictions and /config slash commands + update .env.example</name>
  <files>src/commands/predictions/predictions.ts, src/commands/predictions/config.ts, src/commands/predictions/index.ts, .env.example</files>
  <read_first>
    - src/commands/game/start.ts — command export pattern
    - src/commands/game/profile.ts — embed reply pattern, pagination if any
    - src/db/schema/footballBets.ts — bet schema for query
    - src/db/schema/predictionChannels.ts — prediction_channels for per-channel + per-league toggle (league_id=0 = global)
    - src/utils/commandContext.ts — fetchCommandContext pattern
    - src/ui/embeds/buildSuccessEmbed.ts — success embed pattern
    - src/ui/embeds/buildErrorEmbed.ts — error embed pattern
  </read_first>
  <action>
    Create `src/commands/predictions/predictions.ts`:
    - SlashCommandBuilder: name 'predictions', description localized via t().
    - Add subcommand 'status': shows user's active (pending) bets.
    - Add subcommand 'history': shows user's resolved bets (won/lost/void) with pagination.
    - Execute handler:
      - Fetch user by interaction.user.id.
      - For 'status': SELECT from football_bets WHERE user_id = X AND status = 'pending', JOIN football_matches for match details. Display as embed with list: "Match | Prediction | Wager | Potential Payout".
      - For 'history': SELECT from football_bets WHERE user_id = X AND status != 'pending', ORDER BY resolved_at DESC, LIMIT 10. Display as embed with result indicators (✅/❌/⚪).
      - If no bets found, show appropriate message from i18n.
      - Use buildSuccessEmbed for results, buildErrorEmbed for errors.
    - Export named `data` and `execute` (NOT default export — codebase uses named exports pattern).

    Create `src/commands/predictions/config.ts`:
    - SlashCommandBuilder: name 'config', description localized.
    - Add subcommand 'predictions' with options:
      - `action` (type String, required, choices: 'on', 'off', 'league')
      - `league` (type String, required if action='league', choices: league names from CURATED_LEAGUES — e.g., 'Premier League', 'La Liga', etc.)
    - Require admin permission (Interaction.member.permissions.has('Administrator')).
    - Execute handler:
      - Get `channelId` from `interaction.channelId` — the channel where the command was invoked.
      - Get `guildId` from `interaction.guildId`.
      - **action = 'on':** UPSERT `prediction_channels` SET enabled = true WHERE guild_id = guildId AND channel_id = channelId AND league_id = 0. Reply "Đã bật dự đoán bóng đá cho kênh này."
      - **action = 'off':** UPSERT `prediction_channels` SET enabled = false WHERE guild_id = guildId AND channel_id = channelId AND league_id = 0. Reply "Đã tắt dự đoán bóng đá cho kênh này."
      - **action = 'league':**
        - Look up league_id from CURATED_LEAGUES by name.
        - Check if row exists in `prediction_channels` for (guildId, channelId, league_id).
        - If no row: INSERT with enabled = true (default on, first toggle turns it off).
        - If row exists: UPDATE enabled = NOT enabled (toggle).
        - Query new state, reply: "Premier League: ĐÃ BẬT" or "Premier League: ĐÃ TẮT".
    - Export named `data` and `execute` (NOT default export — codebase uses named exports pattern).

    Create `src/commands/predictions/index.ts`:
    - Re-export both commands for auto-discovery.

    Update `.env.example`:
    - Add `# API-Football API key(s) — comma-separated for key rotation (D-17)`.
    - Add `API_FOOTBALL_KEYS=your_api_key_here`.
    - Add comment: "⚠️ API-Football ToS forbids multiple free accounts. Use single paid key or member-invite keys from same account."
  </action>
  <verify>
    <automated>npx tsc --noEmit — exits 0 with no errors</automated>
  </verify>
  <acceptance_criteria>
    - src/commands/predictions/predictions.ts exports default command with data and execute
    - /predictions has 'status' subcommand showing pending bets
    - /predictions has 'history' subcommand showing resolved bets
    - Both subcommands JOIN football_matches for match details
    - src/commands/predictions/config.ts exports default command with data and execute
    - /config predictions uses interaction.channelId as target channel (no channel parameter needed)
    - /config predictions action='on' UPSERTS prediction_channels SET enabled=true WHERE (guildId, channelId, league_id=0)
    - /config predictions action='off' UPSERTS prediction_channels SET enabled=false WHERE (guildId, channelId, league_id=0)
    - /config predictions action='league' toggles prediction_channels.enabled for (guildId, channelId, league_id)
    - /config predictions requires Administrator permission
    - League choices in slash command match CURATED_LEAGUES names
    - src/commands/predictions/index.ts re-exports both commands
    - .env.example contains API_FOOTBALL_KEYS with documentation comment
    - .env.example includes ToS warning about multiple free accounts
    - All user-facing strings use t() from i18n — no hardcoded strings
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Commands compile, i18n-integrated, .env.example updated with API key documentation</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /config → guild_settings | Only admins can change prediction channel |
| /predictions → football_bets | Users can only see their own bets |
| .env.example → production | API keys must never be committed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.2-19 | Authorization | /config predictions-channel | mitigate | Check Administrator permission before allowing channel config |
| T-02.2-20 | Information Disclosure | /predictions shows other users' bets | mitigate | WHERE clause filters by user_id — users only see own bets |
| T-02.2-21 | Information Disclosure | API keys in .env committed to git | mitigate | .env in .gitignore; .env.example has placeholder only |
| T-02.2-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages required |
</threat_model>

<verification>
- All 3 locale files have matching key structure
- i18n namespace 'football' registered and loads on startup
- /predictions status shows pending bets with match details
- /predictions history shows resolved bets with outcomes
- /config predictions on/off toggles global prediction for the channel
- /config predictions league toggles specific league on/off for the channel
- .env.example documents API_FOOTBALL_KEYS with ToS warning
- TypeScript compiles cleanly
</verification>

<success_criteria>
- 3 locale files with 20+ matching keys each (VI, EN, ZH-CN)
- i18n init includes 'football' namespace
- /predictions command works with status and history subcommands
- /config predictions command works with on/off/league/channel actions
- .env.example updated with API key documentation
- Zero hardcoded user-facing strings in prediction code
- TypeScript type-check passes cleanly
</success_criteria>

<output>
Create `.planning/phases/02.2-add-football-prediction-event-system/02.2-05-SUMMARY.md` when done
</output>
