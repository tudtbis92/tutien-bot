---
phase: "02.2-add-football-prediction-event-system"
plan: "02"
subsystem: api
tags: [typescript, postgres, drizzle-orm, api-football, vitest]

requires:
  - phase: "02.2-01"
    provides: "DB schemas for apiCache, footballMatches, footballBets, predictionChannels"
provides:
  - "FootballApiClient class with key rotation, 429 backoff handling, and apiCache DB caching"
  - "oddsCalculator module with BIGINT-safe payout calculations, bet amount validation, and odds parsers"
affects: ["02.2-03"]

tech-stack:
  added: []
  patterns:
    - "Round-robin API key rotation with cooldown tracking map"
    - "PostgreSQL apiCache lookup and upsert on successful fetch"
    - "BIGINT-safe integer multiplication and division for decimal odds payout calculation"

key-files:
  created:
    - "src/services/football/apiClient.ts"
    - "src/services/football/oddsCalculator.ts"
    - "src/services/football/__tests__/apiClient.test.ts"
    - "src/services/football/__tests__/oddsCalculator.test.ts"
  modified:
    - "src/config.ts"

key-decisions:
  - "Used integer math with 10000x scaling for decimal odds payout calculation to prevent floating-point precision loss on BIGINT."
  - "Utilized a robust key cooldown mapping (skipping rate-limited keys for 60s) to handle 429 rate limiting gracefully."

requirements-completed:
  - PRED-01
  - PRED-02
  - PRED-06
  - PRED-08

duration: 25min
completed: 2026-05-21
---

# Phase 02.2: Add Football Prediction Event System - Plan 02 Summary

**API-Football client service with round-robin key rotation and DB caching, plus a BIGINT-safe odds calculator for payout math**

## Performance

- **Duration:** 25 min
- **Started:** 2026-05-21T10:37:23+07:00
- **Completed:** 2026-05-21T10:42:00+07:00
- **Tasks:** 2 completed
- **Files modified:** 5

## Accomplishments
- Implemented `FootballApiClient` with `fetch()`, key rotation, 429 status code handling with backoff, and PostgreSQL `api_cache` integrations.
- Created helper methods `getFixtures`, `getFixtureOdds`, `getFixtureScores`, and `getFixtureResult` in `FootballApiClient`.
- Integrated `API_FOOTBALL_KEYS` into the app config validation schema in `src/config.ts`.
- Implemented `oddsCalculator` containing `calculatePayout` (BIGINT-safe math), `parseOdds`, `validateBetAmount`, and `extractCorrectScoreOdds`.
- Developed complete Vitest unit test suites for both `apiClient.ts` and `oddsCalculator.ts`, validating rate-limiting, key rotation, database cache hits, and math calculations under 100% green tests.

## Files Created/Modified
- [apiClient.ts](file:///E:/Saeth/tutien-bot/src/services/football/apiClient.ts) - FootballApiClient class and apiFootballFetch helper
- [oddsCalculator.ts](file:///E:/Saeth/tutien-bot/src/services/football/oddsCalculator.ts) - BIGINT payout math and odds parsers
- [config.ts](file:///E:/Saeth/tutien-bot/src/config.ts) - Added API_FOOTBALL_KEYS validator
- [apiClient.test.ts](file:///E:/Saeth/tutien-bot/src/services/football/__tests__/apiClient.test.ts) - Unit tests for FootballApiClient (caching, key rotation)
- [oddsCalculator.test.ts](file:///E:/Saeth/tutien-bot/src/services/football/__tests__/oddsCalculator.test.ts) - Unit tests for payout math, odds extraction, and validation

## Decisions Made
- None - followed the plan as specified.

## Deviations from Plan
- None - plan executed exactly as written.

## Issues Encountered
- **Vitest Hoisting Mock Issue:** In `apiClient.test.ts`, the mock functions (`mockSelect`, etc.) were referenced before initialization because Vitest hoists `vi.mock()` calls to the top of the file.
  - *Resolution:* Utilized `vi.hoisted()` to initialize variables and process environment configurations before module imports. This is the recommended practice for Vitest 3.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Core services (`FootballApiClient`, `oddsCalculator`) are ready.
- The next step (`02.2-03`) can proceed with the betting UI components, the predictions commands, and the match lifecycle orchestrator.

---
*Phase: 02.2-add-football-prediction-event-system*
*Completed: 2026-05-21*
