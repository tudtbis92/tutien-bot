---
phase: "02.2-add-football-prediction-event-system"
plan: "05"
subsystem: "predictions"
tags: [i18n, football, predictions, discord.js, slash-commands]

# Dependency graph
requires:
  - phase: "02.2-01"
    provides: "football schema (footballBets, predictionChannels, footballMatches)"
provides:
  - "Football prediction i18n strings in VI/EN/ZH-CN"
  - "/predictions slash command with status and history subcommands"
  - "/config predictions admin command with on/off/league toggles"
  - "API_FOOTBALL_KEYS documented in .env.example"
affects: ["02.2-06", "02.2-ui"]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "i18n namespace 'football' added to existing ns array"
    - "Named exports { data, execute } for slash commands"
    - "UPSERT via onConflictDoUpdate for prediction channel config"

key-files:
  created:
    - locales/vi/football.json
    - locales/en/football.json
    - locales/zh-cn/football.json
    - src/commands/predictions/predictions.ts
    - src/commands/predictions/config.ts
    - src/commands/predictions/index.ts
  modified:
    - src/i18n/index.ts
    - .env.example

key-decisions:
  - "53 keys per locale file across 4 categories: predictions.*, embed.*, status.*, config.*"
  - "predictions.page key added for pagination i18n (hardcoded 'Trang' fixed)"
  - "Named export pattern (data + execute) consistency with existing commands"
  - "interaction.channelId used as target channel — no channel parameter needed"
  - "league_id=0 for global on/off, league_id>0 for per-league toggle"

patterns-established:
  - "i18n namespace naming: 'football' as top-level key in i18n init"
  - "Command file exports: named exports only (no default exports)"
  - "Subcommand pattern: status and history for /predictions"

requirements-completed:
  - PRED-11
  - PRED-12

# Metrics
duration: 12min
completed: 2026-05-21
---

# Phase 02.2 Plan 05: i18n + Slash Commands + .env.example Summary

**Football prediction i18n strings (53 keys in VI/EN/ZH-CN), /predictions and /config slash commands, and API_FOOTBALL_KEYS environment documentation**

## Performance

- **Duration:** 12 min
- **Completed:** 2026-05-21
- **Tasks:** 2
- **Files modified/created:** 8

## Accomplishments

- Created 3 locale files (vi/en/zh-cn) with 53 matching keys each across predictions, embed, status, and config categories
- Registered 'football' i18n namespace in `src/i18n/index.ts`
- Created `/predictions` command with 'status' (active bets) and 'history' (resolved bets) subcommands — both JOIN football_matches for match details
- Created `/config predictions` admin command with on/off/league actions — UPSERT via onConflictDoUpdate for per-channel + per-league toggling
- Created `src/commands/predictions/index.ts` re-exporting both commands for auto-discovery
- Updated `.env.example` with `API_FOOTBALL_KEYS` and API-Football ToS warning

## Task Commits

Each task was committed atomically:

1. **Task 1: Create i18n locale files for football prediction namespace** - `4d98976` (feat)
2. **Task 2: Create /predictions and /config slash commands + update .env.example** - `ca04d9b` (feat)

## Files Created/Modified

- `locales/vi/football.json` — 53 Vietnamese prediction keys
- `locales/en/football.json` — 53 English prediction keys
- `locales/zh-cn/football.json` — 53 Chinese prediction keys
- `src/i18n/index.ts` — 'football' added to ns array
- `src/commands/predictions/predictions.ts` — /predictions with status + history subcommands
- `src/commands/predictions/config.ts` — /config predictions with on/off/league actions
- `src/commands/predictions/index.ts` — re-exports both commands
- `.env.example` — API_FOOTBALL_KEYS documented with ToS warning

## Decisions Made

- **53 keys per locale file** — Covers predictions.* (18 key paths), embed.* (14 keys), status.* (8 match statuses), config.* (9 config paths). All 3 files share identical key structure.
- **predictions.page key added** — Fixed hardcoded Vietnamese "Trang" in pagination title to use i18n (Rule 2 fix)
- **Named exports** — Both command files export `data` and `execute` (NOT default export) to match project conventions
- **interaction.channelId** — The /config command uses the invocation channel's ID directly, no separate channel parameter
- **league_id=0** — Global on/off uses league_id=0; per-league toggle uses actual league IDs from CURATED_LEAGUES

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical] Added i18n for pagination page indicator**
- **Found during:** Task 1 verification (acceptance criteria check)
- **Issue:** Line 104 of predictions.ts had hardcoded `(Trang ...)` (Vietnamese) in the history title — violates "zero hardcoded user-facing strings" criteria
- **Fix:** Added `predictions.page` key to all 3 locale files (VI: "Trang {{current}}/{{total}}", EN: "Page {{current}}/{{total}}", ZH-CN: "第 {{current}}/{{total}} 页"). Updated predictions.ts to use `t('football:predictions.page', { current, total })`
- **Files modified:** locales/vi/football.json, locales/en/football.json, locales/zh-cn/football.json, src/commands/predictions/predictions.ts
- **Verification:** TypeScript compiles clean, locale keys match across all 3 files
- **Committed in:** 4d98976 (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (1 missing critical)
**Impact on plan:** Minor — essential for i18n compliance. No scope creep.

## Issues Encountered

None — plan executed as specified. The only deviation was the auto-fixed hardcoded string caught during acceptance criteria verification.

## User Setup Required

None — no external service configuration required for this plan. API key documentation added to .env.example.

## Next Phase Readiness

- All prediction strings localized in 3 languages
- `/predictions` command ready for user interaction (status + history)
- `/config predictions` command ready for admin channel configuration
- TypeScript compiles cleanly
- Ready for remaining Phase 02.2 plans

## Self-Check: PASSED

- [x] All 8 files exist on disk
- [x] Both commits (4d98976, ca04d9b) present in git log
- [x] `npx tsc --noEmit` exits 0 with no errors
- [x] Locale key structures match across vi/en/zh-cn (53 keys each)
- [x] All acceptance criteria verified

---

*Phase: 02.2-add-football-prediction-event-system*
*Completed: 2026-05-21*
