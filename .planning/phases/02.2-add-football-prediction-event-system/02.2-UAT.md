---
status: partial
phase: 02.2-add-football-prediction-event-system
source:
  - 02.2-01-SUMMARY.md
  - 02.2-02-SUMMARY.md
  - 02.2-03-SUMMARY.md
  - 02.2-04-SUMMARY.md
  - 02.2-05-SUMMARY.md
started: 2026-05-21T11:53:00Z
updated: 2026-05-21T12:01:00Z
---

## Current Test

[testing paused — 10 items blocked by fixture data]

## Tests

### 1. Cold Start Smoke Test
expected: Kill any running server. Start the application from scratch. Migration (0003_fat_pandemic.sql) runs without errors, all 4 cron jobs register in pg-boss. Health check returns status:"ok" with db/redis ok.
result: pass

### 2. TypeScript Compilation & Test Suite
expected: `npx tsc --noEmit` exits 0 with zero errors. `npm test` runs all 47 tests green (including 24 new football prediction tests for apiClient, oddsCalculator, predictionService).
result: pass
note: Verified by agent run — tsc exits 0, npm test 47/47 green

### 3. /config predictions on/off — Enable/Disable Predictions in Channel
expected: `/config predictions on` in a Discord channel returns confirmation embed saying predictions are enabled. `/config predictions off` disables and confirms. Prediction embeds only appear in enabled channels.
result: pass

### 4. /config predictions league — Per-League Toggle
expected: `/config predictions league premier-league` — on first use in a channel, the league should be enabled (ĐÃ BẬT), reflecting the intuitive expectation.
result: issue
reported: "sử dụng lệnh lần đầu trong kênh thì bot trả về kết quả Đã tắt, tôi nghĩ nên phải là Đã bật sẽ phù hợp hơn"
severity: major
fix: "config.ts line 219 — first-time league toggle always set enabled=false regardless of global state. Now checks global toggle: if global ON → first toggle disables (override), if global OFF/missing → first toggle enables. Fixed in config.ts:219-241."

### 5. /predictions status — View Active Bets
expected: `/predictions status` shows a list of the user's active (unresolved) bets with match details, bet type (result/score), wager amount, odds, and potential payout.
result: blocked
blocked_by: server
reason: "No fixture data in DB — API key was just added, cron fires every 6h. Added fetch action to /config predictions for manual trigger."

### 6. /predictions history — View Resolved Bets
expected: `/predictions history` shows paginated list of resolved bets with match result, bet outcome (won/lost/refunded), wager, and payout.
result: blocked
blocked_by: server
reason: "No fixture data in DB — same as test 5. Use /config predictions fetch to trigger immediately, or restart bot for auto-fetch."

### 7. Prediction Embed Renders in Channel
expected: Bot posts an embed for each upcoming match showing: team names, kickoff time (localized), league name, Win/Draw/Lose odds, correct score odds, "Predict Result" dropdown, and 8 popular score buttons.
result: blocked
blocked_by: server
reason: "Requires API fixture data. Use /config predictions fetch to trigger."

### 8. Match Result Prediction Flow (Win/Draw/Lose)
expected: User selects option from Predict Result dropdown → a Discord Modal appears asking for wager amount → user submits → embed confirms bet with match details, bet type, odds, wager, and potential payout.
result: blocked
blocked_by: server
reason: "Requires fixture data in DB."

### 9. Exact Score Prediction Flow
expected: User clicks a score button (e.g., 2-1) → Modal appears for wager → user submits → confirmation embed shows exact score prediction with odds and potential payout.
result: blocked
blocked_by: server
reason: "Requires fixture data in DB."

### 10. Prediction Edit Before Kickoff
expected: User places a bet on a match, then places a different bet on the same match+type before kickoff. Old wager is automatically refunded and new bet placed. Confirmation reflects updated prediction.
result: blocked
blocked_by: server
reason: "Requires fixture data + active bet in DB."

### 11. Balance Deduction on Bet Placement
expected: User checks balance with `/profile`, places a prediction bet, then checks balance again — balance decreased by the wager amount immediately.
result: blocked
blocked_by: server
reason: "Requires fixture data + active bet."

### 12. Auto-Resolve & Payout After Match Ends
expected: After a match finishes (FT/AET/PEN), cron job resolves all bets. Winners receive payout credited to their balance. Losers' wagers are kept. Check /predictions history shows resolved outcome.
result: blocked
blocked_by: server
reason: "Requires fixture data + resolved match."

### 13. Refund on Cancelled/Postponed Match
expected: If a match ends with PST (postponed), CANC (cancelled), or ABD (abandoned) status, all bets for that match are voided and the full wager amount is refunded to each player's balance.
result: blocked
blocked_by: server
reason: "Requires fixture data + cancelled match state."

### 14. Live Score Updates During Match
expected: When a match is in progress (1H/HT/2H/ET), the prediction embed updates with current score every 15 minutes. Score change visible in the embed without manual refresh.
result: blocked
blocked_by: server
reason: "Requires live match data."

### 15. i18n — Prediction UI Displays in User's Locale
expected: User with locale set to `en` sees English strings in all prediction embeds, commands, and modals. User with `vi` sees Vietnamese. User with `zh-cn` sees Chinese. No hardcoded Vietnamese/English strings visible.
result: pass
note: Verified by code inspection — 53 keys per locale, identical structure across vi/en/zh-cn.

## Summary

total: 15
passed: 4
issues: 1
blocked: 11
skipped: 0
skipped: 0
blocked: 0

## Gaps

- truth: "First-time league toggle should enable the league (ĐÃ BẬT)"
  status: fixed
  reason: "User reported: bot returns ĐÃ TẮT on first league toggle in a new channel"
  severity: major
  test: 4
  root_cause: "config.ts:219 — always set enabled=false on first league insert, didn't check global toggle state. When no global ON row exists, first toggle should enable (true), not disable (false)."
  artifacts:
    - path: "src/commands/predictions/config.ts"
      issue: "first-time league toggle always sets enabled=false"
  missing:
    - "Check global toggle state (league_id=0) to determine correct default for first league override"
  fix: "Added global toggle lookup before first insert. If global is ON → disable league (explicit override). If global is OFF/missing → enable league. TypeScript compiles cleanly."
