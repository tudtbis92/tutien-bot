---
phase: 09-travel-encounters
plan: 04
type: execute
wave: 3
depends_on:
  - 09-02
  - 09-03
files_modified:
  - src/services/sanguo/encounterService.ts
  - src/services/sanguo/__tests__/encounterService.test.ts
  - src/ui/embeds/buildSanguoEncounterEmbed.ts
  - src/commands/sanguo/travel.ts
  - src/services/sanguo/travelCheckInService.ts
  - locales/vi/sanguo.json
  - locales/en/sanguo.json
  - locales/zh-cn/sanguo.json
autonomous: true
requirements:
  - TQC-08

estimate:
  tokens: 40000
  raw_tokens: 20000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "encounterService provides the PURE roll math used by the check-in loop (09-03): positionFraction, shouldRoll, shouldRollBoss, pickEncounterHero, capHit, cryptoUniform — all crypto-backed, no db/redis imports (the check-in owns I/O)."
    - "The check-in's per-minute roll (D-24) calls capHit FIRST (silent skip when ≥20/hr — D-13, travel never blocked), then position-blended zone-pool pick (D-15), then boss sub-roll (D-14)."
    - "All player-facing rolls use crypto-backed RNG (crypto.randomInt) — never the predictable global PRNG (milestone mandate, V6)."
    - "A successful roll records encounter_runs (user_id, travel_id, zone, hero_id NULL for boss, encounter_type 'hero'|'boss', status 'pending') and returns the encounter result inline (D-14 — Phase 9 is roll + ack + record only)."
    - "The check-in loop writes ONLY encounter_runs + Redis — never player_travel_state columns beyond the encounterActive/remaining/updatedAt it owns (single-writer rule, Pitfall 5)."
    - "pickEncounterHero zone attribution is dominant-zone-correct: a hero present in BOTH pools gets its weight summed and the returned zone is the pos-dominant contributing zone, not a loop-order overwrite (B6 fix)."
  artifacts:
    - src/services/sanguo/encounterService.ts — positionFraction, shouldRoll, shouldRollBoss, pickEncounterHero, capHit, cryptoUniform, ZoneRate, EncounterRollResult
    - src/jobs/__tests__/encounterService.test.ts → moved to src/services/sanguo/__tests__/encounterService.test.ts (pure math suite)
    - src/services/sanguo/travelCheckInService.ts — rollMinute implementation wired to encounterService + cap + boss + record
    - src/ui/embeds/buildSanguoEncounterEmbed.ts — buildSanguoEncounterEmbed(data, t) with boss GOLD variant
    - locales/{vi,en,zh-cn}/sanguo.json — encounter.* keys
    - src/commands/sanguo/travel.ts — encounter embed dispatch (09-03 wired the ack; this plan finalizes the embed)
  key_links:
    - "encounterService pickEncounterHero ← hero_zone_rates rows (09-02) keyed by the from-zone and to-zone codes — the blend math is only correct if both zone pools are fetched by zone code."
    - "travelCheckInService (09-03) ← encounterService — the per-minute loop calls the pure roll; position = 1 − (remaining/total) per D-15."
    - "travelCheckInService ← Redis cap ZSET (sanguo:enc:win:{userId}) — capHit before each roll (D-13)."
    - "buildSanguoEncounterEmbed ← travel.ts dispatch + buildAckButton (09-01) — the inline encounter result."
  prohibitions:
    - "No Math.random anywhere in encounter roll code (milestone mandate — crypto.randomInt only; tests may inject a fake rng function)."
    - "No fixed-hour cap bucket — sliding ZSET only (D-13 'cap window clears', RESEARCH A1)."
    - "No batch of encounters — the check-in loop stops at the first hit (D-24); encounterService itself is pure (one roll per call)."
    - "No notification on cap-skip — D-13 silent skip: no record, no inline encounter, travel continues."
    - "No hardcoded user-facing strings — encounter.* via t('sanguo:...') (check-i18n gate)."
    - "No wallet/balance interaction anywhere in the encounter path (D-01)."
  flagged_assumptions:
    - "FLAGGED ASSUMPTION (TQC-08 unclassified): encounter semantics are implemented per D-10/D-13/D-14/D-15/D-24 with research defaults — per-counted-minute zone probability 0.35 (map_zones.encounter_rate, D-10 band 30-50%), boss sub-roll 0.07 (map_zones.boss_rate, D-14 band 5-10%), sliding 60-min window cap 20/hr, boss counts toward the cap, position computed per minute at the check-in (A5)."
    - "FLAGGED ASSUMPTION (A1 research): cap window is a sliding 60-min ZSET, not a fixed-hour bucket — fairer and matches 'cap window clears' semantics."
    - "FLAGGED ASSUMPTION (A5 research): position is computed at per-minute granularity from stored remaining — no finer interpolation."
    - "FLAGGED ASSUMPTION (A9 research): encounters resolve via the ack button (D-25) — encounter_active stays true until the ack; the check-in returns the pending encounter and counts no time."
    - "FLAGGED ASSUMPTION (F7): the cap ZSET gets a best-effort `redis.expire(key, 86_400)` each rollMinute — inactive users' keys do not persist forever."
    - "FLAGGED ASSUMPTION (F8): hero_zone_rates.rate is `numeric(4,2)` (string from Drizzle) — converted with Number() before the blend math; the seed writes 1.0/0.5/0.3."

---

<objective>
What this plan accomplishes: the **encounter system (TQC-08)** — the pure `encounterService` roll math (position-blended pool pick, boss sub-roll, cap predicate — D-15/D-14/D-13), the `rollMinute` implementation wired into the 09-03 check-in loop (1 roll per counted minute, stop-at-first, cap-first, boss, record), and the inline encounter embed (normal + boss GOLD). No job, no cron — the roll engine runs inside `/sanguo travel` (D-22/D-24).

Purpose: make every journey a live hunt — encounters surface when the player checks in, capped so the economy and anti-abuse posture hold from day one (SC4).

Output: pure encounter roll engine + check-in integration + encounter embed (boss GOLD variant) + i18n encounter keys + unit tests.
</objective>

<execution_context>
@C:/Users/901107/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/901107/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-travel-encounters/09-CONTEXT.md
@.planning/phases/09-travel-encounters/09-RESEARCH.md
@.planning/phases/09-travel-encounters/09-PATTERNS.md
@.planning/phases/09-travel-encounters/09-UI-SPEC.md
@src/services/sanguo/encounterService.ts
@src/services/football/oddsCalculator.ts
@src/services/sanguo/travelCheckInService.ts
@src/db/schema/heroZoneRates.ts
@src/db/schema/mapZones.ts
@src/db/schema/mapEdges.ts
@src/db/schema/encounterRuns.ts
@src/db/schema/playerTravelState.ts
@src/cache/redis.ts
@src/ui/theme.ts
@src/ui/embeds/buildSanguoEncounterEmbed.ts
@src/assets/sanguoEmojis.ts
</context>

<tasks>

<task type="expansion" tdd="true">
  <name>Task 1 (expansion): encounterService — position blend, weighted pick, boss roll, cap helpers (pure math, crypto RNG, B6 zone-attribution fix)</name>
  <files>src/services/sanguo/encounterService.ts, src/services/sanguo/__tests__/encounterService.test.ts</files>
  <read_first>
    - .planning/phases/09-travel-encounters/09-RESEARCH.md Pattern 3 (lines 322-347, the locked D-15 blend formula) + §7 encounter roll specifics + Assumptions A1/A3/A5/A7
    - src/services/football/oddsCalculator.ts (pure-function module shape — the stated analog for encounterService)
    - src/db/schema/heroZoneRates.ts (rate numeric(4,2) — research weights 1.0/0.5/0.3)
    - src/db/schema/mapZones.ts (encounterRate/bossRate columns — the zone-configurable rates)
  </read_first>
  <behavior>
    - Test 1: positionFraction(remaining, total) returns 0 when remaining=total (departure), 1 when remaining=0 (arrival), 0.5 mid-hop; clamps out-of-range inputs.
    - Test 2: pickEncounterHero at pos=0 returns only zone-A heroes (zone-B weights × 0); at pos=1 returns only zone-B heroes; at pos=0.5 blends both pools' weights linearly (D-15 boundary proof).
    - Test 3: weighted pick distribution respects rate ratios — with an injected deterministic rng the cumulative-walk selection returns the expected hero for threshold rolls; a hero shared by both zones accumulates rate×(1−pos) + rate×pos.
    - Test 4 (B6 fix): a hero in BOTH pools returns the pos-dominant contributing zone (pos<0.5 → A zone, pos>0.5 → B zone) — NOT a loop-order overwrite.
    - Test 5: shouldRoll(0.35, rng) rolls true when rng() < 0.35, false otherwise; shouldRollBoss(0.07, rng) likewise (D-10/D-14 thresholds).
    - Test 6: capHit(now, windowStart, limit) — pure window predicate used by the check-in's ZSET logic.
    - Test 7: the module source contains no call to the predictable global PRNG (grep gate in verify).
  </behavior>
  <action>
    Create `src/services/sanguo/encounterService.ts` — pure functions, no db/redis imports (the check-in owns I/O). RESEARCH Pattern 3 is the locked D-15 formula:
    - `export function positionFraction(remainingSeconds: number, totalSeconds: number): number` — `1 − (remaining/total)` clamped to [0,1] (total <= 0 → 0).
    - `export function shouldRoll(encounterRate: number, rng: () => number = cryptoUniform): boolean` — `rng() < encounterRate` (D-10; zone rate from map_zones.encounter_rate, default 0.35 — A7).
    - `export function shouldRollBoss(bossRate: number, rng: () => number = cryptoUniform): boolean` — `rng() < bossRate` (D-14; map_zones.boss_rate, default 0.07 — A7).
    - `export function pickEncounterHero(poolA: ZoneRate[], poolB: ZoneRate[], pos: number, rng: () => number = cryptoUniform): { heroId: number; zone: string }` — the D-15 blend: union of both pools; weight(hero) = rate(zoneA) × (1−pos) + rate(zoneB) × pos (sum when the hero appears in both); cumulative-walk with `rng() * totalWeight` (crypto-backed default). **B6 fix:** the returned `zone` must be the pos-dominant contributing zone — when a hero is in both pools, attribute it to the pool with the higher blended weight (i.e., zone A when `rateA·(1−pos) >= rateB·pos`, else zone B), NOT the last-written loop value. The research Pattern 3 sketch's `heroZone.set` overwrite is a known bug — implement the dominant-zone attribution.
    - `export function capHit(windowCount: number, limit: number = 20): boolean` — `windowCount >= limit` (D-13 ~20/hr; the check-in feeds ZCARD into this).
    - `export function cryptoUniform(): number` — `crypto.randomInt(0, 1_000_000) / 1_000_000` — the ONLY default rng; player-facing rolls therefore always ride crypto.randomInt (milestone mandate). **Do NOT reference the predictable global PRNG anywhere.**
    - `export interface ZoneRate { heroId: number; zone: string; rate: number }` — shape of hero_zone_rates rows consumed by pickEncounterHero.
    - `export interface EncounterRollResult { heroId: number | null; zone: string; boss: boolean }` — the result the check-in records + displays.
    - Module-level doc comment stating the crypto-RNG mandate and the D-15 locked formula (research source line refs).

    Create `src/services/sanguo/__tests__/encounterService.test.ts` per the 7 behaviors — inject deterministic rng functions (`() => 0.2`, `() => 0.9`, etc.) for threshold tests; the module default path is covered by a smoke test calling pickEncounterHero with cryptoUniform once (statistical sanity, not flaky assertions).
  </action>
  <verify>
    <automated>npx vitest run src/services/sanguo/__tests__/encounterService.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Command: `npx vitest run src/services/sanguo/__tests__/encounterService.test.ts` exits 0 (7 behaviors).
    - Command: `npm run typecheck` exits 0.
    - Source: `src/services/sanguo/encounterService.ts` exports `positionFraction`, `shouldRoll`, `shouldRollBoss`, `pickEncounterHero`, `capHit`, `cryptoUniform`, `ZoneRate`, `EncounterRollResult`.
    - Source: `pickEncounterHero` weight formula contains `rate * (1 - pos)` and `rate * pos` terms (D-15 linear blend) AND the dominant-zone attribution (B6).
    - Command (crypto-RNG gate): `grep -v "^[[:space:]]*//" src/services/sanguo/encounterService.ts | grep -c "Math\.random"` outputs `0`; and `grep -c "crypto\.randomInt" src/services/sanguo/encounterService.ts` outputs at least `1`.
  </acceptance_criteria>
  <done>Pure encounter engine with locked D-15 blend, crypto-backed default RNG (never the predictable PRNG), boss sub-roll thresholds, cap predicate, dominant-zone attribution (B6); blend boundary tests prove A-only at pos=0 / B-only at pos=1.</done>
</task>

<task type="integration" tdd="true">
  <name>Task 2 (integration): rollMinute in the check-in loop + encounter embed + i18n — no job</name>
  <files>src/services/sanguo/travelCheckInService.ts, src/ui/embeds/buildSanguoEncounterEmbed.ts, src/commands/sanguo/travel.ts, locales/vi/sanguo.json, locales/en/sanguo.json, locales/zh-cn/sanguo.json</files>
  <read_first>
    - src/services/sanguo/travelCheckInService.ts (09-03 — the loop that calls the roll; this plan provides the real `rollMinute` implementation)
    - src/services/sanguo/encounterService.ts (Task 1)
    - src/db/schema/heroZoneRates.ts + src/db/schema/mapZones.ts + src/db/schema/mapEdges.ts + src/db/schema/encounterRuns.ts (09-02 tables + D-14 encounter_type column)
    - src/cache/redis.ts (cap ZSET ops)
    - src/ui/embeds/buildSanguoArrivalEmbed.ts (embed builder pattern for the encounter embed)
    - .planning/phases/09-travel-encounters/09-RESEARCH.md §7 encounter roll specifics (the 6-step algorithm) + Pitfall 7 (cap check ordering)
    - .planning/phases/09-travel-encounters/09-UI-SPEC.md §Copywriting Contract (encounter + boss keys) + §Color (SEASON normal / GOLD boss)
  </read_first>
  <behavior>
    - Test 1: cap window >= 20 → the roll is skipped silently — no encounter_runs insert, no inline encounter (D-13).
    - Test 2: cap window < 20 and shouldRoll true → weighted pick → encounter_runs INSERT with encounter_type 'hero' + hero_id + zone → ZADD timestamp → encounter result returned.
    - Test 3: shouldRoll true and shouldRollBoss true → encounter_runs INSERT with encounter_type 'boss', hero_id NULL, zone = dominant zone; boss counts toward the cap (ZADD also fires).
    - Test 4: the roll reads total hop seconds from map_edges (OR-match both orientations of the from/to pair) + from/to zone codes from map_nodes + hero_zone_rates rows for both zones; missing edge → the minute is skipped with a warn log (no crash).
    - Test 5: the check-in loop NEVER writes player_travel_state beyond the encounterActive/remaining/updatedAt it owns — the single-writer rule (Pitfall 5).
  </behavior>
  <action>
    Edit `src/services/sanguo/travelCheckInService.ts`:
    - Implement the default `rollMinute` (replacing the 09-03 injectable stub) using `encounterService`:
       1. **Cap check FIRST (D-13, Pitfall 7):** key `sanguo:enc:win:${userId}`; `await redis.zremrangebyscore(key, '-inf', '(' + (now - 3600_000))`; `const count = await redis.zcard(key)`; `if (capHit(count)) return { hit: false, skipped: true }` — silent skip: no record, no inline encounter. **F7:** best-effort `await redis.expire(key, 86_400)` after the cleanup so a user who stops checking in does not leave a persistent key.
      2. **Position (D-15):** `totalSeconds` from map_edges (OR-match both orientations); missing edge → `logger.warn` + skip the minute (no crash). `pos = positionFraction(remainingAfter, totalSeconds)`.
       3. **Zone rates:** read from/to zone codes from map_nodes; fetch `hero_zone_rates` rows for both zones (`inArray(heroZoneRates.zone, [fromZone, toZone])`); dominant zone = `pos < 0.5 ? fromZone : toZone`. **F8:** hero_zone_rates.rate is `numeric(4,2)` → Drizzle returns strings — build the ZoneRate[] passed to pickEncounterHero with an explicit `Number(row.rate)` conversion (pickEncounterHero takes `number`).
      4. **Hero roll (D-10/D-24):** read `map_zones.encounterRate` for the dominant zone (default 0.35); `if (!shouldRoll(Number(rate))) return { hit: false }`.
      5. **Boss sub-roll (D-14):** read `map_zones.bossRate` (default 0.07); `const isBoss = shouldRollBoss(Number(bossRate))`.
      6. **Pick + record (D-24 stop-at-first happens in the 09-03 loop):** if isBoss → `{ heroId: null, zone: dominantZone, boss: true }`; else `const pick = pickEncounterHero(poolFromZone, poolToZone, pos); { heroId: pick.heroId, zone: pick.zone, boss: false }`. `await tx.insert(encounterRuns).values({ userId, travelId: row.id, zone, heroId, encounterType: boss ? 'boss' : 'hero', status: 'pending' })`. `await redis.zadd(key, now, String(now))` — boss counts toward the cap (it IS an encounter).
    - Return `{ hit: true, heroId, zone, boss }` so the 09-03 loop pins `encounterActive` + `updatedAt` and returns `{ mode: 'encounter', encounter }`.

    Create `src/ui/embeds/buildSanguoEncounterEmbed.ts`:
    - `export interface SanguoEncounterEmbedData { nodeName: string; heroName?: string; heroEmoji?: string; zoneName: string; boss: boolean; shardId?: number }`.
    - `buildSanguoEncounterEmbed(data, t: TFunction): EmbedBuilder` — `.setColor(data.boss ? COLORS.GOLD : COLORS.SEASON)` (UI-SPEC color contract: GOLD accent reserved for boss only), `.setTitle(t(data.boss ? 'sanguo:encounter.boss_title' : 'sanguo:encounter.title'))`, description = boss ? `t('sanguo:encounter.boss_body', { zone: data.zoneName })` : `t('sanguo:encounter.body', { node: data.nodeName, hero_emoji: data.heroEmoji ?? '', hero: data.heroName ?? '' })`, `.setFooter(embedFooter(data.shardId))` + `.setTimestamp()`.
    - Guard heroEmoji usage at the call site (EMOJI_NOT_FOUND → name-only) per the map.ts:98 pattern.

    Edit `locales/{vi,en,zh-cn}/sanguo.json` — add an `encounter` block, identical key structure in all 3 locales (VI canonical; EN/ZH translate):
    - `encounter.title` = "⚔️ Kỳ ngộ"
    - `encounter.body` = "Trên đường đến **{node}**, bạn chạm trán **{hero_emoji} {hero}**."
    - `encounter.boss_title` = "👑 Boss thường xuất hiện!"
    - `encounter.boss_body` = "Một Boss thường trấn giữ **{zone}** đã xuất hiện trên đường của bạn. Hãy chuẩn bị!"

    Edit `src/commands/sanguo/travel.ts`:
    - Finalize the `'encounter'` dispatch to use `buildSanguoEncounterEmbed` (resolve hero name/emoji via heroes table + heroEmoji guard; node name per-locale) + `buildAckButton(t)` — the ack resume was wired in 09-03.
  </action>
  <verify>
    <automated>npx vitest run src/services/sanguo/__tests__/encounterService.test.ts; npm run check-i18n; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Command: `npx vitest run src/services/sanguo/__tests__/encounterService.test.ts` exits 0; `npm run check-i18n` exits 0; `npm run typecheck` exits 0.
    - Source: `src/services/sanguo/travelCheckInService.ts` (or a new `rollMinute.ts`) contains `zremrangebyscore`, `zcard`, `zadd` Redis ops and `capHit(` called BEFORE any roll (Pitfall 7 ordering).
    - Source (F7): the cap key gets a best-effort `redis.expire(key, 86_400)` in rollMinute.
    - Source (F8): the roll converts hero_zone_rates rate strings with `Number(...)` before passing the ZoneRate[] to `pickEncounterHero`.
    - Source: the roll inserts encounterRuns with `encounterType` and `heroId: null` on the boss branch (D-14).
    - Command (crypto-RNG gate): `grep -v "^[[:space:]]*//" src/services/sanguo/travelCheckInService.ts | grep -c "Math\.random"` outputs `0`.
    - Config: `locales/{vi,en,zh-cn}/sanguo.json` each contain `encounter.title`, `encounter.body`, `encounter.boss_title`, `encounter.boss_body`.
    - Source: `src/ui/embeds/buildSanguoEncounterEmbed.ts` uses `COLORS.GOLD` on the boss branch and `COLORS.SEASON` otherwise.
    - Grep: no `sanguo-tick-encounters`, no `schedule(`, no `createQueue('sanguo` in this plan's files (D-22 — no cron).
  </acceptance_criteria>
  <done>Encounter rolls run inside the check-in loop (cap-first, position-blended pick, boss sub-roll, record) with stop-at-first handled by 09-03; encounter embed (boss GOLD variant) + i18n keys; no job, no cron; tests + gates green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| check-in loop → Redis cap ZSET | Cap counter mutation races only with the user's own check-in tx (single user) |
| roll → encounter_runs INSERT | Roll outcomes are player-facing game state; RNG integrity matters |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-10 | Spoofing / Information Disclosure | Roll prediction via the predictable global PRNG | high | mitigate | `crypto.randomInt` backed `cryptoUniform()` is the only default rng in encounterService (milestone mandate, ASVS V6); grep gates (`Math\.random` == 0) enforce it; deterministic rng only in injected tests. |
| T-09-11 | Elevation / Tampering | Cap evasion (burst past 20/hr) | medium | mitigate | Sliding-window ZSET with cap check BEFORE any roll (Pitfall 7); multi-account evasion is accepted as a soft-brake limitation (Phase 12 TQC-18 bot detection hardens). |
| T-09-12 | Tampering | Cap check-then-act race within one check-in | low | accept | ZREMRANGEBYSCORE → ZCARD → (roll) → ZADD ordering is not atomic; the micro-race allows at most 1 boundary overshoot — the cap is a soft brake per D-13, hardened in Phase 12. |
| T-09-13 | Tampering | Encounter loop corrupting the D-07 clock | high | mitigate | The check-in owns remaining/updatedAt; the roll writes only encounter_runs + Redis — zero stray `update(playerTravelState)` calls (Pitfall 5). |
| T-09-SC | Tampering | npm/pip/cargo installs | — | not-triggered | No new packages this phase (RESEARCH Package Legitimacy Audit); all deps installed + production-running. |
</threat_model>

<verification>
- `npx vitest run src/services/sanguo/__tests__/encounterService.test.ts` green (blend boundaries, dominant-zone attribution, threshold rolls, cap predicate).
- `npm run typecheck` + `npm run check-i18n` green (encounter.* keys in all 3 locales).
- Grep gates: `Math\.random` == 0 in encounterService.ts and travelCheckInService.ts; `crypto.randomInt` present.
- Grep: no `sanguo-tick-encounters`, no `schedule(`, no `createQueue('sanguo` in this plan's files.
</verification>

## Artifacts this phase produces

| Symbol | Kind | File |
|--------|------|------|
| `positionFraction(remaining, total)` | pure position math (D-15) | src/services/sanguo/encounterService.ts |
| `shouldRoll(rate, rng)` / `shouldRollBoss(rate, rng)` | per-minute probability + boss sub-roll thresholds (D-10/D-14) | src/services/sanguo/encounterService.ts |
| `pickEncounterHero(poolA, poolB, pos, rng)` | position-blended weighted pick (linear, crypto RNG, dominant-zone B6 fix) | src/services/sanguo/encounterService.ts |
| `capHit(windowCount, limit)` / `cryptoUniform()` | cap predicate + crypto-backed uniform | src/services/sanguo/encounterService.ts |
| `rollMinute` implementation (cap-first, blend, boss, record) | check-in loop integration (no job) | src/services/sanguo/travelCheckInService.ts |
| `buildSanguoEncounterEmbed(data, t)` | encounter embed (SEASON / GOLD boss variant) | src/ui/embeds/buildSanguoEncounterEmbed.ts |
| `encounter.title` / `encounter.body` / `encounter.boss_title` / `encounter.boss_body` | i18n keys (3 locales) | locales/{vi,en,zh-cn}/sanguo.json |
| encounterService test suite (blend/zone-attribution/roll/cap) | test file | src/services/sanguo/__tests__/encounterService.test.ts |

<success_criteria>
- Encounters roll inside the check-in with the D-15 position-blended pool, D-14 boss sub-roll, and stop-at-first (D-24).
- The ~20/hr sliding-window cap silently skips over-cap rolls; travel is never blocked (D-13).
- All player-facing rolls ride crypto.randomInt (grep-enforced).
- encounter_runs records carry the D-14 encounter_type flag; results inline (D-23).
- No job, no cron (D-22).
</success_criteria>

<output>
Create `.planning/phases/09-travel-encounters/09-04-SUMMARY.md` when done.
</output>
