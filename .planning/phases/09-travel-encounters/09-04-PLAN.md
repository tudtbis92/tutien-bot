---
phase: 09-travel-encounters
plan: 04
type: execute
wave: 3
depends_on:
  - 09-02
  - 09-03
files_modified:
  - src/services/sanguo/encounterService.ts
  - src/services/sanguo/__tests__/encounterService.test.ts
  - src/jobs/sanguoTickEncounters.ts
  - src/workers/pgBoss.ts
  - src/ui/embeds/buildSanguoEncounterEmbed.ts
  - src/services/sanguo/sanguoNotificationService.ts
  - locales/vi/sanguo.json
  - locales/en/sanguo.json
  - locales/zh-cn/sanguo.json
autonomous: true
requirements:
  - TQC-08

estimate:
  tokens: 40000
  raw_tokens: 20000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "sanguo-tick-encounters pg-boss cron (every 45s, manager process only, 6-field cron) rolls encounters for traveling rows: cap check FIRST (silent skip when ≥20/hr — D-13, travel never blocked), then position-blended zone-pool pick (D-15), then boss sub-roll (D-14)."
    - "All player-facing rolls use crypto-backed RNG (crypto.randomInt) — never the predictable global PRNG (milestone mandate, V6)."
    - "The ~20/hr cap is a Redis sliding-window ZSET (key sanguo:enc:win:{userId}): ZREMRANGEBYSCORE older than 60min → ZCARD >= 20 → skip silently (no record, no DM); successful rolls ZADD the timestamp; boss encounters count toward the cap."
    - "Encounter pool is position-blended: pos = 1 − (travel_seconds_remaining / total_hop_seconds); hero weight = rate(zone A) × (1−pos) + rate(zone B) × pos — near A → A-heavy, near B → B-heavy (D-15, linear)."
    - "A successful roll records encounter_runs (user_id, travel_id, zone, hero_id NULL for boss, encounter_type 'hero'|'boss', status 'pending') and sends the encounter DM via the notification service (D-14 — Phase 9 is roll + notify + record only)."
    - "The encounter tick writes ONLY encounter_runs + Redis — never player_travel_state columns (single-writer rule, Pitfall 5)."
  artifacts:
    - src/services/sanguo/encounterService.ts — positionFraction, shouldRoll, shouldRollBoss, pickEncounterHero, capHit
    - src/jobs/sanguoTickEncounters.ts — runSanguoTickEncounters(job)
    - src/workers/pgBoss.ts — 'sanguo-tick-encounters' queue @ */45 * * * * *
    - src/ui/embeds/buildSanguoEncounterEmbed.ts — buildSanguoEncounterEmbed(data, t) with boss GOLD variant
    - src/services/sanguo/sanguoNotificationService.ts — notifyEncounter added
    - locales/{vi,en,zh-cn}/sanguo.json — encounter.* keys
    - src/services/sanguo/__tests__/encounterService.test.ts
  key_links:
    - "encounterService pickEncounterHero ← hero_zone_rates rows (09-02) keyed by the from-zone and to-zone codes — the blend math is only correct if both zone pools are fetched by zone code."
    - "sanguoTickEncounters ← travelService's remaining model + map_edges total-seconds lookup (09-01 table) — pos is computed from stored remaining / edge travelSeconds."
    - "sanguoTickEncounters ← sanguoNotificationService (09-03) — notifyEncounter reuses sendUserDM + resolveUserLocale + 3-strike."
    - "pgBoss registration ← 09-03's arrivals block — the encounters block appends AFTER it (shared file, sequential waves)."
  prohibitions:
    - "No Math.random anywhere in encounter roll code (milestone mandate — crypto.randomInt only; tests may inject a fake rng function)."
    - "No writes to player_travel_state columns from the encounter tick (single-writer rule, Pitfall 5)."
    - "No fixed-hour cap bucket — sliding ZSET only (D-13 'cap window clears', RESEARCH A1)."
    - "No notification on cap-skip — D-13 silent skip: no record, no DM, travel continues visibly unperturbed."
    - "No hardcoded user-facing strings — encounter.* via t('sanguo:...') (check-i18n gate)."
    - "No wallet/balance interaction anywhere in the encounter path (D-01)."
  flagged_assumptions:
    - "FLAGGED ASSUMPTION (TQC-08 unclassified): encounter semantics are implemented per D-10/D-13/D-14/D-15 with research defaults — per-tick zone probability 0.35 (map_zones.encounter_rate, D-10 band 30-50%), boss sub-roll 0.07 (map_zones.boss_rate, D-14 band 5-10%), sliding 60-min window cap 20/hr, boss counts toward the cap, position fraction computed once per 45s tick (A5)."
    - "FLAGGED ASSUMPTION (A1 research): cap window is a sliding 60-min ZSET, not a fixed-hour bucket — fairer and matches 'cap window clears' semantics."
    - "FLAGGED ASSUMPTION (A5 research): position is computed at tick granularity (once per 45s roll) from stored remaining — no finer interpolation."
    - "FLAGGED ASSUMPTION (A9 research): encounters resolve the instant they are recorded (no battle in Phase 9) — encounter_active stays observably false and the D-07 pause branch (09-03) remains Phase-10-ready."

---

<objective>
What this plan accomplishes: the **encounter system (TQC-08)** — the `sanguo-tick-encounters` pg-boss cron (every 45s, manager process only) that, for each traveling journey: checks the ~20/hr sliding-window cap (silent skip, D-13), computes the position-blended encounter pool (D-15), rolls zone probability then the boss sub-roll (D-14), picks a hero from the blended pool via crypto RNG, records `encounter_runs`, and sends the encounter DM cross-shard.

Purpose: make every journey a live hunt — encounters arrive along the route, capped so the economy and anti-abuse posture hold from day one.

Output: pure encounter roll engine + encounter tick + encounter embed (boss GOLD variant) + notifyEncounter + i18n encounter keys + pg-boss registration + unit tests.
</objective>

<execution_context>
@C:/Users/901107/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/901107/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-travel-encounters/09-CONTEXT.md
@.planning/phases/09-travel-encounters/09-RESEARCH.md
@.planning/phases/09-travel-encounters/09-PATTERNS.md
@.planning/phases/09-travel-encounters/09-UI-SPEC.md
@src/services/sanguo/encounterService.ts
@src/services/football/oddsCalculator.ts
@src/jobs/sanguoTickArrivals.ts
@src/workers/pgBoss.ts
@src/services/sanguo/sanguoNotificationService.ts
@src/db/schema/heroZoneRates.ts
@src/db/schema/mapZones.ts
@src/db/schema/mapEdges.ts
@src/db/schema/encounterRuns.ts
@src/db/schema/playerTravelState.ts
@src/cache/redis.ts
@src/ui/theme.ts
@src/ui/embeds/buildSanguoArrivalEmbed.ts
@src/assets/sanguoEmojis.ts
</context>

<tasks>

<task type="expansion" tdd="true">
  <name>Task 1 (expansion): encounterService — position blend, weighted pick, boss roll, cap helpers (pure math, crypto RNG)</name>
  <files>src/services/sanguo/encounterService.ts, src/services/sanguo/__tests__/encounterService.test.ts</files>
  <read_first>
    - .planning/phases/09-travel-encounters/09-RESEARCH.md Pattern 3 (lines 322-347, the locked D-15 blend formula) + §7 encounter roll specifics (lines 738-748) + Assumptions A1/A3/A5/A7
    - src/services/football/oddsCalculator.ts (pure-function module shape — the stated analog for encounterService)
    - src/db/schema/heroZoneRates.ts (rate numeric(4,2) — research weights 1.0/0.5/0.3)
    - src/db/schema/mapZones.ts (encounterRate/bossRate columns — the zone-configurable rates)
    - src/services/sanguo/__tests__/sanguoNotificationService.test.ts (mock style reference)
  </read_first>
  <behavior>
    - Test 1: positionFraction(remaining, total) returns 0 when remaining=total (departure), 1 when remaining=0 (arrival), 0.5 mid-hop; clamps out-of-range inputs.
    - Test 2: pickEncounterHero at pos=0 returns only zone-A heroes (zone-B weights × 0); at pos=1 returns only zone-B heroes; at pos=0.5 blends both pools' weights linearly (D-15 boundary proof).
    - Test 3: weighted pick distribution respects rate ratios — with an injected deterministic rng the cumulative-walk selection returns the expected hero for threshold rolls; a hero shared by both zones accumulates rate×(1−pos) + rate×pos.
    - Test 4: shouldRoll(0.35, rng) rolls true when rng() < 0.35, false otherwise; shouldRollBoss(0.07, rng) likewise (D-10/D-14 thresholds).
    - Test 5: capHit(now, windowStart, limit) — pure window predicate used by the tick's ZSET logic.
    - Test 6: the module source contains no call to the predictable global PRNG (grep gate in verify).
  </behavior>
  <action>
    Create `src/services/sanguo/encounterService.ts` — pure functions, no db/redis imports (the tick owns I/O). RESEARCH Pattern 3 is the locked D-15 formula:
    - `export function positionFraction(remainingSeconds: number, totalSeconds: number): number` — `1 − (remaining/total)` clamped to [0,1] (total <= 0 → 0).
    - `export function shouldRoll(encounterRate: number, rng: () => number = cryptoUniform): boolean` — `rng() < encounterRate` (D-10; zone rate from map_zones.encounter_rate, default 0.35 — A7).
    - `export function shouldRollBoss(bossRate: number, rng: () => number = cryptoUniform): boolean` — `rng() < bossRate` (D-14; map_zones.boss_rate, default 0.07 — A7).
    - `export function pickEncounterHero(poolA: ZoneRate[], poolB: ZoneRate[], pos: number, rng: () => number = cryptoUniform): { heroId: number; zone: string }` — the D-15 blend: union of both pools; weight(hero) = rate(zoneA) × (1−pos) + rate(zoneB) × pos (sum when the hero appears in both); cumulative-walk with `rng() * totalWeight` (crypto-backed default); returns { heroId, zone } where zone = the contributing zone (A-pool zone when pos < 0.5 dominant, else B-pool zone — for the boss record's zone attribution).
    - `export function capHit(windowCount: number, limit: number = 20): boolean` — `windowCount >= limit` (D-13 ~20/hr; the tick feeds ZCARD into this).
    - `export function cryptoUniform(): number` — `crypto.randomInt(0, 1_000_000) / 1_000_000` — the ONLY default rng; player-facing rolls therefore always ride crypto.randomInt (milestone mandate). **Do NOT reference the predictable global PRNG anywhere.**
    - `export interface ZoneRate { heroId: number; zone: string; rate: number }` — shape of hero_zone_rates rows consumed by pickEncounterHero.
    - Module-level doc comment stating the crypto-RNG mandate and the D-15 locked formula (research source line refs).

    Create `src/services/sanguo/__tests__/encounterService.test.ts` per the 6 behaviors — inject deterministic rng functions (`() => 0.2`, `() => 0.9`, etc.) for threshold tests; the module default path is covered by a smoke test calling pickEncounterHero with cryptoUniform once (statistical sanity, not flaky assertions).
  </action>
  <verify>
    <automated>npx vitest run src/services/sanguo/__tests__/encounterService.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Command: `npx vitest run src/services/sanguo/__tests__/encounterService.test.ts` exits 0 (6 behaviors).
    - Command: `npm run typecheck` exits 0.
    - Source: `src/services/sanguo/encounterService.ts` exports `positionFraction`, `shouldRoll`, `shouldRollBoss`, `pickEncounterHero`, `capHit`, `cryptoUniform`, `ZoneRate`.
    - Source: `pickEncounterHero` weight formula contains `rate * (1 - pos)` and `rate * pos` terms (D-15 linear blend).
    - Command (crypto-RNG gate): `grep -v "^[[:space:]]*//" src/services/sanguo/encounterService.ts | grep -c "Math\.random"` outputs `0`; and `grep -c "crypto\.randomInt" src/services/sanguo/encounterService.ts` outputs at least `1`.
  </acceptance_criteria>
  <done>Pure encounter engine with locked D-15 blend, crypto-backed default RNG (never the predictable PRNG), boss sub-roll thresholds, cap predicate; blend boundary tests prove A-only at pos=0 / B-only at pos=1.</done>
</task>

<task type="integration" tdd="true">
  <name>Task 2 (integration): sanguoTickEncounters job + notifyEncounter + encounter embed + i18n + pg-boss registration</name>
  <files>src/jobs/sanguoTickEncounters.ts, src/workers/pgBoss.ts, src/ui/embeds/buildSanguoEncounterEmbed.ts, src/services/sanguo/sanguoNotificationService.ts, locales/vi/sanguo.json, locales/en/sanguo.json, locales/zh-cn/sanguo.json</files>
  <read_first>
    - src/jobs/sanguoTickArrivals.ts (job-body + SKIP LOCKED + post-commit notification structure — created in 09-03)
    - src/workers/pgBoss.ts (append after the arrivals block; the 6-field cron note at RESEARCH Pattern 1)
    - src/services/sanguo/sanguoNotificationService.ts (extend with notifyEncounter reusing sendUserDM/resolveUserLocale)
    - src/ui/embeds/buildSanguoArrivalEmbed.ts (embed builder pattern for the encounter embed)
    - src/db/schema/heroZoneRates.ts + src/db/schema/mapZones.ts + src/db/schema/mapEdges.ts + src/db/schema/encounterRuns.ts (09-02 tables + D-14 encounter_type column)
    - .planning/phases/09-travel-encounters/09-RESEARCH.md §7 encounter roll specifics (lines 738-748 — the 6-step per-tick algorithm) + Pitfall 7 (cap check ordering)
    - .planning/phases/09-travel-encounters/09-UI-SPEC.md §Copywriting Contract (encounter + boss keys) + §Color (SEASON normal / GOLD boss)
  </read_first>
  <behavior>
    - Test 1: a traveling row with cap window >= 20 is skipped silently — no encounter_runs insert, no DM (D-13).
    - Test 2: cap window < 20 and shouldRoll true → weighted pick → encounter_runs INSERT with encounter_type 'hero' + hero_id + zone → ZADD timestamp → notifyEncounter called.
    - Test 3: shouldRoll true and shouldRollBoss true → encounter_runs INSERT with encounter_type 'boss', hero_id NULL, zone = dominant zone; boss counts toward the cap (ZADD also fires).
    - Test 4: the tick reads total hop seconds from map_edges (OR-match both orientations of the from/to pair); missing edge → row skipped with a warn log (no crash).
    - Test 5: the tick NEVER calls tx.update(playerTravelState) — single-writer rule enforced by construction (Pitfall 5).
  </behavior>
  <action>
    Extend `src/services/sanguo/sanguoNotificationService.ts`:
    - Add `notifyEncounter(userId: number, data: { nodeName: string; heroName?: string; heroEmoji?: string; zoneName: string; boss: boolean; shardId?: number }): Promise<void>` — resolveUserLocale → getT(locale) → sendUserDM(userId, [buildSanguoEncounterEmbed(data, t)]). Boss = true uses the GOLD embed variant + boss copy; normal uses hero copy with heroEmoji prefix.

    Create `src/ui/embeds/buildSanguoEncounterEmbed.ts`:
    - `export interface SanguoEncounterEmbedData { nodeName: string; heroName?: string; heroEmoji?: string; zoneName: string; boss: boolean; shardId?: number }`.
    - `buildSanguoEncounterEmbed(data, t: TFunction): EmbedBuilder` — `.setColor(data.boss ? COLORS.GOLD : COLORS.SEASON)` (UI-SPEC color contract: GOLD accent reserved for boss only), `.setTitle(t(data.boss ? 'sanguo:encounter.boss_title' : 'sanguo:encounter.title'))`, description = boss ? `t('sanguo:encounter.boss_body', { zone: data.zoneName })` : `t('sanguo:encounter.body', { node: data.nodeName, hero_emoji: data.heroEmoji ?? '', hero: data.heroName ?? '' })`, `.setFooter(embedFooter(data.shardId))` + `.setTimestamp()`.
    - Guard heroEmoji usage at the call site (EMOJI_NOT_FOUND → name-only) per the map.ts:98 pattern.

    Edit `locales/{vi,en,zh-cn}/sanguo.json` — add an `encounter` block, identical key structure in all 3 locales (VI canonical; EN/ZH translate):
    - `encounter.title` = "⚔️ Kỳ ngộ"
    - `encounter.body` = "Trên đường đến **{node}**, bạn chạm trán **{hero_emoji} {hero}**."
    - `encounter.boss_title` = "👑 Boss thường xuất hiện!"
    - `encounter.boss_body` = "Một Boss thường trấn giữ **{zone}** đã xuất hiện trên đường của bạn. Hãy chuẩn bị!"

    Create `src/jobs/sanguoTickEncounters.ts` per RESEARCH §7 (the 6-step algorithm), with the cap check FIRST (Pitfall 7 — never roll before the cap check):
    - `export async function runSanguoTickEncounters(job: Job): Promise<void>` — logger.info start; `const now = Date.now()`.
    - Inside `db.transaction(async (tx) => ...)`: `const rows = await tx.select().from(playerTravelState).where(eq(playerTravelState.status, 'traveling')).for('update', { skipLocked: true })` (shared-lock intent — the row read is consistent, but this job writes NO player_travel_state columns; Pitfall 5).
    - Per row:
      1. **Cap check FIRST (D-13, Pitfall 7):** key `sanguo:enc:win:${row.userId}`; `await redis.zremrangebyscore(key, '-inf', '(' + (now - 3600_000))`; `const count = await redis.zcard(key)`; `if (capHit(count)) continue;` — silent skip: no record, no DM, travel continues.
      2. **Position (D-15):** total seconds from map_edges — `tx.select({ travelSeconds: mapEdges.travelSeconds }).from(mapEdges).where(or(and(eq(mapEdges.nodeAId, row.fromNodeId), eq(mapEdges.nodeBId, row.toNodeId)), and(eq(mapEdges.nodeAId, row.toNodeId), eq(mapEdges.nodeBId, row.fromNodeId))))`; missing edge → `logger.warn` + continue (no crash). `pos = positionFraction(row.travelSecondsRemaining, totalSeconds)`.
      3. **Zone rates:** read the from-zone and to-zone codes from map_nodes (by row.fromNodeId / row.toNodeId); fetch `hero_zone_rates` rows for both zones (`inArray(heroZoneRates.zone, [fromZone, toZone])`); dominant zone = `pos < 0.5 ? fromZone : toZone`.
      4. **Hero roll (D-10):** read `map_zones.encounterRate` for the dominant zone (default 0.35); `if (!shouldRoll(Number(rate))) continue;` — silent no-roll.
      5. **Boss sub-roll (D-14):** read `map_zones.bossRate` (default 0.07); `const isBoss = shouldRollBoss(Number(bossRate))`.
      6. **Pick + record + notify:** if isBoss → `heroId: null, encounterType: 'boss', zone: dominantZone`; else `const pick = pickEncounterHero(poolFromZone, poolToZone, pos); heroId: pick.heroId, encounterType: 'hero', zone: pick.zone`. `await tx.insert(encounterRuns).values({ userId: row.userId, travelId: row.id, zone, heroId, encounterType, status: 'pending' })`. `await redis.zadd(key, now, String(now))` — boss counts toward the cap (it IS an encounter). Collect for post-commit DMs.
    - AFTER the tx commits: for each recorded encounter resolve names (hero per-locale name + heroEmoji guard from assets/sanguoEmojis.ts; node per-locale name from map_nodes; zone name from map_zones) and `await notifyEncounter(userId, { nodeName, heroName, heroEmoji, zoneName, boss, shardId: undefined })` — outside the tx (D-12; DM failure must not roll back the record).
    - Per-row try/catch (footballResolveMatches pattern); final `logger.info('SanguoTickEncounters', `Job completed: ${job.id}. Encounters ${encounterCount}`)`.

    Edit `src/workers/pgBoss.ts`:
    - Import `runSanguoTickEncounters` from '../jobs/sanguoTickEncounters.js'.
    - In `registerJobs()` AFTER the arrivals block: `await b.createQueue('sanguo-tick-encounters'); await b.schedule('sanguo-tick-encounters', '*/45 * * * * *', {});` (every 45s — RESEARCH D-11 locked interval; 6-field cron verified against installed cron-parser) + the same `work` handler shape with localConcurrency 1 + per-job try/catch.
    - Append `sanguo-tick-encounters @ */45 * * * * *` to the final logger.info line.
    - Manager-only invariant preserved (registerJobs only from initPgBoss in bot.ts).
  </action>
  <verify>
    <automated>npx vitest run src/services/sanguo/__tests__/encounterService.test.ts; npm run check-i18n; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Command: `npx vitest run src/services/sanguo/__tests__/encounterService.test.ts` exits 0; `npm run check-i18n` exits 0; `npm run typecheck` exits 0.
    - Source: `src/jobs/sanguoTickEncounters.ts` contains `zremrangebyscore`, `zcard`, `zadd` Redis ops and `capHit(` called BEFORE any roll (Pitfall 7 ordering).
    - Source: `src/jobs/sanguoTickEncounters.ts` inserts encounterRuns with `encounterType` and `heroId: null` on the boss branch (D-14).
    - Source: `src/jobs/sanguoTickEncounters.ts` contains NO `tx.update(playerTravelState)` / `.update(playerTravelState)` call (single-writer rule).
    - Command (crypto-RNG gate, whole job): `grep -v "^[[:space:]]*//" src/jobs/sanguoTickEncounters.ts | grep -c "Math\.random"` outputs `0`.
    - Source: `src/workers/pgBoss.ts` contains `createQueue('sanguo-tick-encounters')` + `schedule('sanguo-tick-encounters', '*/45 * * * * *', {})` + the import of `runSanguoTickEncounters`.
    - Config: `locales/{vi,en,zh-cn}/sanguo.json` each contain `encounter.title`, `encounter.body`, `encounter.boss_title`, `encounter.boss_body`.
    - Source: `src/services/sanguo/sanguoNotificationService.ts` exports `notifyEncounter`.
    - Source: `src/ui/embeds/buildSanguoEncounterEmbed.ts` uses `COLORS.GOLD` on the boss branch and `COLORS.SEASON` otherwise.
  </acceptance_criteria>
  <done>Encounter tick rolls position-blended heroes + boss sub-rolls every 45s with cap-first silent skipping, records encounter_runs (D-14 type flag), notifies cross-shard, and never touches player_travel_state columns; pg-boss registration appended; tests + gates green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pg-boss encounter tick → Redis cap ZSET | Cap counter mutation races with the arrival tick's row locks |
| manager process → Discord REST API | Encounter DMs cross the same rate-limited external boundary as arrivals |
| tick → encounter_runs INSERT | Roll outcomes are player-facing game state; RNG integrity matters |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-13 | Spoofing / Information Disclosure | Roll prediction via the predictable global PRNG | high | mitigate | `crypto.randomInt` backed `cryptoUniform()` is the only default rng in encounterService (milestone mandate, ASVS V6); grep gates (`Math\.random` == 0) enforce it; deterministic rng only in injected tests. |
| T-09-14 | Elevation / Tampering | Cap evasion (burst past 20/hr) | medium | mitigate | Sliding-window ZSET with cap check BEFORE any roll (Pitfall 7) — a boundary user cannot squeeze an extra roll past the window; multi-account evasion is accepted as a soft-brake limitation (Phase 12 TQC-18 bot detection hardens). |
| T-09-15 | Tampering | Cap check-then-act race across two ticks in the same second | low | accept | ZREMRANGEBYSCORE → ZCARD → (roll) → ZADD ordering is not atomic; the micro-race allows at most 1 boundary overshoot — the cap is a soft brake per D-13, hardened in Phase 12. |
| T-09-16 | Tampering | Encounter tick corrupting the D-07 clock | high | mitigate | Single-writer rule enforced by construction: the tick inserts encounter_runs + ZADDs Redis only — zero `update(playerTravelState)` calls (Pitfall 5); SKIP LOCKED on the read keeps it consistent with the arrival tick's sweep. |
| T-09-17 | DoS | Encounter DM storm on closed DMs | medium | mitigate | Reuses the 3-strike Redis cap + silent skip from sanguoNotificationService (09-03); cap-skipped rolls are silent by design (D-13). |
| T-09-SC | Tampering | npm/pip/cargo installs | — | not-triggered | No new packages this phase (RESEARCH Package Legitimacy Audit); all deps installed + production-running. |
</threat_model>

<verification>
- `npx vitest run src/services/sanguo/__tests__/encounterService.test.ts` green (blend boundaries, threshold rolls, cap predicate).
- `npm run typecheck` + `npm run check-i18n` green (encounter.* keys in all 3 locales).
- Grep gates: `Math\.random` == 0 in encounterService.ts and sanguoTickEncounters.ts; `crypto.randomInt` present.
- Grep: no `update(playerTravelState)` in sanguoTickEncounters.ts (single-writer).
- Grep: `sanguo-tick-encounters` queue registered exactly once with `*/45 * * * * *`.
</verification>

## Artifacts this phase produces

| Symbol | Kind | File |
|--------|------|------|
| `positionFraction(remaining, total)` | pure position math (D-15) | src/services/sanguo/encounterService.ts |
| `shouldRoll(rate, rng)` / `shouldRollBoss(rate, rng)` | per-tick probability + boss sub-roll thresholds (D-10/D-14) | src/services/sanguo/encounterService.ts |
| `pickEncounterHero(poolA, poolB, pos, rng)` | position-blended weighted pick (linear, crypto RNG) | src/services/sanguo/encounterService.ts |
| `capHit(windowCount, limit)` / `cryptoUniform()` | cap predicate + crypto-backed uniform | src/services/sanguo/encounterService.ts |
| `runSanguoTickEncounters(job)` | encounter-roll cron job (cap-first, blend, boss, record, notify) | src/jobs/sanguoTickEncounters.ts |
| `sanguo-tick-encounters` queue @ `*/45 * * * * *` | pg-boss cron registration (manager-only) | src/workers/pgBoss.ts |
| `notifyEncounter(userId, data)` | encounter DM notification (normal + boss) | src/services/sanguo/sanguoNotificationService.ts |
| `buildSanguoEncounterEmbed(data, t)` | encounter DM embed (SEASON / GOLD boss variant) | src/ui/embeds/buildSanguoEncounterEmbed.ts |
| `encounter.title` / `encounter.body` / `encounter.boss_title` / `encounter.boss_body` | i18n keys (3 locales) | locales/{vi,en,zh-cn}/sanguo.json |
| encounterService test suite (blend/roll/cap) | test file | src/services/sanguo/__tests__/encounterService.test.ts |

<success_criteria>
- Encounters roll every 45s along journeys with the D-15 position-blended pool and D-14 boss sub-roll.
- The ~20/hr sliding-window cap silently skips over-cap rolls; travel is never blocked (D-13).
- All player-facing rolls ride crypto.randomInt (grep-enforced).
- encounter_runs records carry the D-14 encounter_type flag; DMs reach players cross-shard.
</success_criteria>

<output>
Create `.planning/phases/09-travel-encounters/09-04-SUMMARY.md` when done.
</output>
