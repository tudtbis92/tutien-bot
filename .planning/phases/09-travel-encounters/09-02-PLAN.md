---
phase: 09-travel-encounters
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - src/db/schema/mapZones.ts
  - src/db/schema/heroZoneRates.ts
  - src/db/schema/index.ts
  - scripts/data/sanguo-map-data.json
  - scripts/seed-sanguo.ts
  - src/commands/sanguo/map.ts
  - src/commands/sanguo/__tests__/map.test.ts
autonomous: false
requirements:
  - TQC-09

estimate:
  tokens: 64000
  raw_tokens: 32000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The map is a graph: 73 nodes + 162 undirected edges (canonical node_a < node_b, unique pair) + 18 zones + 208 hero_zone_rates rows covering all 132 roster heroes — committed as scripts/data/sanguo-map-data.json (D-17/D-18/D-19/D-21)."
    - "The placeholder Phase 8 map_nodes (7 nodes) are deleted and replaced by the 73 researched nodes; the hero seed (132) is untouched (D-20)."
    - "The seed is idempotent: re-running npm run seed:sanguo upserts zones/nodes/edges/hero_zone_rates on natural keys and never duplicates (D-11 pattern)."
    - "zone labels in /sanguo map now come from the map_zones per-locale names (not the first node's name) — 18 zones render accurate labels (A8)."
    - "hero_zone_rates.rate values are the research-set weights (1.0/0.5/0.3) — not tier-derived, not uniform (D-16)."
  artifacts:
    - src/db/schema/mapZones.ts — mapZones table (code unique, name_vi/en, name_zh nullable, sort_order, encounter_rate numeric default 0.35, boss_rate numeric default 0.07)
    - src/db/schema/heroZoneRates.ts — heroZoneRates table (hero_id FK, zone varchar, rate numeric(4,2), unique hero+zone)
    - src/db/schema/index.ts — Phase 9 re-exports (mapZones/heroZoneRates created here + mapEdges table authored in 09-01)
    - scripts/data/sanguo-map-data.json — full TQC-09 dataset (18 zones / 73 nodes / 162 edges / 208 rates)
    - scripts/seed-sanguo.ts — dataset loader + map data reseed (D-20 replace)
    - src/commands/sanguo/map.ts — zone label source switched to map_zones (A8)
  key_links:
    - "map_nodes.zone (varchar code) ↔ map_zones.code — zone code is the join key for encounter-pool blend math (per-zone rates key on the same code)."
    - "hero_zone_rates.hero_id ↔ heroes.id and hero_zone_rates.zone ↔ map_zones.code — the encounter roll reads these at tick time (09-04)."
  prohibitions:
    - "Hero seed rows (132 heroes + factions/families/relations/items) must not be deleted or altered — D-20 replaces ONLY node/zone/edge/hero-zone-rate data."
    - "nameZh must never be written as '' — keep the clobber-safe conditional spread `...(zh ? { nameZh: zh } : {})` (Pitfall 6)."
    - "No hand-written migration SQL — the 0018 migration is generated by npx drizzle-kit generate in plan 09-05."
    - "No new npm packages."
  flagged_assumptions:
    - "FLAGGED ASSUMPTION (TQC-09 unclassified): the dataset is machine-verified (132/132 heroes covered, 0 rep-hero/zone mismatches, 0 isolated nodes, 0 duplicate edges per RESEARCH); per D-21 the user reviews the committed dataset in Task 1 below before it is seeded."
    - "FLAGGED ASSUMPTION (A3 research): hero_zone_rates granularity is per-zone (hero_id + zone + rate), NOT per-node — chosen over the D-16 'zone(+node?)' alternative (208 rows vs 73×132)."
    - "FLAGGED ASSUMPTION (A7 research): map_zones.encounter_rate default 0.35 and boss_rate default 0.07 are zone-configurable data, not code — tunable without redeploy."

---

<objective>
What this plan accomplishes: the **TQC-09 map/zone data layer** — two new schema tables (`map_zones`, `hero_zone_rates`; the `map_edges` table itself is authored in 09-01 and only re-exported here), the committed TQC-09 dataset (`scripts/data/sanguo-map-data.json`: 18 zones, 73 nodes, 162 edges, 208 hero-zone rates), and the idempotent seed extension that REPLACES the Phase 8 placeholder nodes (D-20). The `/sanguo map` command switches its zone-label source to `map_zones` (A8).

Purpose: make the research dataset (RESEARCH.md §TQC-09 Dataset Design — the phase's primary deliverable) the seed source of truth, consumed at seed time (dev-time only, never runtime), with a user review gate (D-21) before the one-way placeholder replacement (D-20).

Output: graph map data model + full dataset + extended seed + map command zone-label accuracy.

**Scope note (2026-08-12 revision — checker scope_sanity WARNING accepted):** this plan intentionally stays at 4 tasks (checkpoint:decision + schema + data + integration). Task 3 (map.ts zone-label switch) was kept here rather than split into a later plan: it is the sole consumer of the Task-1 map_zones per-locale labels, it cannot start before 09-02 finishes anyway (it shares map.ts with 09-01's wave-1 travel wiring), and splitting it would create a single-task plan with no parallelism gain. The estimate (64% of budget) is within the advisory band — monitor context during execution and, if the executor reports pressure, Task 3 is the cleanest defer point.
</objective>

<execution_context>
@C:/Users/901107/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/901107/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-travel-encounters/09-CONTEXT.md
@.planning/phases/09-travel-encounters/09-RESEARCH.md
@.planning/phases/09-travel-encounters/09-PATTERNS.md
@.planning/phases/09-travel-encounters/09-UI-SPEC.md
@src/db/schema/mapNodes.ts
@src/db/schema/heroRelations.ts
@src/db/schema/heroFactions.ts
@src/db/schema/heroes.ts
@src/db/schema/index.ts
@scripts/seed-sanguo.ts
@src/commands/sanguo/map.ts
@src/commands/sanguo/__tests__/map.test.ts
@src/assets/sanguoEmojis.ts
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <decision>Approve the TQC-09 dataset for seeding (D-21 user review gate) and confirm the one-way placeholder replacement (D-20): the Phase 8 placeholder map_nodes rows (7 nodes) will be DELETED and replaced by the 73-node researched graph (18 zones, 162 edges, 208 hero_zone_rates) committed to scripts/data/sanguo-map-data.json. Hero seed (132) is untouched.</decision>
  <context>D-20 is one-way (a data migration that deletes placeholder nodes; re-seeding old nodes requires re-creating them). D-21 locks a user review of the research data before implementation. The dataset is machine-verified per RESEARCH (132/132 heroes covered, 0 isolated nodes, 0 duplicate edges). This checkpoint is the review gate — review the zone/node/edge/hero-zone-rate lists in RESEARCH.md §TQC-09 Dataset Design (or the committed JSON after Task 1) before the seed deletes anything.</context>
  <options>
    <option id="approve">
      <name>Approve dataset + proceed with D-20 replacement</name>
      <pros>Unlocks the graph map, position-blended encounter pools (D-15/D-16), and the D-18 ≥50-node scale; matches the locked decisions and RESEARCH dataset.</pros>
      <cons>None in scope — rate weights (1.0/0.5/0.3) and per-zone encounter/boss rates are tunable DATA (A3/A7), adjustable later without code changes.</cons>
    </option>
    <option id="adjust">
      <name>Request dataset changes first</name>
      <pros>Allows zone/node/edge/rate corrections before the one-way reseed.</pros>
      <cons>Halts this plan until the dataset is amended; D-20 deletion is deferred.</cons>
    </option>
  </options>
  <resume-signal>Select: approve or adjust (list the specific dataset changes)</resume-signal>
</task>

<task type="expansion">
  <name>Task 1 (expansion): map_zones + hero_zone_rates schema tables + index re-exports</name>
  <files>src/db/schema/mapZones.ts, src/db/schema/heroZoneRates.ts, src/db/schema/index.ts</files>
  <read_first>
    - src/db/schema/heroFactions.ts (reference-table pattern: code unique + per-locale names + sortOrder — analog for mapZones)
    - src/db/schema/heroes.ts lines 40-55 (FK style `references(() => heroes.id)` — needed for heroZoneRates.hero_id)
    - src/db/schema/mapNodes.ts (the zone column is a varchar code — mapZones.code is its key)
    - src/db/schema/mapEdges.ts (authored in 09-01 — the module the index re-export references; do NOT recreate the table)
    - src/db/schema/index.ts (Phase 8 re-export block at lines 27-43 — append a Phase 9 block)
    - .planning/phases/09-travel-encounters/09-PATTERNS.md §mapZones/§heroZoneRates (exact schema shapes, lines 371-399)
  </read_first>
  <action>
    Create `src/db/schema/mapZones.ts` per PATTERNS §mapZones (D-19 reference table):
    - `mapZones = pgTable('map_zones', { id: serial('id').primaryKey(), code: varchar('code', { length: 50 }).notNull().unique(), nameVi: varchar('name_vi', { length: 100 }).notNull(), nameEn: varchar('name_en', { length: 100 }).notNull(), nameZh: varchar('name_zh', { length: 100 }) /* nullable — clobber-safe seed */, sortOrder: smallint('sort_order').notNull(), encounterRate: numeric('encounter_rate').notNull().default('0.35'), bossRate: numeric('boss_rate').notNull().default('0.07') })`.
    - Zone codes are the 18 codes from RESEARCH §1: trung_nguyen, quan_trung, du_chau, duyen_chau, tu_chau, thanh_chau, ky_chau, u_chau, tinh_chau, luong_chau, kinh_chau, duong_chau, ich_chau, giao_chau, trieu_tien, o_hoan, tien_ti, hung_no.

    Create `src/db/schema/heroZoneRates.ts` per PATTERNS §heroZoneRates (D-16, A3 per-zone granularity):
    - `heroZoneRates = pgTable('hero_zone_rates', { id: serial('id').primaryKey(), heroId: integer('hero_id').notNull().references(() => heroes.id), zone: varchar('zone', { length: 50 }).notNull(), rate: numeric('rate', { precision: 4, scale: 2 }).notNull() }, (table) => [ uniqueIndex('hero_zone_rates_hero_zone_unique').on(table.heroId, table.zone) ])`.
    - Import `heroes` from './heroes.js'.

    Edit `src/db/schema/index.ts` — append a Phase 9 block after the Phase 8 post-gate block: `export * from './mapZones.js'; export * from './mapEdges.js'; export * from './heroZoneRates.js';` (mirror lines 27-43 style). `./mapEdges.js` is the module authored in 09-01 — re-export it here so drizzle-kit generate (09-05, schema = index.ts) and the rest of the system see the table; do NOT recreate the mapEdges module itself.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Command: `npm run typecheck` exits 0.
    - Source: `src/db/schema/mapZones.ts` defines columns code/name_vi/name_en/name_zh/sort_order/encounter_rate/boss_rate.
    - Source: `src/db/schema/heroZoneRates.ts` defines hero_id (references heroes.id) + zone + rate numeric(4,2) + uniqueIndex(hero_id, zone).
    - Source: `src/db/schema/index.ts` contains `export * from './mapZones.js'`, `'./mapEdges.js'`, `'./heroZoneRates.js'` (mapEdges re-export points at the 09-01-authored module).
  </acceptance_criteria>
  <done>Two new tables (map_zones, hero_zone_rates) defined with the researched column contracts; index re-exports them plus map_edges (module authored in 09-01); typecheck green. (Migration generated later in 09-05.)</done>
</task>

<task type="data">
  <name>Task 2 (data): commit the TQC-09 dataset + extend the seed for D-20 replacement</name>
  <files>scripts/data/sanguo-map-data.json, scripts/seed-sanguo.ts</files>
  <read_first>
    - .planning/phases/09-travel-encounters/09-RESEARCH.md §TQC-09 Dataset Design — the CANONICAL dataset source (zones §1 table, nodes §2 table, edges §3 lists, hero_zone_rates §4 list, data contract §Data Contract)
    - scripts/data/sanguo-classifications.json (dataset file pattern — committed dev-time JSON consumed by the seed)
    - scripts/seed-sanguo.ts lines 393-422 (map_nodes upsert loop to be replaced) + lines 71-79 (loadZhNames) + lines 162-169 (loadClassifications FATAL-on-missing pattern) + lines 455-475 (pair-table seed with onConflictDoNothing)
  </read_first>
  <action>
    Create `scripts/data/sanguo-map-data.json` — the full TQC-09 dataset transcribed from RESEARCH.md §TQC-09 Dataset Design (the canonical copy). Shape per the RESEARCH data contract:
    - `zones`: 18 entries `{ code, nameVi, nameEn, nameZh, sortOrder }` from RESEARCH §1 (nameZh per the table — e.g. trung_nguyen → 中原, hung_no → 匈奴).
    - `nodes`: 73 entries `{ code, nameVi, nameEn, nameZh, zone, nodeOrder, representativeHeroId }` from RESEARCH §2 (nameZh where provided, else omit — the seed's clobber-safe spread tolerates missing zh per A10; representativeHeroId = the hero_id snake_case code e.g. 'dong_trac'). **nodeOrder REQUIRED (B4):** `map_nodes.node_order` is `smallint notNull` (mapNodes.ts:20) and `/sanguo map` orders by `asc(nodeOrder)` (map.ts:68) — the dataset MUST carry a deterministic nodeOrder per node (e.g., a zone-grouped sequence: nodes of zone 1 first in the RESEARCH §2 table order, then zone 2, etc.) so the seed's NOT NULL constraint and the map ordering both hold. Do NOT rely on research §2 omitting it.
    - `edges`: 162 entries `{ nodeA, nodeB, travelSeconds }` from RESEARCH §3 (travelSeconds = minutes × 60; e.g. luoyang–hongnong 15 min → 900). All 6 region clusters must be transcribed completely.
    - `heroZoneRates`: 208 entries `{ heroId, zone, rate }` from RESEARCH §4 (the full hero → zone:rate list; hero with multiple zones = multiple rows; rate 1.0/0.5/0.3).
    - Counts MUST match: 18 zones, 73 nodes, 162 edges, 208 rates.

    Edit `scripts/seed-sanguo.ts`:
    - Add a loader for `sanguo-map-data.json` mirroring loadClassifications (seed-sanguo.ts:162-169): missing/corrupt file → console.error + process.exit(1) FATAL (this dataset is REQUIRED, unlike zh-names). Type the shape: `{ zones: ZoneDef[]; nodes: NodeDef[]; edges: EdgeDef[]; heroZoneRates: HeroZoneRateDef[] }`.
    - REPLACE the hardcoded `MAP_NODES` map-node loop (lines 393-422) with the dataset flow, in this order inside the existing seed transaction. **D-20 idempotency fix (B3):** because the delete+re-insert of map_nodes assigns NEW serial ids each run, the map_edges pairs and hero_zone_rates rows reference those ids — so the delete scope MUST include the child collections, otherwise edges/rates accumulate on every re-run (onConflictDoNothing can never collide with fresh ids). Delete in child→parent order:
      1. `await db.delete(schema.mapEdges)` (child — no FK, delete first)
      2. `await db.delete(schema.heroZoneRates)` (child — references heroes + zone code; delete next)
      3. `await db.delete(schema.mapNodes)` — D-20 placeholder replacement (heroes/factions/families/relations/items untouched)
      4. Upsert the 18 zones into `schema.mapZones` (onConflictDoUpdate target code, set nameVi/nameEn/sortOrder/encounterRate/bossRate + clobber-safe nameZh spread).
      5. Upsert the 73 nodes into `schema.mapNodes` (existing upsert shape, target code — now sourced from the dataset) and build a `code → id` map from the returned ids.
      6. Insert the 162 edges: canonicalize each pair (`node_a_id = min(idA, idB)`, `node_b_id = max(idA, idB)` per the heroRelations spouse-pairs loop at lines 460-475), `onConflictDoNothing()` keyed on the unique pair index; count only inserted rows.
      7. Insert the 208 hero_zone_rates: resolve heroId via the existing `heroIdToDbId` map (lines 457-459 — build it BEFORE the rates loop), `onConflictDoNothing()` keyed on (hero_id, zone) unique index.
      8. Update the final log line (line 477) to include zoneCount/nodeCount/edgeCount/rateCount, e.g. `[Seed] ... ${nodeCount} map_nodes, ${zoneCount} map_zones, ${edgeCount} map_edges, ${rateCount} hero_zone_rates upserted`.
    - **Full-replace is deterministic and idempotent:** each run ends with exactly 18 zones / 73 nodes / 162 edges / 208 rates — re-running yields identical row counts (the D-20 intent: map data fully owned by the committed dataset). The `nodeOrder` fix (B4) below keeps node ordering stable for the map embed.
    - Keep hero/faction/family/relation/item seeding identical (hero seed untouched per D-20).
    - Never write an empty string into the nameZh column anywhere — keep the clobber-safe conditional spread so a missing zh value yields NULL, never an empty string (Pitfall 6).
  </action>
  <verify>
    <automated>node -e "const d=require('./scripts/data/sanguo-map-data.json'); if(d.zones.length!==18||d.nodes.length!==73||d.edges.length!==162||d.heroZoneRates.length!==208) process.exit(1); console.log('dataset counts OK', d.zones.length, d.nodes.length, d.edges.length, d.heroZoneRates.length)"</automated>
  </verify>
  <acceptance_criteria>
    - Command: dataset count check (above) prints `dataset counts OK 18 73 162 208`.
    - Command: `npm run typecheck` exits 0.
    - Source: `scripts/seed-sanguo.ts` contains `db.delete(schema.mapEdges)`, `db.delete(schema.heroZoneRates)`, `db.delete(schema.mapNodes)` (child→parent full-replace — B3) and a FATAL-on-missing loader for sanguo-map-data.json.
    - Source: `scripts/seed-sanguo.ts` map-node loop reads from the dataset (no remaining reference to the hardcoded 7-node placeholder array as the node source).
    - Source: seed writes delete(mapEdges/heroZoneRates/mapNodes) → zones → nodes → edges (canonical min/max pair) → hero_zone_rates in that order, each idempotent (onConflict).
    - Source: every node entry in `scripts/data/sanguo-map-data.json` carries a `nodeOrder` (B4 — mapNodes.node_order NOT NULL; map.ts orders by it).
    - Grep: `grep -c "nameZh: ''" scripts/seed-sanguo.ts` outputs 0.
  </acceptance_criteria>
  <done>Full dataset committed with verified counts; seed extended to delete+reseed map data (D-20) while hero seed stays; idempotent upserts for all four new collections; typecheck green.</done>
  <precondition>The user approved the dataset in the preceding checkpoint:decision (D-21 review gate passed).</precondition>
</task>

<task type="integration">
  <name>Task 3 (integration): /sanguo map zone labels from map_zones + test update</name>
  <files>src/commands/sanguo/map.ts, src/commands/sanguo/__tests__/map.test.ts</files>
  <read_first>
    - src/commands/sanguo/map.ts (the file being modified — 110 lines; zoneMap construction at 70-86, pickName at 38-42)
    - src/commands/sanguo/__tests__/map.test.ts (both tests mock db.select returning map_nodes only — must add the map_zones select mock)
    - src/db/schema/mapZones.ts (created in Task 1 — the new label source)
  </read_first>
  <action>
    Edit `src/commands/sanguo/map.ts` to switch the zone label source from the first node's per-locale name to `map_zones` (A8):
    - Import `mapZones` from '../../db/schema/mapZones.js' and `eq` from 'drizzle-orm' as needed.
    - In execute(), load zones: `const zoneRows = await db.select().from(mapZones).orderBy(asc(mapZones.sortOrder));` build `zoneCodeToLabel: Map<string, string>` via a pickName-style per-locale read (nameEn for 'en', nameZh ?? nameVi for 'zh-cn', nameVi default).
    - Rebuild zoneMap: label = zoneCodeToLabel.get(row.zone) ?? pickName(row, locale) (fallback keeps label-only rendering safe when a zone row is missing); heroId stays = first node's representativeHeroId (unchanged behavior — zone marker emoji still node-derived per D-07).
    - Everything else (zonesContent '# ' headers, heroEmoji guard, embed) unchanged.
    - **Do not alter the travel subcommand wiring added by plan 09-01.**

    Edit `src/commands/sanguo/__tests__/map.test.ts`:
    - Update both tests' db mocks to also serve the map_zones select (a second `db.select()` chain returning zone rows, e.g. `[{ code: 'trung_nguyen', nameVi: 'Trung Nguyên', nameEn: 'Central Plains (Sili)', nameZh: '中原', sortOrder: 1 }, ...]`).
    - Assert the zone marker content now uses the zone name: first test expects `# <a:dtr_t0: Trung Nguyên`-style label (or whatever the mocked zone row provides) instead of the node-derived label where they differ.
  </action>
  <verify>
    <automated>npx vitest run src/commands/sanguo/__tests__/map.test.ts; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Command: `npx vitest run src/commands/sanguo/__tests__/map.test.ts` exits 0 with the updated zone-label assertions.
    - Command: `npm run typecheck` exits 0.
    - Source: `src/commands/sanguo/map.ts` contains `from(mapZones)` and builds zone labels from the zone table with a pickName fallback.
    - Source: `src/commands/sanguo/map.ts` still contains `.addSubcommand(travelSubcommand)` (plan 09-01 wiring intact).
  </acceptance_criteria>
  <done>/sanguo map renders 18 accurate zone labels from map_zones (fallback-safe); the map command's travel wiring from 09-01 is intact; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed script → production DB | The D-20 reseed DELETES map_nodes rows — a data-destructive write crossing into the live database |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-05 | Tampering | `db.delete(schema.mapNodes)` (D-20) | high | mitigate | Scope the delete to the map-data collections ONLY (mapEdges + heroZoneRates + mapNodes, child→parent) inside the seed transaction — heroes/factions/families/relations/items never touched; seed logs final counts (nodeCount 73, heroCount 132) so a stray delete is visible in output. |
| T-09-06 | Tampering | Dataset integrity (silent transcription error) | medium | mitigate | Committed JSON validated by the count gate (18/73/162/208) before seeding; RESEARCH dataset is machine-verified (0 isolated nodes, 0 duplicate edges); seed inserts edges canonicalized (min/max) with onConflictDoNothing so duplicates cannot double-insert. |
| T-09-07 | Repudiation | Seed idempotency (double-run) | high | mitigate | **Full-replace flow (B3):** the delete scope includes mapEdges + heroZoneRates + mapNodes (child→parent), so re-running deletes the previous run's rows (whose node ids differ after the serial advance) before re-inserting — the final state is identical every run, never accumulating duplicates. |
| T-09-08 | Spoofing | Zone-code injection via dataset | low | accept | Zone codes are dev-time committed data, not user input; mismatched codes surface as label fallback + encounter-pool miss at runtime (caught by Phase 12 monitoring). |
| T-09-SC | Tampering | npm/pip/cargo installs | — | not-triggered | No new packages this phase (RESEARCH Package Legitimacy Audit — all deps installed + production-running); no package-legitimacy checkpoint required. |
</threat_model>

<verification>
- Dataset count gate: `node -e "...zones.length!==18||nodes.length!==73||edges.length!==162||heroZoneRates.length!==208..."` prints `dataset counts OK 18 73 162 208`.
- `npm run typecheck` green.
- `npx vitest run src/commands/sanguo/__tests__/map.test.ts` green (zone labels from map_zones).
- Grep: `grep -c "nameZh: ''" scripts/seed-sanguo.ts` == 0 (clobber-safe).
- Grep (B3): the seed's map-data section deletes mapEdges + heroZoneRates + mapNodes (child→parent) before re-insert — full-replace idempotency.
- Grep (B4): every node in the committed dataset has a nodeOrder.
- Migration + seed RUN are gated to plan 09-05 ([BLOCKING] task) — this plan only authors schema/data/seed code.
</verification>

## Artifacts this phase produces

| Symbol | Kind | File |
|--------|------|------|
| `mapZones` table (code unique, name_vi/en, name_zh nullable, sort_order, encounter_rate 0.35, boss_rate 0.07) | table definition (D-19) | src/db/schema/mapZones.ts |
| `heroZoneRates` table (hero_id FK, zone, rate numeric(4,2), unique hero+zone) | table definition (D-16) | src/db/schema/heroZoneRates.ts |
| Phase 9 schema re-exports (mapZones/heroZoneRates + mapEdges from 09-01) | index wiring | src/db/schema/index.ts |
| 18 zones / 73 nodes / 162 edges / 208 hero_zone_rates | committed dataset (TQC-09) | scripts/data/sanguo-map-data.json |
| `loadSanguoMapData()` (FATAL-on-missing) + map delete/reseed flow + zone/edge/rate upserts | seed extension (D-20) | scripts/seed-sanguo.ts |
| zone label source = map_zones per-locale names (fallback pickName) | map command improvement (A8) | src/commands/sanguo/map.ts |
| updated zone-label test assertions | test update | src/commands/sanguo/__tests__/map.test.ts |

<success_criteria>
- The 73-node/162-edge graph + 18 zones + 208 rates dataset is committed and machine-verified by the count gate.
- The seed replaces placeholder nodes (D-20) via a **full-replace flow** (delete mapEdges + heroZoneRates + mapNodes child→parent → re-insert) that is **idempotent across re-runs** (B3 — identical final state, no edge/rate accumulation).
- Every dataset node carries a deterministic nodeOrder (B4) so map_nodes NOT NULL + map ordering hold.
- /sanguo map labels come from map_zones with fallback-safe rendering.
- Everything is migration-ready for 09-05's [BLOCKING] generate+migrate+seed run.
</success_criteria>

<output>
Create `.planning/phases/09-travel-encounters/09-02-SUMMARY.md` when done.
</output>
