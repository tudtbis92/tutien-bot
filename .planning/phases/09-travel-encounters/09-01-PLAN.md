---
phase: 09-travel-encounters
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema/playerTravelState.ts
  - src/db/schema/encounterRuns.ts
  - src/db/schema/mapEdges.ts
  - src/services/sanguo/travelService.ts
  - src/services/sanguo/__tests__/travelService.test.ts
  - src/commands/sanguo/travel.ts
  - src/commands/sanguo/map.ts
  - src/events/interactionCreate.ts
  - src/ui/components/sanguoTravelDestinationMenu.ts
  - src/ui/components/sanguoTravelButtons.ts
  - src/ui/embeds/buildSanguoTravelReplyEmbed.ts
  - src/commands/sanguo/__tests__/travel.test.ts
  - locales/vi/sanguo.json
  - locales/en/sanguo.json
  - locales/zh-cn/sanguo.json
autonomous: false
requirements:
  - TQC-06

estimate:
  tokens: 56000
  raw_tokens: 28000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "User runs /sanguo travel with NO active journey → the command replies an embed (current position) + a StringSelectMenu of adjacent nodes + a disabled 'Bắt đầu hành trình' button. Selecting a destination enables the button; pressing it writes the travel row (D-26 confirm gate, D-09 userId.unique())."
    - "The destination select menu lists only adjacent nodes (map_edges), nearest first by travel_seconds, capped at 25, value = stable node code, label = emoji+per-locale name when representative_hero_id exists (D-26, UI-SPEC)."
    - "A user already traveling (row status='traveling') invoking /sanguo travel does NOT start a new journey — the command takes the CHECK-IN path (delegates to travelCheckInService.checkInTravel, returns status/encounter/arrival inline — D-22/D-24)."
    - "A fabricated/non-adjacent destination never writes state — startTravel re-validates adjacency server-side and throws NO_ROUTE (Pitfall 4 defense in depth)."
    - "travelService contains no balance-deduction import and no deduction API call — travel costs only time (D-01)."
    - "The interaction router routes isStringSelectMenu() and isButton() with customId prefix 'sanguo:travel:' (dest/start) BEFORE the chat-input gate; travelService calls use user.id (users.id), NEVER char.id (characters.id) — playerTravelState.userId references users.id."
  artifacts:
    - src/services/sanguo/travelService.ts — START_NODE const + getCurrentPosition + getAdjacentNodes + startTravel(userId, toNodeCode)
    - src/db/schema/playerTravelState.ts — travel_seconds_remaining int notNull default 0 + encounter_active boolean notNull default false; arrive_at/cost dropped
    - src/db/schema/encounterRuns.ts — encounter_type varchar(20) notNull default 'hero'
    - src/db/schema/mapEdges.ts — map_edges table (node_a_id, node_b_id, travel_seconds, unique pair index) — the adjacency source getAdjacentNodes + startTravel NO_ROUTE validation read (D-17)
    - src/commands/sanguo/travel.ts — travelSubcommand builder + execute + select-menu/button handlers
    - src/commands/sanguo/map.ts — travel subcommand appended to the 'sanguo' builder
    - src/events/interactionCreate.ts — isStringSelectMenu()/isButton() branches for sanguo:travel:* customIds before line 448 gate
    - src/ui/components/sanguoTravelDestinationMenu.ts — buildDestinationMenu + customId consts
    - src/ui/components/sanguoTravelButtons.ts — buildStartButton + buildAckButton + customId consts
    - src/ui/embeds/buildSanguoTravelReplyEmbed.ts — buildSanguoTravelReplyEmbed(data, t)
    - locales/{vi,en,zh-cn}/sanguo.json — cmd.travel.description + travel.* keys (incl. start_button, dest_placeholder, ack_button)
    - src/services/sanguo/__tests__/travelService.test.ts + src/commands/sanguo/__tests__/travel.test.ts
  key_links:
    - "map.ts builder ↔ travel.ts subcommand module: exactly ONE file exports data with name 'sanguo' (Pitfall 3 — no second sanguo command file)."
    - "interactionCreate select/button branches ↔ component customId consts (sanguoTravelDestinationMenu.ts / sanguoTravelButtons.ts) ↔ travel.ts handlers — the wiring that makes components reachable."
    - "startTravel in-place UPDATE semantics ↔ userId.unique() — the row is simultaneously last-arrived-position and active-journey record."
    - "travelService user.id ↔ fetchCommandContext user.id (users.id) — NOT char.id (playersTravelState.userId references users.id)."
    - "mapEdges table (created in THIS wave) ↔ travelService.getAdjacentNodes + startTravel NO_ROUTE validation — the adjacency source must ship in the same wave as the service that queries it (09-01 typecheck would fail TS2307 module-not-found otherwise)."
  prohibitions:
    - "No balance-deduction module import or deduction API call anywhere in travelService (D-01)."
    - "No cancel path: player_travel_state.status values are 'traveling'|'arrived' only — 'cancelled' does not exist (D-03)."
    - "No second file exporting data with name 'sanguo' (Pitfall 3)."
    - "No autocomplete option/branch — destination picker is a StringSelectMenu (D-26)."
    - "No travelService call keyed on char.id — always user.id (users.id)."
    - "No Math.random for any player-facing roll."
    - "No hardcoded user-facing strings — all new UI strings via t('sanguo:...') (check-i18n gate)."
  flagged_assumptions:
    - "FLAGGED ASSUMPTION (TQC-06 unclassified): travel flow is implemented per D-01..D-09 + D-22..D-28 + UI-SPEC interaction contract (select menu ≤25, value=node code, no cost field, no cancel, confirm gate via Start button); the stale REQUIREMENTS.md TQC-06 'atomic wallet deduct + travel-cancel component' text is treated as INVALIDATED by D-01/D-03."
    - "FLAGGED ASSUMPTION (A6 research): START_NODE = 'luoyang' for first-ever journey (no travel row yet) — new users' first hop originates at Lạc Dương."
    - "FLAGGED ASSUMPTION (identity): travelService keys on users.id; the command passes user.id from fetchCommandContext (or char.userId), never char.id."

---

<objective>
What this plan accomplishes: the **travel journey-start slice** — the phase's tracer. A user runs `/sanguo travel`: with no active journey they see a destination select menu + Start button (D-26 confirm gate); pressing Start writes `player_travel_state` (INSERT on first journey, in-place UPDATE on subsequent per `userId.unique()`), with the D-07 pause-aware remaining-seconds clock and no money involved anywhere (D-01). With an active journey the command delegates to the **check-in path** (D-22 — `travelCheckInService.checkInTravel`, implemented in 09-03; this plan wires the call and result dispatch).

Purpose: prove the full interaction path (select menu + button routing → command dispatch → pure time/state service → DB row → inline reply embed) end-to-end before the tick/encounter systems build on it. This is the thinnest vertical slice that touches every layer the phase modifies on the travel side.

Output: evolved travel schema (D-07), pure `travelService` (TQC-06) keyed on users.id, `/sanguo travel` subcommand with StringSelectMenu + Start button (first components in the codebase), check-in dispatch stub, travel reply embed, `travel.*` i18n keys, unit tests.
</objective>

<execution_context>
@C:/Users/901107/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/901107/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-travel-encounters/09-CONTEXT.md
@.planning/phases/09-travel-encounters/09-RESEARCH.md
@.planning/phases/09-travel-encounters/09-PATTERNS.md
@.planning/phases/09-travel-encounters/09-UI-SPEC.md
@src/db/schema/playerTravelState.ts
@src/db/schema/encounterRuns.ts
@src/commands/sanguo/map.ts
@src/events/interactionCreate.ts
@src/utils/commandLoader.ts
@src/utils/commandContext.ts
@src/ui/embeds/buildSanguoMapEmbed.ts
@src/ui/embeds/buildErrorEmbed.ts
@src/ui/theme.ts
@src/assets/sanguoEmojis.ts
@src/i18n/index.ts
@src/commands/sanguo/__tests__/map.test.ts
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <decision>Confirm the two one-way economy/travel-commitment decisions before implementing travelService: D-01 (travel costs only TIME — no Linh thạch, no balance-deduction call anywhere in travel; inverts the milestone-init "paid travel = main sink") and D-03 (no cancel — travel is a one-way commitment; player_travel_state.status has no 'cancelled' value; ROADMAP SC2 will be amended in plan 09-05).</decision>
  <context>D-01 and D-03 are locked in CONTEXT.md but rated one-way (reversibility gates): undoing D-01 requires a new economy sign-off; undoing D-03 requires refund logic + a position model rework. The implementation that follows (schema drop of cost, service with no deduction call, no cancel branch) is structurally irreversible without a re-sign. This checkpoint is the last cheap moment to confirm.</context>
  <options>
    <option id="proceed">
      <name>Proceed per D-01/D-03</name>
      <pros>Matches the locked CONTEXT decisions and the approved UI-SPEC (no cost field, no destructive actions); travel stays pure time/state.</pros>
      <cons>None within scope — D-01 inverts the milestone-init paid-travel economy assumption (already flagged for the D-18 re-sign in 09-05).</cons>
    </option>
    <option id="revisit">
      <name>Revisit the travel cost model</name>
      <pros>Would re-open the milestone-init "paid travel" sink before it is structurally removed.</pros>
      <cons>Contradicts D-01/D-03, the UI-SPEC, and the phase scope; requires a new discuss-phase round.</cons>
    </option>
  </options>
  <resume-signal>Select: proceed or revisit</resume-signal>
</task>

<task type="tracer" tdd="true">
  <name>Task 1 (tracer): travelService + D-07/D-14 schema evolution + map_edges — the journey-start domain path (users.id)</name>
  <files>src/db/schema/playerTravelState.ts, src/db/schema/encounterRuns.ts, src/db/schema/mapEdges.ts, src/services/sanguo/travelService.ts, src/services/sanguo/__tests__/travelService.test.ts</files>
  <read_first>
    - src/db/schema/playerTravelState.ts (the file being modified — 30 lines; D-07 remaining-seconds model)
    - src/db/schema/encounterRuns.ts (the file being modified — D-14 boss flag)
    - src/services/football/matchLifecycleService.ts lines 333-345 (FOR UPDATE / tx shape)
    - src/db/schema/heroRelations.ts (undirected pair-table + unique-index style — the DIRECT analog for the map_edges table created below, D-17)
    - .planning/phases/09-travel-encounters/09-PATTERNS.md §mapEdges (exact schema shape, lines 356-369 — created in this plan, wave 1)
    - src/db/schema/mapNodes.ts (column set the service reads: code/name_vi/name_en/name_zh/zone/representative_hero_id)
    - src/db/schema/users.ts (userId FK + locale column)
    - src/commands/sanguo/__tests__/map.test.ts lines 8-54 (db-mocking pattern for the test file)
    - .planning/phases/09-travel-encounters/09-RESEARCH.md §5 travelService design + §6 schema evolution (A3/A4/A6/A7 discretion decisions)
  </read_first>
  <behavior>
    - Test 1: getCurrentPosition returns { nodeId, nodeCode } = START_NODE('luoyang') when no travel row exists; = toNodeId when status='arrived'; = fromNodeId when status='traveling'.
    - Test 2: getAdjacentNodes returns edges joined to node/zone data ordered by travelSeconds ASC, capped at 25.
    - Test 3: startTravel(userId, toNodeCode) resolves the code → node id via map_nodes.code, INSERTs on first journey and UPDATES the existing row in place on subsequent journeys (fromNodeId = old to, toNodeId = dest, travelSecondsRemaining = edge.travelSeconds, encounterActive = false, status = 'traveling', departAt/updatedAt = now).
    - Test 4: startTravel throws ALREADY_TRAVELING when the row status='traveling' (D-09).
    - Test 5: startTravel throws NO_ROUTE when the destination is not adjacent to current position (Pitfall 4); throws when the code is unknown.
    - Test 6: the module imports no balance-deduction API and calls none (D-01) — grep gate in verify.
  </behavior>
  <action>
    Edit `src/db/schema/playerTravelState.ts` to the D-07 remaining-seconds model per RESEARCH §6:
    - DROP `arriveAt: timestamp('arrive_at'...)` and `cost: bigint('cost'...)` columns entirely (D-01 — travel is free; money never involved).
    - ADD `travelSecondsRemaining: integer('travel_seconds_remaining').notNull().default(0)` (decrementing, pause-aware).
    - ADD `encounterActive: boolean('encounter_active').notNull().default(false)` (D-07/D-25 pause flag — set true on a roll hit, cleared by the ack button in 09-03; Phase 9 leaves it observably false between check-ins).
    - KEEP `id serial PK`, `userId int notNull references users.id unique` (D-09 one row per user forever), `fromNodeId/toNodeId integer` (plain ints, no FK — keep the existing header-comment rationale), `departAt timestamp withTimezone notNull`, `status varchar(20) notNull default 'traveling'` (comment: values now 'traveling'|'arrived' only — 'cancelled' removed per D-03), `createdAt/updatedAt` (updatedAt is the elapsed-time anchor).
    - Column style must mirror the existing lines: `timestamp('depart_at', { withTimezone: true }).notNull()`.

    Edit `src/db/schema/encounterRuns.ts` to add the D-14 boss flag per RESEARCH §6:
    - ADD `encounterType: varchar('encounter_type', { length: 20 }).notNull().default('hero')` — values 'hero'|'boss'.
    - KEEP `heroId integer references heroes.id` nullable — boss encounters write hero_id NULL + encounter_type='boss' + zone.

    Create `src/db/schema/mapEdges.ts` per PATTERNS §mapEdges (D-17 graph — created HERE in wave 1 because getAdjacentNodes and startTravel's NO_ROUTE validation query this table; 09-01's `npm run typecheck` cannot pass without it):
    - `mapEdges = pgTable('map_edges', { id: serial('id').primaryKey(), nodeAId: integer('node_a_id').notNull(), nodeBId: integer('node_b_id').notNull(), travelSeconds: integer('travel_seconds').notNull() }, (table) => [ uniqueIndex('map_edges_pair_unique').on(table.nodeAId, table.nodeBId) ])`.
    - Plain integers, NO references() — mirror heroRelations' undirected unique-pair pattern; import `pgTable`, `serial`, `integer`, `uniqueIndex` from 'drizzle-orm/pg-core'.
    - The `index.ts` re-export for mapEdges lands in 09-02 (with mapZones/heroZoneRates — drizzle-kit generate reads index.ts at 09-05 migration time); travelService below imports mapEdges via the DIRECT module path (codebase convention — wallet.ts:3, map.ts:7) so this plan is self-contained for typecheck.

    Create `src/services/sanguo/travelService.ts` — pure time/state service per RESEARCH §5. **It must not import, reference, or call any balance-deduction module or API (D-01)** — structurally no money. Imports: `import { mapEdges } from '../../db/schema/mapEdges.js'` (direct module path — the file created in this plan) and `import { mapNodes } from '../../db/schema/mapNodes.js'` (existing Phase 8 schema). Export:
    - `export const START_NODE = 'luoyang'` (A6).
    - `getCurrentPosition(userId: number): Promise<{ nodeId: number; nodeCode: string }>` — read player_travel_state by userId (users.id); status='arrived' → toNodeId; status='traveling' → fromNodeId (in-flight position); no row → START_NODE resolved via mapNodes.code lookup.
    - `getAdjacentNodes(nodeId: number): Promise<AdjacentNode[]>` — query map_edges where node_a_id = :nodeId OR node_b_id = :nodeId, join map_nodes for the neighbor's per-locale names + representative_hero_id + zone, order by travelSeconds ASC, limit 25. AdjacentNode = { nodeId, code, nameVi, nameEn, nameZh, zone, travelSeconds, representativeHeroId }.
    - `startTravel(userId: number, toNodeCode: string): Promise<{ etaSeconds: number }>` — in ONE transaction: (a) resolve toNodeCode → node id via mapNodes.code lookup (D-20-resilient: codes stable across reseeds, ids are not); (b) read the current row; if status='traveling' throw `Error('ALREADY_TRAVELING')` (D-09); (c) re-validate the edge from current position to the resolved dest against map_edges (OR-match both orientations); missing edge → throw `Error('NO_ROUTE')` (Pitfall 4 — the select value is advisory); (d) INSERT on first journey / in-place UPDATE on subsequent (userId.unique() = one row forever): set fromNodeId=current nodeId, toNodeId=dest, travelSecondsRemaining=edge.travelSeconds, encounterActive=false, status='traveling', departAt=now, updatedAt=now; (e) NO deduction call; return { etaSeconds: edge.travelSeconds }.
    - Error style: plain `throw new Error('CODE')` mirroring wallet.ts:61; transaction style `db.transaction(async (tx) => ...)` per matchLifecycleService.ts:334.

    Create `src/services/sanguo/__tests__/travelService.test.ts` per the 6 behaviors above. Mock `db/client.js` with the map.test.ts pattern (lines 8-54): chainable select/from/where/orderBy mocks per call site; mock `map_edges`/`map_nodes`/`playerTravelState` rows as plain objects. The no-deduction test is the grep gate in verify, not a runtime assertion.
  </action>
  <verify>
    <automated>npx vitest run src/services/sanguo/__tests__/travelService.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Command: `npx vitest run src/services/sanguo/__tests__/travelService.test.ts` exits 0 with all 6 behaviors passing.
    - Command: `npm run typecheck` exits 0.
    - Source: `src/db/schema/playerTravelState.ts` contains `travel_seconds_remaining` and `encounter_active` and does NOT contain `arrive_at` or `cost`.
    - Source: `src/db/schema/encounterRuns.ts` contains `encounter_type`.
    - Source: `src/db/schema/mapEdges.ts` defines node_a_id/node_b_id/travel_seconds and `uniqueIndex('map_edges_pair_unique')` on the pair.
    - Source: `src/services/sanguo/travelService.ts` exports `START_NODE`, `getCurrentPosition`, `getAdjacentNodes`, `startTravel`; `startTravel(userId, toNodeCode)` resolves the code → id and contains `throw new Error('ALREADY_TRAVELING')` and `throw new Error('NO_ROUTE')`.
    - Command (D-01 gate): `grep -v "^[[:space:]]*//" src/services/sanguo/travelService.ts | grep -cE "deductBalance|services/wallet"` outputs `0`.
    - Source: `src/services/sanguo/travelService.ts` contains `userId.unique()`-preserving in-place update via `.update(playerTravelState)` keyed on `userId` (not a delete+insert).
  </acceptance_criteria>
  <done>travelService implements getCurrentPosition/getAdjacentNodes/startTravel per RESEARCH §5 keyed on users.id; schema carries the D-07 remaining-seconds + D-14 boss-type columns + the D-17 map_edges table; all 6 unit behaviors pass; no deduction API anywhere in the service; typecheck green.</done>
  <reversibility rating="one-way">Implements D-01 (no-money travel, cost column dropped) and D-03 (no cancel status) — structural removal gated by the preceding checkpoint:decision.</reversibility>
</task>

<task type="expansion" tdd="true">
  <name>Task 2 (expansion): /sanguo travel subcommand + destination StringSelectMenu + Start button + reply embed + i18n travel keys</name>
  <files>src/commands/sanguo/travel.ts, src/commands/sanguo/map.ts, src/events/interactionCreate.ts, src/ui/components/sanguoTravelDestinationMenu.ts, src/ui/components/sanguoTravelButtons.ts, src/ui/embeds/buildSanguoTravelReplyEmbed.ts, locales/vi/sanguo.json, locales/en/sanguo.json, locales/zh-cn/sanguo.json, src/commands/sanguo/__tests__/travel.test.ts</files>
  <read_first>
    - src/commands/sanguo/map.ts (the file being modified — 110 lines; builder at 14-31, pickName at 38-42, execute at 44-109)
    - src/events/interactionCreate.ts (button branch at ~380-445; the chat-input gate at 448 — select/button branches go BEFORE it; `interaction.user.id` user-row lookup pattern at 462-465)
    - src/utils/commandContext.ts (fetchCommandContext — returns user.id = users.id; char is the characters row; do NOT use char.id for travel keys)
    - src/ui/embeds/buildSanguoMapEmbed.ts (embed builder pattern) + src/ui/theme.ts (COLORS.SEASON, embedFooter) + src/ui/embeds/buildErrorEmbed.ts (DANGER pattern)
    - src/assets/sanguoEmojis.ts heroEmoji() (throws EMOJI_NOT_FOUND on unknown heroId — guard per map.ts:98)
    - locales/vi/sanguo.json (key shape: cmd.* + map.*)
    - .planning/phases/09-travel-encounters/09-PATTERNS.md §travel.ts + §interactionCreate + §embed builders + RESEARCH Pattern 4 (inline result + ack button) + §Code Examples "StringSelectMenu + buttons"
    - .planning/phases/09-travel-encounters/09-UI-SPEC.md §Interaction contract — destination picker (StringSelectMenu) + §Copywriting Contract
  </read_first>
  <behavior>
    - Test 1: execute('travel') with NO active journey replies the select menu + disabled Start button (start mode).
    - Test 2: selecting a destination (StringSelectMenu sanguo:travel:dest) updates the reply with the chosen destination + ETA and enables the Start button.
    - Test 3: pressing Start (sanguo:travel:start) calls travelService.startTravel(user.id, selectedCode) and editReplies with buildSanguoTravelReplyEmbed containing destination/ETA/from fields and NO cost field.
    - Test 4: startTravel ALREADY_TRAVELING → the command takes the check-in path (delegates to checkInTravel, 09-03 stub returns { mode: 'status' } in this wave) — replies a status embed.
    - Test 5: startTravel NO_ROUTE → no_route_title DANGER embed (server-side re-validation).
    - Test 6: interactionCreate routes isStringSelectMenu()/isButton() with customId prefix 'sanguo:travel:' before the chat-input gate.
    - Test 7: all travelService calls pass user.id (users.id), never char.id.
  </behavior>
  <action>
    Create `src/ui/components/sanguoTravelDestinationMenu.ts`:
    - `export const DEST_MENU_ID = 'sanguo:travel:dest';`
    - `buildDestinationMenu(adjacent: AdjacentNode[], locale: SupportedLocale): StringSelectMenuBuilder` — `new StringSelectMenuBuilder().setCustomId(DEST_MENU_ID).setPlaceholder(t('sanguo:travel.dest_placeholder')).setMinValues(1).setMaxValues(1).addOptions(adjacent.slice(0, 25).map(n => new StringSelectMenuOptionBuilder().setLabel(...).setValue(n.code).setDescription(...)))` — label = representativeHeroId present ? `${emoji}${perLocaleName}` : perLocaleName (guard heroEmoji in try/catch, EMOJI_NOT_FOUND → name-only per map.ts:98); value = node.code (stable — D-26); description = `~${Math.round(n.travelSeconds/60)} phút` (i18n via t).
    - Import `StringSelectMenuBuilder`, `StringSelectMenuOptionBuilder` from 'discord.js'; `pickName`/locale logic per map.ts:38-42.

    Create `src/ui/components/sanguoTravelButtons.ts`:
    - `export const START_BTN_ID = 'sanguo:travel:start';`
    - `export const ACK_BTN_ID = 'sanguo:travel:ack';` (used by 09-03; defined here for the shared customId contract)
    - `buildStartButton(t: TFunction, disabled = true): ButtonBuilder` — `.setCustomId(START_BTN_ID).setLabel(t('sanguo:travel.start_button')).setStyle(ButtonStyle.Primary).setDisabled(disabled)`.
    - `buildAckButton(t: TFunction): ButtonBuilder` — `.setCustomId(ACK_BTN_ID).setLabel(t('sanguo:travel.ack_button')).setStyle(ButtonStyle.Secondary)`.
    - Import `ButtonBuilder`, `ButtonStyle` from 'discord.js'.

    Create `src/commands/sanguo/travel.ts` exporting:
    - `travelSubcommand: SlashCommandSubcommandBuilder` — `.setName('travel')` + localized descriptions (static Discord API strings inside the i18next-lint disable block, mirroring map.ts:14-31). **NO options** — the destination picker is the select menu (D-26).
    - `execute(interaction: ChatInputCommandInteraction): Promise<void>` — defer → fetchCommandContext → not-registered guard (buildErrorEmbed + `t('common:errors.notRegistered')`) → call `travelService.getCurrentPosition(user.id)`; if the user is traveling or encounter-active → **check-in path**: `travelCheckInService.checkInTravel(user.id)` (imported from 09-03; in this wave a thin stub that returns `{ mode: 'status', remaining }` via a minimal inline read) and dispatch the result embed; else **start mode**: `getAdjacentNodes(pos.nodeId)` → reply embed (current position + hint) + `buildDestinationMenu(adjacent, locale)` + `buildStartButton(t, true)` in a component row.
    - `handleDestinationSelect(interaction: StringSelectMenuInteraction): Promise<void>` — read the chosen code; fetch user row (users.id); `getCurrentPosition(user.id)` → `getAdjacentNodes` → find the selected node → update the reply with destination + ETA (buildSanguoTravelReplyEmbed) + an ENABLED Start button; store the selection for the Start press (either re-validate from the interaction's persisted value or re-derive; the Start handler must NOT trust the select value blindly — it re-validates adjacency via startTravel's NO_ROUTE).
    - `handleStartPress(interaction: ButtonInteraction): Promise<void>` — fetch user row; `travelService.startTravel(user.id, selectedCode)` in try/catch: ALREADY_TRAVELING → check-in path; NO_ROUTE → DANGER `no_route`; success → buildSanguoTravelReplyEmbed; generic → DANGER `travel.error`.
    - All travelService/check-in calls use `user.id` (users.id) from the direct users-row lookup (fetchCommandContext types ChatInputCommandInteraction; for StringSelectMenuInteraction/ButtonInteraction read the user row directly per interactionCreate.ts:462-465).

    Edit `src/commands/sanguo/map.ts`:
    - Append `.addSubcommand(travelSubcommand)` to the existing `data` builder (import `{ travelSubcommand } from './travel.js'`).
    - In execute(), dispatch: `if (subcommand === 'travel') return travelExecute(interaction)` (import travel's execute) — keep the existing map flow for 'map'.
    - **Do NOT create a second file exporting `data` with name 'sanguo'** (Pitfall 3 — registerCommands PUTs per file; two files with the same name = last-wins flakiness).

    Edit `src/events/interactionCreate.ts`:
    - Insert BEFORE the `if (!interaction.isChatInputCommand()) return;` gate at line 448:
      - `if (interaction.isStringSelectMenu())` — if `interaction.customId === DEST_MENU_ID`: fetch the travel command module (`client.commands.get('sanguo')`) and dispatch to its `handleDestinationSelect`; try/catch + log `logger.error('InteractionCreate', ...)`.
      - Extend the existing button branch (`interaction.isButton()`, ~line 380) with `sanguo:travel:start` → dispatch `handleStartPress`; `sanguo:travel:ack` → (09-03, defined here as the shared customId contract — a stub that replies deferUpdate in this wave).
    - Import the customId consts from `../ui/components/sanguoTravelDestinationMenu.js` + `sanguoTravelButtons.js`.

    Create `src/ui/embeds/buildSanguoTravelReplyEmbed.ts`:
    - `export interface SanguoTravelReplyEmbedData { destinationName: string; fromNodeName: string; etaSeconds: number; shardId?: number }`.
    - `buildSanguoTravelReplyEmbed(data, t: TFunction): EmbedBuilder` — `.setColor(COLORS.SEASON)` (never hardcode hex), `.setTitle(t('sanguo:travel.started_title'))`, fields: destination = `t('sanguo:travel.destination', { node: data.destinationName })`, eta = `t('sanguo:travel.eta', { eta: humanizeEta(data.etaSeconds, t) })`, from = `t('sanguo:travel.from', { from_node: data.fromNodeName })`. **NO cost field (D-01).** `.setFooter(embedFooter(data.shardId))` + `.setTimestamp()`.
    - Export a `humanizeEta(seconds, t)` helper: >= 3600 → `t('sanguo:travel.eta_hours_minutes', { count: hours, hours, minutes })` else `t('sanguo:travel.eta_minutes', { count: minutes, n: Math.max(1, Math.round(seconds/60)) })` — i18next plural forms `_one`/`_other` (identical structure in all 3 locales for check-i18n).

    Edit `locales/vi/sanguo.json`, `locales/en/sanguo.json`, `locales/zh-cn/sanguo.json` — add to the `cmd` block `"travel": { "description": ... }` and a new top-level `travel` block with keys (VI canonical below; EN/ZH translate per existing sanguo.json tone, identical key structure):
    - `travel.started_title` = "🧭 Hành trình bắt đầu"
    - `travel.destination` = "Điểm đến: **{node}**"
    - `travel.eta` = "Thời gian dự kiến: **{eta}**"
    - `travel.from` = "Xuất phát: **{from_node}**"
    - `travel.eta_minutes_one` = "~{n} phút" / `travel.eta_minutes_other` = "~{n} phút"
    - `travel.eta_hours_minutes_one` = "~{h} giờ {m} phút" / `_other` same
    - `travel.dest_placeholder` = "Chọn điểm đến"
    - `travel.start_button` = "Bắt đầu hành trình"
    - `travel.ack_button` = "Tiếp tục hành trình"
    - `travel.no_route_title` = "Không có đường đi"
    - `travel.no_route_body` = "Vị trí hiện tại của bạn không kết nối với địa danh nào. Hãy mở /sanguo map để xem bản đồ."
    - `travel.error` = "Có lỗi khi bắt đầu hành trình. Hãy thử lại."
    - `cmd.travel.description` = "Bắt đầu hành trình đến một địa danh" (VI).
    All three locale files MUST carry the identical key set (check-i18n compares against the VI reference).

    Create `src/commands/sanguo/__tests__/travel.test.ts` per the 7 behaviors — mock db/client.js + commandContext per map.test.ts:8-54; mock travelService with vi.mock; mock the select/button builders if needed; assert embed field presence, DANGER color path via buildErrorEmbed, user.id (not char.id) passed to startTravel, and the component rows in the reply.
  </action>
  <verify>
    <automated>npx vitest run src/commands/sanguo/__tests__/travel.test.ts; npm run check-i18n; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Command: `npx vitest run src/commands/sanguo/__tests__/travel.test.ts` exits 0.
    - Command: `npm run check-i18n` exits 0 (all 3 locales key-synced).
    - Command: `npm run typecheck` exits 0.
    - Source: `src/commands/sanguo/map.ts` contains `.addSubcommand(travelSubcommand)`.
    - Source: `src/commands/sanguo/travel.ts` exports `travelSubcommand`, `execute`, `handleDestinationSelect`, `handleStartPress`.
    - Source: `src/events/interactionCreate.ts` contains `isStringSelectMenu()` and the `sanguo:travel:start`/`sanguo:travel:ack` button branches BEFORE the `isChatInputCommand()` gate.
    - Source: `src/ui/components/sanguoTravelDestinationMenu.ts` exports `DEST_MENU_ID` + `buildDestinationMenu`; `sanguoTravelButtons.ts` exports `START_BTN_ID`/`ACK_BTN_ID` + `buildStartButton`/`buildAckButton`.
    - Config: `locales/vi/sanguo.json`, `locales/en/sanguo.json`, `locales/zh-cn/sanguo.json` each contain `"travel.started_title"`, `"travel.no_route_title"`, `"travel.start_button"`, `"travel.ack_button"`, `"cmd.travel.description"`.
    - Source: `src/ui/embeds/buildSanguoTravelReplyEmbed.ts` uses `COLORS.SEASON` and contains no `cost` field and no balance-deduction reference.
    - Grep: no second `data` export with name 'sanguo' — `grep -rl "setName('sanguo')" src/commands/sanguo/` returns exactly `[map.ts]`-only output (travel.ts must not call setName('sanguo')).
    - Grep: no autocomplete option — `grep -c "setAutocomplete" src/commands/sanguo/travel.ts` outputs `0`.
    - Grep (identity): `grep -cE "startTravel\(char\.id|getCurrentPosition\(char\.id" src/commands/sanguo/travel.ts` outputs `0`.
  </acceptance_criteria>
  <done>/sanguo travel renders the destination select menu + Start button (confirm gate); Start writes the journey and replies the SEASON embed (destination/ETA/from, no cost); check-in dispatch wired (stub in this wave); select/button routing in interactionCreate; check-i18n + typecheck green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (Discord) → interaction router | Untrusted interaction payload crosses here — select-menu/button values are advisory strings, never authoritative |
| interaction → travelService | Node-code input from the payload; must be re-validated against map_edges before any state write |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Tampering | `startTravel` destination input | high | mitigate | Server-side adjacency re-validation inside the transaction: missing edge → `NO_ROUTE` error before any write (Pitfall 4). The select value is never authoritative. |
| T-09-02 | Spoofing | Stale/fabricated select-menu choice | medium | mitigate | Same server-side re-validation (T-09-01); the reply path surfaces `no_route_title` DANGER embed when the pick no longer matches the edge set. |
| T-09-03 | Tampering | Concurrent `startTravel` for the same user (double-journey race) | medium | mitigate | Single transaction reads the row first; `userId.unique()` backstops the INSERT path; the UPDATE path re-checks status inside the tx → second writer sees 'traveling' and throws ALREADY_TRAVELING (D-09). |
| T-09-04 | Spoofing | Destination code values | low | accept | Node codes are opaque DB keys with no user-facing semantics; a valid-but-wrong code simply yields NO_ROUTE. No injection surface (drizzle parameterization). |
| T-09-05 | Tampering | Fabricated component customId (`sanguo:travel:*`) | low | mitigate | Router validates the customId prefix before dispatch; handlers re-check row status + adjacency server-side. |
| T-09-SC | Tampering | npm/pip/cargo installs | — | not-triggered | Phase 9 introduces NO new packages (RESEARCH Package Legitimacy Audit — all dependencies already installed + production-running); no package-legitimacy checkpoint required. |
</threat_model>

<verification>
- `npx vitest run src/services/sanguo/__tests__/travelService.test.ts src/commands/sanguo/__tests__/travel.test.ts` green.
- `npm run typecheck` green; `npm run check-i18n` green (travel.* keys present in all 3 locales).
- Grep gate: `grep -v "^[[:space:]]*//" src/services/sanguo/travelService.ts | grep -cE "deductBalance|services/wallet"` == 0.
- Grep gate: exactly one file in src/commands/sanguo/ exports `setName('sanguo')`.
- Grep gate: no `setAutocomplete` in travel.ts; no `startTravel(char.id`/`getCurrentPosition(char.id` in travel.ts.
</verification>

## Artifacts this phase produces

| Symbol | Kind | File |
|--------|------|------|
| `travel_seconds_remaining` / `encounter_active` columns; `arrive_at` + `cost` dropped | schema columns | src/db/schema/playerTravelState.ts |
| `encounter_type varchar(20) default 'hero'` | schema column (D-14 boss flag) | src/db/schema/encounterRuns.ts |
| `mapEdges` table (node_a_id, node_b_id, travel_seconds, unique pair index) | table definition (D-17) — the adjacency source getAdjacentNodes + startTravel NO_ROUTE validation read | src/db/schema/mapEdges.ts |
| `START_NODE`, `getCurrentPosition()`, `getAdjacentNodes()`, `startTravel(userId, toNodeCode)` | service functions (pure time/state, D-01, users.id) | src/services/sanguo/travelService.ts |
| `travelSubcommand`, `execute()`, `handleDestinationSelect()`, `handleStartPress()` | command module exports | src/commands/sanguo/travel.ts |
| travel subcommand appended to `data` builder | command wiring (Pitfall 3-safe) | src/commands/sanguo/map.ts |
| `DEST_MENU_ID`, `buildDestinationMenu()` | StringSelectMenu (D-26) | src/ui/components/sanguoTravelDestinationMenu.ts |
| `START_BTN_ID`/`ACK_BTN_ID`, `buildStartButton()`/`buildAckButton()` | buttons (D-25/D-26) | src/ui/components/sanguoTravelButtons.ts |
| `isStringSelectMenu()` + `sanguo:travel:*` button branches before the chat-input gate | interaction router (first components in codebase) | src/events/interactionCreate.ts |
| `buildSanguoTravelReplyEmbed(data, t)` + `humanizeEta(seconds, t)` | embed builder functions | src/ui/embeds/buildSanguoTravelReplyEmbed.ts |
| `cmd.travel.description` + `travel.*` keys (3 locales) | i18n keys | locales/{vi,en,zh-cn}/sanguo.json |
| travelService flow tests (6 behaviors) + travel command tests (7 behaviors) | test suites | src/services/sanguo/__tests__/travelService.test.ts, src/commands/sanguo/__tests__/travel.test.ts |

<success_criteria>
- The journey-start path is proven end-to-end: select menu + Start button → router → command → travelService → DB row → SEASON reply embed with no cost field.
- The D-07 remaining-seconds schema, D-14 boss-type column, and D-17 map_edges table are committed (migration generated in 09-05) — the adjacency table ships in the same wave as the service that reads it, so 09-01 typecheck passes standalone.
- No money path exists in travel (grep gate == 0).
- First message components (select + buttons) in the codebase are routed and tested.
- Check-in dispatch is wired (stub) so 09-03 can replace it with the full engine.
</success_criteria>

<output>
Create `.planning/phases/09-travel-encounters/09-01-SUMMARY.md` when done.
</output>
