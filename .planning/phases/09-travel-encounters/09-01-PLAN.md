---
phase: 09-travel-encounters
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema/playerTravelState.ts
  - src/db/schema/encounterRuns.ts
  - src/db/schema/mapEdges.ts
  - src/services/sanguo/travelService.ts
  - src/services/sanguo/__tests__/travelService.test.ts
  - src/commands/sanguo/travel.ts
  - src/commands/sanguo/map.ts
  - src/utils/commandLoader.ts
  - src/events/interactionCreate.ts
  - src/ui/embeds/buildSanguoTravelReplyEmbed.ts
  - src/commands/sanguo/__tests__/travel.test.ts
  - locales/vi/sanguo.json
  - locales/en/sanguo.json
  - locales/zh-cn/sanguo.json
autonomous: false
requirements:
  - TQC-06

estimate:
  tokens: 56000
  raw_tokens: 28000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "User runs /sanguo travel [destination] and the journey starts immediately: player_travel_state row written (or updated in place) with status='traveling', travel_seconds_remaining = edge travel_seconds, encounter_active=false — and the reply embed shows destination/ETA/from with NO cost field (D-01)."
    - "The destination autocomplete lists only adjacent nodes (map_edges), nearest first by travel_seconds, capped at 25, value = stable node code, name = emoji+per-locale name when representative_hero_id exists."
    - "A user already traveling (row status='traveling') cannot start a second journey — command replies in_progress DANGER embed (D-09, userId.unique())."
    - "A fabricated/non-adjacent destination never writes state — server-side adjacency re-validation replies no_route DANGER embed (Pitfall 4 defense in depth)."
    - "travelService contains no balance-deduction import and no deduction API call — travel costs only time (D-01)."
    - "The interaction router handles isAutocomplete() BEFORE the isChatInputCommand() gate (first autocomplete in codebase) and the command module's autocomplete export is reachable via client.commands."
  artifacts:
    - src/services/sanguo/travelService.ts — START_NODE const + getCurrentPosition + getAdjacentNodes + startTravel
    - src/db/schema/playerTravelState.ts — travel_seconds_remaining int notNull default 0 + encounter_active boolean notNull default false; arrive_at/cost dropped
    - src/db/schema/encounterRuns.ts — encounter_type varchar(20) notNull default 'hero'
    - src/db/schema/mapEdges.ts — map_edges table (node_a_id, node_b_id, travel_seconds, unique pair index) — the adjacency source getAdjacentNodes + startTravel NO_ROUTE validation read (D-17)
    - src/commands/sanguo/travel.ts — travelSubcommand builder + execute + autocomplete exports
    - src/commands/sanguo/map.ts — travel subcommand appended to the 'sanguo' builder; autocomplete re-exported
    - src/utils/commandLoader.ts — Command.autocomplete optional field
    - src/events/interactionCreate.ts — isAutocomplete() branch before line 448 gate
    - src/ui/embeds/buildSanguoTravelReplyEmbed.ts — buildSanguoTravelReplyEmbed(data, t)
    - locales/{vi,en,zh-cn}/sanguo.json — cmd.travel.description + travel.* keys
    - src/services/sanguo/__tests__/travelService.test.ts + src/commands/sanguo/__tests__/travel.test.ts
  key_links:
    - "map.ts builder ↔ travel.ts subcommand module: exactly ONE file exports data with name 'sanguo' (Pitfall 3 — no second sanguo command file)."
    - "travel.ts autocomplete export ↔ commandLoader Command interface ↔ interactionCreate isAutocomplete() branch — the three-file wiring that makes autocomplete reachable."
    - "startTravel in-place UPDATE semantics ↔ userId.unique() — the row is simultaneously last-arrived-position and active-journey record."
    - "mapEdges table (created in THIS wave) ↔ travelService.getAdjacentNodes + startTravel NO_ROUTE validation — the adjacency source must ship in the same wave as the service that queries it (09-01 typecheck would fail TS2307 module-not-found otherwise)."
  prohibitions:
    - "No balance-deduction module import or deduction API call anywhere in travelService (D-01)."
    - "No cancel path: player_travel_state.status values are 'traveling'|'arrived' only — 'cancelled' does not exist (D-03)."
    - "No second file exporting data with name 'sanguo' (Pitfall 3)."
    - "No Math.random for any player-facing roll."
    - "No hardcoded user-facing strings — all new UI strings via t('sanguo:...') (check-i18n gate)."
  flagged_assumptions:
    - "FLAGGED ASSUMPTION (TQC-06 unclassified): travel flow is implemented per D-01..D-09 + UI-SPEC interaction contract (autocomplete ≤25, value=node code, no cost field, no cancel); the stale REQUIREMENTS.md TQC-06 'atomic wallet deduct + travel-cancel component' text is treated as INVALIDATED by D-01/D-03."
    - "FLAGGED ASSUMPTION (A6 research): START_NODE = 'luoyang' for first-ever journey (no travel row yet) — new users' first hop originates at Lạc Dương."

---

<objective>
What this plan accomplishes: the **travel journey-start slice** — the phase's tracer. A user runs `/sanguo travel` with an autocomplete-picked adjacent destination and the journey begins: `player_travel_state` is written (INSERT on first journey, in-place UPDATE on subsequent per `userId.unique()`), with the D-07 pause-aware remaining-seconds clock model and no money involved anywhere (D-01).

Purpose: prove the full interaction path (autocomplete routing → command dispatch → pure time/state service → DB row → reply embed) end-to-end before the tick/encounter systems build on it. This is the thinnest vertical slice that touches every layer the phase modifies on the travel side.

Output: evolved travel schema (D-07), pure `travelService` (TQC-06), `/sanguo travel` subcommand + first autocomplete in the codebase, travel reply embed, `travel.*` i18n keys, unit tests.
</objective>

<execution_context>
@C:/Users/901107/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/901107/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-travel-encounters/09-CONTEXT.md
@.planning/phases/09-travel-encounters/09-RESEARCH.md
@.planning/phases/09-travel-encounters/09-PATTERNS.md
@.planning/phases/09-travel-encounters/09-UI-SPEC.md
@src/db/schema/playerTravelState.ts
@src/db/schema/encounterRuns.ts
@src/commands/sanguo/map.ts
@src/events/interactionCreate.ts
@src/utils/commandLoader.ts
@src/utils/commandContext.ts
@src/services/football/matchLifecycleService.ts
@src/ui/embeds/buildSanguoMapEmbed.ts
@src/ui/embeds/buildErrorEmbed.ts
@src/ui/theme.ts
@src/assets/sanguoEmojis.ts
@src/i18n/index.ts
@src/commands/sanguo/__tests__/map.test.ts
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <decision>Confirm the two one-way economy/travel-commitment decisions before implementing travelService: D-01 (travel costs only TIME — no Linh thạch, no balance-deduction call anywhere in travel; inverts the milestone-init "paid travel = main sink") and D-03 (no cancel — travel is a one-way commitment; player_travel_state.status has no 'cancelled' value; ROADMAP SC2 will be amended in plan 09-05).</decision>
  <context>D-01 and D-03 are locked in CONTEXT.md but rated one-way (reversibility gates): undoing D-01 requires a new economy sign-off; undoing D-03 requires refund logic + a position model rework. The implementation that follows (schema drop of cost, service with no deduction call, no cancel branch) is structurally irreversible without a re-sign. This checkpoint is the last cheap moment to confirm.</context>
  <options>
    <option id="proceed">
      <name>Proceed per D-01/D-03</name>
      <pros>Matches the locked CONTEXT decisions and the approved UI-SPEC (no cost field, no destructive actions); travel stays pure time/state.</pros>
      <cons>None within scope — D-01 inverts the milestone-init paid-travel economy assumption (already flagged for the D-18 re-sign in 09-05).</cons>
    </option>
    <option id="revisit">
      <name>Revisit the travel cost model</name>
      <pros>Would re-open the milestone-init "paid travel" sink before it is structurally removed.</pros>
      <cons>Contradicts D-01/D-03, the UI-SPEC, and the phase scope; requires a new discuss-phase round.</cons>
    </option>
  </options>
  <resume-signal>Select: proceed or revisit</resume-signal>
</task>

<task type="tracer" tdd="true">
  <name>Task 1 (tracer): travelService + D-07/D-14 schema evolution — the journey-start domain path</name>
  <files>src/db/schema/playerTravelState.ts, src/db/schema/encounterRuns.ts, src/db/schema/mapEdges.ts, src/services/sanguo/travelService.ts, src/services/sanguo/__tests__/travelService.test.ts</files>
  <read_first>
    - src/db/schema/playerTravelState.ts (the file being modified — 30 lines; D-07 remaining-seconds model)
    - src/db/schema/encounterRuns.ts (the file being modified — D-14 boss flag)
    - src/services/football/matchLifecycleService.ts lines 333-345 (FOR UPDATE / tx shape) and lines 16-112 (REST/locale/3-strike patterns — only the tx shape is needed here)
    - src/db/schema/heroRelations.ts (undirected pair-table + unique-index style — the DIRECT analog for the map_edges table created below, D-17)
    - .planning/phases/09-travel-encounters/09-PATTERNS.md §mapEdges (exact schema shape, lines 356-369 — created in this plan, wave 1)
    - src/db/schema/mapNodes.ts (column set the service reads: code/name_vi/name_en/name_zh/zone/representative_hero_id)
    - src/db/schema/users.ts (userId FK + locale column)
    - src/commands/sanguo/__tests__/map.test.ts lines 8-54 (db-mocking pattern for the test file)
    - .planning/phases/09-travel-encounters/09-RESEARCH.md §5 travelService design + §6 schema evolution + §7 encounter roll notes (A3/A4/A6 discretion decisions)
  </read_first>
  <behavior>
    - Test 1: getCurrentPosition returns { nodeId, nodeCode } = START_NODE('luoyang') when no travel row exists; = toNodeId when status='arrived'; = fromNodeId when status='traveling'.
    - Test 2: getAdjacentNodes returns edges joined to node/zone data ordered by travelSeconds ASC, capped at 25.
    - Test 3: startTravel INSERTs on first journey and UPDATES the existing row in place on subsequent journeys (fromNodeId = old to, toNodeId = dest, travelSecondsRemaining = edge.travelSeconds, encounterActive = false, status = 'traveling', departAt/updatedAt = now).
    - Test 4: startTravel throws ALREADY_TRAVELING when the row status='traveling' (D-09).
    - Test 5: startTravel throws NO_ROUTE when the destination is not adjacent to current position (Pitfall 4).
    - Test 6: the module imports no balance-deduction API and calls none (D-01) — grep gate in verify.
  </behavior>
  <action>
    Edit `src/db/schema/playerTravelState.ts` to the D-07 remaining-seconds model per RESEARCH §6:
    - DROP `arriveAt: timestamp('arrive_at'...)` and `cost: bigint('cost'...)` columns entirely (D-01 — travel is free; money never involved).
    - ADD `travelSecondsRemaining: integer('travel_seconds_remaining').notNull().default(0)` (decrementing, pause-aware).
    - ADD `encounterActive: boolean('encounter_active').notNull().default(false)` (D-07 pause flag — Phase-10-ready; Phase 9 leaves it observably false between ticks).
    - KEEP `id serial PK`, `userId int notNull references users.id unique` (D-09 one row per user forever), `fromNodeId/toNodeId integer` (plain ints, no FK — keep the existing header-comment rationale), `departAt timestamp withTimezone notNull`, `status varchar(20) notNull default 'traveling'` (comment: values now 'traveling'|'arrived' only — 'cancelled' removed per D-03), `createdAt/updatedAt` (updatedAt is the elapsed-time anchor).
    - Column style must mirror the existing lines: `timestamp('depart_at', { withTimezone: true }).notNull()`.

    Edit `src/db/schema/encounterRuns.ts` to add the D-14 boss flag per RESEARCH §6 (assumption A2):
    - ADD `encounterType: varchar('encounter_type', { length: 20 }).notNull().default('hero')` — values 'hero'|'boss'.
    - KEEP `heroId integer references heroes.id` nullable — boss encounters write hero_id NULL + encounter_type='boss' + zone.

    Create `src/db/schema/mapEdges.ts` per PATTERNS §mapEdges (D-17 graph — created HERE in wave 1 because getAdjacentNodes and startTravel's NO_ROUTE validation query this table; 09-01's `npm run typecheck` cannot pass without it):
    - `mapEdges = pgTable('map_edges', { id: serial('id').primaryKey(), nodeAId: integer('node_a_id').notNull(), nodeBId: integer('node_b_id').notNull(), travelSeconds: integer('travel_seconds').notNull() }, (table) => [ uniqueIndex('map_edges_pair_unique').on(table.nodeAId, table.nodeBId) ])`.
    - Plain integers, NO references() — mirror heroRelations' undirected unique-pair pattern; import `pgTable`, `serial`, `integer`, `uniqueIndex` from 'drizzle-orm/pg-core'.
    - The `index.ts` re-export for mapEdges lands in 09-02 (with mapZones/heroZoneRates — drizzle-kit generate reads index.ts at 09-05 migration time); travelService below imports mapEdges via the DIRECT module path (codebase convention — wallet.ts:3, map.ts:7) so this plan is self-contained for typecheck.

    Create `src/services/sanguo/travelService.ts` — pure time/state service per RESEARCH §5. **It must not import, reference, or call any balance-deduction module or API (D-01)** — structurally no money. Imports: `import { mapEdges } from '../../db/schema/mapEdges.js'` (direct module path — the file created in this plan) and `import { mapNodes } from '../../db/schema/mapNodes.js'` (existing Phase 8 schema). Export:
    - `export const START_NODE = 'luoyang'` (A6).
    - `getCurrentPosition(userId: number): Promise<{ nodeId: number; nodeCode: string }>` — read player_travel_state by userId; status='arrived' → toNodeId; status='traveling' → fromNodeId (in-flight position); no row → START_NODE resolved via mapNodes.code lookup.
    - `getAdjacentNodes(nodeId: number): Promise<AdjacentNode[]>` — query map_edges where node_a_id = :nodeId OR node_b_id = :nodeId, join map_nodes for the neighbor's per-locale names + representative_hero_id + zone, order by travelSeconds ASC, limit 25. AdjacentNode = { nodeId, code, nameVi, nameEn, nameZh, zone, travelSeconds, representativeHeroId }.
    - `startTravel(userId: number, toNodeId: number): Promise<{ etaSeconds: number }>` — in ONE transaction: (a) read the current row; if status='traveling' throw `Error('ALREADY_TRAVELING')` (D-09); (b) re-validate the edge from current position to toNodeId against map_edges (OR-match both orientations); missing edge → throw `Error('NO_ROUTE')` (Pitfall 4 — autocomplete is advisory); (c) INSERT on first journey / in-place UPDATE on subsequent (userId.unique() = one row forever): set fromNodeId=current nodeId, toNodeId=dest, travelSecondsRemaining=edge.travelSeconds, encounterActive=false, status='traveling', departAt=now, updatedAt=now; (d) NO deduction call; return { etaSeconds: edge.travelSeconds }.
    - Error style: plain `throw new Error('CODE')` mirroring wallet.ts:61; transaction style `db.transaction(async (tx) => ...)` per matchLifecycleService.ts:334.

    Create `src/services/sanguo/__tests__/travelService.test.ts` per the 6 behaviors above. Mock `db/client.js` with the map.test.ts pattern (lines 8-54): chainable select/from/where/orderBy mocks per call site; mock `map_edges`/`map_nodes`/`playerTravelState` rows as plain objects. The no-deduction test is the grep gate in verify, not a runtime assertion.
  </action>
  <verify>
    <automated>npx vitest run src/services/sanguo/__tests__/travelService.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Command: `npx vitest run src/services/sanguo/__tests__/travelService.test.ts` exits 0 with all 6 behaviors passing.
    - Command: `npm run typecheck` exits 0.
    - Source: `src/db/schema/playerTravelState.ts` contains `travel_seconds_remaining` and `encounter_active` and does NOT contain `arrive_at` or `cost`.
    - Source: `src/db/schema/encounterRuns.ts` contains `encounter_type`.
    - Source: `src/db/schema/mapEdges.ts` defines node_a_id/node_b_id/travel_seconds and `uniqueIndex('map_edges_pair_unique')` on the pair.
    - Source: `src/services/sanguo/travelService.ts` exports `START_NODE`, `getCurrentPosition`, `getAdjacentNodes`, `startTravel` and contains `throw new Error('ALREADY_TRAVELING')` and `throw new Error('NO_ROUTE')`.
    - Command (D-01 gate): `grep -v "^[[:space:]]*//" src/services/sanguo/travelService.ts | grep -cE "deductBalance|services/wallet"` outputs `0`.
    - Source: `src/services/sanguo/travelService.ts` contains `userId.unique()`-preserving in-place update via `.update(playerTravelState)` keyed on `userId` (not a delete+insert).
  </acceptance_criteria>
  <done>travelService implements getCurrentPosition/getAdjacentNodes/startTravel per RESEARCH §5; schema carries the D-07 remaining-seconds + D-14 boss-type columns + the D-17 map_edges table (the adjacency source the service queries); all 6 unit behaviors pass; no deduction API anywhere in the service; typecheck green.</done>
  <reversibility rating="one-way">Implements D-01 (no-money travel, cost column dropped) and D-03 (no cancel status) — structural removal gated by the preceding checkpoint:decision.</reversibility>
</task>

<task type="expansion" tdd="true">
  <name>Task 2 (expansion): /sanguo travel subcommand + first autocomplete + reply embed + i18n travel keys</name>
  <files>src/commands/sanguo/travel.ts, src/commands/sanguo/map.ts, src/utils/commandLoader.ts, src/events/interactionCreate.ts, src/ui/embeds/buildSanguoTravelReplyEmbed.ts, locales/vi/sanguo.json, locales/en/sanguo.json, locales/zh-cn/sanguo.json, src/commands/sanguo/__tests__/travel.test.ts</files>
  <read_first>
    - src/commands/sanguo/map.ts (the file being modified — 110 lines; builder at 14-31, pickName at 38-42, execute at 44-109)
    - src/utils/commandLoader.ts (interface at 9-12, load loop at 44-49)
    - src/events/interactionCreate.ts line 448 area (chat-input gate — the autocomplete branch goes BEFORE it)
    - src/utils/commandContext.ts (fetchCommandContext signature; note it types ChatInputCommandInteraction — autocomplete needs a direct users-row lookup instead)
    - src/ui/embeds/buildSanguoMapEmbed.ts (embed builder pattern) + src/ui/theme.ts (COLORS.SEASON, embedFooter) + src/ui/embeds/buildErrorEmbed.ts (DANGER pattern)
    - src/assets/sanguoEmojis.ts heroEmoji() (throws EMOJI_NOT_FOUND on unknown heroId — guard per map.ts:98)
    - locales/vi/sanguo.json (key shape: cmd.* + map.*)
    - .planning/phases/09-travel-encounters/09-PATTERNS.md §travel.ts + §commandLoader + §interactionCreate + §embed builders
    - .planning/phases/09-travel-encounters/09-RESEARCH.md §Code Examples "Autocomplete handler (first in codebase — new pattern)" lines 804-822
    - .planning/phases/09-travel-encounters/09-UI-SPEC.md §Interaction contract — autocomplete + §Copywriting Contract
  </read_first>
  <behavior>
    - Test 1: execute('travel') with valid adjacent destination calls travelService.startTravel and editReplies with buildSanguoTravelReplyEmbed containing destination/ETA/from fields and NO cost field.
    - Test 2: execute('travel') when already traveling replies in_progress_title DANGER embed.
    - Test 3: execute('travel') with a non-adjacent destination replies no_route_title DANGER embed (server-side re-validation).
    - Test 4: autocomplete returns ≤25 choices ordered by travelSeconds ASC; choice value = stable node code; name = `{emoji}{nameVi}` when representativeHeroId resolves, else plain nameVi; zero adjacent → responds [].
    - Test 5: interactionCreate routes isAutocomplete() to client.commands.get(name).autocomplete before the chat-input gate.
  </behavior>
  <action>
    Create `src/commands/sanguo/travel.ts` exporting:
    - `travelSubcommand: SlashCommandSubcommandBuilder` — `.setName('travel')` + localized descriptions; `.addStringOption(o => o.setName('destination').setDescription(...).setDescriptionLocalizations({...}).setRequired(true).setAutocomplete(true))` — the first autocomplete option in the codebase (static Discord API strings inside the i18next-lint disable block, mirroring map.ts:14-31).
    - `execute(interaction: ChatInputCommandInteraction): Promise<void>` — defer → fetchCommandContext → not-registered guard (buildErrorEmbed + `t('common:errors.notRegistered')`) → call travelService.getCurrentPosition(char.id) → travelService.startTravel(char.id, Number(interaction.options.getString('destination'))) in try/catch — **one adjacent hop only, D-08** (the destination must be directly connected; no multi-hop, no route planning):
      - ALREADY_TRAVELING → DANGER embed with `t('sanguo:travel.in_progress_title')` + `t('sanguo:travel.in_progress_body')`.
      - NO_ROUTE → DANGER embed with `t('sanguo:travel.no_route_title')` + `t('sanguo:travel.no_route_body')`.
      - generic error → DANGER embed `t('sanguo:travel.error')`.
      - success → `buildSanguoTravelReplyEmbed({ destinationName, fromNodeName, etaSeconds, shardId }, t)`.
    - `autocomplete(interaction: AutocompleteInteraction): Promise<void>` — if `interaction.options.getFocused(true).name !== 'destination'` return; look up the user row directly (`db.select({ id: users.id }).from(users).where(eq(users.discordId, interaction.user.id))` — fetchCommandContext types ChatInput, so do NOT reuse it); no row → `interaction.respond([])`; else `getCurrentPosition(charId)` → `getAdjacentNodes(pos.nodeId)` → filter `code.includes(focused.value.toLowerCase())` → respond first 25 as `{ name, value }` where value = node.code and name = representativeHeroId present ? `${emoji}${perLocaleName}` : perLocaleName — guard heroEmoji() in try/catch (EMOJI_NOT_FOUND → fall back to name-only, mirroring map.ts:98 guard); perLocaleName via the pickName logic (vi/en/zh-cn, nameZh fallback nameVi).

    Edit `src/commands/sanguo/map.ts`:
    - Append `.addSubcommand(travelSubcommand)` to the existing `data` builder (import `{ travelSubcommand, autocomplete as travelAutocomplete } from './travel.js'`).
    - Re-export autocomplete so the router reaches it through the one owned command module: `export { travelAutocomplete as autocomplete } from './travel.js'`.
    - In execute(), dispatch: `if (subcommand === 'travel') return travelExecute(interaction)` (import travel's execute) — keep the existing map flow for 'map'.
    - **Do NOT create a second file exporting `data` with name 'sanguo'** (Pitfall 3 — registerCommands PUTs per file; two files with the same name = last-wins flakiness).

    Edit `src/utils/commandLoader.ts`:
    - Extend `interface Command` with `autocomplete?: (interaction: AutocompleteInteraction) => Promise<void>;` (import the type from 'discord.js').
    - The existing `client.commands.set(command.data.name, command)` already stores the whole module — the field rides along; no load-loop change needed.

    Edit `src/events/interactionCreate.ts`:
    - Insert an `if (interaction.isAutocomplete())` branch BEFORE the `if (!interaction.isChatInputCommand()) return;` gate at line 448: fetch `interaction.client.commands?.get(interaction.commandName)`; if present and `typeof command.autocomplete === 'function'`, try/catch `await command.autocomplete(interaction)` logging `logger.error('InteractionCreate', `Autocomplete error in ${interaction.commandName}`, err)`; return in all cases.

    Create `src/ui/embeds/buildSanguoTravelReplyEmbed.ts`:
    - `export interface SanguoTravelReplyEmbedData { destinationName: string; fromNodeName: string; etaSeconds: number; shardId?: number }`.
    - `buildSanguoTravelReplyEmbed(data, t: TFunction): EmbedBuilder` — `.setColor(COLORS.SEASON)` (never hardcode hex), `.setTitle(t('sanguo:travel.started_title'))`, fields: destination = `t('sanguo:travel.destination', { node: data.destinationName })`, eta = `t('sanguo:travel.eta', { eta: humanizeEta(data.etaSeconds, t) })`, from = `t('sanguo:travel.from', { from_node: data.fromNodeName })`. **NO cost field (D-01).** `.setFooter(embedFooter(data.shardId))` + `.setTimestamp()`.
    - Export a `humanizeEta(seconds, t)` helper: >= 3600 → `t('sanguo:travel.eta_hours_minutes', { count: hours, hours, minutes })` else `t('sanguo:travel.eta_minutes', { count: minutes, n: Math.max(1, Math.round(seconds/60)) })` — i18next plural forms `_one`/`_other` (identical structure in all 3 locales for check-i18n).

    Edit `locales/vi/sanguo.json`, `locales/en/sanguo.json`, `locales/zh-cn/sanguo.json` — add to the `cmd` block `"travel": { "description": ... }` and a new top-level `travel` block with keys (VI canonical below; EN/ZH translate per existing sanguo.json tone, identical key structure):
    - `travel.started_title` = "🧭 Hành trình bắt đầu"
    - `travel.destination` = "Điểm đến: **{node}**"
    - `travel.eta` = "Thời gian dự kiến: **{eta}**"
    - `travel.from` = "Xuất phát: **{from_node}**"
    - `travel.eta_minutes_one` = "~{n} phút" / `travel.eta_minutes_other` = "~{n} phút"
    - `travel.eta_hours_minutes_one` = "~{h} giờ {m} phút" / `_other` same
    - `travel.no_route_title` = "Không có đường đi"
    - `travel.no_route_body` = "Vị trí hiện tại của bạn không kết nối với địa danh nào. Hãy mở /sanguo map để xem bản đồ."
    - `travel.in_progress_title` = "Bạn đang trong một hành trình"
    - `travel.in_progress_body` = "Hãy đợi đến nơi rồi bắt đầu hành trình mới."
    - `travel.error` = "Có lỗi khi bắt đầu hành trình. Hãy thử lại."
    - `cmd.travel.description` = "Bắt đầu hành trình đến một địa danh" (VI).
    All three locale files MUST carry the identical key set (check-i18n compares against the VI reference).

    Create `src/commands/sanguo/__tests__/travel.test.ts` per the 5 behaviors — mock db/client.js + commandContext per map.test.ts:8-54; mock travelService with vi.mock; mock the embed builder if needed; assert embed field presence and DANGER color path via buildErrorEmbed for the error branches.
  </action>
  <verify>
    <automated>npx vitest run src/commands/sanguo/__tests__/travel.test.ts; npm run check-i18n; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Command: `npx vitest run src/commands/sanguo/__tests__/travel.test.ts` exits 0.
    - Command: `npm run check-i18n` exits 0 (all 3 locales key-synced).
    - Command: `npm run typecheck` exits 0.
    - Source: `src/commands/sanguo/map.ts` contains `.addSubcommand(travelSubcommand)` and re-exports `autocomplete`.
    - Source: `src/commands/sanguo/travel.ts` exports `travelSubcommand`, `execute`, `autocomplete`.
    - Source: `src/utils/commandLoader.ts` interface contains `autocomplete?:`.
    - Source: `src/events/interactionCreate.ts` contains `interaction.isAutocomplete()` BEFORE the `isChatInputCommand()` gate.
    - Config: `locales/vi/sanguo.json`, `locales/en/sanguo.json`, `locales/zh-cn/sanguo.json` each contain `"travel.started_title"`, `"travel.no_route_title"`, `"travel.in_progress_title"`, `"cmd.travel.description"`.
    - Source: `src/ui/embeds/buildSanguoTravelReplyEmbed.ts` uses `COLORS.SEASON` and contains no `cost` field and no balance-deduction reference.
    - Grep: no second `data` export with name 'sanguo' — `grep -rl "setName('sanguo')" src/commands/sanguo/` returns exactly the file list `[map.ts]`-only output (travel.ts must not call setName('sanguo')).
  </acceptance_criteria>
  <done>/sanguo travel with autocomplete-picked adjacent destination starts the journey and replies the SEASON embed (destination/ETA/from, no cost); already-traveling and no-route paths reply DANGER embeds; autocomplete is routed by the new interactionCreate branch through the commandLoader-wired export; check-i18n + typecheck green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (Discord) → interaction router | Untrusted interaction payload crosses here — autocomplete values are advisory strings, never authoritative |
| interaction → travelService | Node-code input from the payload; must be re-validated against map_edges before any state write |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Tampering | `startTravel` destination input | high | mitigate | Server-side adjacency re-validation inside the transaction: missing edge → `NO_ROUTE` error before any write (Pitfall 4). Autocomplete is never treated as authoritative (V5 Input Validation). |
| T-09-02 | Spoofing | Autocomplete race/stale choice | medium | mitigate | Same server-side re-validation (T-09-01); the reply path surfaces `no_route_title` DANGER embed when the pick no longer matches the edge set. |
| T-09-03 | Tampering | Concurrent `startTravel` for the same user (double-journey race) | medium | mitigate | Single transaction reads the row first; `userId.unique()` backstops the INSERT path; the UPDATE path re-checks status inside the tx → second writer sees 'traveling' and throws ALREADY_TRAVELING (D-09). |
| T-09-04 | Spoofing | Destination code values | low | accept | Node codes are opaque DB keys with no user-facing semantics; a valid-but-wrong code simply yields NO_ROUTE. No injection surface (drizzle parameterization). |
| T-09-SC | Tampering | npm/pip/cargo installs | — | not-triggered | Phase 9 introduces NO new packages (RESEARCH Package Legitimacy Audit — all dependencies already installed + production-running); no package-legitimacy checkpoint required. |
</threat_model>

<verification>
- `npx vitest run src/services/sanguo/__tests__/travelService.test.ts src/commands/sanguo/__tests__/travel.test.ts` green.
- `npm run typecheck` green; `npm run check-i18n` green (travel.* keys present in all 3 locales).
- Grep gate: `grep -v "^[[:space:]]*//" src/services/sanguo/travelService.ts | grep -cE "deductBalance|services/wallet"` == 0.
- Grep gate: exactly one file in src/commands/sanguo/ exports `setName('sanguo')`.
</verification>

## Artifacts this phase produces

| Symbol | Kind | File |
|--------|------|------|
| `travel_seconds_remaining` / `encounter_active` columns; `arrive_at` + `cost` dropped | schema columns | src/db/schema/playerTravelState.ts |
| `encounter_type varchar(20) default 'hero'` | schema column (D-14 boss flag) | src/db/schema/encounterRuns.ts |
| `mapEdges` table (node_a_id, node_b_id, travel_seconds, unique pair index) | table definition (D-17) — the adjacency source getAdjacentNodes + startTravel NO_ROUTE validation read | src/db/schema/mapEdges.ts |
| `START_NODE`, `getCurrentPosition()`, `getAdjacentNodes()`, `startTravel()` | service functions (pure time/state, D-01) | src/services/sanguo/travelService.ts |
| `travelSubcommand`, `execute()`, `autocomplete()` | command module exports | src/commands/sanguo/travel.ts |
| travel subcommand appended to `data` builder + `autocomplete` re-export | command wiring (Pitfall 3-safe) | src/commands/sanguo/map.ts |
| `autocomplete?: (i: AutocompleteInteraction) => Promise<void>` | loader interface field | src/utils/commandLoader.ts |
| `interaction.isAutocomplete()` branch before the chat-input gate | interaction router (first autocomplete in codebase) | src/events/interactionCreate.ts |
| `buildSanguoTravelReplyEmbed(data, t)` + `humanizeEta(seconds, t)` | embed builder functions | src/ui/embeds/buildSanguoTravelReplyEmbed.ts |
| `cmd.travel.description` + `travel.started_title/destination/eta/from/eta_minutes/eta_hours_minutes/no_route_title/no_route_body/in_progress_title/in_progress_body/error` | i18n keys (3 locales) | locales/{vi,en,zh-cn}/sanguo.json |
| travelService flow tests (6 behaviors) + travel command tests (5 behaviors) | test suites | src/services/sanguo/__tests__/travelService.test.ts, src/commands/sanguo/__tests__/travel.test.ts |

<success_criteria>
- The journey-start path is proven end-to-end: autocomplete → router → command → travelService → DB row → SEASON reply embed with no cost field.
- The D-07 remaining-seconds schema, D-14 boss-type column, and D-17 map_edges table are committed (migration generated in 09-05) — the adjacency table ships in the same wave as the service that reads it, so 09-01 typecheck passes standalone.
- No money path exists in travel (grep gate == 0).
- First autocomplete in the codebase is routed and tested.
</success_criteria>

<output>
Create `.planning/phases/09-travel-encounters/09-01-SUMMARY.md` when done.
</output>
