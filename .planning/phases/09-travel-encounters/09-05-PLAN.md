---
phase: 09-travel-encounters
plan: 05
type: execute
wave: 4
depends_on:
  - 09-01
  - 09-02
  - 09-03
  - 09-04
files_modified:
  - migrations/0018_*.sql
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - docs/economy-budget.md
autonomous: true
requirements:
  - TQC-06
  - TQC-07
  - TQC-08
  - TQC-09

estimate:
  tokens: 28000
  raw_tokens: 14000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The schema push/migration ships: player_travel_state drops arrive_at + cost and gains travel_seconds_remaining + encounter_active; encounter_runs gains encounter_type; map_zones/map_edges/hero_zone_rates tables are created — applied to the dev DB and committed as migrations/0018_*.sql (schema push AFTER all schema file modifications, BEFORE phase verification — [BLOCKING])."
    - "npm run seed:sanguo runs clean after the migration: 132 heroes kept, 18 zones / 73 nodes / 162 edges / 208 hero_zone_rates upserted, re-runnable without duplicates (D-20/D-11)."
    - "ROADMAP §Phase 9 SC1 + SC2 are amended to the time-only/no-cancel truth (D-01/D-03): travel shows destination + ETA only; user cannot cancel; position always equals the last arrived node."
    - "docs/economy-budget.md records the D-01 inversion (travel is no longer a sink) and flags the D-02/D-18 capture-fee re-sign gate before Phase 10 content ships."
    - "The full phase gate passes: npm test + npm run typecheck + npm run lint + npm run check-i18n all green."
  artifacts:
    - migrations/0018_*.sql — generated by npx drizzle-kit generate (not hand-written)
    - .planning/ROADMAP.md §Phase 9 SC1/SC2 + Goal amendments
    - .planning/REQUIREMENTS.md TQC-06 amended note (D-01/D-03 invalidation)
    - docs/economy-budget.md D-01/D-02 re-sign flag
  key_links:
    - "migration 0018 ↔ all schema files (09-01 playerTravelState/encounterRuns + 09-02 mapZones/mapEdges/heroZoneRates) — the generated SQL must reflect every phase-9 schema edit before verification."
    - "seed run ↔ scripts/data/sanguo-map-data.json (09-02) — the reseed consumes the committed dataset; hero seed untouched."
    - "ROADMAP/economy docs ↔ D-01/D-03/D-18 — the doc amendments are the paper trail for the one-way economy/commitment decisions."
  prohibitions:
    - "No hand-written migration SQL — generate via npx drizzle-kit generate and review the diff before applying (dropped columns are intentional: arrive_at, cost)."
    - "No npm install during this phase (no new packages)."
    - "No editing ROADMAP/REQUIREMENTS/economy-budget beyond the SC1/SC2/Goal/TQC-06/D-18-flag amendments."
    - "No deleting hero seed data — the seed's delete targets map_nodes only (D-20)."
  flagged_assumptions:
    - "FLAGGED ASSUMPTION (TQC-06 unclassified): the travel flow is implemented per D-01..D-09 + UI-SPEC; REQUIREMENTS.md TQC-06's 'atomic wallet deduct + travel-cancel component' is INVALIDATED by D-01/D-03 — the TQC-06 amendment task below records this in the requirements doc."
    - "FLAGGED ASSUMPTION (TQC-07 probe: 'runs twice on the same input'): pg-boss exactly-once + FOR UPDATE SKIP LOCKED make a double-fired tick resolve each row once — safe by construction (09-03)."
    - "FLAGGED ASSUMPTION (TQC-07 probe: 'interrupted or run in parallel'): overdue self-heal (D-05) + single-writer rule (Pitfall 5) cover interruption; SKIP LOCKED prevents cross-tick deadlock (09-03/09-04)."
    - "FLAGGED ASSUMPTION (TQC-08 unclassified): encounter semantics per D-10/D-13/D-14/D-15 with research defaults 0.35/0.07/20-per-60min-window (09-04)."
    - "FLAGGED ASSUMPTION (TQC-09 unclassified): dataset machine-verified and approved by the user at the 09-02 D-21 review gate before seeding (09-02)."

---

<objective>
What this plan accomplishes: the **integration close-out of Phase 9** — the [BLOCKING] database schema push (generate migration 0018 via `npx drizzle-kit generate`, apply via `npm run migrate`), the D-20 reseed run (`npm run seed:sanguo`) consuming the committed TQC-09 dataset, the ROADMAP/REQUIREMENTS/economy-budget doc amendments (D-01/D-03/D-18 paper trail), and the full phase gate (test + typecheck + lint + check-i18n).

Purpose: make the schema real in the database, prove the dataset seeds idempotently, and leave the planning docs consistent with the locked decisions before Phase 10 consumes this phase.

Output: applied migration 0018, seeded map data, amended docs, green phase gate.
</objective>

<execution_context>
@C:/Users/901107/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/901107/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-travel-encounters/09-CONTEXT.md
@.planning/phases/09-travel-encounters/09-RESEARCH.md
@.planning/phases/09-travel-encounters/09-PATTERNS.md
@.planning/phases/09-travel-encounters/09-UI-SPEC.md
@docs/economy-budget.md
@drizzle.config.ts
@migrations/0017_hero_spouse_relations.sql
@src/db/schema/playerTravelState.ts
@src/db/schema/encounterRuns.ts
@src/db/schema/mapZones.ts
@src/db/schema/mapEdges.ts
@src/db/schema/heroZoneRates.ts
@scripts/seed-sanguo.ts
@scripts/data/sanguo-map-data.json
</context>

<tasks>

<task type="auto">
  <name>Task 1 ([BLOCKING]): generate + apply migration 0018, then run the D-20 reseed</name>
  <files>migrations/0018_*.sql</files>
  <read_first>
    - drizzle.config.ts (schema: './src/db/schema/index.ts', out: './migrations', dbCredentials from DATABASE_URL_DIRECT ?? DATABASE_URL)
    - migrations/0017_hero_spouse_relations.sql (drizzle-kit generated format with --> statement-breakpoint separators)
    - src/db/schema/playerTravelState.ts, src/db/schema/encounterRuns.ts, src/db/schema/mapZones.ts, src/db/schema/mapEdges.ts, src/db/schema/heroZoneRates.ts (the schema the migration must reflect)
    - scripts/seed-sanguo.ts (the D-20 reseed logic authored in 09-02)
    - scripts/data/sanguo-map-data.json (the dataset the seed consumes)
  </read_first>
  <action>
    1. **Generate the migration:** run `npx drizzle-kit generate --name=sanguo_travel_map` from the repo root (DATABASE_URL_DIRECT must be set in .env — drizzle.config.ts reads it; the dev DB must be reachable, same as Phase 8's 08-04 migration flow). This produces `migrations/0018_sanguo_travel_map.sql` (or the next available 0018-* name). Do NOT hand-write SQL.
    2. **Review the generated SQL** before applying — it MUST contain exactly: `ALTER TABLE "player_travel_state" DROP COLUMN "arrive_at"` + `DROP COLUMN "cost"` + `ADD COLUMN "travel_seconds_remaining" integer NOT NULL DEFAULT 0` + `ADD COLUMN "encounter_active" boolean NOT NULL DEFAULT false`; `ALTER TABLE "encounter_runs" ADD COLUMN "encounter_type" varchar(20) NOT NULL DEFAULT 'hero'`; `CREATE TABLE "map_zones"` (code unique, name_vi/en, name_zh, sort_order, encounter_rate, boss_rate); `CREATE TABLE "map_edges"` (node_a_id, node_b_id, travel_seconds, unique pair index); `CREATE TABLE "hero_zone_rates"` (hero_id FK → heroes, zone, rate, unique hero+zone). If any expected statement is missing (e.g. a schema file was not picked up), STOP and investigate before applying.
    3. **Apply:** `npm run migrate` (drizzle-kit migrate — DATABASE_URL_DIRECT per the config comment; advisory locks break under PgBouncer, so never run against the pooled URL). Must exit 0 and apply 0018.
    4. **Reseed:** `npm run seed:sanguo` — the D-20 replacement flow (delete map_nodes → upsert 18 zones → 73 nodes → 162 edges → 208 hero_zone_rates). Verify the final log line reports the expected counts and heroCount stays 132. Run it a SECOND time to prove idempotency (no duplicate rows, same counts).
    - If `drizzle-kit generate` or `migrate` prompts interactively (missing credentials), HALT and report — do not guess credentials (precondition below).
  </action>
  <verify>
    <automated>npm run migrate; npm run seed:sanguo</automated>
  </verify>
  <acceptance_criteria>
    - Command: `npm run migrate` exits 0 with migration 0018 applied.
    - Command: `npm run seed:sanguo` exits 0; its final log line contains nodeCount 73, zoneCount 18, edgeCount 162, heroZoneRates 208 (per the 09-02 log format) and heroCount 132.
    - Command: `npm run seed:sanguo` run a second time exits 0 with the SAME counts (idempotent, no duplicates).
    - Source: `migrations/0018_sanguo_travel_map.sql` exists and contains `DROP COLUMN "arrive_at"`, `DROP COLUMN "cost"`, `ADD COLUMN "travel_seconds_remaining"`, `ADD COLUMN "encounter_active"`, `ADD COLUMN "encounter_type"`, `CREATE TABLE "map_zones"`, `CREATE TABLE "map_edges"`, `CREATE TABLE "hero_zone_rates"`.
    - Command (schema applied): `npx tsx -e "import { drizzle } from 'drizzle-orm/node-postgres'; import { Pool } from 'pg'; import * as s from './src/db/schema/index.js'; const db = drizzle({ client: new Pool({ connectionString: process.env.DATABASE_URL_DIRECT! }), schema: s }); console.log('columns', await db.execute(s.sql\`select column_name from information_schema.columns where table_name='player_travel_state' order by column_name\`))"` shows travel_seconds_remaining + encounter_active and NO arrive_at/cost.
  </acceptance_criteria>
  <done>Migration 0018 generated, reviewed, applied; map data reseeded idempotently with hero seed intact; the DB schema matches the phase's schema files.</done>
  <precondition>DATABASE_URL_DIRECT is set in .env and the dev PostgreSQL is reachable (Phase 8 established this env — verify with a trivial psql/drizzle query before running generate). If any drizzle-kit step prompts interactively, halt and report rather than guess credentials.</precondition>
</task>

<task type="docs">
  <name>Task 2 (docs): ROADMAP SC1/SC2 + Goal amendments, REQUIREMENTS TQC-06 amendment, economy-budget re-sign flag</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, docs/economy-budget.md</files>
  <read_first>
    - .planning/ROADMAP.md §Phase 9 (Goal line 118, SC1/SC2 lines 123-124)
    - .planning/REQUIREMENTS.md §v3 Travel & Encounters TQC-06 (line 243)
    - docs/economy-budget.md (Status block lines 1-6, Sink/Source model lines 22-50, Convertibility Matrix lines 74-86, Sign-off lines 107-127)
    - .planning/phases/09-travel-encounters/09-CONTEXT.md §Phase Boundary (the required amendment wording) + D-01/D-02/D-03
  </read_first>
  <action>
    Edit `.planning/ROADMAP.md` §Phase 9 (scoped replacements — do NOT rewrite the file):
    - **Goal** (line 118): replace the stale goal text with the CONTEXT-amended wording: `Người chơi di chuyển real-time trên bản đồ mốc địa danh (time-only cost, atomic state) và nhận encounters dọc hành trình qua sanguoTick cron — core loop thời gian thực của game.` (D-01: no Linh thạch cost.)
    - **SC1** (line 123): rewrite to: `User can start travel to a map node with /sanguo travel — sees destination and ETA; travel costs only time, never Linh thạch (D-01); arrival resolves at the displayed time (D-07).`
    - **SC2** (line 124): rewrite to the CONTEXT-mandated wording: `User cannot cancel a journey; travel is a one-way commitment (D-03); position always equals the last arrived node; travel state resolves at arrival — no cancel, no refund path anywhere.`
    - **SC4** (line 126): confirm the cap wording already matches D-13 (per-user ~20/hr cap, cooldown from day one) — leave as-is if accurate.

    Edit `.planning/REQUIREMENTS.md` TQC-06 (line 243): annotate the stale text with the D-01/D-03 invalidation. Replace with: `TQC-06: Pure travelService: ETA/transitions; /sanguo travel (time-only — travel costs never touch Linh thạch, D-01; atomic state row write; NO travel-cancel component — travel is a one-way commitment, D-03).` (The old "atomic wallet deduct + travel-cancel component" wording is INVALIDATED by D-01/D-03.)

    Edit `docs/economy-budget.md` (scoped edits — keep the net-sink/neutral D-19 hard constraint intact):
    - **Status block** (lines 1-6): add an amendment note under the one-line summary: `AMENDMENT (2026-08-12, Phase 9 D-01): travel is now TIME-ONLY — no Linh thạch cost, no travel sink. The main sink moves to the Phase 10 capture fee (D-02). This document's "travel prices are sinks" references are superseded; a RE-SIGN with Phase 10 capture-fee values is required before Phase 10 content ships (D-18 one-way gate).`
    - **Convertibility Matrix** row `Linh thạch | Future travel / shop / evolution` (line 80): remove the `Phase 9 (travel, TQC-06)` reference — change to `Phase 11 (shop/evolution, TQC-16/TQC-15) — travel is time-only per D-01`.
    - **Sinks (live) table** (lines 26-33): leave live rows untouched; the D-18 gate statement at lines 117-122 already gates Phase 9-11 content — add one line under the gating statement: `Phase 9 travel contributes NO sink (D-01 — time-only); the Phase 10 capture fee is the planned main sink and MUST be priced + re-signed (D-02/D-18) before Phase 10 content ships.`
  </action>
  <verify>
    <automated>npm run check-i18n</automated>
  </verify>
  <acceptance_criteria>
    - Source: `.planning/ROADMAP.md` §Phase 9 SC1 contains `costs only time, never Linh thạch` and SC2 contains `one-way commitment` and `no cancel`; the Goal line contains `time-only cost`.
    - Source: `.planning/REQUIREMENTS.md` TQC-06 contains `INVALIDATED by D-01/D-03` (or the annotation marker) and no longer claims a wallet deduct or a travel-cancel component as required behavior.
    - Source: `docs/economy-budget.md` contains `AMENDMENT (2026-08-12, Phase 9 D-01)` and `RE-SIGN` / `re-signed` for Phase 10 capture-fee pricing.
    - Grep: `grep -c "travel prices are sinks" docs/economy-budget.md` outputs `0` (superseded wording removed or explicitly marked superseded).
  </acceptance_criteria>
  <done>Planning docs reflect the locked decisions: time-only travel (SC1/Goal/TQC-06), no cancel (SC2), and the D-18 capture-fee re-sign gate flagged before Phase 10.</done>
</task>

<task type="verification">
  <name>Task 3 (gate): full phase gate — test + typecheck + lint + check-i18n</name>
  <files>none (read-only verification)</files>
  <read_first>
    - package.json scripts (test/typecheck/lint/check-i18n)
    - vitest.config.ts (include pattern src/**/__tests__/**/*.test.ts)
    - scripts/check-i18n.ts (VI reference, all 3 locales key-synced)
  </read_first>
  <action>
    Run the four phase gates in order. Fix any failures before moving on:
    1. `npm test` — full vitest suite (all phase suites: travelService, travel command, map, notification, arrivals tick, encounterService + pre-existing suites must stay green).
    2. `npm run typecheck` — tsc --noEmit, zero errors.
    3. `npm run lint` — eslint src --max-warnings=0 (i18next/no-literal-string + all rules).
    4. `npm run check-i18n` — all three locale files key-synced with the VI reference.
    - Any lint/type error in phase files → fix in the owning file (do not silence rules). Any check-i18n gap → add the missing key to all 3 locales.
    - Re-run the full set after any fix — the phase is NOT done until all four exit 0.
  </action>
  <verify>
    <automated>npm test; npm run typecheck; npm run lint; npm run check-i18n</automated>
  </verify>
  <acceptance_criteria>
    - Command: `npm test` exits 0 (all suites including the 5 new phase test files).
    - Command: `npm run typecheck` exits 0.
    - Command: `npm run lint` exits 0 with `--max-warnings=0`.
    - Command: `npm run check-i18n` exits 0.
  </acceptance_criteria>
  <done>All four phase gates green — the phase is verification-ready for /gsd-verify-work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| drizzle-kit generate → dev database | Introspects the dev DB to diff the schema — reads live state |
| migration apply → dev database | DDL writes (drop columns, create tables) — destructive if mis-generated |
| seed script → dev database | D-20 DELETE on map_nodes + bulk upserts |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-18 | Tampering | Migration dropping wrong columns | high | mitigate | Migration is GENERATED by drizzle-kit (never hand-written) and REVIEWED against the expected statement list in Task 1 step 2 before `npm run migrate` — the drops (arrive_at, cost) are intentional per D-01/D-07, and the table is empty in production (no travel shipped). |
| T-09-19 | DoS / Integrity | Migrate against PgBouncer pooled URL (advisory-lock breakage) | high | mitigate | `npm run migrate` must run with DATABASE_URL_DIRECT (drizzle.config.ts comment); runtime code uses the pooled DATABASE_URL — never mix (established Phase 8 rule). |
| T-09-20 | Tampering | Seed deleting the wrong data (hero seed loss) | high | mitigate | The seed's delete is scoped to `schema.mapNodes` only (09-02 authored); the verify re-run asserts heroCount stays 132 and the second seed run is idempotent. |
| T-09-21 | Repudiation | Doc amendments drifting from locked decisions | low | mitigate | Task 2 acceptance greps pin the exact amended wording (time-only, no cancel, re-sign flag); edits are scoped replacements, not rewrites. |
| T-09-SC | Tampering | npm/pip/cargo installs | — | not-triggered | No new packages this phase (RESEARCH Package Legitimacy Audit); no npm install anywhere. |
</threat_model>

<verification>
- `npm run migrate` applies 0018; `npm run seed:sanguo` twice, both with identical counts (132 heroes / 18 zones / 73 nodes / 162 edges / 208 rates).
- Migration file contains the exact expected DDL (drops + adds + 3 CREATE TABLEs).
- `npm test` + `npm run typecheck` + `npm run lint` + `npm run check-i18n` all exit 0.
- ROADMAP/REQUIREMENTS/economy-budget carry the D-01/D-03/D-18 amendments.
</verification>

## Artifacts this phase produces

| Symbol | Kind | File |
|--------|------|------|
| `migrations/0018_sanguo_travel_map.sql` | generated migration (D-07/D-14/D-17/D-19/D-16) | migrations/0018_*.sql |
| Applied DB state: player_travel_state (remaining/pause columns), encounter_runs.encounter_type, map_zones/map_edges/hero_zone_rates tables | database schema | (dev DB) |
| Seeded map data: 18 zones / 73 nodes / 162 edges / 208 hero_zone_rates (132 heroes preserved) | seed output | (dev DB via npm run seed:sanguo) |
| ROADMAP §Phase 9 Goal/SC1/SC2 amendments | planning doc (D-01/D-03 wording) | .planning/ROADMAP.md |
| REQUIREMENTS TQC-06 invalidation annotation | planning doc | .planning/REQUIREMENTS.md |
| economy-budget D-01 amendment + D-02/D-18 re-sign flag | economy doc | docs/economy-budget.md |

<success_criteria>
- Schema push shipped and verified against the schema files; dataset reseeded idempotently.
- Docs are consistent with the locked one-way decisions (time-only travel, no cancel, capture-fee re-sign gate).
- The full phase gate is green — Phase 9 is verification-ready.
</success_criteria>

<output>
Create `.planning/phases/09-travel-encounters/09-05-SUMMARY.md` when done.
</output>
