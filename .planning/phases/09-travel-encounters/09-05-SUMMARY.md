---
phase: 09-travel-encounters
plan: 05
subsystem: database
tags: [drizzle-kit, migration, seed, idempotency, roadmap, economy-budget, phase-gate]

# Dependency graph
requires:
  - phase: 09-travel-encounters
    provides: "09-01 D-07 travel schema (player_travel_state remaining/pause columns), 09-02 map_zones/map_edges/hero_zone_rates schema + committed TQC-09 dataset + D-20 seed flow, 09-03 check-in engine (F2 index), 09-04 encounter engine"
provides:
  - "Applied migration 0018: player_travel_state drops arrive_at/cost, adds travel_seconds_remaining + encounter_active; encounter_runs gains encounter_type + user_status_idx; map_zones/map_edges/hero_zone_rates tables live in the dev DB"
  - "D-20 full-replace reseed proven idempotent: 132 heroes / 18 zones / 73 nodes / 162 edges / 208 hero_zone_rates, zero duplicates across two runs"
  - "ROADMAP §Phase 9 Goal/SC1/SC2/SC3 amended to D-01/D-03/D-22/D-23 truth; REQUIREMENTS TQC-06/TQC-07 annotated INVALIDATED; economy-budget re-baselined + Phase 10 capture-fee re-sign flag (D-02/D-18)"
  - "Green phase gate: test + typecheck + lint + check-i18n all exit 0"
affects: [phase-10-battle-capture, economy-budget, roadmap, requirements]

actuals:
  tokens: 26728
  tasks: 3
  commits: 2

tech-stack:
  added: []
  patterns:
    - "Non-interactive drizzle-kit generate: TTY-faking wrapper (process.stdin/stdout getter override + auto-return keypress) drives hanji Select prompts to their default option without touching node_modules — reusable if generate ever prompts in CI"
    - "D-20 full-replace seed idempotency verified by DB row-count + duplicate-key greps after two consecutive runs, not just seed log output"

key-files:
  created:
    - migrations/0018_sanguo_travel_map.sql
    - migrations/meta/0018_snapshot.json
  modified:
    - migrations/meta/_journal.json
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - docs/economy-budget.md

key-decisions:
  - "Migration 0018 generated via a controlled non-interactive shim (user-approved Option A at the rename-conflict checkpoint): both hanji prompts resolved to their default — travel_seconds_remaining and encounter_active are NEW columns, arrive_at/cost are DROPPED; zero renames (matches plan expected DDL exactly)"
  - "economy-budget amendment note reworded to avoid the literal phrase 'travel prices are sinks' so the plan's own acceptance grep (count=0) passes — the supersession is stated as 'earlier travel-as-sink references superseded' instead"
  - "STATE.md required no new edit for Task 2: the D-11/D-12 supersession and the charge-todo RESOLVED marker were already committed by quick task 260812-j7r — verified present rather than duplicated"

requirements-completed: [TQC-06, TQC-07, TQC-08, TQC-09]

coverage:
  - id: D1
    description: "Migration 0018 generated by drizzle-kit (not hand-written), reviewed against the plan's expected DDL, applied to the dev DB via npm run migrate (DATABASE_URL_DIRECT) — player_travel_state drops arrive_at/cost + adds travel_seconds_remaining/encounter_active, encounter_runs adds encounter_type + user_status_idx (F2), map_zones/map_edges/hero_zone_rates created"
    requirement: TQC-06
    verification:
      - kind: other
        ref: "npm run migrate (exit 0, applied 0018)"
        status: pass
      - kind: other
        ref: "information_schema columns check: travel_seconds_remaining+encounter_active present, arrive_at/cost absent => PASS; encounter_type NOT NULL DEFAULT 'hero'; encounter_runs_user_status_idx exists; 3 map tables exist"
        status: pass
      - kind: other
        ref: "grep of migrations/0018_sanguo_travel_map.sql against plan's expected statement list (drops/adds/index/3 CREATE TABLEs) — all present, no extras"
        status: pass
    human_judgment: false
  - id: D2
    description: "D-20 full-replace reseed run twice via npm run seed:sanguo — identical counts both runs (132 heroes / 18 zones / 73 nodes / 162 edges / 208 rates), DB row counts verified and zero duplicates on unique keys; hero seed untouched"
    requirement: TQC-09
    verification:
      - kind: other
        ref: "npm run seed:sanguo run 1 => 14 factions, 12 families, 0 relations, 132 heroes, 73 map_nodes, 18 map_zones, 162 map_edges, 208 hero_zone_rates, 3 items"
        status: pass
      - kind: other
        ref: "npm run seed:sanguo run 2 => identical counts (idempotency)"
        status: pass
      - kind: other
        ref: "DB count check: heroes 132 / map_zones 18 / map_nodes 73 / map_edges 162 / hero_zone_rates 208 all PASS; dup greps (zone code, node code, hero+zone) all 0"
        status: pass
    human_judgment: false
  - id: D3
    description: "Planning-doc amendments: ROADMAP §Phase 9 Goal/SC1/SC2/SC3 to time-only (D-01), one-way/no-cancel (D-03), pull-based check-in inline results (D-22/D-23); REQUIREMENTS TQC-06/TQC-07 annotated INVALIDATED; economy-budget AMENDMENT + re-baseline (pull-driven supply ≤20/hr) + Phase 10 capture-fee re-sign flag (D-02/D-18); STATE D-11/D-12 supersession verified present"
    verification:
      - kind: other
        ref: "grep checks: ROADMAP SC1 'costs only time, never Linh thạch'=1, SC2 'one-way commitment'+'no cancel'=1/1, SC3 'when checking in with'+'no push notification'=1/1, Goal 'time-only cost'=1; REQUIREMENTS 'INVALIDATED by D-01/D-03'=1, 'INVALIDATED by D-22/D-23'=1; economy-budget 'AMENDMENT (2026-08-12, Phase 9 D-01/D-22)'=1, RE-SIGN/re-signed=2; 'travel prices are sinks'=0; STATE SUPERSEDES D-11/D-12=1, RESOLVED marker=1"
        status: pass
    human_judgment: false
  - id: D4
    description: "Full phase gate green — npm test (186 tests / 24 files), npm run typecheck (0 errors), npm run lint (max-warnings=0), npm run check-i18n (all locales in sync)"
    verification:
      - kind: other
        ref: "npm test => Test Files 24 passed, Tests 186 passed"
        status: pass
      - kind: other
        ref: "npm run typecheck => exit 0"
        status: pass
      - kind: other
        ref: "npm run lint => exit 0"
        status: pass
      - kind: other
        ref: "npm run check-i18n => 'All locale files are in sync', exit 0"
        status: pass
    human_judgment: false

# Metrics
duration: 30min
completed: 2026-08-13
status: complete
---

# Phase 9 Plan 5: Integration Close-out — Migration 0018, D-20 Reseed, Doc Amendments, Phase Gate

**Migration 0018 applied (schema made real), TQC-09 dataset reseeded idempotently (132 heroes / 18 zones / 73 nodes / 162 edges / 208 rates, zero duplicates), ROADMAP/REQUIREMENTS/economy-budget amended to the D-01/D-03/D-22/D-23 one-way + pull-based truth, and the full phase gate green — Phase 9 is verification-ready.**

## Performance

- **Duration:** 30 min
- **Started:** 2026-08-13T07:30:00Z (approx)
- **Completed:** 2026-08-13T08:00:22Z
- **Tasks:** 3
- **Files modified:** 6 (2 commits)

## Accomplishments

- **[BLOCKING] Schema push shipped:** `npx drizzle-kit generate --name=sanguo_travel_map` produced `migrations/0018_sanguo_travel_map.sql` (generated, not hand-written); reviewed against the plan's expected statement list before applying — player_travel_state drops `arrive_at`/`cost` (D-01/D-07), adds `travel_seconds_remaining`/`encounter_active`; encounter_runs adds `encounter_type` (D-14) + `encounter_runs_user_status_idx` (F2); `map_zones`/`map_edges`/`hero_zone_rates` created. `npm run migrate` (DATABASE_URL_DIRECT) applied cleanly; live DB column check PASS.
- **D-20 reseed proven idempotent:** `npm run seed:sanguo` ran twice with byte-identical counts (132 heroes / 18 zones / 73 nodes / 162 edges / 208 hero_zone_rates) and a post-run DB count + duplicate-key check confirmed zero duplicates — the full-replace flow (B3) holds; hero seed untouched.
- **Planning docs consistent with locked decisions:** ROADMAP §Phase 9 Goal/SC1/SC2/SC3 rewritten to time-only travel (D-01), one-way no-cancel commitment (D-03), pull-based inline check-in results with no cron/push (D-22/D-23); REQUIREMENTS TQC-06/TQC-07 annotated INVALIDATED by the redesign; `docs/economy-budget.md` records the AMENDMENT (D-01/D-22), drops the travel convertibility row, re-baselines encounter supply to check-in cadence (≤20/hr), and flags the Phase 10 capture-fee re-sign gate (D-02/D-18).
- **Phase gate fully green:** `npm test` (186/186), `npm run typecheck` (0 errors), `npm run lint` (0 warnings), `npm run check-i18n` (in sync) — all exit 0.

## Task Commits

Each task was committed atomically:

1. **Task 1: [BLOCKING] generate + apply migration 0018 + D-20 reseed** - `c317fc8` (feat)
2. **Task 2: ROADMAP/REQUIREMENTS/economy-budget amendments + STATE supersession verification** - `c981bb0` (docs)
3. **Task 3: full phase gate (test + typecheck + lint + check-i18n)** - read-only verification, no commit (plan `files: none`)

**Plan metadata:** pending — orchestrator commits the final docs commit (SUMMARY/STATE/ROADMAP) after the wave.

## Files Created/Modified

- `migrations/0018_sanguo_travel_map.sql` - drizzle-kit generated DDL: player_travel_state drop arrive_at/cost + add travel_seconds_remaining/encounter_active; encounter_runs add encounter_type + user_status_idx; create map_zones/map_edges/hero_zone_rates
- `migrations/meta/0018_snapshot.json` - drizzle-kit snapshot for the new migration (journal consistency)
- `migrations/meta/_journal.json` - journal entry for 0018
- `.planning/ROADMAP.md` - §Phase 9 Goal/SC1/SC2/SC3 amended (D-01/D-03/D-22/D-23); SC4 confirmed matching D-13
- `.planning/REQUIREMENTS.md` - TQC-06 annotated INVALIDATED by D-01/D-03; TQC-07 annotated INVALIDATED by D-22/D-23
- `docs/economy-budget.md` - AMENDMENT block (D-01/D-22), convertibility row cleanup, gating-line reword, Phase 10 re-sign flag (D-02/D-18)

## Decisions Made

- **Rename-conflict checkpoint (user-approved Option A):** the interactive `promptColumnsConflicts` fired (expected — any table with both added and removed columns triggers drizzle-kit's rename heuristic) and the plan mandates HALT on interactive drizzle-kit prompts. Resolved by the user: answer both prompts with the default (create column) — `travel_seconds_remaining` and `encounter_active` are NEW columns, `arrive_at`/`cost` are DROPPED, zero renames. The approved mechanism was a TTY-faking wrapper (getter-overridden `process.stdin`/`process.stdout` + auto `return` keypress) that drives the hanji prompts to their default without modifying node_modules; the shim was deleted immediately after use. Generated SQL matched the plan's expected statement list exactly.
- **economy-budget wording vs its own acceptance grep:** the plan's amendment template contained the literal phrase "travel prices are sinks", which the plan's Task 2 acceptance criterion requires to be absent (grep count = 0). Reworded the amendment to "earlier travel-as-sink references superseded" — supersession semantics preserved, acceptance grep passes.
- **STATE.md supersession already recorded:** the D-11/D-12 supersession (D-22/D-23) and the charge-on-arrival todo RESOLVED marker were committed by quick task 260812-j7r during the pull-based redesign; verified present rather than re-added (no duplication).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] economy-budget amendment template self-contradicted its own acceptance grep**
- **Found during:** Task 2 (economy-budget amendment)
- **Issue:** The plan's AMENDMENT text included the literal string "travel prices are sinks", but Task 2's acceptance criterion requires `grep -c "travel prices are sinks"` to output 0 — the template as written could never pass its own gate. The same phrase also survived in the Phase 9 gating line (line 120).
- **Fix:** Reworded the amendment note to reference "earlier travel-as-sink references (gating statement below) superseded by the added line", and reworded the gating line to "travel TQC-06 — time-only per D-01, no travel sink". Supersession + re-sign flag semantics unchanged.
- **Files modified:** docs/economy-budget.md
- **Verification:** `grep -c "travel prices are sinks" docs/economy-budget.md` → 0; AMENDMENT + RE-SIGN greps still pass
- **Committed in:** c981bb0 (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (1 bug — plan-internal inconsistency)
**Impact on plan:** Minimal. No scope creep; the fix was required for the plan's own acceptance criteria to pass.

## Issues Encountered

- **drizzle-kit interactive prompt in non-TTY shell (Task 1):** `generate` aborted with "Interactive prompts require a TTY terminal" because `player_travel_state` has both created and deleted columns. Handled per the plan's HALT mandate — surfaced as a checkpoint with full root-cause analysis (drizzle-kit 0.31.10 `promptColumnsConflicts` fires whenever a table has both added and removed columns; the only such table is player_travel_state). User approved Option A (controlled non-interactive resolution); generated SQL verified against the expected statement list before applying. No credentials were guessed; no node_modules touched.

## Known Stubs

None - no stubs introduced by this plan (integration/migration/docs/gate plan, no new runtime code).

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- **Phase 9 is verification-ready** — all four phase gates green; `/gsd-verify-work` can run.
- **Phase 10 (Battle & Capture) is gated on the economy re-sign:** `docs/economy-budget.md` now flags that the Phase 10 capture fee (D-02) MUST be priced and re-signed (D-18) assuming pull-driven encounter supply (check-in cadence ≤ 20/hr) before Phase 10 content ships — the pull-model re-baseline is recorded.
- **Blockers:** none.

---

## Self-Check: PASSED

- ✅ `09-05-SUMMARY.md` exists at the plan directory
- ✅ `migrations/0018_sanguo_travel_map.sql` exists; contains all expected DDL (drops/adds/index/3 CREATE TABLEs)
- ✅ `migrations/meta/0018_snapshot.json` exists; `_journal.json` updated
- ✅ Commits `c317fc8` + `c981bb0` present in git log
- ✅ `npm run typecheck` re-run at self-check time: exit 0
- ✅ All Task 1/2/3 acceptance greps passed (documented in coverage block)

*Phase: 09-travel-encounters*
*Completed: 2026-08-13*
