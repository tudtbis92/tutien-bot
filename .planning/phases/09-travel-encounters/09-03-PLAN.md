---
phase: 09-travel-encounters
plan: 03
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - src/services/sanguo/sanguoNotificationService.ts
  - src/services/sanguo/__tests__/sanguoNotificationService.test.ts
  - src/jobs/sanguoTickArrivals.ts
  - src/jobs/__tests__/sanguoTickArrivals.test.ts
  - src/workers/pgBoss.ts
  - src/ui/embeds/buildSanguoArrivalEmbed.ts
  - locales/vi/sanguo.json
  - locales/en/sanguo.json
  - locales/zh-cn/sanguo.json
autonomous: true
requirements:
  - TQC-07

estimate:
  tokens: 36000
  raw_tokens: 18000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "sanguo-tick-arrivals pg-boss cron (every minute, manager process only) resolves due journeys: locks traveling rows with FOR UPDATE SKIP LOCKED, subtracts counted elapsed time, fires arrival when travel_seconds_remaining reaches 0, and self-heals overdue journeys (clamped to 0 → arrives — D-05, never stuck)."
    - "The pause-aware clock honors D-07: while encounter_active=true the tick advances the updatedAt anchor WITHOUT subtracting counted time (Phase-10-ready; Phase 9 leaves the flag observably false)."
    - "The arrival tick is the ONLY writer of travel_seconds_remaining/updatedAt for traveling rows (single-writer rule, Pitfall 5)."
    - "Arrival notifications reach the player via cross-shard REST DM (@discordjs/rest): POST /users/@me/channels then POST /channels/{id}/messages, user-level locale (users.locale → vi fallback, Redis-cached 1h), and 50007/10003 DM-closed failures are 3-strike-capped with silent skip (D-12, UI-SPEC DM contract)."
    - "DMs are sent AFTER the transaction commits (never inside the lock tx)."
  artifacts:
    - src/services/sanguo/sanguoNotificationService.ts — sendUserDM, notifyArrival, resolveUserLocale, 3-strike helpers
    - src/jobs/sanguoTickArrivals.ts — runSanguoTickArrivals(job)
    - src/workers/pgBoss.ts — 'sanguo-tick-arrivals' queue @ * * * * *
    - src/ui/embeds/buildSanguoArrivalEmbed.ts — buildSanguoArrivalEmbed(data, t)
    - locales/{vi,en,zh-cn}/sanguo.json — arrival.* keys
    - src/services/sanguo/__tests__/sanguoNotificationService.test.ts + src/jobs/__tests__/sanguoTickArrivals.test.ts
  key_links:
    - "sanguoTickArrivals ← travelService's remaining-seconds model: reads travelSecondsRemaining/updatedAt written by startTravel (09-01) — the D-07 clock is only correct if the arrival tick is the sole writer."
    - "sanguoNotificationService ← matchLifecycleService.ts REST/locale/3-strike pattern (D-12 mirror)."
    - "pgBoss registration ← bot.ts initPgBoss() manager-only (shards send-only — header comment pgBoss.ts:14-16)."
  prohibitions:
    - "No DM retry storm: a 50007/10003 failure is counted once (Redis incr + expire 86400) and skipped at 3 strikes — never retried in a loop."
    - "No counting time while encounter_active: the tick advances the anchor, it does not subtract (D-07)."
    - "No arrival resolution writes outside the FOR UPDATE SKIP LOCKED transaction."
    - "No hardcoded user-facing strings — arrival.* via t('sanguo:...') (check-i18n gate)."
    - "No wallet/balance interaction anywhere in arrival resolution (D-01/D-04)."
  flagged_assumptions:
    - "FLAGGED ASSUMPTION (TQC-07 probe: 'What happens if this runs twice on the same input?'): pg-boss schedule() is idempotent (updated, not duplicated) and job dispatch is exactly-once; FOR UPDATE SKIP LOCKED prevents two concurrent sweeps from resolving the same row — a double-fired arrival tick resolves each row once and fires one DM."
    - "FLAGGED ASSUMPTION (TQC-07 probe: 'If interrupted or run in parallel, what is guaranteed?'): an interrupted tick leaves remaining/updatedAt mid-state; the next sweep recomputes elapsed from updatedAt and self-heals overdue journeys to arrival (D-05 'đến trễ, never stuck'); the two sanguoTick crons never deadlock because SKIP LOCKED skips rows held by the other tick."
    - "FLAGGED ASSUMPTION (A9 research + UI-SPEC unresolved item): DM failure (50007) is handled as skip + log + Redis 3-strike counter — the user receives no notification and travel continues; the encounter-active pause is observably false in Phase 9 (instant resolve)."

---

<objective>
What this plan accomplishes: the **arrival-resolution half of TQC-07** — the `sanguo-tick-arrivals` pg-boss cron (every minute, manager process only) that sweeps traveling journeys with `FOR UPDATE SKIP LOCKED`, decrements the pause-aware remaining-seconds clock (D-07), resolves arrivals (including overdue self-heal, D-05), and delivers the arrival DM cross-shard via the new `sanguoNotificationService` (REST, user-level locale, 3-strike failure cap — D-12).

Purpose: close the loop started in 09-01 — a journey the player starts must resolve at the displayed ETA and notify the player regardless of which shard hosts them. Notifications are event-only (D-06): arrival DM is the single notification — no persistent status embed, no live countdown UI.

Output: arrival tick job + REST notification service + arrival embed + i18n arrival keys + pg-boss registration + unit tests.
</objective>

<execution_context>
@C:/Users/901107/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/901107/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-travel-encounters/09-CONTEXT.md
@.planning/phases/09-travel-encounters/09-RESEARCH.md
@.planning/phases/09-travel-encounters/09-PATTERNS.md
@.planning/phases/09-travel-encounters/09-UI-SPEC.md
@src/workers/pgBoss.ts
@src/jobs/footballResolveMatches.ts
@src/services/football/matchLifecycleService.ts
@src/services/sanguo/travelService.ts
@src/db/schema/playerTravelState.ts
@src/db/schema/users.ts
@src/db/schema/mapNodes.ts
@src/cache/redis.ts
@src/i18n/index.ts
@src/ui/theme.ts
@src/ui/embeds/buildSanguoMapEmbed.ts
@src/ui/embeds/buildErrorEmbed.ts
@src/config.ts
</context>

<tasks>

<task type="expansion" tdd="true">
  <name>Task 1 (expansion): sanguoNotificationService — cross-shard REST DM, user-level locale, 3-strike cap</name>
  <files>src/services/sanguo/sanguoNotificationService.ts, src/services/sanguo/__tests__/sanguoNotificationService.test.ts, src/ui/embeds/buildSanguoArrivalEmbed.ts, locales/vi/sanguo.json, locales/en/sanguo.json, locales/zh-cn/sanguo.json</files>
  <read_first>
    - src/services/football/matchLifecycleService.ts (the D-12 mirror — REST client at 16, getGuildLocale at 21-46, DiscordErrorLike/isChannelNotFoundError at 48-62, handleChannelFailure 3-strike at 74-91, DM POST shape at ~199-208)
    - src/cache/redis.ts (singleton `redis` export)
    - src/i18n/index.ts (getT, SupportedLocale, resolveLocale)
    - src/db/schema/users.ts (discordId + locale columns)
    - src/ui/theme.ts (COLORS.SEASON, embedFooter) + src/ui/embeds/buildSanguoMapEmbed.ts (embed builder pattern)
    - .planning/phases/09-travel-encounters/09-RESEARCH.md Pattern 4 (lines 351-377) — REST DM + 3-strike shape
    - .planning/phases/09-travel-encounters/09-UI-SPEC.md §Interaction contract — DM notifications + §Copywriting Contract (arrival keys)
  </read_first>
  <behavior>
    - Test 1: sendUserDM opens the DM via POST /users/@me/channels with recipient_id and posts embeds via POST /channels/{id}/messages (REST mocked).
    - Test 2: a 50007/10003-style failure increments the Redis strike counter (sanguo:dm:strike:{userId}) with 86400 expiry; at count >= 3 the user's DMs are skipped without further REST calls.
    - Test 3: resolveUserLocale reads users.locale (Redis cache key user:locale:{userId}, 1h TTL) and falls back to 'vi'.
    - Test 4: notifyArrival builds the arrival embed (SEASON, title sanguo:arrival.title, body with {node}, CTA) and sends it via sendUserDM.
  </behavior>
  <action>
    Create `src/services/sanguo/sanguoNotificationService.ts` mirroring matchLifecycleService.ts (D-12):
    - Imports to copy from matchLifecycleService.ts:1-16: `REST, Routes` from 'discord.js', `config` from '../../config.js', `redis` from '../../cache/redis.js', `getT, type SupportedLocale` from '../../i18n/index.js', `logger` from '../../utils/logger.js', `db` from '../../db/client.js', `users` from '../../db/schema/users.js'. Module-level `const rest = new REST().setToken(config.DISCORD_TOKEN)`.
    - `resolveUserLocale(userId: number): Promise<SupportedLocale>` — key `user:locale:{userId}`: Redis get (cache hit → return), else read `db.select({ locale: users.locale }).from(users).where(eq(users.id, userId))`, normalize via the resolveLocale shape (users.locale → 'vi' fallback), `redis.set(key, locale, 'EX', 3600)` (1h per UI-SPEC). Never trusts interaction locale (DMs have none).
    - `isDMClosedError(err: unknown): boolean` — DiscordErrorLike duck-type: status 403/404 OR code 50007 (Cannot send messages to this user) / 10003 (Unknown Channel) / 50001 (Missing Access) — mirror isChannelNotFoundError at matchLifecycleService.ts:54-62 but extended with 50007.
    - `recordDMStrike(userId: number, err: unknown): Promise<void>` — if isDMClosedError: `redis.incr('sanguo:dm:strike:' + userId)` + `redis.expire(key, 86400)`; log warn `SanguoNotify` `User ${userId} DM failed. Count: ${c}/3`; at count >= 3 log error (user's DMs permanently skipped until the key expires). Non-DM-closed errors log error only (no strike).
    - `sendUserDM(userId: number, embeds: EmbedBuilder[]): Promise<void>` — check strikes: `Number(await redis.get('sanguo:dm:strike:' + userId) ?? '0') >= 3` → log warn + return silently; lookup `users.discordId`; `rest.post(Routes.userChannels(), { body: { recipient_id: u.discordId } })` → `rest.post(Routes.channelMessages(dm.id), { body: { embeds: embeds.map(e => e.toJSON()) } })`; on error → recordDMStrike; never retry in a loop.
    - `notifyArrival(userId: number, data: { nodeName: string; shardId?: number }): Promise<void>` — `const locale = await resolveUserLocale(userId)`; `const t = getT(locale)`; `await sendUserDM(userId, [buildSanguoArrivalEmbed({ nodeName: data.nodeName, shardId: data.shardId }, t)])`.

    Create `src/ui/embeds/buildSanguoArrivalEmbed.ts`:
    - `export interface SanguoArrivalEmbedData { nodeName: string; shardId?: number }`.
    - `buildSanguoArrivalEmbed(data, t: TFunction): EmbedBuilder` — `.setColor(COLORS.SEASON)`, `.setTitle(t('sanguo:arrival.title'))`, `.setDescription(t('sanguo:arrival.body', { node: data.nodeName }))`, field/description CTA `t('sanguo:arrival.cta')`, `.setFooter(embedFooter(data.shardId))` + `.setTimestamp()` (theme.ts pattern — never hardcode hex).

    Edit `locales/{vi,en,zh-cn}/sanguo.json` — add a top-level `arrival` block, identical key structure in all 3 locales (VI canonical; EN/ZH translate):
    - `arrival.title` = "📍 Đã đến nơi"
    - `arrival.body` = "Bạn đã đến **{node}**."
    - `arrival.cta` = "Dùng /sanguo travel để tiếp tục hành trình."

    Create `src/services/sanguo/__tests__/sanguoNotificationService.test.ts` per the 4 behaviors — mock 'discord.js' REST (vi.mock), mock db/client.js (users select), mock cache/redis.js (get/set/incr/expire), assert REST POST call shapes and strike-skip behavior.
  </action>
  <verify>
    <automated>npx vitest run src/services/sanguo/__tests__/sanguoNotificationService.test.ts; npm run check-i18n; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Command: `npx vitest run src/services/sanguo/__tests__/sanguoNotificationService.test.ts` exits 0.
    - Command: `npm run check-i18n` exits 0; `npm run typecheck` exits 0.
    - Source: `src/services/sanguo/sanguoNotificationService.ts` contains `Routes.userChannels()` and `Routes.channelMessages(` calls.
    - Source: contains `user:locale:${userId}` (or equivalent template) and `sanguo:dm:strike:${userId}` Redis keys.
    - Source: strike skip at 3 — contains `>= 3` guard before the REST DM POST.
    - Config: `locales/{vi,en,zh-cn}/sanguo.json` each contain `arrival.title`, `arrival.body`, `arrival.cta`.
    - Source: `src/ui/embeds/buildSanguoArrivalEmbed.ts` uses `COLORS.SEASON` + `embedFooter` + `setTimestamp()`.
  </acceptance_criteria>
  <done>Cross-shard REST DM service with user-level locale (Redis-cached 1h) and 3-strike DM-closed cap; arrival embed + arrival.* i18n keys in all 3 locales; tests green.</done>
</task>

<task type="integration" tdd="true">
  <name>Task 2 (integration): sanguoTickArrivals job + pg-boss registration</name>
  <files>src/jobs/sanguoTickArrivals.ts, src/jobs/__tests__/sanguoTickArrivals.test.ts, src/workers/pgBoss.ts</files>
  <read_first>
    - src/jobs/footballResolveMatches.ts (job-body structure: job param, per-job try/catch, log lines at 32-33/106-108)
    - src/services/football/matchLifecycleService.ts lines 333-345 (the exact `.for('update', { skipLocked: true })` object form)
    - src/workers/pgBoss.ts (registerJobs() at 83-179 — createQueue/schedule/work pattern; the final logger.info at 178)
    - src/db/schema/playerTravelState.ts (columns to read/write: status, travelSecondsRemaining, encounterActive, updatedAt)
    - src/db/schema/mapNodes.ts (toNodeId → per-locale node name for the arrival DM)
    - src/services/sanguo/sanguoNotificationService.ts (notifyArrival — created in Task 1)
    - .planning/phases/09-travel-encounters/09-RESEARCH.md Pattern 2 (lines 286-320) — arrival resolution algorithm + Pitfall 1/2/5
  </read_first>
  <behavior>
    - Test 1: a traveling row with elapsed >= remaining is set status='arrived', travelSecondsRemaining=0, and notifyArrival is called AFTER commit (once).
    - Test 2: a traveling row with elapsed < remaining has travelSecondsRemaining decremented and updatedAt advanced.
    - Test 3: an overdue row (elapsed >> remaining) is clamped to 0 and resolves to arrived (D-05 self-heal — no 'failed' status).
    - Test 4: a row with encounterActive=true has updatedAt advanced WITHOUT subtraction (D-07 pause).
    - Test 5: the SELECT uses `.for('update', { skipLocked: true })` (mocked and asserted).
  </behavior>
  <action>
    Create `src/jobs/sanguoTickArrivals.ts` per RESEARCH Pattern 2 (D-05/D-07):
    - `export async function runSanguoTickArrivals(job: Job): Promise<void>` — `logger.info('SanguoTickArrivals', `Job started: ${job.id}`)`; `const now = new Date()`.
    - Inside `db.transaction(async (tx) => ...)`: `const rows = await tx.select().from(playerTravelState).where(eq(playerTravelState.status, 'traveling')).for('update', { skipLocked: true })` (exact object form from matchLifecycleService.ts:336-345).
    - Per row:
      - If `row.encounterActive` (D-07 pause): `tx.update(playerTravelState).set({ updatedAt: now }).where(eq(playerTravelState.id, row.id))` — anchor advances, NO time counted. Continue.
      - `elapsedSec = Math.max(0, Math.floor((now.getTime() - row.updatedAt.getTime()) / 1000))`.
      - `remaining = Math.max(0, row.travelSecondsRemaining - elapsedSec)` — overdue clamped to 0 → arrives (D-05, no failed status).
      - If `remaining === 0`: set `{ status: 'arrived', travelSecondsRemaining: 0, updatedAt: now }`; collect `{ userId, toNodeId, id }` into an `arrived` list.
      - Else: set `{ travelSecondsRemaining: remaining, updatedAt: now }`.
    - AFTER the transaction commits: for each arrived row, resolve the node's per-locale name (read map_nodes by id: nameVi/nameEn/nameZh + locale from the user's row — or reuse resolveUserLocale) and `await notifyArrival(userId, { nodeName, shardId: undefined })` — outside the tx (D-12; a DM failure must not roll back the arrival).
    - Per-row try/catch with `logger.error('SanguoTickArrivals', ...)` (footballResolveMatches pattern) so one failure does not abort the sweep; final `logger.info('SanguoTickArrivals', `Job completed: ${job.id}. Arrived ${arrivedCount}`)`.
    - **Single-writer rule (Pitfall 5):** this job is the ONLY writer of travelSecondsRemaining/updatedAt for traveling rows — the encounter tick (09-04) writes neither.

    Edit `src/workers/pgBoss.ts`:
    - Import `runSanguoTickArrivals` from '../jobs/sanguoTickArrivals.js' (import block style at lines 4-12).
    - In `registerJobs()` after the football blocks (after line 176), add:
      - `await b.createQueue('sanguo-tick-arrivals');`
      - `await b.schedule('sanguo-tick-arrivals', '* * * * *', {});` (every minute — RESEARCH D-11 locked schedule)
      - `await b.work('sanguo-tick-arrivals', { localConcurrency: 1 }, async (jobs: Job[]) => { for (const job of jobs) { try { await runSanguoTickArrivals(job); } catch (err) { logger.error('pgBoss', `Job ${job.id} (sanguo-tick-arrivals) failed`, err); } } });`
    - Update the final `logger.info('pgBoss', 'Jobs registered: ...')` line (178) to append `sanguo-tick-arrivals @ * * * * *`.
    - Manager-only invariant preserved: crons registered ONLY in registerJobs() called from initPgBoss() (bot.ts); shards use initPgBossForShard() send-only (header comment pgBoss.ts:14-16).

    Create `src/jobs/__tests__/sanguoTickArrivals.test.ts` per the 5 behaviors — mock db/client.js with a `transaction` mock whose callback receives a fake tx exposing select().from().where().for() chain (assert `.for('update', { skipLocked: true })` was called) + update(); mock the notification service (vi.mock) and assert notifyArrival called once per arrived row, after the tx callback resolves.
  </action>
  <verify>
    <automated>npx vitest run src/jobs/__tests__/sanguoTickArrivals.test.ts; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Command: `npx vitest run src/jobs/__tests__/sanguoTickArrivals.test.ts` exits 0 (5 behaviors).
    - Command: `npm run typecheck` exits 0.
    - Source: `src/jobs/sanguoTickArrivals.ts` contains `.for('update', { skipLocked: true })`, the encounterActive anchor-advance branch, and `Math.max(0, row.travelSecondsRemaining - elapsedSec)` clamp.
    - Source: notifyArrival is called outside the transaction body (after `db.transaction(...)` resolves).
    - Source: `src/workers/pgBoss.ts` contains `createQueue('sanguo-tick-arrivals')` + `schedule('sanguo-tick-arrivals', '* * * * *', {})` + the work handler, and the imports line references `runSanguoTickArrivals`.
    - Source: `src/jobs/sanguoTickArrivals.ts` contains the `.update(playerTravelState)` calls that set `travel_seconds_remaining`/`updatedAt`/`status` (it is the single writer of the D-07 clock — the complementary no-write gate for the encounter tick lives in 09-04's acceptance criteria).
  </acceptance_criteria>
  <done>Arrival tick resolves journeys every minute with SKIP LOCKED + pause-aware decrement + overdue self-heal; DMs fire post-commit; pg-boss registration in the manager-only path; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pg-boss job → DB rows | The arrival tick mutates player_travel_state rows; concurrent sweeps (arrivals + encounters) touch the same table |
| manager process → Discord REST API | DM sends cross a rate-limited, failure-prone external boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-09 | Tampering | Double-resolve race (both ticks or two sweeps) | high | mitigate | `.for('update', { skipLocked: true })` on the traveling-row scan (matchLifecycleService.ts:345) — a row locked by one tick is skipped by the other; single-writer rule (arrival tick alone writes remaining/updatedAt, Pitfall 5). |
| T-09-10 | DoS | DM storm on closed DMs (50007) | high | mitigate | 3-strike Redis counter (`sanguo:dm:strike:{userId}`, 86400 expiry) — skip at >= 3, never retry-storm (Pattern 4, UI-SPEC DM contract). |
| T-09-11 | Information Disclosure | Locale cache poisoning | low | accept | `user:locale:{userId}` is written only from users.locale (DB-authoritative); 1h TTL bounds staleness. |
| T-09-12 | Tampering | Overdue journey stuck forever (D-05 violation) | medium | mitigate | Remaining clamped `Math.max(0, ...)` → overdue rows resolve to arrived at the next sweep; no failed/cancelled status exists (D-03/D-05). |
| T-09-SC | Tampering | npm/pip/cargo installs | — | not-triggered | No new packages this phase; all dependencies already installed + production-running (RESEARCH Package Legitimacy Audit). |
</threat_model>

<verification>
- `npx vitest run src/jobs/__tests__/sanguoTickArrivals.test.ts src/services/sanguo/__tests__/sanguoNotificationService.test.ts` green.
- `npm run typecheck` + `npm run check-i18n` green (arrival.* keys present).
- Grep: `sanguo-tick-arrivals` queue registered exactly once in pgBoss.ts.
- Behavioral: arrival fires only when remaining reaches 0; overdue self-heals; encounterActive pauses the clock.
</verification>

## Artifacts this phase produces

| Symbol | Kind | File |
|--------|------|------|
| `sendUserDM(userId, embeds)` | REST DM send with 3-strike cap | src/services/sanguo/sanguoNotificationService.ts |
| `resolveUserLocale(userId)` | user-level locale resolution (Redis-cached 1h, vi fallback) | src/services/sanguo/sanguoNotificationService.ts |
| `notifyArrival(userId, { nodeName, shardId })` | arrival DM notification (D-12) | src/services/sanguo/sanguoNotificationService.ts |
| `isDMClosedError` / `recordDMStrike` | 50007/10003 failure handling | src/services/sanguo/sanguoNotificationService.ts |
| `runSanguoTickArrivals(job)` | arrival-resolution cron job (SKIP LOCKED, pause-aware, self-heal) | src/jobs/sanguoTickArrivals.ts |
| `sanguo-tick-arrivals` queue @ `* * * * *` | pg-boss cron registration (manager-only) | src/workers/pgBoss.ts |
| `buildSanguoArrivalEmbed(data, t)` | arrival DM embed (SEASON) | src/ui/embeds/buildSanguoArrivalEmbed.ts |
| `arrival.title` / `arrival.body` / `arrival.cta` | i18n keys (3 locales) | locales/{vi,en,zh-cn}/sanguo.json |
| notification + tick test suites | test files | src/services/sanguo/__tests__/sanguoNotificationService.test.ts, src/jobs/__tests__/sanguoTickArrivals.test.ts |

<success_criteria>
- Journeys resolve at ETA every minute; overdue journeys arrive late, never stuck (D-05).
- The D-07 clock pauses correctly on encounters (schema-ready; observably instant in Phase 9).
- Arrival DMs reach users cross-shard with correct locale and 3-strike failure safety.
- Manager-only cron invariant preserved.
</success_criteria>

<output>
Create `.planning/phases/09-travel-encounters/09-03-SUMMARY.md` when done.
</output>
