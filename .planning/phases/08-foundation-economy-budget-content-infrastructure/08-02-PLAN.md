---
phase: 08-foundation-economy-budget-content-infrastructure
plan: 2
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/wallet.ts
  - src/db/schema/walletTransactions.ts
  - src/db/schema/index.ts
  - src/commands/game/gather.ts
  - src/services/farming/subscriptionService.ts
  - src/services/football/predictionService.ts
  - src/services/football/matchLifecycleService.ts
  - src/services/__tests__/wallet.test.ts
autonomous: true
requirements: [TQC-01]
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "wallet.deductBalance(txOrDb, userId, amount, { reason, metadata }) applies the atomic WHERE guard balance >= amount; a zero-row result throws Error('INSUFFICIENT_BALANCE') and the whole transaction rolls back (no balance change, no ledger row) (D-01/D-02)."
    - "Every successful deductBalance/creditBalance call writes exactly one wallet_transactions row (userId, type 'deduct'|'credit', amount bigint, balance_after bigint = post-mutation balance, reason varchar(50), metadata jsonb, created_at) in the SAME transaction as the users.balance update — a committed balance change always has a matching ledger row, satisfying SC1 reconcilability."
    - "creditBalance has no balance guard (a credit cannot go negative) and returns balanceAfter via .returning(); the DB-level balance_non_negative check in src/db/schema/users.ts remains the last line of defense."
    - "All 7 existing balance-write sites (gather, farming purchase, farming upgrade, football wager/refund, football void, push, payout) route through the wallet (D-03); the grep gate finds no remaining inline balance UPDATE outside src/services/wallet.ts."
    - "wallet unit tests (src/services/__tests__/wallet.test.ts, vitest vi.mock pattern) pass: success deduct writes ledger with balance_after, insufficient deduct rejects 'INSUFFICIENT_BALANCE' with no ledger row, explicit-tx passthrough honors D-02, credit succeeds without a guard."
    - "npm run typecheck and npm run lint pass after the refactor; the existing farming and football test suites still pass (behavior preserved)."
  artifacts:
    - path: "src/services/wallet.ts"
      provides: "deductBalance + creditBalance, single choke point for users.balance (TQC-01/D-03)"
      contains: "deductBalance"
    - path: "src/db/schema/walletTransactions.ts"
      provides: "wallet_transactions ledger table (D-01)"
      contains: "wallet_transactions"
    - path: "src/services/__tests__/wallet.test.ts"
      provides: "wallet unit tests (TQC-01)"
      contains: "INSUFFICIENT_BALANCE"
  key_links:
    - from: "src/services/wallet.ts"
      to: "src/db/schema/walletTransactions.ts"
      via: "ledger INSERT shares the balance UPDATE transaction (D-01/D-02)"
      pattern: "insert\\(walletTransactions\\)"
    - from: "src/services/wallet.ts"
      to: "src/db/schema/users.ts"
      via: "WHERE-guard UPDATE on users.balance; balance_non_negative check is the DB backstop"
      pattern: "users.balance"
    - from: "src/commands/game/gather.ts"
      to: "src/services/wallet.ts"
      via: "gather fee deduct refactored to wallet.deductBalance reason 'gather' (D-03)"
      pattern: "wallet.deductBalance"
    - from: "src/services/farming/subscriptionService.ts"
      to: "src/services/wallet.ts"
      via: "purchase/upgrade deducts refactored to wallet reasons 'farming_subscription'/'farming_upgrade'"
      pattern: "wallet.deductBalance"
    - from: "src/services/football/predictionService.ts"
      to: "src/services/wallet.ts"
      via: "wager deduct + edit refund refactored to wallet 'bet_wager'/'bet_refund'"
      pattern: "wallet.deductBalance"
    - from: "src/services/football/matchLifecycleService.ts"
      to: "src/services/wallet.ts"
      via: "void/push/payout credits refactored to wallet.creditBalance reasons 'bet_void'/'bet_push'/'bet_payout'"
      pattern: "wallet.creditBalance"
  prohibitions:
    - statement: "No new call site may write users.balance directly — src/services/wallet.ts is the single source of truth for every balance change (D-03); a direct write anywhere reintroduces drift."
      status: unverified
      flagged: true
---

<objective>
Build the wallet spine of the Milestone v3 economy (TQC-01): the shared wallet service that becomes the single choke point for every `users.balance` change (D-03), the atomic `wallet_transactions` ledger (D-01/D-02), the refactor of all 7 existing balance-write sites through the wallet, and the unit-test suite proving the behavior. This satisfies SC1 (no balance drift, no double-spend, ledger history reconcilable) and is the prerequisite every later phase's money flow (Phase 9 travel, Phase 11 shop/evolution must call the wallet).

Purpose: TQC-01 (wallet + ledger, D-01/D-02/D-03/D-04). The wallet_transactions schema merged here is one of the 9 Phase 8 tables that plan 08-04 migrates as 0014; plan 08-04 (wave 2) also delivers the 7 sanguo domain schemas, the dev-DB provisioning, the content seed and the deploy step.

Output: wallet service + ledger schema, 7 refactored files (4 source files / 7 call sites), wallet unit tests.

**Shared-file note (merge-safety):** This plan runs in wave 1 alongside plan 08-01, which also edits `src/db/schema/index.ts` (mapNodes export). All edits to index.ts are append-only under the '// Phase 8 schemas' comment and must never remove or duplicate the other plan's additions. Plan 08-04 (wave 2, depends on 08-01 + 08-02) appends the 7 sanguo schema exports and generates migration 0014 after both wave-1 merges have landed.

## Artifacts this phase produces (plan 08-02)

| Symbol | Kind | Location |
|--------|------|----------|
| `wallet.deductBalance(txOrDb, userId, amount, { reason, metadata })` | service fn (WHERE guard) | src/services/wallet.ts |
| `wallet.creditBalance(txOrDb, userId, amount, { reason, metadata })` | service fn (no guard) | src/services/wallet.ts |
| `walletTransactions` table + `WalletTransaction`/`NewWalletTransaction` types | Drizzle schema | src/db/schema/walletTransactions.ts |
| `// Phase 8 schemas` export block (walletTransactions line) | merge | src/db/schema/index.ts |
| 7 balance-write sites routed through the wallet (gather, farming x2, football x4) | call-site refactor | src/commands/game/gather.ts, src/services/farming/subscriptionService.ts, src/services/football/predictionService.ts, src/services/football/matchLifecycleService.ts |
| `wallet.test.ts` (vi.mock db pattern) | vitest suite | src/services/__tests__/wallet.test.ts |
</objective>
<execution_context>
@C:/Users/901107/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/901107/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-foundation-economy-budget-content-infrastructure/08-CONTEXT.md
@.planning/phases/08-foundation-economy-budget-content-infrastructure/08-RESEARCH.md
@.planning/phases/08-foundation-economy-budget-content-infrastructure/08-PATTERNS.md
@.planning/phases/08-foundation-economy-budget-content-infrastructure/08-01-PLAN.md
@src/db/schema/users.ts
@src/db/schema/footballBets.ts
@src/db/schema/farming.ts
@src/db/schema/index.ts
@src/db/client.ts
@src/commands/game/gather.ts
@src/services/farming/subscriptionService.ts
@src/services/football/predictionService.ts
@src/services/football/matchLifecycleService.ts
@src/services/farming/__tests__/subscriptionService.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wallet service (deductBalance/creditBalance) + wallet_transactions ledger schema (TQC-01, D-01/D-02/D-03)</name>
  <reversibility rating="costly">D-02: the wallet API shape is the contract every current and future money flow (Phase 9-11) calls; changing it later touches every call site.</reversibility>
  <files>src/services/wallet.ts, src/db/schema/walletTransactions.ts, src/db/schema/index.ts</files>
  <behavior>
    - Test 1 (task 3): deductBalance with returning [{ balance: 300n }] resolves to 300n and calls tx.insert(walletTransactions) once with type 'deduct', amount, balanceAfter 300n, reason, metadata.
    - Test 2 (task 3): deductBalance with returning [] rejects Error('INSUFFICIENT_BALANCE') and never calls tx.insert.
    - Test 3 (task 3): passing an explicit tx object executes the UPDATE on that tx directly (no db.transaction call) — D-02 tx-first contract.
    - Test 4 (task 3): creditBalance resolves balanceAfter, writes type 'credit' ledger row, and its UPDATE has no balance comparison in the WHERE clause.
  </behavior>
  <read_first>src/db/schema/users.ts (balance bigint mode bigint + balance_non_negative check), src/db/schema/footballBets.ts (FK + bigint + varchar status + index template), src/db/schema/farming.ts (pgEnum + jsonb $type pattern), src/services/farming/subscriptionService.ts lines 60-87 (WHERE-guard deduct template + INSUFFICIENT_BALANCE), src/db/client.ts (db = drizzle({ client: pool, schema }) — derive the tx type), src/db/schema/index.ts (merge point; Phase 8 comment may already exist from 08-01)</read_first>
  <action>
    Create src/db/schema/walletTransactions.ts: pgTable('wallet_transactions') with id serial pk; userId integer notNull references users.id (import users from './users.js'); type as pgEnum('wallet_transaction_type', ['deduct', 'credit']) named walletTransactionTypeEnum (farming.ts pgEnum pattern); amount bigint mode bigint notNull; balanceAfter bigint mode bigint notNull (MANDATORY mode: 'bigint' per users.ts comment — never mode: 'number' for currency); reason varchar(50) notNull (first-class column for future /profile history + Phase 12 audit TQC-19); metadata jsonb with $type<Record<string, unknown>>() nullable (farming.ts jsonb pattern); createdAt timestamp withTimezone notNull defaultNow. Table callback: check('amount_non_negative', amount >= 0) and index('wallet_transactions_user_created_idx').on(userId, createdAt) for history queries. Export WalletTransaction/NewWalletTransaction infer types.
    Create src/services/wallet.ts: import { db } from '../db/client.js'; import { users } from '../db/schema/users.js'; import { walletTransactions } from '../db/schema/walletTransactions.js'; import { eq, sql } from 'drizzle-orm'. Define the transaction type as `type DbClient = typeof db; type Tx = Parameters<DbClient['transaction']>[0];` (do NOT use any — RESEARCH anti-pattern note re predictionService db: any). Export async function deductBalance(txOrDb: Tx | DbClient, userId: number, amount: bigint, opts: { reason: string; metadata?: Record<string, unknown> }): Promise<bigint> and symmetric creditBalance. Inside, if the passed object exposes a transaction function treat it as the client and run the body inside its own db.transaction (D-02: wallet drives the transaction internally where a flow has no other write needs); otherwise run the body directly on the passed tx. Body for deduct: UPDATE users SET balance = balance - amount WHERE id = userId AND balance >= amount RETURNING balance; if the returned array is empty throw new Error('INSUFFICIENT_BALANCE') (the returning-empty check is the rowCount-0 equivalent and supplies balanceAfter in one shot); then INSERT wallet_transactions values { userId, type: 'deduct', amount, balanceAfter: row.balance, reason: opts.reason, metadata: opts.metadata } in the same tx (D-01: ledger row and balance update are atomic); return row.balance. Body for credit: UPDATE users SET balance = balance + amount WHERE id = userId RETURNING balance (NO balance comparison — a credit cannot go negative; the users balance_non_negative check is the DB backstop); INSERT wallet_transactions type 'credit' with balanceAfter; return row.balance. Preserve error semantics: insufficient balance surfaces Error('INSUFFICIENT_BALANCE') so refactored callers that catch that message keep working (gather.ts:184, subscriptionService).
    Add `export * from './walletTransactions.js';` to src/db/schema/index.ts under the '// Phase 8 schemas' comment. If that comment already exists (added by plan 08-01 with the mapNodes export), append under it without removing or duplicating the mapNodes line; if it does not exist yet, create the comment block and include the walletTransactions export (mapNodes will be appended by 08-01 when it lands).
    Do NOT write tests in this task — the wallet test suite is task 3; here run typecheck to prove the service compiles.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>wallet.ts exports typed deductBalance/creditBalance with WHERE-guard + ledger insert in the same transaction, wallet_transactions schema is merged into index.ts under the Phase 8 comment, typecheck passes.</done>
  <acceptance_criteria>
    - Source assertion: src/services/wallet.ts exports deductBalance and creditBalance with a userId: number and amount: bigint signature and an opts object carrying reason and metadata.
    - Source assertion: src/db/schema/walletTransactions.ts declares amount and balance_after with mode: 'bigint' and a reason column with length 50.
    - Source assertion: src/db/schema/index.ts contains a line exporting walletTransactions under the Phase 8 comment exactly once, and the mapNodes export from plan 08-01 (wave-1 sibling) — if already present — is neither duplicated nor removed by this task's append (append-only merge protocol: before merging, check whether the sibling's Phase 8 lines are present and, if so, leave them untouched; cross-acceptance is finalized by plan 08-04's migration precondition, which verifies both exports are present).
    - CLI: `npm run typecheck` exits 0.
  </acceptance_criteria>
</task>
<task type="auto">
  <name>Task 2: Refactor all 7 balance-write sites through the wallet (TQC-01, D-03)</name>
  <reversibility rating="one-way">D-03: the no-direct-write invariant is the core anti-double-spend guarantee; a direct write anywhere reintroduces drift. One-way door is a user-locked CONTEXT decision (D-03) — no checkpoint:decision required.</reversibility>
  <precondition>src/services/wallet.ts exists and exports deductBalance/creditBalance (task 1 completed).</precondition>
  <files>src/commands/game/gather.ts, src/services/farming/subscriptionService.ts, src/services/football/predictionService.ts, src/services/football/matchLifecycleService.ts</files>
  <read_first>src/commands/game/gather.ts (deduct block lines 153-199, pre-fetched userBalance for error copy lines 106-120), src/services/farming/subscriptionService.ts (purchase deduct 75-87 with price > 0n guard, upgrade deduct 149-161, fee calc 19-32, prices 50-56), src/services/football/predictionService.ts (SELECT FOR UPDATE 115-119, pre-check 154-157, net-diff UPDATE 159-178, InsufficientBalanceError 36-41), src/services/football/matchLifecycleService.ts (SKIP LOCKED select 334-345, void 355-360, push 415-420, payout 431-436), 08-RESEARCH.md TQC-01 section (per-site refactor shapes + behavior-preservation requirements)</read_first>
  <action>
    Refactor each of the 7 sites to call the wallet INSIDE the existing transaction (the wallet call replaces the inline balance UPDATE; all other writes in the transaction stay). Import { deductBalance, creditBalance } from the wallet module. Remove any imports that become unused after the refactor (the lint gate npm run lint --max-warnings=0 flags unused imports — e.g. the sql helper in predictionService once the net-diff UPDATE is gone; the users import in matchLifecycleService if the inline balance UPDATE was its only use — verify each file after editing).
    Site 1 — gather.ts lines 158-166: replace the inline balance UPDATE with await deductBalance(tx, user.id, totalFee, { reason: 'gather', metadata: { amount, feePerRoll, majorRealmIndex } }). Note the wallet matches users.id while the old code matched users.discordId — the command's fetchCommandContext already returns the users row as user, so pass user.id. Keep the item-grant loop (lines 172-181) in the same transaction, and keep the catch block at 183-199 translating the thrown Error('INSUFFICIENT_BALANCE') into the insufficient-balance embed using the pre-fetched userBalance (display behavior unchanged).
    Site 2 — subscriptionService.ts purchase (76-87): replace the inline UPDATE with await deductBalance(tx, userId, price, { reason: 'farming_subscription', metadata: { planType, durationDays } }), keeping the `if (price > 0n)` guard (free plan pays nothing and writes no ledger row).
    Site 3 — subscriptionService.ts upgrade (150-161): replace with await deductBalance(tx, userId, fee, { reason: 'farming_upgrade', metadata: { planType: 'premium' } }), keeping the `if (fee > 0n)` guard.
    Site 4 — predictionService.ts lines 159-178: replace the single net-diff UPDATE with two wallet calls inside the same transaction: if isEdit and oldWagerAmount > 0n, await creditBalance(tx, userId, oldWagerAmount, { reason: 'bet_refund', metadata: { betId: betIdToUpdate, matchId, betType } }); then await deductBalance(tx, userId, wagerAmount, { reason: 'bet_wager', metadata: { betId: isEdit ? betIdToUpdate : undefined, matchId, betType } }). Keep the SELECT FOR UPDATE (115-119), the InsufficientBalanceError pre-check (154-157), and the bet upsert (182-214). The ledger stays reconcilable on edits: one 'bet_refund' row + one 'bet_wager' row. Wrap both wallet calls so that a thrown Error whose message is 'INSUFFICIENT_BALANCE' is caught and rethrown as InsufficientBalanceError (preserve the caller-facing class — this is the only site that throws a class today).
    Site 5 — matchLifecycleService.ts void (357-360): replace the inline credit UPDATE with await creditBalance(tx, bet.userId, bet.wagerAmount, { reason: 'bet_void', metadata: { betId: bet.id, matchId: match.id } }); keep the bet status update to 'void' and the logger line.
    Site 6 — matchLifecycleService.ts push (417-420): await creditBalance(tx, bet.userId, bet.wagerAmount, { reason: 'bet_push', metadata: { betId: bet.id, matchId: match.id } }); keep the status update.
    Site 7 — matchLifecycleService.ts payout (433-436): await creditBalance(tx, bet.userId, payout, { reason: 'bet_payout', metadata: { betId: bet.id, matchId: match.id } }) where payout = bet.potentialPayout ?? 0n; keep the status update to 'won'.
    After all sites are refactored, run the full gate: typecheck, lint, and the existing farming/football test suites (they mock the transaction chains — the wallet calls must be inside those chains so the mocks keep resolving; if a test asserts on the mocked update chain shape, adapt the test mock to include the wallet's insert step — do NOT weaken assertions). Behavior preservation is the contract: same error types, same pre-fetch display values, same transaction boundaries.
  </action>
  <verify>
    <automated>npm run typecheck; if ($?) { npm run lint }; if ($?) { npx vitest run src/services/farming/__tests__/subscriptionService.test.ts src/services/football/__tests__/predictionService.test.ts }</automated>
  </verify>
  <done>All 7 sites call the wallet with the specified reasons/metadata, callers catch the same error types as before, lint + typecheck + existing farming/football suites pass.</done>
  <acceptance_criteria>
    - Source assertion: src/commands/game/gather.ts contains a call to deductBalance with reason 'gather', and the token `${users.balance}` appears zero times in the file.
    - Source assertion: src/services/farming/subscriptionService.ts contains deductBalance calls with reasons 'farming_subscription' and 'farming_upgrade', and the token `${users.balance}` appears zero times in the file.
    - Source assertion: src/services/football/predictionService.ts contains creditBalance with reason 'bet_refund' and deductBalance with reason 'bet_wager', and still throws InsufficientBalanceError on insufficient balance.
    - Source assertion: src/services/football/matchLifecycleService.ts contains creditBalance calls with reasons 'bet_void', 'bet_push', and 'bet_payout', and the token `${users.balance}` appears zero times in the file.
    - Grep gate: `rg -n '\$\{users\.balance\}' src/commands src/services --glob '!**/__tests__/**'` returns matches ONLY in src/services/wallet.ts (zero hits in the four refactored files).
    - CLI: `npm run typecheck`, `npm run lint`, and the vitest runs for subscriptionService + predictionService all exit 0.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wallet unit tests — src/services/__tests__/wallet.test.ts (TQC-01)</name>
  <files>src/services/__tests__/wallet.test.ts</files>
  <behavior>
    - Test 1: deductBalance(tx, 1, 100n, { reason: 'gather' }) with mockTx.returning resolving [{ balance: 300n }] resolves to 300n and calls mockTx.insert once with an object whose type is 'deduct', amount is 100n, balanceAfter is 300n, and reason is 'gather'.
    - Test 2: deductBalance with mockTx.returning resolving [] rejects with Error('INSUFFICIENT_BALANCE') and mockTx.insert is never called (ledger integrity on failure).
    - Test 3: deductBalance passed a mock CLIENT (an object exposing a transaction function) runs the body inside that client's transaction — D-02 wallet-drives-transaction contract.
    - Test 4: creditBalance(tx, 1, 50n, { reason: 'bet_payout' }) resolves to the returned balance and writes a ledger row with type 'credit'; its where() call for the balance UPDATE does not contain a balance comparison.
  </behavior>
  <read_first>src/services/farming/__tests__/subscriptionService.test.ts (vi.mock db/client.js pattern lines 11-15, mockTx chain lines 56-71, empty-returning insufficient case lines 84-101), src/services/wallet.ts (the service under test — created in task 1), src/services/__tests__/breakthrough.test.ts (top-level __tests__ placement convention)</read_first>
  <action>
    Create src/services/__tests__/wallet.test.ts following the subscriptionService.test.ts vi.mock pattern: mock ../../db/client.js so db.transaction is a vi.fn(); build a mockTx with the full chainable surface (update/set/where/returning/insert/values) so the wallet's deduct body resolves; vi.mocked(db.transaction).mockImplementation to run the callback with mockTx. Cover the four behavior cases above, plus one boundary case: deduct succeeds when the returned balance exactly equals the amount (returning [{ balance: 0n }] after a 1:1 deduct resolves 0n — balance 0 is legal per the DB check). Assert the ledger insert payload fields (type, amount, balanceAfter, reason, metadata) — this is the SC1 reconcilability proof. Place the file at src/services/__tests__/wallet.test.ts (the top-level services __tests__ dir already exists with breakthrough.test.ts/encryption.test.ts). Run the suite; the four tests must be green.
  </action>
  <verify>
    <automated>npx vitest run src/services/__tests__/wallet.test.ts</automated>
  </verify>
  <done>wallet.test.ts passes: success deduct + ledger write, insufficient deduct (rejects, no ledger row), explicit-tx passthrough, credit without guard, exact-balance boundary.</done>
  <acceptance_criteria>
    - CLI: `npx vitest run src/services/__tests__/wallet.test.ts` exits 0 with all cases passing.
    - Source assertion: src/services/__tests__/wallet.test.ts uses vi.mock on the db client module and asserts on the ledger insert payload (type, amount, balanceAfter, reason).
    - Source assertion: the insufficient-balance case asserts the ledger insert is NOT called when the UPDATE returns zero rows.
  </acceptance_criteria>
</task>

</tasks>
<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wallet service -> users.balance | the WHERE guard is the integrity boundary against double-spend; the wallet is the ONLY code path that mutates balance (D-03) |
| wallet service -> wallet_transactions | ledger rows must be written in the same DB transaction as the balance change (D-01) — a committed mutation without a ledger row breaks auditability |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-05 | Tampering | src/services/wallet.ts deductBalance | high | mitigate | Atomic WHERE guard (balance >= amount) + returning-empty check throws Error('INSUFFICIENT_BALANCE') and rolls back the whole transaction; DB-level balance_non_negative check (users.ts:15) is the backstop; wallet is the single write path (D-03) so no concurrent direct write can bypass the guard. Unit-tested (task 3, insufficient case). |
| T-08-06 | Repudiation | wallet_transactions ledger | medium | mitigate | Every mutation writes one ledger row (userId, type, amount, balance_after, reason, metadata, created_at) in the same transaction (D-01); reason is a first-class varchar(50) column — every balance change is attributable for Phase 12 audit (TQC-19) and future /profile history (D-04). |
| T-08-08 | Information Disclosure | wallet_transactions.metadata | low | accept | metadata holds only internal ids/amounts (betId, matchId, feePerRoll); no PII beyond userId (already public on users rows); /profile history UI is deferred (D-04). Accept — no player-visible surface in Phase 8. |
| T-08-09 | Tampering | refactored call sites | high | mitigate | D-03 invariant enforced structurally: all 7 write sites route through the wallet; the verification grep gate proves zero remaining inline balance UPDATE outside wallet.ts; future direct writes are caught by code review + the same grep gate in later phases. |
| T-08-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages in this plan (RESEARCH Package Legitimacy Audit: all deps already installed/verified — drizzle-orm, pg, tsx, vitest). No registry installs occur. |
</threat_model>

<verification>
- Run the plan gates in order after each task: npm run typecheck; npm run lint; npx vitest run src/services/__tests__/wallet.test.ts (task 3) plus the existing subscriptionService + predictionService suites (task 2 regression).
- Grep gate: `rg -n '\$\{users\.balance\}' src/commands src/services --glob '!**/__tests__/**'` returns matches only in src/services/wallet.ts — the D-03 no-direct-write invariant.
- DB-backed gates (dev-DB provisioning, migration 0014, seed idempotency, deploy.sh) live in plan 08-04 — the migration task's verify there confirms the 9 Phase 8 tables (incl. wallet_transactions) exist post-migrate, which is the full-boot smoke for SC1/SC2.
</verification>

<success_criteria>
- wallet.deductBalance/creditBalance are the single choke point: all 7 existing write sites refactored, same error types and transaction boundaries preserved, no inline balance UPDATE remains outside wallet.ts (grep-proven).
- wallet_transactions ledger accumulates one row per mutation with balance_after in the same transaction (SC1 reconcilability foundation; history UI deferred per D-04).
- Wallet unit tests + existing farming/football suites + lint + typecheck all pass.
</success_criteria>

<output>
Create `.planning/phases/08-foundation-economy-budget-content-infrastructure/08-02-SUMMARY.md` when done
</output>
