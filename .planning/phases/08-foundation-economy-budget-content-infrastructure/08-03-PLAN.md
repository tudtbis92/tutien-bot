---
phase: 08-foundation-economy-budget-content-infrastructure
plan: 3
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/economy-budget.md
autonomous: true
requirements: [TQC-05]
estimate:
  tokens: 10000
  raw_tokens: 10000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "docs/economy-budget.md exists (docs/ directory created) as a standalone ADR-style artifact (D-17) — not embedded in planning docs."
    - "The doc records the current sink/source model with every number verified against the codebase and cited to its source file:line (D-20): DAILY_CAP 10_000 (src/constants/game.ts:14), gather fees 200n -> 400_000n across 12 tiers (src/constants/gatherFees.ts:17-30), farming prices 10000n/35000n/50000n + upgrade daysLeft*1000 (src/services/farming/subscriptionService.ts:19-32, 50-56), football MIN_BET 100n / MAX_BET 1_000_000n (src/constants/footballConfig.ts:8-11), new-user starting balance 0 (src/commands/game/start.ts:75-79)."
    - "The doc labels the VWAP bands 1.2x/0.7x/2.5x and the 10% seller fee (min 1, all burn) as 'planned (MKT-02/03/04/07), not live' — the marketplace is stubbed (src/jobs/vwapRecalc.ts TODO), so Phase 12 must not audit against nonexistent numbers."
    - "The doc states the expected Linh thạch/hour of the optimal sanguo loop and shows it is below the tu vi cap: DAILY_CAP 10_000 is a tu vi (progression) cap, a different resource pool from linh thạch; the sub-game's expected net linh thạch/hour is <= 0 under the net-sink/neutral constraint (D-19), trivially below any positive cap (SC5 design-sanity check)."
    - "The doc records the convertibility matrix: tu vi never converts to linh thạch; hồn ngọc (Phase 11) is account-bound and never convertible to linh thạch; duplicate heroes convert to hồn ngọc (Phase 11); linh thạch sinks are gather, farming subscriptions, football wagers, and future travel/shop/evolution; the ONLY faucet is the Phase 10 free starter hero (TQC-12); no sanguo item is marketable without a reviewed conversion spec (Phase 12 TQC-20)."
    - "The doc records the net-sink/neutral HARD constraint (D-19): total linh thạch outflow (travel, items, evolution) >= total inflow (boss drops, if any) for the sub-game; no net-source in v1; Phase 11 must comply."
    - "The doc ends with a '## Design Gate Sign-off' section recording the decision + date and stating the gate passes — this gates content authoring in Phases 9-11 (TQC-06/07/08/09, TQC-12/13, TQC-14/15/16/17 must comply with D-19)."
  artifacts:
    - path: "docs/economy-budget.md"
      provides: "Design-gate ADR doc: sink/source model, expected linh thạch/hour below tu vi caps, convertibility matrix, net-sink/neutral constraint (TQC-05/D-17/D-18/D-19/D-20)"
      contains: "Design Gate Sign-off"
  key_links:
    - from: "docs/economy-budget.md"
      to: "src/constants/game.ts"
      via: "DAILY_CAP 10_000 cited as the tu-vi-cap reference for the linh thạch/hour sanity bound (game.ts:14)"
      pattern: "DAILY_CAP"
    - from: "docs/economy-budget.md"
      to: ".planning/REQUIREMENTS.md"
      via: "VWAP bands + market fee cited as spec-only (MKT-02/03/04/07) and labeled planned, not live"
      pattern: "MKT-07"
    - from: "docs/economy-budget.md"
      to: "src/services/farming/subscriptionService.ts"
      via: "farming sink prices + upgrade fee cited as live sinks (subscriptionService.ts:19-32, 50-56)"
      pattern: "farming_subscription"
  prohibitions:
    - statement: "No sanguo content (Phase 9-11) is authored until the design gate is signed off in this document (D-18) — the sign-off section gates all downstream content work."
      status: unverified
      flagged: true
---

<objective>
Close the economy design gate for Milestone v3 (TQC-05, D-17/D-18/D-19/D-20): author the standalone ADR-style economy budget document `docs/economy-budget.md` with concrete, code-verified numbers, and record the design-gate sign-off that blocks any content authoring in Phases 9-11 until the economic guardrails (net-sink/neutral, convertibility matrix, expected linh thạch/hour below tu vi caps) are locked. This satisfies SC5: the economy budget document is approved and the design gate passes before content authoring.

Purpose: TQC-05. The document frames the sub-game economy against the verified current system — whose only live source today is football payouts (matchLifecycleService), whose live sinks are gather (99.8%+ net-loss EV), farming subscriptions, and football wagers — and fixes the constraints every Phase 9-11 money flow must satisfy. It is consumed by Phase 12 monitoring/audit (TQC-19).

Output: `docs/economy-budget.md` (single design artifact; `docs/` directory does not exist yet — the task creates it).

This is a doc-only plan (wave 1, parallel with 08-01/08-02) — no code, no DB, no installs. Every number is already verified in RESEARCH.md TQC-05; the task transcribes them with citations, it does not invent new figures.

## Artifacts this phase produces (plan 08-03)

| Symbol | Kind | Location |
|--------|------|----------|
| `docs/economy-budget.md` | ADR-style design doc (sink/source model, expected linh thạch/hour, convertibility matrix, net-sink/neutral, design-gate sign-off) | docs/ |
</objective>
<execution_context>
@C:/Users/901107/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/901107/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-foundation-economy-budget-content-infrastructure/08-CONTEXT.md
@.planning/phases/08-foundation-economy-budget-content-infrastructure/08-RESEARCH.md
@.planning/phases/08-foundation-economy-budget-content-infrastructure/08-PATTERNS.md
@src/constants/game.ts
@src/constants/gatherFees.ts
@src/constants/footballConfig.ts
@src/services/farming/subscriptionService.ts
@src/commands/game/start.ts
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author docs/economy-budget.md — ADR-style economy design gate document (TQC-05, D-17/D-18/D-20)</name>
  <files>docs/economy-budget.md</files>
  <read_first>08-RESEARCH.md TQC-05 section (verified numbers table + economy-model observations), src/constants/game.ts (DAILY_CAP 10_000 line 14, tu vi rates lines 11-15, streak bonuses 50-56), src/constants/gatherFees.ts (12 fee tiers lines 17-30, EV invariant comment line 10), src/services/farming/subscriptionService.ts (prices 50-56, upgrade fee 19-32), src/constants/footballConfig.ts (MIN_BET/MAX_BET lines 8-11), src/commands/game/start.ts (new-user insert 74-80 — starting balance 0), .planning/REQUIREMENTS.md (MKT-02/03/04/07 spec text), .planning/STATE.md (milestone decisions table — hồn ngọc account-bound, boss drops items never money)</read_first>
  <action>
    Create the docs/ directory (it does not exist) and author docs/economy-budget.md as a standalone ADR-style document (D-17 — Status: Accepted, Context, Decision, Consequences sections). The document is the Milestone v3 economic design gate (D-18); every number must be the verified value from RESEARCH.md TQC-05 with its file:line citation — transcribe, do not invent.
    Content requirements, in order:
    (1) Header: title "Economy Budget — Tam Quốc Collection (Milestone v3)", Status: Accepted, date, and a one-line summary that this is the design-gate document for the sanguo sub-game economy.
    (2) Context — the current live economy (verified): the ONLY live source today is football winnings/refunds (matchLifecycleService void/push/payout credits); live sinks are gather (gacha, 99.8%+ net-loss EV at all fee tiers), farming subscriptions, and football wagers (bookmaker margin makes them ~neutral in expectation but variance is a player-visible source); there is no marketplace yet (Phase 3 PAUSED, src/jobs/vwapRecalc.ts is a TODO stub); new users start at 0 linh thạch (start.ts:75-79) — no faucet.
    (3) Sink/Source Model — a table of sinks and sources with the verified numbers: gather fees 200n -> 400_000n across the 12 realm tiers (gatherFees.ts:17-30, cite the file); farming subscription prices 7D basic 10000n, 30D basic 35000n, 30D VIP 50000n (subscriptionService.ts:50-56) and upgrade fee BigInt(daysLeft * 1000) (subscriptionService.ts:19-32); football wager bounds MIN_BET 100n / MAX_BET 1_000_000n (footballConfig.ts:8-11); daily tu vi cap DAILY_CAP 10_000 (game.ts:14) noted as a PROGRESSION cap, not currency. Label the marketplace numbers as "planned (MKT-02/03/04/07), not live": instant-buy 1.2x, instant-sell 0.7x, limit-sell ceiling 2.5x market price, and the 10% seller fee (min 1 linh thạch, all burned) — Phase 12 audit (TQC-19) must not treat these as live.
    (4) Expected Linh thạch/hour of the Optimal Loop — state explicitly that the sanguo sub-game economy is denominated in linh thạch while DAILY_CAP 10_000 bounds the MAIN game's tu vi progression; they are different resource pools (game.ts:14). The design constraint (SC5 sanity check): the expected net linh thạch/hour of the optimal loop is <= 0 under the net-sink/neutral constraint (D-19), which is trivially below any positive cap; gross per-hour flow (travel costs, shop prices) must stay below the 10_000/day ceiling (~416/hour averaged) as a magnitude bound for Phase 9-11 content. Record the analysis method Phase 9-11 must use to recompute this when concrete travel/encounter numbers exist.
    (5) Convertibility Matrix — a table mapping resource conversions: tu vi never converts to linh thạch; linh thạch converts to sinks (gather, farming, football wagers, and future travel/shop/evolution); duplicate heroes -> hồn ngọc (Phase 11 TQC-14, tier-scaled, account-bound); hồn ngọc NEVER converts back to linh thạch (STATE.md milestone decision); the ONLY faucet in the milestone is the Phase 10 free starter hero (TQC-12); boss thường drops items only, never money (STATE.md); no sanguo item is marketable without a reviewed conversion spec (Phase 12 TQC-20 marketplace gating).
    (6) Net-Sink/Neutral Constraint (D-19) — a HARD constraint paragraph: total linh thạch outflow (travel, items, evolution) >= total inflow (boss drops, if any) for the sub-game; the free starter hero is the only faucet exception; no net-source in v1; Phase 11 must comply (its sinks close the loop).
    (7) Consequences — what this gates: Phase 9-11 content authoring (travel costs, encounter yields, shop prices, evolution fees) MUST satisfy the constraints above; Phase 12 monitoring/audit (TQC-19) consumes this document; any future rebalancing requires a new sign-off (D-18 one-way gate).
    Do NOT include implementation code, formulas beyond simple arithmetic, or values not listed above. Keep the hidden-mechanics philosophy (Phase 2): the document records design numbers; players see outcomes, not formulas (note this once in Context).
  </action>
  <verify>
    <automated>if (-not (Test-Path docs/economy-budget.md)) { exit 1 }; rg -n "DAILY_CAP|gatherFees|footballConfig|MKT-07|net-sink|convertib" docs/economy-budget.md</automated>
  </verify>
  <done>docs/economy-budget.md exists as an ADR-style doc with the verified sink/source model (each number cited), the expected linh thạch/hour bound below the tu vi cap, the convertibility matrix, and the D-19 net-sink/neutral constraint; marketplace numbers labeled planned, not live.</done>
  <acceptance_criteria>
    - File assertion: docs/economy-budget.md exists (docs/ directory created).
    - Source assertion: the doc contains the literal "DAILY_CAP" and "10_000" with the game.ts:14 citation.
    - Source assertion: the doc contains the literal "MKT-07" and labels the marketplace bands/fee as planned/not live.
    - Source assertion: the doc contains the phrase "net-sink" (or "net sink") and states outflow >= inflow as a hard constraint (D-19).
    - Source assertion: the doc contains the phrase "convertibility" and a matrix entry that hồn ngọc never converts back to linh thạch.
    - Source assertion: the doc cites start.ts (new users start at 0) and gatherFees.ts (fee range).
  </acceptance_criteria>
</task>
<task type="auto">
  <name>Task 2: Record the Design Gate Sign-off (D-18) — gate passed, gates Phases 9-11 content</name>
  <files>docs/economy-budget.md</files>
  <read_first>docs/economy-budget.md (the document authored in task 1 — append at the end), .planning/STATE.md (Milestone v3 Decisions / Design Gates table — 'Economy design-gate trước content (TQC-05)' entry to reference), .planning/ROADMAP.md Phase 9-11 requirement IDs (TQC-06/07/08/09, TQC-10/11/12/13, TQC-14/15/16/17)</read_first>
  <action>
    Append a '## Design Gate Sign-off' section to the END of docs/economy-budget.md recording that the design gate passes (D-18). The section must state: (1) the decision — the economy budget as documented above is approved: net-sink/neutral is a hard constraint (D-19), the expected net linh thạch/hour of the optimal loop is <= 0 and below the DAILY_CAP 10_000 tu-vi-cap sanity bound (SC5), and the convertibility matrix is accepted; (2) the date of the sign-off (the date this task runs — use the current date); (3) the explicit gating statement: this sign-off is a prerequisite for content authoring in Phases 9-11 — Phase 9 (TQC-06 travel costs, TQC-08 encounter yields), Phase 10 (TQC-12 starter faucet is the only faucet), and Phase 11 (TQC-14/15/16/17 evolution/shop sinks) must all comply with D-19; no content work in those phases may proceed on the assumption of a net source; (4) the reference that Phase 12 monitoring/audit (TQC-19) consumes this document. Do NOT modify any earlier section of the document — this is an append-only change. After appending, re-run the task 1 acceptance checks to confirm the document is still coherent (single file, single gate record).
  </action>
  <verify>
    <automated>rg -n "Design Gate Sign-off" docs/economy-budget.md; if ($?) { rg -n "Phase 9|TQC-12|TQC-19" docs/economy-budget.md }</automated>
  </verify>
  <done>The document ends with a '## Design Gate Sign-off' section stating the decision + date and that the gate passes, explicitly gating content authoring in Phases 9-11 (D-18).</done>
  <acceptance_criteria>
    - Source assertion: docs/economy-budget.md contains a '## Design Gate Sign-off' heading.
    - Source assertion: the sign-off section contains the date and states the decision (net-sink/neutral hard constraint accepted; expected linh thạch/hour below the tu vi cap bound).
    - Source assertion: the sign-off section references that it gates content authoring in Phases 9-11 (contains "Phase 9" and "Phase 11" or the TQC IDs of those phases).
    - Negative check: the sign-off section is appended after the Consequences section and no earlier section of the document was modified (document still contains all task 1 markers).
  </acceptance_criteria>
</task>

</tasks>
<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| design doc -> downstream phases | docs/economy-budget.md is the economic contract Phases 9-12 read; wrong or drifting numbers propagate into live game content and Phase 12 audit |
| design doc -> codebase | every number cited must match the current code at authoring time (D-20); a stale number becomes a false audit baseline |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-10 | Tampering | docs/economy-budget.md numbers | low | mitigate | Every figure is transcribed from RESEARCH.md TQC-05's verified table with a file:line citation; Phase 12 monitoring (TQC-19) re-verifies against code before auditing; the doc explicitly labels marketplace figures as planned, not live, so they cannot be misread as auditable sinks. |
| T-08-11 | Repudiation | Design Gate Sign-off | low | mitigate | The sign-off section records the decision and the date (D-18) and names the gated phases — the gate's passing is traceable in-repo, and any future rebalancing requires a new sign-off. |
| T-08-SC | Tampering | npm/pip/cargo installs | high | mitigate | Doc-only plan — zero installs; no package enters the dependency graph (RESEARCH Package Legitimacy Audit applies to plans with installs only). |
</threat_model>

<verification>
- File checks: docs/economy-budget.md exists after task 1; `rg` confirms the required markers (DAILY_CAP + game.ts:14, MKT-07 planned-not-live label, net-sink constraint, convertibility matrix, hồn ngọc never-to-linh-thạch, start.ts:75-79 citation).
- Gate record: `rg -n "Design Gate Sign-off" docs/economy-budget.md` matches exactly once (single, appended, unmodified earlier sections) after task 2.
- Cross-check the numbers against RESEARCH.md TQC-05's verified table — any discrepancy means the doc is wrong, not the research.
</verification>

<success_criteria>
- docs/economy-budget.md is an approved ADR-style artifact (D-17): sink/source model with verified citations (D-20), expected net linh thạch/hour <= 0 below the DAILY_CAP 10_000 tu-vi-cap bound (SC5), convertibility matrix, and the D-19 net-sink/neutral hard constraint.
- Marketplace figures are labeled planned (MKT-02/03/04/07), not live — Phase 12 cannot audit against nonexistent numbers.
- The '## Design Gate Sign-off' section records the decision + date (D-18) and gates content authoring in Phases 9-11 — SC5 "design gate passed before content authoring" is satisfied.
</success_criteria>

<output>
Create `.planning/phases/08-foundation-economy-budget-content-infrastructure/08-03-SUMMARY.md` when done
</output>
