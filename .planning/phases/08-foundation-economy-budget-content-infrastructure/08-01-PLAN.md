---
phase: 08-foundation-economy-budget-content-infrastructure
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/gen-sanguo-emojis.ts
  - src/assets/sanguoEmojis.ts
  - src/assets/index.ts
  - src/shard.ts
  - .env.example
  - .env
  - src/i18n/index.ts
  - scripts/check-i18n.ts
  - locales/vi/sanguo.json
  - locales/en/sanguo.json
  - locales/zh-cn/sanguo.json
  - src/db/schema/mapNodes.ts
  - src/db/schema/index.ts
  - src/commands/sanguo/map.ts
  - src/ui/embeds/buildSanguoMapEmbed.ts
  - src/ui/index.ts
  - eslint.config.mjs
  - package.json
  - src/assets/__tests__/sanguoEmojis.test.ts
  - src/ui/embeds/__tests__/buildSanguoMapEmbed.test.ts
  - src/commands/sanguo/__tests__/map.test.ts
autonomous: true
requirements: [TQC-02, TQC-03, TQC-04]
user_setup:
  - service: "Discord application ownership (D-16 contract)"
    why: "The bot's production Discord application must own the 1056-emoji set (applicationId 1381818375633899562). The startup hard-fail (D-14) enforces this; a wrong app means every sanguo emoji renders broken."
    env_vars:
      - name: CLIENT_ID
        source: "Discord Developer Portal -> Applications -> <app> -> General Information -> Application ID"
    dashboard_config:
      - task: "Confirm the production bot application ID equals 1381818375633899562 (the emoji-owning app). If it differs, emojis.json must be regenerated for the real app per D-16 BEFORE this plan runs."
        location: "Discord Developer Portal -> Applications"
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The generated registry file src/assets/sanguoEmojis.ts exists with 1056 SANSUO_EMOJIS entries and SANSUO_EMOJI_APPLICATION_ID = '1381818375633899562' as const, produced by scripts/gen-sanguo-emojis.ts from the sibling repo at build/dev time (D-13)."
    - "heroEmoji(heroId, tier, star) returns the renderable Discord markup '<:heroId_t<tier>[_star]:<emojiId>>' built from SANSUO_EMOJIS (name = registry key, id = registry value); a missing tier key falls back to the hero's t0 entry; a heroId with NO prefix in the registry throws Error('EMOJI_NOT_FOUND:<heroId>') — never an empty string, never a raw literal. Discord renders custom/app emoji ONLY via <:name:id> markup, so a bare ID would render as literal text and break SC3 (D-15, UI-SPEC emoji contract + Discord render requirement)."
    - "assertEmojiApplicationId('1381818375633899562', '1381818375633899562') === true and any mismatched pair === false; the pure function takes explicit args so vitest exercises it without env coupling (testSetup CLIENT_ID mock)."
    - "src/shard.ts calls assertEmojiApplicationId inside main() BEFORE client.login(); on mismatch it logs the mismatch via logger.error and calls process.exit(1) (D-14)."
    - "The sanguo namespace is registered in BOTH src/i18n/index.ts ns array (line 28, 7th entry) AND scripts/check-i18n.ts NAMESPACES (line 13) — the pre-existing 'football' gap is repaired in the same change (D-08, RESEARCH finding #4)."
    - "locales/vi/sanguo.json, locales/en/sanguo.json, locales/zh-cn/sanguo.json exist with identical key sets including cmd.map.description, map.title, map.current_position, map.zones, map.nodes, map.empty, map.empty_hint, map.error — npm run check-i18n exits 0."
    - "A missing locales/{lng}/sanguo.json file makes check-i18n exit non-zero with a 'Missing file' error (empty-edge: absent file is a hard failure, never a silent pass)."
    - "check-i18n key parity uses exact flattened-key string equality between vi (reference) and en/zh-cn — identical keys in all three locales pass; no case folding, no Unicode normalization, no trimming (encoding-edge: exact dotted-path string comparison)."
    - "JSON key order in locale files is irrelevant — check-i18n compares key sets, not order; re-running on unchanged files yields the same exit code (ordering + idempotency edges)."
    - "mapNodes schema (src/db/schema/mapNodes.ts) defines id, code (varchar unique natural key), name_vi/name_en/name_zh varchar columns, zone, node_order, and representative_hero_id (varchar, nullable — zone→hero marker filled by the Phase 8 placeholder seed so /sanguo map renders a heroEmoji() marker per zone, D-07 content-in-DB) — per-locale columns NOT JSONB (D-05); exported from src/db/schema/index.ts."
    - "The /sanguo map command registers as top-level name 'sanguo' with one 'map' subcommand (forward-compatible with /sanguo heroes in Phase 10, RESEARCH A4) and drops into src/commands/sanguo/map.ts for commandLoader autodiscovery."
    - "The map command calls interaction.deferReply() before DB work; not-registered users get common:errors.notRegistered via buildErrorEmbed; generic failure gets sanguo:map.error via buildErrorEmbed (DANGER) (loading/error UI considerations covered)."
    - "buildSanguoMapEmbed renders COLORS.SEASON (0x8B5CF6 from src/ui/theme.ts, never a hex literal), field names emoji-prefixed, values from DB per-locale columns (D-07, never i18n keys), embedFooter(shardId), setTimestamp() — populated branch shows sanguo:map.zones + sanguo:map.nodes lists with a heroEmoji() marker per entry; 0 nodes shows sanguo:map.empty + sanguo:map.empty_hint."
    - "Map scaffold stays within Discord limits: ≤25 fields, ≤1024 chars/value, ≤4096 total; 5–10 placeholder nodes fit in ~1–3 fields (overflow UI consideration covered)."
    - "A custom ESLint rule in eslint.config.mjs built with ESLintUtils.createRule from '@typescript-eslint/utils' (v8.66.0 installed and hoisted — the 'typescript-eslint' meta-package does NOT export createRule) reports any string literal matching emoji markup <a?:name:\d+> or a bare 17-20 digit ID outside src/assets/sanguoEmojis.ts, wired as a flat-config plugin+rule (plugins: { local: { 'no-emoji-id': rule } }; rules: { 'local/no-emoji-id': 'error' }) — npm run lint passes on all new src files (D-15 enforcement)."
    - ".env.example documents that CLIENT_ID must equal the emoji applicationId 1381818375633899562 (D-16); the local .env CLIENT_ID is set to 1381818375633899562 after user confirms the production app in user_setup."
    - "{ statement: empty map_nodes renders map.empty copy, verification: backstop }"
    - "{ statement: boot fails hard on appId mismatch, verification: backstop }"
  artifacts:
    - path: "scripts/gen-sanguo-emojis.ts"
      provides: "Build-time generator reading sibling repo emojis.json, emitting src/assets/sanguoEmojis.ts (D-13)"
      contains: "SANGUO_EMOJI_APPLICATION_ID"
    - path: "src/assets/sanguoEmojis.ts"
      provides: "Committed typed registry SANSUO_EMOJIS + heroEmoji() + assertEmojiApplicationId() + SanguoTier type"
      contains: "SANSUO_EMOJIS"
    - path: "src/i18n/index.ts"
      provides: "sanguo added to ns array (D-08)"
      contains: "sanguo"
    - path: "scripts/check-i18n.ts"
      provides: "sanguo + football in NAMESPACES (D-08 + gap repair)"
      contains: "'football'"
    - path: "src/commands/sanguo/map.ts"
      provides: "/sanguo map command (SC3)"
      contains: "addSubcommand"
    - path: "src/ui/embeds/buildSanguoMapEmbed.ts"
      provides: "Map embed builder (SC3, UI-SPEC contract)"
      contains: "COLORS.SEASON"
    - path: "src/db/schema/mapNodes.ts"
      provides: "map_nodes table schema (TQC-02 slice, D-10)"
      contains: "code"
  key_links:
    - from: "scripts/gen-sanguo-emojis.ts"
      to: "src/assets/sanguoEmojis.ts"
      via: "generator writes the committed registry; runtime never reads the sibling repo (D-13)"
      pattern: "writeFileSync.*sanguoEmojis"
    - from: "src/shard.ts"
      to: "src/assets/sanguoEmojis.ts"
      via: "assertEmojiApplicationId(SANSUO_EMOJI_APPLICATION_ID, config.CLIENT_ID) before client.login() (D-14)"
      pattern: "assertEmojiApplicationId"
    - from: "src/ui/embeds/buildSanguoMapEmbed.ts"
      to: "src/assets/sanguoEmojis.ts"
      via: "heroEmoji() is the sole render point for sanguo emoji (D-15)"
      pattern: "heroEmoji"
    - from: "src/commands/sanguo/map.ts"
      to: "src/db/schema/index.ts"
      via: "queries mapNodes; per-locale names come from DB columns, labels via t('sanguo:...') (D-07)"
      pattern: "from\\(mapNodes\\)"
  prohibitions:
    - statement: "Runtime NEVER reads the sibling asset repo — the committed src/assets/sanguoEmojis.ts is the only runtime source of emoji data (TQC-04/D-13)."
      status: unverified
      flagged: true
    - statement: "Content names (hero/zone/item) NEVER live in the i18n sanguo namespace — DB per-locale columns only (D-07); no lore/title exception."
      status: unverified
      flagged: true
---

<objective>
Build the user-facing scaffold of the Tam Quốc Collection milestone: the generated emoji registry (`sanguoEmojis.ts` + `heroEmoji()` + startup appId check), the `sanguo` i18n namespace (VI/EN/ZH-CN), the `map_nodes` schema slice, and the read-only `/sanguo map` command + embed. This is the phase's tracer plan — one thin path from generator → registry → boot check → command → embed, proving the milestone's content/UI/emoji architecture end-to-end before expansion.

Purpose: SC3 (user invokes /sanguo map, sees hero emoji from heroEmoji(), startup applicationId === CLIENT_ID check) and SC4 (sanguo UI strings localized VI/EN/ZH-CN, zero hardcoded strings, content names from DB per-locale columns) plus the TQC-04 generator contract (D-13) and TQC-03 namespace registration (D-08).

Output: generated registry file, i18n namespace in both registration points + 3 locale files, mapNodes schema, /sanguo map command + embed builder, startup check, ESLint emoji rule, .env CLIENT_ID contract.

**Shared-file note (merge-safety):** This plan runs in wave 1 alongside plan 08-02 (which also edits `src/db/schema/index.ts`), and wave-2 plan 08-04 (which also edits `package.json` scripts and `.env.example`). `src/db/schema/index.ts`: all edits are append-only under the '// Phase 8 schemas' comment — whoever lands first creates the comment block, the other appends under it without removing or duplicating lines (cross-acceptance finalized by plan 08-04's migration precondition). `package.json`: this plan appends the `gen:emojis` script only; 08-04 appends `seed:sanguo` and must not clobber `gen:emojis`. `.env.example`: this plan edits only the CLIENT_ID comment block; 08-04 edits only the DATABASE_URL/DB-URL block — each plan touches its own section, never rewrites the whole file.

## Artifacts this phase produces (plan 08-01)

| Symbol | Kind | Location |
|--------|------|----------|
| `scripts/gen-sanguo-emojis.ts` | CLI script | scripts/ |
| `SANSUO_EMOJIS` (1056-entry const map) | generated module export | src/assets/sanguoEmojis.ts |
| `SANSUO_EMOJI_APPLICATION_ID` (`'1381818375633899562'`) | generated const | src/assets/sanguoEmojis.ts |
| `SanguoEmojiKey` / `SanguoTier` types | generated type exports | src/assets/sanguoEmojis.ts |
| `heroEmoji(heroId, tier = 0, star = false)` | helper fn (sole render point) | src/assets/sanguoEmojis.ts |
| `assertEmojiApplicationId(registryAppId, clientId)` | pure fn (startup check) | src/assets/sanguoEmojis.ts |
| `sanguo` i18n namespace (8 keys) | locale JSON ×3 | locales/{vi,en,zh-cn}/sanguo.json |
| `mapNodes` table + `MapNode`/`NewMapNode` types | Drizzle schema | src/db/schema/mapNodes.ts |
| `/sanguo map` command (top-level `sanguo`, subcommand `map`) | Discord slash command | src/commands/sanguo/map.ts |
| `buildSanguoMapEmbed(data, t)` | embed builder | src/ui/embeds/buildSanguoMapEmbed.ts |
| `SanguoMapEmbedData` interface | type export | src/ui/embeds/buildSanguoMapEmbed.ts |
| custom ESLint rule (emoji-ID block) | config rule | eslint.config.mjs |
| `gen:emojis` npm script | package.json script | package.json |
</objective>

<execution_context>
@C:/Users/901107/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/901107/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-foundation-economy-budget-content-infrastructure/08-CONTEXT.md
@.planning/phases/08-foundation-economy-budget-content-infrastructure/08-RESEARCH.md
@.planning/phases/08-foundation-economy-budget-content-infrastructure/08-PATTERNS.md
@.planning/phases/08-foundation-economy-budget-content-infrastructure/08-UI-SPEC.md
@src/assets/emojis.ts
@src/shard.ts
@src/i18n/index.ts
@scripts/check-i18n.ts
@src/utils/commandContext.ts
@src/ui/theme.ts
@src/ui/embeds/buildProfileEmbed.ts
@src/ui/embeds/buildErrorEmbed.ts
@src/utils/commandLoader.ts
@eslint.config.mjs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Emoji registry generator + heroEmoji + startup appId hard-fail (TQC-04 spine)</name>
  <reversibility rating="one-way">D-13/D-16: the committed generated file + no-sibling-runtime-read is the deployment model every later phase builds on; a wrong applicationId breaks all emoji rendering milestone-wide.</reversibility>
  <precondition>User confirmed in user_setup that the production Discord application ID is 1381818375633899562 (or emojis.json was regenerated for the real app). Sibling repo exists at E:\Saeth\sanguo_assets\assets\emojis.json.</precondition>
  <files>scripts/gen-sanguo-emojis.ts, src/assets/sanguoEmojis.ts, src/assets/index.ts, src/shard.ts, .env.example, .env, src/assets/__tests__/sanguoEmojis.test.ts, package.json</files>
  <behavior>
    - Test 1: heroEmoji('abt', 0) returns the renderable markup '<:abt_t0:1536202064185524378>' (name = registry key 'abt_t0', id = registry value) — NOT a bare ID, because Discord renders custom/app emoji only via <:name:id> markup and a bare ID renders as literal text (SC3).
    - Test 2: heroEmoji('abt', 3, true) returns '<:abt_t3_star:<id>>'; a missing tier variant (e.g. heroEmoji('abt', 5 as SanguoTier)) falls back to the hero's t0 entry '<:abt_t0:<id>>'; heroEmoji('no_such_hero') — a heroId with NO prefix in the registry — throws Error('EMOJI_NOT_FOUND:no_such_hero'); heroEmoji never returns '' or a raw literal.
    - Test 3: assertEmojiApplicationId('1381818375633899562', '1381818375633899562') === true; assertEmojiApplicationId('a', 'b') === false; assertEmojiApplicationId('1381818375633899562', '1234567890') === false (testSetup mock ID).
  </behavior>
  <read_first>src/assets/emojis.ts (typed-registry pattern to mirror), src/shard.ts (main() sequence lines 26-67, insert check before client.login() line 66), src/config.ts (CLIENT_ID zod source), scripts/check-i18n.ts (fs-read conventions for the generator), 08-RESEARCH.md Pattern 4 (emitted shape), E:\Saeth\sanguo_assets\assets\emojis.json (source shape: { applicationId, emojis: { '<hero_id>_t0': '<emojiId>', ... } })</read_first>
  <action>
    Create scripts/gen-sanguo-emojis.ts: a tsx-runnable script that reads the sibling repo JSON (source path resolved from env var SANGUO_EMOJIS_SOURCE with default E:\Saeth\sanguo_assets\assets\emojis.json), reads top-level applicationId and the emojis map, validates 1056 keys with a hero-prefix pattern, and writes src/assets/sanguoEmojis.ts with a header comment "Generated by scripts/gen-sanguo-emojis.ts — DO NOT EDIT BY HAND.", exports SANSUO_EMOJI_APPLICATION_ID as const, SANSUO_EMOJIS as const (all 1056 entries keyed '<hero_id>_t<tier>[_star]'), types SanguoEmojiKey = keyof typeof SANSUO_EMOJIS and SanguoTier = 0|1|2|3, plus two functions: heroEmoji(heroId, tier = 0, star = false) that builds the key '<heroId>_t<tier>[_star]', looks it up, falls back to the '<heroId>_t0' key of the same hero, returns the renderable markup '<:key:emojiId>' (name = registry key, id = registry value — Discord renders custom/app emoji only via markup, never a bare ID), and THROWS Error('EMOJI_NOT_FOUND:<heroId>') when the heroId has no prefix in the registry at all (unknown hero — never ''); and assertEmojiApplicationId(registryAppId, clientId) returning registryAppId === clientId. Emit a deterministic file (no timestamp, keys in stable sorted order) so the idempotency gate (git diff --exit-code on the committed registry) passes. The script must exit non-zero with a clear error if the source file is missing, has a non-18-digit applicationId, or contains fewer than 132 distinct hero prefixes (the count of a hero with no emoji entry is a hard error, not a warning).
    Add the gen:emojis script to package.json scripts (e.g. "gen:emojis": "tsx scripts/gen-sanguo-emojis.ts").
    Run the generator once so src/assets/sanguoEmojis.ts is committed as part of this task (runtime never reads the sibling repo — only this committed file).
    Append `export * from './sanguoEmojis.js';` to src/assets/index.ts barrel.
    In src/shard.ts inside main(), BEFORE client.login(config.DISCORD_TOKEN), import SANSUO_EMOJI_APPLICATION_ID and assertEmojiApplicationId from './assets/sanguoEmojis.js'; on mismatch log via logger.error a message naming both IDs and the D-14 refusal, then process.exit(1). Match the existing fatal-exit style at shard.ts:69-72.
    Update .env.example: CLIENT_ID comment must state the value must equal the emoji registry applicationId 1381818375633899562 (D-16). Update the local .env CLIENT_ID from the placeholder 'your_application_client_id' to '1381818375633899562' (the value the user confirmed in user_setup).
    Write src/assets/__tests__/sanguoEmojis.test.ts covering the three behavior cases above (explicit args; no env reads). Follow the existing vitest conventions (import from '../../../assets/sanguoEmojis.js'; testSetup.ts already mocks CLIENT_ID).
  </action>
  <verify>
    <automated>npx vitest run src/assets/__tests__/sanguoEmojis.test.ts; if ($?) { npx tsx scripts/gen-sanguo-emojis.ts }; if ($?) { git add -N src/assets/sanguoEmojis.ts }; if ($?) { git diff --exit-code -- src/assets/sanguoEmojis.ts }; if ($?) { git diff --stat src/assets/sanguoEmojis.ts }</automated>
  </verify>
  <done>The generator runs clean, the registry is committed with 1056 entries, heroEmoji fallback + assertEmojiApplicationId tests pass, shard.ts hard-fails on mismatch, .env.example + .env carry the D-16 CLIENT_ID value.</done>
  <acceptance_criteria>
    - Source assertion: src/assets/sanguoEmojis.ts contains SANSUO_EMOJI_APPLICATION_ID = '1381818375633899562' and more than 1000 entries in SANSUO_EMOJIS.
    - Test command: `npx vitest run src/assets/__tests__/sanguoEmojis.test.ts` passes (3 cases).
    - Test behavior: the sanguoEmojis suite asserts heroEmoji('abt', 0) === '<:abt_t0:1536202064185524378>' (renderable markup — SC3) and that heroEmoji('no_such_hero') throws Error('EMOJI_NOT_FOUND:no_such_hero').
    - Idempotency: running gen-sanguo-emojis.ts a second time produces no diff in src/assets/sanguoEmojis.ts — `git diff --exit-code -- src/assets/sanguoEmojis.ts` (after `git add -N` marks intent-to-add so the new file's content is diffed) exits 0, and `git diff --stat` is empty.
    - Source assertion: src/shard.ts contains a call to assertEmojiApplicationId before the client.login call.
    - Source assertion: src/assets/index.ts contains an export line referencing sanguoEmojis.
    - Source assertion: .env.example CLIENT_ID comment references 1381818375633899562.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: sanguo i18n namespace + football lint-gap repair (TQC-03, D-08)</name>
  <files>src/i18n/index.ts, scripts/check-i18n.ts, locales/vi/sanguo.json, locales/en/sanguo.json, locales/zh-cn/sanguo.json</files>
  <read_first>src/i18n/index.ts (ns array line 28, lowerCaseLng 'zh-cn' line 27), scripts/check-i18n.ts (NAMESPACES line 13, missing-key logic lines 31-57), locales/vi/football.json (JSON structure analog), 08-UI-SPEC.md Copywriting Contract section (exact keys), 08-RESEARCH.md TQC-03 section</read_first>
  <action>
    Append 'sanguo' to the ns array in src/i18n/index.ts line 28 (7th namespace; keep fallbackLng 'vi', supportedLngs, preload, lowerCaseLng unchanged).
    Update scripts/check-i18n.ts NAMESPACES (line 13) to include 'sanguo' AND the previously-missing 'football' (RESEARCH finding #4 — one-line repair so the lint covers all 7 namespaces).
    Create locales/vi/sanguo.json, locales/en/sanguo.json, locales/zh-cn/sanguo.json with identical key structure (VI is reference; EN + ZH-CN translations are translator's discretion within meaning but all keys MUST exist):
    cmd.map.description ("Xem bản đồ Tam Quốc" VI), map.title ("🗺️ Bản đồ Tam Quốc" VI), map.current_position, map.zones, map.nodes, map.empty ("Bản đồ chưa được vẽ" VI), map.empty_hint, map.error ("Có lỗi khi tải bản đồ. Hãy thử lại." VI) — per UI-SPEC Copywriting Contract. Do NOT duplicate common:errors.notRegistered (reused from common ns).
    Files are flat nested JSON exactly like locales/vi/football.json; directory name is zh-cn (lowercase, matches lowerCaseLng: true).
    Run npm run check-i18n and confirm exit code 0 (all namespaces incl. football + sanguo in sync across vi/en/zh-cn).
  </action>
  <verify>
    <automated>npm run check-i18n</automated>
  </verify>
  <done>sanguo registered in BOTH i18n/index.ts ns and check-i18n.ts NAMESPACES; football gap repaired; 3 locale files with identical key sets; check-i18n exits 0.</done>
  <acceptance_criteria>
    - Source assertion: src/i18n/index.ts ns array contains 'sanguo' as the 7th entry.
    - Source assertion: scripts/check-i18n.ts NAMESPACES contains both 'sanguo' and 'football'.
    - CLI output: `npm run check-i18n` prints '✅ All locale files are in sync.' and exits 0.
    - Negative check: none of the 3 sanguo.json files contains a key named notRegistered (reused from common ns — no duplication).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: map_nodes schema + /sanguo map command + embed + ESLint emoji rule (SC3, D-05/D-07/D-15)</name>
  <files>src/db/schema/mapNodes.ts, src/db/schema/index.ts, src/commands/sanguo/map.ts, src/ui/embeds/buildSanguoMapEmbed.ts, src/ui/index.ts, eslint.config.mjs, src/ui/embeds/__tests__/buildSanguoMapEmbed.test.ts, src/commands/sanguo/__tests__/map.test.ts</files>
  <behavior>
    - Test 1: buildSanguoMapEmbed({ currentZoneName, zones: [], nodes: ['Lạc Dương'], shardId: 0 }, t) produces an EmbedBuilder whose color equals COLORS.SEASON, title equals t('sanguo:map.title'), and whose zones field value equals t('sanguo:map.empty') when zones is empty.
    - Test 2: buildSanguoMapEmbed with a zone list renders each zone as `${heroEmoji(z.heroId)} ${z.label}` joined by newline; the field value contains <:name:id> markup produced by heroEmoji() and no raw 17-20 digit emoji ID literal appears in any field value (SC3 renderability).
    - Test 3: the map command handler, given a mocked db returning zero mapNodes rows, replies via editReply with an embed whose nodes field shows sanguo:map.empty_hint (empty-branch covered).
  </behavior>
  <read_first>src/db/schema/items.ts (content-table analog), src/db/schema/index.ts (merge pattern — add under a '// Phase 8 schemas' comment), src/commands/predictions/predictions.ts (top-level command + addSubcommand skeleton), src/utils/commandContext.ts (fetchCommandContext returns { locale, t, char, user, shardId }), src/ui/embeds/buildProfileEmbed.ts (data + t signature, COLORS, embedFooter, \u200b separator), src/ui/theme.ts (COLORS.SEASON 0x8B5CF6 line 40), src/ui/embeds/buildErrorEmbed.ts, eslint.config.mjs (rules block + i18next rule structure), 08-UI-SPEC.md (Phase-Specific Contract: Interaction + Emoji Rendering), 08-RESEARCH.md TQC-04 section (map command options, embed skeleton)</read_first>
  <action>
    Create src/db/schema/mapNodes.ts: pgTable('map_nodes') with id serial pk, code varchar(50) notNull unique (natural key for D-11 upsert), nameVi/nameEn varchar(100) notNull, nameZh varchar(100) nullable (filled by Tavily re-run per D-06/D-11), zone varchar(50) notNull, nodeOrder smallint notNull, representativeHeroId varchar(50) nullable (zone→hero marker for the scaffold, D-07 content-in-DB; filled by the 08-04 seed so a null value renders a label-only zone entry), following the items.ts content-table pattern. Export MapNode/NewMapNode infer types. Add `export * from './mapNodes.js';` to src/db/schema/index.ts under a '// Phase 8 schemas' comment.
    Create src/commands/sanguo/map.ts: export data = new SlashCommandBuilder().setName('sanguo').setDescriptionLocalizations(...).addSubcommand(map => map.setName('map').setDescription('Xem bản đồ Tam Quốc').setDescriptionLocalizations({ 'en-US': ..., 'zh-CN': ... })) with an eslint-disable i18next/no-literal-string around the static Discord API strings (predictions.ts pattern); execute(interaction) does deferReply, fetchCommandContext, not-registered guard replying buildErrorEmbed(t('common:errors.notRegistered'), shardId), then queries mapNodes ordered by nodeOrder, builds SanguoMapEmbedData (currentZoneName from the first node's per-locale name column selected by locale — map nameVi/nameEn/nameZh to the resolved locale, defaulting vi; zones derived from distinct zone values using each zone's representativeHeroId from DB (D-07 — never hardcoded in the command); nodes from per-locale names), and replies non-ephemeral with the embed. No message components in Phase 8 (UI-SPEC). Generic failures reply buildErrorEmbed(t('sanguo:map.error'), shardId).
    Create src/ui/embeds/buildSanguoMapEmbed.ts: export interface SanguoMapEmbedData { currentZoneName: string; zones: { label: string; heroId: string }[]; nodes: string[]; shardId?: number } and export function buildSanguoMapEmbed(data, t) returning an EmbedBuilder with .setColor(COLORS.SEASON), .setTitle(t('sanguo:map.title')), fields current_position (data.currentZoneName, inline), zones (data.zones.map(z => `${heroEmoji(z.heroId)} ${z.label}`).join('\n') || t('sanguo:map.empty')), nodes (data.nodes.join('\n') || t('sanguo:map.empty_hint')), .setFooter(embedFooter(data.shardId)), .setTimestamp(). Emoji via heroEmoji() only; colors from COLORS only. Add the export to src/ui/index.ts barrel.
    Add the ESLint emoji-ID rule to eslint.config.mjs using ESLintUtils.createRule imported from '@typescript-eslint/utils' (v8.66.0 is installed and hoisted — the 'typescript-eslint' meta-package does NOT export createRule; importing the TS util from a .mjs config is fine because createRule returns a plain rule object): report string literals that match an emoji markup pattern (<a?:name:\d+>) or a bare 17-20 digit numeric literal, skipping files whose path equals the generated registry src/assets/sanguoEmojis.ts (check context.filename). Wire it via flat-config plugin+rule naming — plugins: { local: { 'no-emoji-id': rule } } and rules: { 'local/no-emoji-id': 'error' } in a block covering src/**/*.ts. Keep the rule self-contained in eslint.config.mjs (no new package).
    Write the two test files (embed builder + command handler with mocked db per the vitest vi.mock pattern from subscriptionService.test.ts) and run the full gate: vitest, npm run lint, npm run typecheck.
  </action>
  <verify>
    <automated>npx vitest run src/ui/embeds/__tests__/buildSanguoMapEmbed.test.ts src/commands/sanguo/__tests__/map.test.ts; if ($?) { npm run lint }; if ($?) { npm run typecheck }</automated>
  </verify>
  <done>/sanguo map command + embed registered and tested, mapNodes schema merged, ESLint emoji rule blocks direct emoji-ID embedding, lint + typecheck pass.</done>
  <acceptance_criteria>
    - Test command: both new vitest suites pass.
    - CLI: `npm run lint` exits 0 (rule + i18next both active on all new src files).
    - CLI: `npm run typecheck` exits 0.
    - Source assertion: src/db/schema/mapNodes.ts declares name_vi, name_en, name_zh columns, a unique code column, and a representative_hero_id column (zone→hero marker for the scaffold).
    - Source assertion: src/db/schema/index.ts has a '// Phase 8 schemas' comment followed by the mapNodes export, appended exactly once — append-only merge protocol: this task never removes or duplicates the walletTransactions export from plan 08-02 Task 1 (wave-1 sibling; if the sibling has not landed yet, this task appends only mapNodes and does NOT add a placeholder for walletTransactions).
    - Source assertion: src/commands/sanguo/map.ts uses heroEmoji only via buildSanguoMapEmbed — no direct emoji-ID literal anywhere in the command file.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shard startup → Discord | appId contract: the emoji registry applicationId must equal config.CLIENT_ID before the bot touches Discord (D-14) |
| generator script → sibling repo | dev-time read of E:\Saeth\sanguo_assets; the committed registry is the ONLY runtime source (D-13) |
| command code → embed output | emoji IDs flow to the Discord client surface only via heroEmoji() (D-15) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Spoofing | src/shard.ts startup | high | mitigate | Startup check `assertEmojiApplicationId(SANSUO_EMOJI_APPLICATION_ID, config.CLIENT_ID)` before client.login(); mismatch logs and process.exit(1) (D-14). The pure function is unit-tested with explicit args so the check is proven, not assumed. |
| T-08-02 | Tampering | src/assets/sanguoEmojis.ts | medium | mitigate | Generated file is committed and regenerated only by scripts/gen-sanguo-emojis.ts (D-13); the generator validates ≥132 hero prefixes and an 18-digit applicationId before writing. ESLint emoji-ID rule blocks embedding raw IDs outside this module (D-15). |
| T-08-03 | Tampering | scripts/gen-sanguo-emojis.ts | low | accept | Dev-time only; output is committed and diffed at review; runtime never executes the generator or reads the sibling repo. No runtime attack surface. |
| T-08-04 | Tampering | i18n locale files | low | accept | Locale JSON is repo-committed content; CI check-i18n guarantees vi/en/zh-cn key parity; worst case is a missing translation string, not an integrity breach. |
| T-08-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages in this plan (RESEARCH Package Legitimacy Audit: all deps already installed/verified). Rule file uses typescript-eslint createRule from the existing dependency set; no registry installs occur. |
</threat_model>

<verification>
- Run the plan gates in order: vitest (sanguoEmojis, buildSanguoMapEmbed, map command), npm run check-i18n, npm run lint, npm run typecheck.
- Confirm src/shard.ts contains the appId assertion before client.login and that a mismatched CLIENT_ID is rejected by the pure function test (boot hard-fail is backstop-verified; full boot requires DB — the dev DB is provisioned/verified in plan 08-04 Task 1 [wave-0] and the migration task's verify in 08-04 Task 3 confirms all 9 Phase 8 tables exist post-migrate, which is the full-boot smoke for SC2/SC3).
- Grep gate: no emoji-ID literal appears outside src/assets/sanguoEmojis.ts (enforced by the new ESLint rule; spot-check with rg for the emoji markup pattern under src/commands src/ui).
</verification>

<success_criteria>
- heroEmoji() + assertEmojiApplicationId() are exported from the committed generated registry; all unit tests pass.
- /sanguo map registers via commandLoader and renders SEASON-colored embed with per-locale node names and heroEmoji markers; empty state covered.
- check-i18n exits 0 with sanguo + football namespaces in parity across vi/en/zh-cn; zero hardcoded user-facing strings in new code (lint-enforced).
- The startup check hard-fails on applicationId/CLIENT_ID mismatch (unit-proven).
</success_criteria>

<output>
Create `.planning/phases/08-foundation-economy-budget-content-infrastructure/08-01-SUMMARY.md` when done
</output>
