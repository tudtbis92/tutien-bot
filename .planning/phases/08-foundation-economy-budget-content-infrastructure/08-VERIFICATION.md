---
phase: 08-foundation-economy-budget-content-infrastructure
verified: 2026-08-11T07:30:00Z
status: human_needed
score: 30/30 must-haves verified
behavior_unverified: 2
behavior_unverified_items:
  - truth: "src/shard.ts calls assertEmojiApplicationId inside main() BEFORE client.login(); on mismatch it logs via logger.error and calls process.exit(1) (D-14) — the pure-function unit test asserts the logic, but a live boot against the real Discord application is required to prove the hard-fail and that the 1056-emoji set renders as <:name:id> markup in a real channel."
    test: "Boot the production bot shard with CLIENT_ID=1381818375633899562 and confirm startup proceeds; then invoke /sanguo map in a test server and confirm hero/zone emojis render (not literal text). Optionally boot with a wrong CLIENT_ID and confirm process.exit(1)."
    expected: "Correct app id → bot boots and /sanguo map renders emoji markup for all 7 zone markers; wrong app id → hard-fail at startup with the logged mismatch and non-zero exit."
    why_human: "Requires a live Discord gateway connection, the real application-owned emoji set, and a running dev DB — none of which a unit/integration test can exercise in CI."
  - truth: "All 9 Phase 8 tables (wallet_transactions, heroes, user_heroes, map_nodes, player_travel_state, sanguo_battles, sanguo_items, user_sanguo_items, encounter_runs) are created by migration 0014 and present in the dev database after migrate; the seed upserts 132 heroes + 7 map_nodes + 3 items idempotently (D-11) — the seed's own idempotent double-run was verified by the executor, but a fresh-database from-scratch chain (0000→0014) end-to-end is a human-managed environment verification."
    test: "On a fresh dev database, run npx drizzle-kit migrate (DATABASE_URL_DIRECT), then npx tsx scripts/seed-sanguo.ts twice, then query row counts: heroes=132, map_nodes=7, sanguo_items=3 with name_zh NOT NULL on all 132 heroes."
    expected: "Both migrate and both seed runs exit 0; second seed run leaves counts unchanged; SELECT count(*) FROM heroes WHERE name_zh IS NOT NULL = 132."
    why_human: "Requires a reachable dev PostgreSQL and a real migrate+seed cycle against it; the executor's earlier run proved the chain on the current DB but a clean-chain re-run is an environment-dependent check."
---

# Phase 8: Foundation, Economy Budget & Content Infrastructure Verification Report

**Phase Goal:** Nền tảng chung cho toàn bộ milestone — shared wallet service, schemas + idempotent seed, i18n sanguo (content/UI split), emoji registry, và economy design-gate trước khi viết bất kỳ content nào.
**Verified:** 2026-08-11T07:30:00Z
**Status:** human_needed

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Wallet `deductBalance` WHERE-guard (`balance >= amount`), zero-row → `INSUFFICIENT_BALANCE`, full rollback | ✓ VERIFIED | `src/services/wallet.ts:47`; unit test `src/services/__tests__/wallet.test.ts:98` rejects insufficient with no ledger row; 140/140 tests pass |
| 2 | Every successful deduct/credit writes exactly one `wallet_transactions` row in the same transaction as `users.balance` (SC1 reconcilability) | ✓ VERIFIED | `wallet.ts` ledger INSERT shares the balance UPDATE (key-link `insert(walletTransactions)`); `wallet.test.ts` success-deduct asserts ledger + balanceAfter |
| 3 | `creditBalance` has no balance guard and returns balanceAfter via `.returning()` | ✓ VERIFIED | `wallet.ts:90`; `wallet.test.ts:148` credit-succeeds-without-guard |
| 4 | All 7 balance-write sites (gather, farming purchase, farming upgrade, football wager/refund, football void, push, payout) route through the wallet (D-03) | ✓ VERIFIED | grep: `subscriptionService.ts:76,143`; `predictionService.ts:166,171`; `matchLifecycleService.ts:357,417,433`; `gather.ts` refactored; no inline balance UPDATE outside wallet.ts |
| 5 | `fetchCommandContext` returns `users.id` so gather passes numeric user id | ✓ VERIFIED | `src/utils/commandContext.ts` extended per plan; gather test suite passes |
| 6 | Wallet detects client vs transaction by object identity (`txOrDb === db`), not method probe (D-02) | ✓ VERIFIED | `wallet.ts` client/tx branch; `wallet.test.ts:120` explicit-tx passthrough honors D-02 |
| 7 | `src/assets/sanguoEmojis.ts` exists with SANSUO_EMOJIS registry and `SANSUO_EMOJI_APPLICATION_ID = '1381818375633899562'` as const | ✓ VERIFIED | File exists; registry generated by `scripts/gen-sanguo-emojis.ts` |
| 8 | `heroEmoji()` returns `<:name:id>` markup; unknown prefix throws `EMOJI_NOT_FOUND`, never empty string | ✓ VERIFIED | `sanguoEmojis.ts` + `sanguoEmojis.test.ts` (snake_case resolution + 132-hero full-coverage loop); regression tests from CR-01 |
| 9 | `assertEmojiApplicationId` pure function; mismatched pair → false | ✓ VERIFIED | `sanguoEmojis.test.ts` pure-function tests; 6/6 map+emoji regression tests pass |
| 10 | `src/shard.ts` calls `assertEmojiApplicationId` before `client.login()`, exits 1 on mismatch (D-14) | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Call present in `src/shard.ts` (verified by code review CR-01 context); live-boot hard-fail requires a real Discord gateway — see Human Verification #1 |
| 11 | `sanguo` namespace registered in `src/i18n/index.ts` AND `scripts/check-i18n.ts`; football gap repaired | ✓ VERIFIED | `npm run check-i18n` exits 0 ("All locale files are in sync"); namespace present in both files |
| 12 | `locales/{vi,en,zh-cn}/sanguo.json` exist with identical key sets | ✓ VERIFIED | check-i18n key-parity (exact flattened-key equality) passes for all 3 locales |
| 13 | Missing locale file → check-i18n non-zero | ✓ VERIFIED | `check-i18n.ts` hard-fail on missing file (documented in 08-01-SUMMARY) |
| 14 | `mapNodes` schema: `id`, `code` (varchar unique), `name_vi/en/zh`, `zone`, `node_order`, `representative_hero_id` (nullable), not JSONB (D-05) | ✓ VERIFIED | `src/db/schema/mapNodes.ts`; `representative_hero_id` values resolve via `heroEmoji` (CR-01 fix) |
| 15 | `/sanguo map` registers as top-level `sanguo` with `map` subcommand; drops into commandLoader autodiscovery | ✓ VERIFIED | `src/commands/sanguo/map.ts`; `map.test.ts` command regression test passes |
| 16 | Map command defers reply, not-registered → `common:errors.notRegistered`, failure → `sanguo:map.error` | ✓ VERIFIED | `map.ts` handler branches; tested |
| 17 | `buildSanguoMapEmbed` renders with COLORS.SEASON, emoji-prefixed fields, per-locale DB values (D-07), embedFooter(shardId), setTimestamp | ✓ VERIFIED | `src/ui/embeds/buildSanguoMapEmbed.ts`; `buildSanguoMapEmbed.test.ts` |
| 18 | Zone labels derived from per-locale node names, never raw `zone` codes (WR-02 fix) | ✓ VERIFIED | `map.ts` `pickName(row, locale)`; regression test |
| 19 | Map scaffold within Discord limits (25 fields / 1024 chars / 4096 total) | ✓ VERIFIED | 7 nodes fit in 1–3 fields; embed test asserts structure |
| 20 | `docs/economy-budget.md` exists as standalone ADR-style artifact (D-17) | ✓ VERIFIED | File exists; contains "Design Gate Sign-off" section |
| 21 | Every budget number cited to source file:line (D-20): DAILY_CAP, gather fees, farming prices, football MIN/MAX_BET, starting balance 0 | ✓ VERIFIED | Cited: `src/constants/game.ts:14`, `gatherFees.ts:17-30`, `subscriptionService.ts:19-32,50-56`, `footballConfig.ts:8-11`, `start.ts:75-79` |
| 22 | VWAP bands + 10% fee labeled "planned, not live" (marketplace stubbed) | ✓ VERIFIED | economy-budget.md labels MKT-02/03/04/07 as planned |
| 23 | Expected Linh thạch/hour of optimal loop documented and below tu vi caps (SC5) | ✓ VERIFIED | Net linh thạch/hour <= 0 under D-19 net-sink/neutral, trivially below DAILY_CAP 10_000; doc section present |
| 24 | Convertibility matrix recorded (tu vi→LT never; hồn ngọc account-bound; dupe→hồn ngọc; sinks; only faucet = starter hero; no marketable item without spec) | ✓ VERIFIED | economy-budget.md convertibility matrix |
| 25 | Net-sink/neutral HARD constraint (D-19) recorded; Phase 11 must comply | ✓ VERIFIED | economy-budget.md constraint section |
| 26 | Design Gate Sign-off PASSED (D-18) gating Phases 9–11 content | ✓ VERIFIED | "## Design Gate Sign-off — Status: PASSED (2026-08-11)"; decision logged in STATE.md |
| 27 | 9 Phase 8 tables migrated via 0014; map_nodes REQUIRED in applied set (SC2) | ✓ VERIFIED | `migrations/0014_next_chimera.sql` (all 9 CREATE TABLE, no DROP/ALTER on existing); `migrations/meta/_journal.json` idx 14; schema-drift check clean |
| 28 | `heroes` per-locale name columns + faction/role enums; `user_heroes` 6 IV smallint columns with CHECK 0–31 | ✓ VERIFIED | `heroes.ts:35-39` (hero_id unique, name_vi/en/zh); `userHeroes.ts:24-37` (iv_hp..iv_luck + iv_*_range checks) |
| 29 | `scripts/seed-sanguo.ts` idempotent upsert (132 heroes + 7 map_nodes + 3 items), `name_zh` from committed `sanguo-zh-names.json` (D-06), Windows-safe path | ✓ VERIFIED | `seed-sanguo.ts` reads repo-copy heroes-v1.json + zh-names map; seed double-run verified idempotent (REVIEW-FIX.md final gates); `fileURLToPath` fix |
| 30 | `scripts/deploy.sh` runs seed after migrate (D-12); `package.json` `seed:sanguo` script | ✓ VERIFIED | deploy.sh:23 migrate → deploy.sh:26 seed; package.json:18 `seed:sanguo` (gen:emojis preserved at :17) |

**Score:** 30/30 truths verified (28 present + wired, 2 present behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/services/wallet.ts` | deductBalance + creditBalance choke point (TQC-01/D-03) | ✓ EXISTS + SUBSTANTIVE | WHERE-guard, ledger INSERT, client/tx identity branch |
| `src/db/schema/walletTransactions.ts` | wallet_transactions ledger (D-01) | ✓ EXISTS + SUBSTANTIVE | userId, type, amount bigint, balance_after, reason, metadata, created_at |
| `src/db/schema/mapNodes.ts` | map_nodes per-locale schema | ✓ EXISTS + SUBSTANTIVE | code natural key, name_vi/en/zh, zone, node_order, representative_hero_id |
| `src/db/schema/heroes.ts` | heroes catalog | ✓ EXISTS + SUBSTANTIVE | hero_id unique, name_vi/en/zh, faction/role enums |
| `src/db/schema/userHeroes.ts` | user_heroes with 6 IV 0-31 checks | ✓ EXISTS + SUBSTANTIVE | iv_hp..iv_luck smallint + 6 range checks |
| `src/db/schema/playerTravelState.ts` | player_travel_state | ✓ EXISTS + SUBSTANTIVE | Phase 9 consumer |
| `src/db/schema/sanguoBattles.ts` | sanguo_battles | ✓ EXISTS + SUBSTANTIVE | Phase 10 consumer |
| `src/db/schema/sanguoItems.ts` | sanguo_items | ✓ EXISTS + SUBSTANTIVE | item catalog per-locale names |
| `src/db/schema/userSanguoItems.ts` | user_sanguo_items | ✓ EXISTS + SUBSTANTIVE | Phase 11 consumer |
| `src/db/schema/encounterRuns.ts` | encounter_runs | ✓ EXISTS + SUBSTANTIVE | Phase 9 consumer |
| `src/commands/sanguo/map.ts` | /sanguo map command | ✓ EXISTS + SUBSTANTIVE | deferReply, notRegistered/error branches, per-locale zone labels |
| `src/ui/embeds/buildSanguoMapEmbed.ts` | map embed | ✓ EXISTS + SUBSTANTIVE | COLORS.SEASON, emoji-prefixed, per-locale values |
| `src/assets/sanguoEmojis.ts` | generated registry | ✓ EXISTS + SUBSTANTIVE | SANSUO_EMOJIS + SANSUO_HERO_EMOJI_CODES + application id const |
| `docs/economy-budget.md` | design-gate ADR | ✓ EXISTS + SUBSTANTIVE | sink/source citations, convertibility, D-19, Sign-off |
| `scripts/seed-sanguo.ts` | idempotent seed | ✓ EXISTS + SUBSTANTIVE | upserts on natural keys, clobber-safe nameZh |
| `scripts/data/sanguo-zh-names.json` | Tavily-researched ZH map | ✓ EXISTS + SUBSTANTIVE | 132 heroes + 7 nodes + 3 items |
| `scripts/deploy.sh` | deploy pipeline | ✓ EXISTS + SUBSTANTIVE | migrate → seed step |
| `migrations/0014_next_chimera.sql` | migration 0014 | ✓ EXISTS + SUBSTANTIVE | 9 CREATE TABLE, additive only |
| `.env.example` | CLIENT_ID + local-dev DB convention | ✓ EXISTS + SUBSTANTIVE | both blocks updated |
| `package.json` | gen:emojis + seed:sanguo scripts | ✓ EXISTS + SUBSTANTIVE | :17, :18 |

**Artifacts:** 20/20 verified

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| wallet.ts | walletTransactions.ts | `insert(walletTransactions)` in same tx | ✓ WIRED | ledger row per committed balance change |
| wallet.ts | users.ts | WHERE-guard UPDATE on users.balance | ✓ WIRED | `users.balance` guard; balance_non_negative DB backstop |
| gather.ts | wallet.ts | deductBalance reason 'gather' | ✓ WIRED | refactored call site |
| subscriptionService.ts | wallet.ts | deductBalance (purchase/upgrade) | ✓ WIRED | lines 76, 143 |
| predictionService.ts | wallet.ts | deduct/credit (wager/edit) | ✓ WIRED | lines 166, 171 |
| matchLifecycleService.ts | wallet.ts | credit (refund/payout) | ✓ WIRED | lines 357, 417, 433 |
| map.ts | buildSanguoMapEmbed | pickName + heroEmoji | ✓ WIRED | per-locale labels + emoji markers |
| map.ts | map_nodes DB | per-locale columns | ✓ WIRED | representative_hero_id → heroEmoji resolution |
| seed-sanguo.ts | heroes/map_nodes/sanguo_items | ON CONFLICT upsert on natural keys | ✓ WIRED | idempotent re-runs |
| deploy.sh | seed-sanguo.ts | `DATABASE_URL="$DATABASE_URL_DIRECT" npx tsx scripts/seed-sanguo.ts` | ✓ WIRED | line 26 after migrate |
| economy-budget.md | src/constants/* | cited file:line sinks/sources | ✓ WIRED | D-20 citations |

**Wiring:** 11/11 connections verified

## Requirements Coverage

| Requirement | Status | Blocking Issue |
|-------------|--------|----------------|
| TQC-01: Shared wallet service + refactor | ✓ SATISFIED | - |
| TQC-02: Schemas + migration 0014 + idempotent seed | ✓ SATISFIED | - |
| TQC-03: i18n sanguo namespace (VI/EN/ZH-CN), content in DB per-locale | ✓ SATISFIED | - |
| TQC-04: Emoji registry + heroEmoji() + appId startup check | ✓ SATISFIED | boot hard-fail smoke is human (see below) |
| TQC-05: Economy budget doc + design gate | ✓ SATISFIED | - |

**Coverage:** 5/5 requirements satisfied

## Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| scripts/seed-sanguo.ts | - | Non-transactional seed (WR-04) | ⚠️ Warning | Accepted by design: idempotent upserts + re-run heal partial failures (D-11); no integrity property gained by a transaction at this scale |
| src/commands/sanguo/map.ts | - | pre-CR-01 `EMOJI_NOT_FOUND` on snake_case ids | ✓ RESOLVED | Fixed in REVIEW-FIX.md CR-01 (heroEmojiPrefix + SANSUO_HERO_EMOJI_CODES) with regression tests |
| scripts/seed-sanguo.ts | - | pre-CR-02 hardcoded `E:\Saeth\...` path | ✓ RESOLVED | Fixed in REVIEW-FIX.md CR-02 (committed repo copy + SANGUO_HEROES_SOURCE override) |
| src/services/wallet.ts | - | pre-WR-01 opaque TypeError on missing user | ✓ RESOLVED | Fixed: `USER_NOT_FOUND:<id>` with ledger skip |
| src/commands/sanguo/map.ts | - | pre-WR-02 raw snake_case zone codes | ✓ RESOLVED | Fixed: per-locale node-name labels |

**Anti-patterns:** 5 found (0 blockers, 1 warning documented, 4 resolved)

## Human Verification Required

### 1. Live boot + emoji render smoke test (TQC-04 / SC3 / D-14)
**Test:** Boot the production bot shard (CLIENT_ID=1381818375633899562), confirm startup proceeds past the appId assertion, then invoke `/sanguo map` in a test server and confirm all 7 zone markers render as emoji (never literal text). Optionally boot with a wrong CLIENT_ID and confirm hard-fail `process.exit(1)`.
**Expected:** Correct app → boots + emojis render; wrong app → logged mismatch + non-zero exit.
**Why human:** Needs live Discord gateway + real application-owned 1056-emoji set + running dev DB; not exercisable in CI.
**Result:** ✅ PASS 2026-08-12 — D-14 hard-fail verified (wrong CLIENT_ID → exit 1). Emoji render FAILED first attempt (`:dtr_t0:` literal) → root-caused: ALL sanguo emojis are animated, need `<a:name:id>` markup (D-21). Fixed + redeployed → emoji render confirmed live. Zone markers moved to message content `# ` headers for larger emoji (D-22).

### 2. Fresh-database migrate + seed chain (SC2 / TQC-02 / D-11)
**Test:** On a fresh dev DB, run `npx drizzle-kit migrate` (DATABASE_URL_DIRECT) then `npx tsx scripts/seed-sanguo.ts` twice; query counts: heroes=132, map_nodes=7, sanguo_items=3, and `name_zh NOT NULL` = 132.
**Expected:** Both runs exit 0; second run unchanged counts; 132/132 name_zh populated.
**Why human:** Requires a reachable dev PostgreSQL and a clean from-scratch (0000→0014) chain; environment-dependent.
**Result:** ✅ PASS 2026-08-12 — staging DB `tutien_staging`: migrate 0000→0017 exit 0 (18 journal rows), seed ×2 exit 0 counts unchanged. heroes=132, map_nodes=7, sanguo_items=3, hero_factions=14, hero_families=12, hero_relations=2, name_zh=132.

## Gaps Summary

**No critical gaps found.** All 30 must-haves verified (28 programmatically, 2 human smoke tests PASSED 2026-08-12). Phase goal achieved + deployed to production.

### Non-Critical (Can Defer)

1. **Non-transactional seed (WR-04)** — Issue: seed runs outside a single transaction. Impact: limited — idempotent upserts + re-run heal partial failures. Recommendation: defer; revisit if content grows beyond placeholder scale.

## Recommended Fix Plans

None — no gaps requiring fix plans.

## Verification Metadata

**Verification approach:** Goal-backward (derived from phase goal)
**Must-haves source:** PLAN.md frontmatter (08-01..08-04)
**Automated checks:** 28 passed, 0 failed (typecheck ✓, lint ✓, check-i18n ✓, 140/140 tests ✓)
**Human checks required:** 2
**Human checks passed:** 2 (2026-08-12) — plus 1 follow-up finding resolved (animated emoji prefix D-21, header-in-content D-22)
**Total verification time:** ~8 min automated + deploy verification 2026-08-12

---
*Verified: 2026-08-11T07:30:00Z (automated) — human smoke tests passed + production deploy: 2026-08-12*
*Verifier: orchestrator inline (verifier agent disabled per workflow.verifier=false)*
