---
phase: 08-foundation-economy-budget-content-infrastructure
plan: 4
type: execute
wave: 2
depends_on: [08-01, 08-02]
files_modified:
  - docker-compose.yml
  - .env.example
  - src/db/schema/heroes.ts
  - src/db/schema/userHeroes.ts
  - src/db/schema/playerTravelState.ts
  - src/db/schema/sanguoBattles.ts
  - src/db/schema/sanguoItems.ts
  - src/db/schema/userSanguoItems.ts
  - src/db/schema/encounterRuns.ts
  - src/db/schema/index.ts
  - migrations/0014_*.sql
  - migrations/meta/0014_snapshot.json
  - migrations/meta/_journal.json
  - scripts/seed-sanguo.ts
  - package.json
  - scripts/deploy.sh
autonomous: false
requirements: [TQC-02]
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 5
  confidence: low
must_haves:
  truths:
    - "A dev PostgreSQL is reachable at DATABASE_URL_DIRECT before migration/seed run (RESEARCH OQ5 resolved): the wave-0 node pg probe (SELECT 1) exits 0, and docker-compose.yml (postgres 16 + redis 7, with a pg_isready healthcheck) or a documented local-install/external-DB alternative exists when provisioning was needed."
    - "The 9 Phase 8 tables (wallet_transactions from plan 08-02, heroes, user_heroes, map_nodes from plan 08-01, player_travel_state, sanguo_battles, sanguo_items, user_sanguo_items, encounter_runs) are created by migration 0014 (or 0014 + 0015 recovery) and present in the dev database after migrate — map_nodes is REQUIRED in the applied migration set (SC2/SC3)."
    - "heroes carries hero_id (unique natural key), name_vi/name_en/name_zh per-locale varchar columns (D-05, NOT JSONB), and faction/role pgEnums; user_heroes has 6 IV smallint columns (iv_hp, iv_atk, iv_def, iv_spd, iv_crit, iv_luck) each CHECKed 0-31."
    - "scripts/seed-sanguo.ts upserts all 132 heroes from E:\Saeth\sanguo_assets\src\data\heroes-v1.json (name -> name_vi, en -> name_en, name_zh left NULL/empty per D-06), 5-10 placeholder map_nodes (unique code), and sanguo_items (unique code); running it twice leaves row counts unchanged (idempotent per D-11)."
    - "scripts/deploy.sh runs npx tsx scripts/seed-sanguo.ts after drizzle-kit migrate (D-12); package.json has a 'seed:sanguo' script entry."
  artifacts:
    - path: "docker-compose.yml"
      provides: "local dev stack: postgres 16 + redis 7 (RESEARCH OQ5; Redis unused in Phase 8 but part of the local stack)"
      contains: "postgres:16"
    - path: "src/db/schema/heroes.ts"
      provides: "heroes catalog, hero_id natural key + per-locale names (TQC-02/D-05)"
      contains: "heroId"
    - path: "src/db/schema/userHeroes.ts"
      provides: "user_heroes with 6 IV smallint 0-31 checks (TQC-02 -> Phase 10 TQC-12)"
      contains: "ivHp"
    - path: "src/db/schema/playerTravelState.ts"
      provides: "player_travel_state (Phase 9 TQC-06 consumer)"
      contains: "arriveAt"
    - path: "src/db/schema/sanguoBattles.ts"
      provides: "sanguo_battles (Phase 10 TQC-10 consumer)"
      contains: "roundLogs"
    - path: "src/db/schema/sanguoItems.ts"
      provides: "sanguo_items catalog, code natural key + per-locale names (D-05)"
      contains: "nameZh"
    - path: "src/db/schema/userSanguoItems.ts"
      provides: "user_sanguo_items (Phase 11 TQC-16 consumer)"
      contains: "quantity"
    - path: "src/db/schema/encounterRuns.ts"
      provides: "encounter_runs (Phase 9 TQC-08 consumer)"
      contains: "encounterRuns"
    - path: "scripts/seed-sanguo.ts"
      provides: "idempotent content seed: 132 heroes + placeholder nodes + items (D-11/D-12)"
      contains: "onConflictDoUpdate"
    - path: "scripts/deploy.sh"
      provides: "seed step after migrate (D-12)"
      contains: "seed-sanguo"
  key_links:
    - from: "src/db/schema/index.ts"
      to: "migrations/0014_*.sql"
      via: "drizzle-kit generate diffs the merged Phase 8 block — the applied migration set MUST contain 9 CREATE TABLEs incl. map_nodes (08-01) and wallet_transactions (08-02); precondition + 0015 recovery guarantee coverage"
      pattern: "CREATE TABLE"
    - from: "scripts/seed-sanguo.ts"
      to: "src/db/schema/index.ts"
      via: "heroes upsert by hero_id, map_nodes by code, sanguo_items by code (D-11)"
      pattern: "onConflictDoUpdate"
    - from: "docker-compose.yml"
      to: ".env"
      via: "local dev DATABASE_URL_DIRECT (5432, direct — drizzle-kit advisory locks break under PgBouncer 6432) matches the compose postgres service"
      pattern: "DATABASE_URL_DIRECT"
  prohibitions:
    - statement: "Sanguo content names live in DB per-locale columns, never in i18n keys (D-07) — the seed writes name_vi/name_en/name_zh columns only."
      status: unverified
      flagged: true
    - statement: "Migration 0014 must never be applied without the full 9-table Phase 8 set incl. map_nodes — a missing table silently breaks SC2/SC3 while every gate passes."
      status: unverified
      flagged: true
---

<objective>
Deliver the Phase 8 content infrastructure (TQC-02): the 7 sanguo domain schemas merged into `src/db/schema/index.ts` (joining `map_nodes` from plan 08-01 and `wallet_transactions` from plan 08-02), migration 0014 covering the full 9-table Phase 8 set — `map_nodes` REQUIRED — the idempotent content seed (D-11/D-12), the deploy-pipeline seed step, and the wave-0 environment task that makes migration/seed executable by provisioning or verifying a reachable dev PostgreSQL (RESEARCH.md OQ5: no docker-compose.yml in the repo, 5432 closed, no psql/initdb on PATH).

Purpose: TQC-02 (schemas + migration + seed, D-05/D-06/D-09/D-10/D-11/D-12). Satisfies SC2 (all 9 Phase 8 schemas migrated and idempotently seeded — heroes + map nodes present at boot) and provides the DB foundation for the 08-01 `/sanguo map` command and all later phases. Runs in wave 2 after 08-01 and 08-02 so the migration's precondition (all Phase 8 schema exports merged, incl. mapNodes from 08-01 Task 3 and walletTransactions from 08-02 Task 1) is guaranteed by plan ordering, not just by convention.

Output: reachable dev PostgreSQL (or documented alternative), docker-compose.yml scaffold, 7 schema files + index.ts merge, migration 0014 (+0015 recovery if needed), idempotent seed script + package.json script, deploy.sh seed step.

**Split note (revision):** This plan was split out of the original 08-02 (which is now wallet-only, TQC-01) to keep each plan within scope: 5 tasks here, the wallet plan has 3. The [BLOCKING] migration checkpoint at task 3, the wave-0 env gate at task 1, and the wave-2 ordering de-risk the cross-plan schema merges.

## Artifacts this phase produces (plan 08-04)

| Symbol | Kind | Location |
|--------|------|----------|
| reachable dev PostgreSQL (probe-proven) | environment | DATABASE_URL_DIRECT target |
| `docker-compose.yml` (postgres 16 + redis 7, healthcheck) | local-dev stack scaffold | docker-compose.yml |
| `heroes`, `userHeroes`, `playerTravelState`, `sanguoBattles`, `sanguoItems`, `userSanguoItems`, `encounterRuns` tables + types | Drizzle schemas x7 | src/db/schema/*.ts |
| `// Phase 8 schemas` export block (7 new lines) | merge | src/db/schema/index.ts |
| migration `0014_*.sql` + `meta/0014_snapshot.json` + journal idx 14 (9 CREATE TABLEs incl. map_nodes; 0015 recovery if any table missing) | Drizzle migration | migrations/ |
| `scripts/seed-sanguo.ts` (132 heroes + 5-10 nodes + items, idempotent) | CLI script | scripts/ |
| `"seed:sanguo": "tsx scripts/seed-sanguo.ts"` | package.json script | package.json |
| seed step after `drizzle-kit migrate` | deploy edit | scripts/deploy.sh |
</objective>
<execution_context>
@C:/Users/901107/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/901107/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-foundation-economy-budget-content-infrastructure/08-CONTEXT.md
@.planning/phases/08-foundation-economy-budget-content-infrastructure/08-RESEARCH.md
@.planning/phases/08-foundation-economy-budget-content-infrastructure/08-PATTERNS.md
@.planning/phases/08-foundation-economy-budget-content-infrastructure/08-01-PLAN.md
@.planning/phases/08-foundation-economy-budget-content-infrastructure/08-02-PLAN.md
@src/db/schema/items.ts
@src/db/schema/character_items.ts
@src/db/schema/footballBets.ts
@src/db/schema/farming.ts
@src/db/schema/users.ts
@src/db/schema/mapNodes.ts
@src/db/schema/index.ts
@src/db/client.ts
@drizzle.config.ts
@src/db/seed.ts
@scripts/deploy.sh
@package.json
@.env.example
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: [WAVE-0] Provision or verify a reachable dev PostgreSQL (blocking prerequisite for migration 0014 + seed; RESEARCH OQ5)</name>
  <files>docker-compose.yml, .env.example</files>
  <action>
    This task MUST complete before task 3 (generate + migrate 0014) and task 4 (seed): RESEARCH.md OQ5 + Environment Availability found no reachable dev DB — no docker-compose.yml in the repo, local probes showed 5432/6432 closed, and no psql/initdb binaries on PATH. RESEARCH.md mandates this environment task ("planner MUST include") before migration/seed. The executor's job is to make DATABASE_URL_DIRECT reachable or prove it already is:
    1. Probe first (no provisioning yet): load .env and test DATABASE_URL_DIRECT with a node pg one-liner (pg + dotenv are existing dependencies — no install): `node -e "require('dotenv/config');const {Client}=require('pg');const c=new Client({connectionString:process.env.DATABASE_URL_DIRECT});c.connect().then(()=>c.query('SELECT 1')).then(()=>{console.log('DB_REACHABLE');process.exit(0)}).catch(e=>{console.error('DB_UNREACHABLE: '+e.message);process.exit(1)})"` run from the repo root. If it exits 0, the dev DB is reachable: record the result in the summary, add the local-dev note to .env.example (step 3 doc entry), and present the probe output to the human for approval — nothing else to provision.
    2. If the probe fails AND docker is available: create docker-compose.yml at the repo root with a postgres service using image postgres:16-alpine (POSTGRES_DB=tutien, POSTGRES_USER=tutien, POSTGRES_PASSWORD=tutien — local-dev-only convention, documented in .env.example), ports "5432:5432", and a healthcheck `pg_isready -U tutien -d tutien`; include a redis:7-alpine service (port 6379) as part of the local stack per RESEARCH OQ5 (Redis is unused in Phase 8 — starting it is optional). Then run `docker compose up -d postgres`, wait for the healthcheck to report healthy, and re-run the probe from step 1.
    3. If the probe fails AND docker is NOT installed (verified on this machine: `docker` is not on PATH, no docker-compose.yml exists): do NOT fabricate success and do NOT skip the gate. Escalate to the user with the three accepted alternatives from RESEARCH.md OQ5 — (a) install Docker Desktop and re-run step 2, (b) install PostgreSQL 16 locally (winget install PostgreSQL.PostgreSQL.16 or initdb) and set DATABASE_URL_DIRECT in .env, (c) provide a reachable external dev DB URL (e.g. a hosted dev instance) as DATABASE_URL_DIRECT in .env. The executor may NOT proceed to task 3 until the probe exits 0. Update .env.example with the local-dev DATABASE_URL_DIRECT / DATABASE_URL entries documenting whichever convention the user chose.
    Throughout: production secrets stay out of this task — deploy.sh sources /etc/tutien/.env on the server; the compose credentials are local-dev only and must be marked as such in .env.example.
  </action>
  <verify>
    <automated>node -e "require('dotenv/config');const {Client}=require('pg');const c=new Client({connectionString:process.env.DATABASE_URL_DIRECT});c.connect().then(()=>c.query('SELECT 1')).then(()=>{console.log('DB_REACHABLE');process.exit(0)}).catch(e=>{console.error('DB_UNREACHABLE: '+e.message);process.exit(1)})"</automated>
  </verify>
  <what-built>Evidence that a dev PostgreSQL is reachable — a passing node pg probe against DATABASE_URL_DIRECT (SELECT 1) — plus, only if provisioning was needed, docker-compose.yml (postgres 16 + redis 7 with a pg_isready healthcheck) and .env.example local-dev entries documenting the chosen option (compose / local install / external DB).</what-built>
  <how-to-verify>
    1. The executor ran the node pg probe against DATABASE_URL_DIRECT — it printed DB_REACHABLE and exited 0. 2. If provisioning was needed: docker-compose.yml exists at the repo root with postgres:16-alpine + a pg_isready healthcheck, `docker compose ps` shows the postgres container healthy (or the chosen alternative — local install or external DB — is reflected in .env + .env.example). 3. .env.example documents the local-dev DATABASE_URL_DIRECT / DATABASE_URL convention and marks the credentials as dev-only. 4. Type "approved" to unlock task 3 (migration 0014).
  </how-to-verify>
  <resume-signal>approved (probe exit 0) or describe the chosen DB option / provided credentials</resume-signal>
  <done>DATABASE_URL_DIRECT resolves to a reachable PostgreSQL that answers SELECT 1 (probe exits 0); docker-compose.yml + .env.example updated if provisioning was needed; the migration task is unblocked.</done>
  <acceptance_criteria>
    - CLI: the node pg probe against DATABASE_URL_DIRECT exits 0 (prints DB_REACHABLE).
    - File assertion: docker-compose.yml exists if provisioning was needed — postgres:16-alpine with a pg_isready healthcheck; redis:7-alpine optional.
    - File assertion: .env.example documents the local-dev DATABASE_URL_DIRECT (and DATABASE_URL) convention and marks the credentials dev-only.
  </acceptance_criteria>
</task>
<task type="auto">
  <name>Task 2: 7 sanguo domain schemas + index.ts merge (TQC-02, D-05)</name>
  <files>src/db/schema/heroes.ts, src/db/schema/userHeroes.ts, src/db/schema/playerTravelState.ts, src/db/schema/sanguoBattles.ts, src/db/schema/sanguoItems.ts, src/db/schema/userSanguoItems.ts, src/db/schema/encounterRuns.ts, src/db/schema/index.ts</files>
  <read_first>src/db/schema/items.ts (content-table catalog pattern), src/db/schema/character_items.ts (user-owned rows + uniqueIndex pattern), src/db/schema/footballBets.ts (FK + status varchar + check + index pattern), src/db/schema/farming.ts (pgEnum + jsonb $type pattern), src/db/schema/users.ts (bigint currency convention), src/db/schema/mapNodes.ts (map_nodes from 08-01 — playerTravelState deliberately does NOT import it), src/db/schema/index.ts (merge point — walletTransactions from 08-02 and mapNodes from 08-01 are already present, wave-2 ordering guarantees it), 08-RESEARCH.md TQC-02 section (enum values, IV note, D-10 map_nodes guidance)</read_first>
  <action>
    Create src/db/schema/heroes.ts: heroFactionEnum pgEnum('hero_faction', ['hoang_toc','thap_thuong_thi','trieu_dinh','dang_nhan','tuong_trieu','khan_vang','luong_chau','quan_hung','chau_muc','ngoai_toc']) and heroRoleEnum pgEnum('hero_role', ['royal','eunuch','military','civil','religious']) (10 factions / 5 roles verified in RESEARCH from heroes-v1.json). heroes pgTable('heroes'): id serial pk; heroId varchar(50) notNull unique (D-11 natural key for upsert); nameVi varchar(100) notNull; nameEn varchar(100) notNull; nameZh varchar(100) nullable (D-05 per-locale columns, NOT JSONB; NULL until Tavily research fills it per D-06); faction heroFactionEnum notNull; role heroRoleEnum notNull; plus nullable content columns from the JSON source: gender varchar(20), people varchar(50), titleVi varchar(200), weapon varchar(50), detailEn text. Export Hero/NewHero types.
    Create src/db/schema/userHeroes.ts: pgTable('user_heroes'): id serial pk; userId integer notNull references users.id; heroId integer notNull references heroes.id; level integer notNull default 1; ivHp/ivAtk/ivDef/ivSpd/ivCrit/ivLuck six smallint notNull columns (IV 0-31 per TQC-02 -> Phase 10 TQC-12); capturedAt timestamp withTimezone notNull defaultNow. Table callback: six check constraints iv_hp_range ... iv_luck_range each asserting the column is >= 0 AND <= 31, and index('user_heroes_user_idx').on(userId). Do NOT add a unique constraint on (userId, heroId) — duplicates MUST be allowed (Phase 11 TQC-14 dupe -> hồn ngọc conversion consumes duplicate rows).
    Create src/db/schema/playerTravelState.ts: pgTable('player_travel_state'): id serial pk; userId integer notNull references users.id unique (one active journey per user, Phase 9 TQC-06); fromNodeId and toNodeId as plain integer nullable columns WITHOUT Drizzle references() — deliberate: map_nodes lives in plan 08-01's module and an import dependency would couple the plans; Phase 9 adds the FK in its own migration if needed; departAt and arriveAt timestamp withTimezone notNull; cost bigint mode bigint notNull; status varchar(20) notNull default 'traveling' (values 'traveling'|'cancelled'|'arrived'); createdAt/updatedAt timestamp withTimezone notNull defaultNow.
    Create src/db/schema/sanguoBattles.ts: pgTable('sanguo_battles'): id serial pk; userId integer notNull references users.id; status varchar(20) notNull default 'pending'; roundLogs jsonb $type<unknown[]> nullable (Phase 10 TQC-10 replay logs); createdAt/updatedAt timestamp withTimezone notNull defaultNow; resolvedAt timestamp withTimezone nullable. Minimal-but-complete — Phase 10 extends.
    Create src/db/schema/sanguoItems.ts: pgTable('sanguo_items'): id serial pk; code varchar(50) notNull unique (D-11 natural key); nameVi varchar(100) notNull; nameEn varchar(100) notNull; nameZh varchar(100) nullable (D-05/D-06); itemType varchar(30) notNull default 'support'; rarity smallint notNull default 1; basePrice bigint mode bigint notNull default sql 0 (items.ts BigInt-default pattern); descriptionVi text nullable; createdAt timestamp withTimezone notNull defaultNow.
    Create src/db/schema/userSanguoItems.ts: pgTable('user_sanguo_items'): id serial pk; userId integer notNull references users.id; itemId integer notNull references sanguoItems.id; quantity integer notNull default 1. Table callback: check('quantity_positive', quantity > 0), index('user_sanguo_items_user_idx').on(userId), uniqueIndex('user_sanguo_items_unique_user_item').on(userId, itemId) (character_items.ts upsert pattern).
    Create src/db/schema/encounterRuns.ts: pgTable('encounter_runs'): id serial pk; userId integer notNull references users.id; travelId integer nullable references playerTravelState.id; zone varchar(50) notNull; heroId integer nullable references heroes.id (the encountered hero, Phase 9/10); status varchar(20) notNull default 'pending'; createdAt timestamp withTimezone notNull defaultNow.
    Every file exports $inferSelect and $inferInsert types. Merge: in src/db/schema/index.ts, append export lines for all 7 new files (heroes, userHeroes, playerTravelState, sanguoBattles, sanguoItems, userSanguoItems, encounterRuns) under the '// Phase 8 schemas' comment, merge-safe with plans 08-01 and 08-02 (append only; never remove or duplicate the mapNodes or walletTransactions lines — wave-2 ordering means both are already present). All references are intra-plan (heroes/userHeroes/sanguoItems referenced by other new files in this same task) so no cross-plan import exists.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>All 7 schema files exist with per-locale columns, pgEnums, IV checks, and natural keys; index.ts Phase 8 block covers mapNodes (08-01) + walletTransactions (08-02) + 7 new files; typecheck passes.</done>
  <acceptance_criteria>
    - Source assertion: src/db/schema/heroes.ts declares a unique hero_id column and name_vi, name_en, name_zh columns, plus faction/role pgEnum columns.
    - Source assertion: src/db/schema/userHeroes.ts declares six smallint iv_* columns and six check constraints each bounding 0-31, and has NO unique index on (user_id, hero_id).
    - Source assertion: src/db/schema/playerTravelState.ts declares arrive_at and cost (bigint mode bigint) and does NOT import from a mapNodes module.
    - Source assertion: src/db/schema/sanguoItems.ts and src/db/schema/heroes.ts use plain varchar columns for per-locale names (no jsonb, no i18n-key column).
    - Source assertion: src/db/schema/index.ts contains all 9 Phase 8 export lines (mapNodes + walletTransactions + 7) under the Phase 8 comment without duplicating any.
    - CLI: `npm run typecheck` exits 0.
  </acceptance_criteria>
</task>
<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Generate + apply migration 0014 (all 9 Phase 8 tables incl. map_nodes)</name>
  <precondition>All Phase 8 schema exports are merged into src/db/schema/index.ts — walletTransactions (plan 08-02 Task 1), the 7 sanguo files (task 2 done), and mapNodes (plan 08-01 Task 3). This is guaranteed by wave-2 ordering (depends_on 08-01/08-02), but verify it before running: if mapNodes is missing from index.ts, STOP — do not generate — and wait for the 08-01 merge. ALSO: DATABASE_URL_DIRECT is set in .env AND reachable (task 1 done) — drizzle.config.ts requires the direct port-5432 connection for generate/migrate; PgBouncer (port 6432) breaks drizzle-kit advisory locks.</precondition>
  <files>migrations/0014_*.sql, migrations/meta/0014_snapshot.json, migrations/meta/_journal.json</files>
  <read_first>drizzle.config.ts (schema/out/dbCredentials, DATABASE_URL_DIRECT requirement lines 4-13), migrations/meta/_journal.json (14 entries 0000-0013 — next idx is 14), src/db/schema/index.ts (the merged Phase 8 block this migration diffs from — must contain mapNodes + walletTransactions + the 7 new files)</read_first>
  <action>
    Run npx drizzle-kit generate with DATABASE_URL_DIRECT present in the environment. It diffs src/db/schema/index.ts against the latest snapshot (0013) and emits migrations/0014_<random>.sql plus migrations/meta/0014_snapshot.json, and appends the 0014 entry (idx 14) to migrations/meta/_journal.json. Expected diff: CREATE TABLE for wallet_transactions, heroes, user_heroes, map_nodes (REQUIRED — the precondition guarantees the 08-01 merge landed before generation), player_travel_state, sanguo_battles, sanguo_items, user_sanguo_items, encounter_runs — all 9 Phase 8 tables; CREATE TYPE for hero_faction, hero_role, wallet_transaction_type; CHECK constraints for IV ranges, amount_non_negative, quantity_positive; indexes for wallet_transactions_user_created_idx, user_heroes_user_idx, user_sanguo_items_user_idx, user_sanguo_items_unique_user_item. There must be NO DROP TABLE, NO ALTER TABLE on pre-existing tables, and no changes to any table outside the Phase 8 set. Do not hand-edit the generated SQL.
    COVERAGE RECOVERY (map_nodes gate): inspect the generated 0014 diff for a `CREATE TABLE ... map_nodes` statement. If map_nodes is absent (e.g. the 08-01 merge landed after generate started), do NOT apply 0014 — re-run `npx drizzle-kit generate` once the mapNodes export is present in index.ts so a follow-up migrations/0015_*.sql emits the map_nodes CREATE TABLE, and verify the full 9-table set is covered across the applied migration files. The migration set applied to the dev database MUST contain 9 CREATE TABLEs including map_nodes — an 8-table 0014 is a failed gate, not a partial success (SC2/SC3 would silently break).
    After the human approves the diff, run npx drizzle-kit migrate (with DATABASE_URL_DIRECT) to apply the migration to the dev database, then confirm all 9 tables exist via a psql or node pg query against pg_catalog.pg_tables for schemaname 'public'.
  </action>
  <verify>
    <automated>if (Get-ChildItem migrations -Filter "0014_*.sql") { rg -c "CREATE TABLE" migrations/0014_*.sql }; if ($?) { rg -n "CREATE TYPE" migrations/0014_*.sql }; if ($?) { rg -n "map_nodes" migrations/0014_*.sql }; if ($?) { npx drizzle-kit migrate }</automated>
  </verify>
  <what-built>Migration 0014 (migrations/0014_*.sql + meta/0014_snapshot.json + journal idx 14) generated from the merged Phase 8 schemas — the 9 new tables incl. map_nodes, 3 pgEnums, IV/amount/quantity CHECK constraints, and history/inventory indexes — applied to the reachable dev DB (task 1 gate).</what-built>
  <how-to-verify>
    1. Open migrations/0014_*.sql and confirm CREATE TABLE statements for ALL NINE tables: wallet_transactions, heroes, user_heroes, map_nodes, player_travel_state, sanguo_battles, sanguo_items, user_sanguo_items, encounter_runs (map_nodes is REQUIRED, not optional — if missing, the executor must run the 0015 re-generate recovery before any migrate). 2. Confirm the file contains no DROP TABLE and no ALTER TABLE on tables outside the Phase 8 set (it must be purely additive). 3. Confirm migrations/meta/_journal.json now lists the 0014 tag at idx 14. 4. Type "approved" to let the executor run npx drizzle-kit migrate against the dev database (requires DATABASE_URL_DIRECT, reachable per task 1).
  </how-to-verify>
  <resume-signal>approved (or describe the required SQL changes)</resume-signal>
  <done>Migration covering all 9 Phase 8 tables (incl. map_nodes) generated (purely additive: 9 CREATE TABLE + 3 CREATE TYPE, no DROP/ALTER on existing tables; 0015 recovery applied if map_nodes was missing), reviewed by the user, applied to the reachable dev database, and all 9 tables confirmed present.</done>
  <acceptance_criteria>
    - CLI: `npx drizzle-kit generate` exits 0 and creates migrations/0014_*.sql (plus migrations/0015_*.sql if the recovery path ran).
    - Source assertion: the Phase 8 migration set (0014 + any 0015) contains exactly 9 "CREATE TABLE" statements — wallet_transactions, heroes, user_heroes, map_nodes, player_travel_state, sanguo_battles, sanguo_items, user_sanguo_items, encounter_runs — and "CREATE TYPE" for hero_faction, hero_role, wallet_transaction_type, and does not contain "DROP TABLE".
    - CLI: `npx drizzle-kit migrate` (DATABASE_URL_DIRECT) exits 0.
    - DB assertion: pg_catalog.pg_tables lists all 9 tables — wallet_transactions, heroes, user_heroes, map_nodes, player_travel_state, sanguo_battles, sanguo_items, user_sanguo_items, encounter_runs — under schemaname 'public'.
  </acceptance_criteria>
</task>
<task type="auto">
  <name>Task 4: Idempotent seed script scripts/seed-sanguo.ts + package.json script (TQC-02, D-06/D-09/D-10/D-11/D-12)</name>
  <precondition>Migration 0014 has been generated AND applied to the dev database (task 3 completed) so the target tables exist, and the dev DB is reachable (task 1). The dev-time content source exists at E:\Saeth\sanguo_assets\src\data\heroes-v1.json (never read at runtime — dev-only source).</precondition>
  <files>scripts/seed-sanguo.ts, package.json</files>
  <read_first>src/db/seed.ts (connection pattern lines 24-31, upsert pattern lines 455-477, teardown lines 565-570), 08-PATTERNS.md Seed Script Connection + Teardown section, 08-RESEARCH.md TQC-02 section (heroes-v1.json field mapping, 10 factions/5 roles), package.json (scripts block lines 7-17 — gen:emojis is already present from 08-01), src/db/schema/index.ts (imported tables)</read_first>
  <action>
    Create scripts/seed-sanguo.ts as a standalone tsx script mirroring src/db/seed.ts structure: import dotenv/config; import drizzle from drizzle-orm/node-postgres; import Pool from pg; import * as schema from '../src/db/schema/index.js'; resolve DATABASE_URL_DIRECT ?? DATABASE_URL, exit(1) with a clear message if unset; new Pool({ connectionString, max: 2 }); drizzle({ client: pool, schema }); wrap the run in a catch that logs a fatal error and process.exit(1); pool.end() in finally.
    Seed heroes: read E:\Saeth\sanguo_assets\src\data\heroes-v1.json (132 entries, each { id snake_case, name VI, en, title, faction VI, weapon, detail EN, gender, people, role }). Map fields: id -> heroId, name -> nameVi, en -> nameEn, title -> titleVi, weapon, gender, people, detail -> detailEn. Map the VI faction string to the hero_faction enum via a const FACTION_MAP (Hoàng tộc -> hoang_toc, Thập Thường Thị -> thap_thuong_thi, Triều đình -> trieu_dinh, Đảng nhân -> dang_nhan, Tướng triều -> tuong_trieu, Khăn Vàng -> khan_vang, Lương Châu -> luong_chau, Quần hùng -> quan_hung, Châu mục -> chau_muc, Ngoại tộc -> ngoai_toc); role maps directly to hero_role (royal|eunuch|military|civil|religious). Fail fast with an explicit error naming the bad value if any faction/role string is unmapped — never silently skip a hero; assert the parsed array has exactly 132 entries and throw if not (D-09: all 132 heroes).
    Upsert heroes with onConflictDoUpdate target heroes.heroId (D-11): the set clause covers nameVi, nameEn, titleVi, weapon, gender, people, detailEn, faction, role. Do NOT include nameZh in the set clause — name_zh stays NULL on this phase's runs (D-06: ZH-CN names come from a later Tavily research pass; the upsert must not clobber a researched value with NULL on re-runs). Use .returning() and throw if a row is not returned.
    Seed map_nodes placeholders (D-10, 5-10 nodes): a const array of 5-10 entries with unique snake_case code (e.g. luoyang, xuchang, changan, yecheng, jianye), nameVi/nameEn, zone, nodeOrder. Upsert onConflictDoUpdate target mapNodes.code, set clause covering nameVi/nameEn/zone/nodeOrder (nameZh omitted — NULL per D-06). The mapNodes schema is guaranteed exported from index.ts by wave-2 ordering (08-01 Task 3 landed before this plan ran); if it is somehow missing, the import fails at compile time — do NOT silently drop the node seed.
    Seed sanguo_items placeholders: 2-4 items with unique code (e.g. heal_pill, xian_tea), nameVi/nameEn, itemType, rarity, basePrice. Upsert onConflictDoUpdate target sanguoItems.code.
    Print per-entity upsert counts at the end (e.g. "[Seed] 132 heroes, 7 map_nodes, 3 items upserted") so idempotency is observable from output.
    Add "seed:sanguo": "tsx scripts/seed-sanguo.ts" to package.json scripts (append-only; keep the gen:emojis entry from 08-01).
    Run the seed twice against the dev database — the second run must print the same counts and add zero rows (idempotency proof). scripts/** is eslint-exempt (eslint.config.mjs) but npm run typecheck still applies to the file.
  </action>
  <verify>
    <automated>npx tsx scripts/seed-sanguo.ts; if ($?) { npx tsx scripts/seed-sanguo.ts }; if ($?) { npm run typecheck }</automated>
  </verify>
  <done>Seed script upserts 132 heroes + 5-10 map nodes + items idempotently (second run adds zero rows), package.json has seed:sanguo, typecheck passes.</done>
  <acceptance_criteria>
    - CLI: running `npx tsx scripts/seed-sanguo.ts` twice in a row exits 0 both times and the second run reports unchanged counts (no duplicate rows — D-11 idempotency).
    - DB assertion: SELECT count(*) FROM heroes equals 132; SELECT count(*) FROM map_nodes is between 5 and 10; running the seed again leaves both counts identical.
    - Source assertion: scripts/seed-sanguo.ts contains onConflictDoUpdate with a target on heroes.heroId and mapNodes.code, and does not set nameZh in any set clause.
    - Source assertion: package.json scripts contains "seed:sanguo" and still contains "gen:emojis" (no clobber of 08-01's entry).
    - CLI: `npm run typecheck` exits 0.
  </acceptance_criteria>
</task>
<task type="auto">
  <name>Task 5: Add seed step to deploy pipeline after migrate (TQC-02, D-12)</name>
  <precondition>scripts/seed-sanguo.ts exists and runs clean against the dev DB (task 4 completed) — the deploy step invokes it.</precondition>
  <files>scripts/deploy.sh</files>
  <read_first>scripts/deploy.sh (migrate step at line 23, set -e at line 5 — the seed step must sit between migrate and the pm2 restart at line 26), scripts/seed-sanguo.ts (the script being invoked, created in task 4)</read_first>
  <action>
    Edit scripts/deploy.sh to insert a seed step immediately after the drizzle-kit migrate line (line 23) and before the "Restarting bot..." pm2 line: an echo line announcing the idempotent sanguo seed, then `npx tsx scripts/seed-sanguo.ts`. Because the script runs under `set -e`, a seed failure aborts the deploy (correct behavior — the bot must not restart over missing/migrated-but-unseeded content). The seed is idempotent (D-11 upsert), so running it on every deploy is safe and matches D-12 ("run in CI/deploy pipeline"). Do not move or reorder the migrate, restart, or health-check steps.
  </action>
  <verify>
    <automated>bash -n scripts/deploy.sh; if ($?) { rg -n "seed-sanguo" scripts/deploy.sh }</automated>
  </verify>
  <done>deploy.sh runs npx tsx scripts/seed-sanguo.ts between migrate and restart; bash syntax check passes.</done>
  <acceptance_criteria>
    - Source assertion: scripts/deploy.sh contains a line invoking seed-sanguo placed AFTER the drizzle-kit migrate line and BEFORE the pm2 restart line.
    - CLI: `bash -n scripts/deploy.sh` exits 0 (syntax valid).
  </acceptance_criteria>
</task>

</tasks>
<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev environment -> PostgreSQL | wave-0 provisioning: migration + seed run against a DB reachable at DATABASE_URL_DIRECT (port 5432, direct — not PgBouncer 6432); the node pg probe is the gate |
| schema index -> migration diff | drizzle-kit generate diffs src/db/schema/index.ts against the last snapshot; the Phase 8 set MUST be complete (9 tables incl. map_nodes) before migrate — an incomplete set silently breaks SC2/SC3 |
| seed script / deploy pipeline -> DB | dev/deploy-time writes of content rows; idempotency (D-11) is the integrity property |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-07 | Tampering | scripts/seed-sanguo.ts | medium | mitigate | ON CONFLICT DO UPDATE keyed on natural keys (hero_id/code) is idempotent (D-11): re-runs update changed content and never duplicate rows; fail-fast on unmapped faction/role and on count != 132 (D-09) — no silent partial seeds. Seed runs only in deploy/CI (D-12), never at runtime. |
| T-08-12 | Tampering | migration 0014 diff (map_nodes coverage) | high | mitigate | Wave-2 ordering + the task-3 precondition guarantee all 9 Phase 8 exports (incl. mapNodes from 08-01, walletTransactions from 08-02) are merged before generate; the acceptance gate requires exactly 9 CREATE TABLEs incl. map_nodes; the pg_catalog DB assertion lists all 9 tables; the 0015 re-generate recovery is the backstop if map_nodes is absent from the 0014 diff — an 8-table migration can never be applied while gates pass. |
| T-08-13 | Information Disclosure | docker-compose.yml dev credentials | low | mitigate | Compose credentials are a local-dev-only convention (tutien/tutien), explicitly marked as such in .env.example; production secrets live in /etc/tutien/.env on the server (deploy.sh:10) and are never committed. Worst case of exposure is a throwaway local DB. |
| T-08-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages in this plan (RESEARCH Package Legitimacy Audit: all deps already installed/verified — drizzle-orm, pg, tsx, dotenv). The node pg probe uses the existing pg dependency. No registry installs occur. |
</threat_model>

<verification>
- Env gate (task 1): the node pg probe against DATABASE_URL_DIRECT exits 0 (prints DB_REACHABLE) BEFORE any migration/seed task — this is the full-boot smoke prerequisite (RESEARCH OQ5 resolved).
- Migration gate (task 3): `npx drizzle-kit generate` produces a purely additive Phase 8 migration set — exactly 9 CREATE TABLEs incl. map_nodes, no DROP TABLE, no ALTER outside the Phase 8 set — human-verified at the blocking checkpoint before `npx drizzle-kit migrate` is applied to the dev DB; DB assertion confirms all 9 tables in pg_catalog.pg_tables.
- Seed idempotency (task 4): run `npx tsx scripts/seed-sanguo.ts` twice; SELECT count(*) FROM heroes stays 132 and map_nodes stays 5-10 (D-11/D-09).
- Confirm scripts/deploy.sh contains the seed step between migrate and restart (task 5), and `bash -n scripts/deploy.sh` passes.
- Cross-plan regression: `npm run typecheck` after task 2 (schemas) and task 4 (seed) — index.ts carries mapNodes (08-01) + walletTransactions (08-02) + the 7 new files.
</verification>

<success_criteria>
- A dev PostgreSQL is reachable at DATABASE_URL_DIRECT (probe-proven, task 1) — migration 0014 and the seed are executable; docker-compose.yml + .env.example document the local stack (or the chosen external/local alternative).
- Migration covering all 9 Phase 8 tables (incl. map_nodes) generated, human-approved, and applied; SC2 table presence confirmed via pg_catalog.
- scripts/seed-sanguo.ts seeds 132 heroes + 5-10 map nodes + sanguo_items idempotently; re-runs add zero rows; deploy.sh runs it after migrate (D-12); package.json has seed:sanguo.
- npm run typecheck passes on the merged schema set.
</success_criteria>

<output>
Create `.planning/phases/08-foundation-economy-budget-content-infrastructure/08-04-SUMMARY.md` when done
</output>
